# Scripts 脚本说明

- **程序级薄索引（按目录速查）** → **[INDEX.md](./INDEX.md)**（**TT-MOD-B1-06**）。

## 单一入口（B-184）

- **单人直推 vs PR（改索引 / CI / gate / 广播证据链）** → [CONTRIBUTING.md](../CONTRIBUTING.md#solo-push-vs-pr)。
- **本文件** `scripts/README.md` 是 **`scripts/` 树的人类说明与索引**；**命令行仍优先使用历史路径** `scripts/<脚本名>`（见下「兼容策略」）。
- **实现按用途分目录**（**不改变**各脚本的退出码与串联语义）：

| 目录 | 用途 |
|------|------|
| **`scripts/gates/`** | CI / 预合并门禁、路由与文档对照、SSOT Guard、版本三元组、wave 文件存在性、基线回归、发版前聚合（`pre-release-automation`）、审计类检查等 |
| **`scripts/ops/`** | 索引器 / internal / admin 观测与留痕、对账探针、治理 SSOT ops-check、导出与只读 smoke 等**运维向**脚本 |
| **`scripts/dev/`** | 本地起停、ABI 同步与校验、前端 manifest、SQLx 迁移前缀、dev preflight、Windows 一键 bat、个人向 `windows/` 辅助脚本 |

**兼容策略（稳定对外路径）**

- **`scripts/<原名>.{sh,ps1,py,bat}`** 根路径保留为**薄转发**：调用 `gates/` / `ops/` / `dev/` 内同名实现。**Workflow、Runbook、文档、肌肉记忆**可继续写 `bash scripts/run-check-04-routes.sh` 等，**无需**先改外链。
- **新代码 / 新文档**若希望显式指向实现，可写 `scripts/gates/...`、`scripts/ops/...`、`scripts/dev/...`；二者**等价**于根路径（行为一致）。
- **SSOT Guard 附属物**：`templates/`、`ssot-guard-fixtures/` 已置于 **`scripts/gates/`** 下；编排器与契约脚本内路径已同步。
- **`docs/spec` 路径依赖（盘点 + registry + validator）**：凡是**新增、删除或改动** **`docs/spec` 路径依赖**（**`scripts/`** / **`docs/`**（spec 外）/ **`.github/workflows/`** 硬编码 **`docs/spec/…`**，或 **`registry/spec-path-dependencies*.yaml`** / 盘点文），**须同步**更新 **[盘点](../docs/spec-path-dependency-migration-inventory.md)** 与 **[`registry/spec-path-dependencies.v1.yaml`](../registry/spec-path-dependencies.v1.yaml)**。**提交前**本地依次 **`python registry/validate-spec-path-dependencies-registry.py`**、**`bash scripts/check-handbook-frontmatter.sh`**、**`bash scripts/check-handbook-engineering-content.sh`**（见 **[`registry/README.md`](../registry/README.md)**、**[CONTRIBUTING](../CONTRIBUTING.md)**）；**不以** PR / CI 绿灯为收口；**registry** workflow 为 **CI 恢复后可选**。**不**借此迁移或删除 **`docs/spec`**，**除非单独立项**；不改 **`build.yml`** 主链必过。

**迁移说明**

- **B-184 前**：脚本均在 `scripts/` 根下扁平存放。  
- **B-184 后**：实现文件物理位置按上表归类；**若你发现某外链直接指向子目录外的已删路径**，请改为 **`scripts/<同名>`** 根入口或对应 **`scripts/{gates,ops,dev}/<同名>`**。

---

**快速使用**（工程总纲读前入口：**[07-开发流程与顺序](../docs/spec/07-开发流程与顺序.md)** 文首**读前摘要**「**本地开发起停（Win · Unix · Next 15）**」行、**§二 2.3**）：Windows 一键启动（终止旧进程 + Docker + **编译** + 后端 8080 + 前端 3012）→ 项目根执行 `scripts\start-api-with-seed.bat`（**第 1 步**会校验 `crates/api/migrations/*.sql` 的版本前缀是否唯一，避免 SQLx `_sqlx_migrations` 主键冲突）。仅终止前后端（不关 Docker）→ `scripts\stop-all.bat`。仅需前端 → `scripts\check-3000-and-start.bat`（检查 **3012**）。验证 → `scripts\e2e-verify.bat`。Linux/Mac → `./scripts/start_dev.sh`（会先校验迁移前缀并编译后端），停止 → `./scripts/stop_dev.sh`。**数据库**：使用 DB 时请在项目根 `.env` 设置 `DATABASE_URL`（如 `postgres://traveltrust:traveltrust@localhost:5432/traveltrust`）；API 启动时会**自动执行迁移**，无需单独运行 `sqlx migrate run`。若 API 报错 `duplicate key value violates unique constraint "_sqlx_migrations_pkey"`：通常是两个迁移文件共用了同一数字前缀；修复后若本地库曾在错误版本下跑过一半，可 **`docker compose down -v`** 再 `up -d` 清空库后重迁。一键脚本 **默认保留数据库数据**（仅 `docker compose up -d`）；若需每次清空卷，启动前执行 **`set RESET_DOCKER_DB=1`** 再运行 `start-api-with-seed.bat`。单独自检：`powershell -File scripts\check-sqlx-migration-prefixes.ps1` 或 `bash scripts/check-sqlx-migration-prefixes.sh`。

**手测前清环境**：`scripts\prepare-local-manual-test.bat`（停 API/Next：**`resolve-dev-stack-ports.ps1`** + 可选 **`API_PORT`/`FRONTEND_PORT`**；清 `frontend/.next` 与 Playwright 报告；跑 **`scripts/dev/preflight-local-stack.ps1`**）。**若改 Next 端口**：根 **`.env` 的 `CORS_ORIGINS`** 须含 **`http://localhost:<该端口>`**。 **认证 HTTP 真源**：**`POST /auth/register`**、**`POST /auth/login`** 等根路径 **`/auth/*`**（`crates/api/src/routes/auth.rs`）；旧文档中的 **`/api/v1/auth/*`** 为**已废弃草案**，以 **[04 §3.4](../docs/spec/04-后端与API.md)** 与 **[38-端口与接口完整清单](../docs/spec/38-端口与接口完整清单.md)**（§2.1.1，v1.0.2+）为准。

**企业级预检（不启动服务）**：`powershell -File scripts/enterprise-preflight.ps1`（Windows）或 `bash scripts/enterprise-preflight.sh`（Unix）串联 SQLx 前缀 + **55-S13**（`contracts/abi` ↔ `frontend/dapp/abis`）；可选在运行前设置 **`TRAVELTRUST_ABI_FORGE_VERIFY=1`** 以额外跑 **forge multiset**（须 **Foundry** + **Python**）。无 Git Bash 时可用 **`powershell -File scripts/dev/check-55-s13.ps1`** 单独跑 55-S13。**一键启动** `start-api-with-seed.bat` 已内置：开头 **`scripts/dev/resolve-dev-stack-ports.ps1`** 解析 **API / Next** 端口（与 **`_dev_stack_ports.sh`** 同源：根 `.env` **PORT**、**API_PORT**、**FRONTEND_PORT**）。**Step 1** SQLx、**Step 1b** **55-S13**（可 **`SKIP_ABI_GATE=1`**）、可选 **`TRAVELTRUST_ABI_FORGE_VERIFY=1`** → **`run-verify-abi-forge.ps1`**、**Step 1c** **`run-check-04-routes.ps1`**（可 **`SKIP_ROUTES_GATE=1`**；预检已要求 **Python**）、**Step 1d** **`scripts/gates/check-auth-email-resend-gate.ps1`**（可 **`SKIP_AUTH_EMAIL_RESEND_GATE=1`** 跳过；与 **`.sh`** 同源，免 **PATH** 误用 **WSL** **bash.exe**）、**Step 3** 后 **`wait-for-postgres.ps1`**（**`SKIP_WAIT_POSTGRES=1`**）、**`wait-for-minio.ps1`**（**`SKIP_WAIT_MINIO=1`**）。**API 窗口**默认 **`set DATABASE_URL=postgres://…127.0.0.1:5432/traveltrust`** 覆盖继承环境（远程库不误连）；保留原 **`DATABASE_URL`** 请设 **`USE_ENV_DATABASE_URL=1`**。**Step 6** **`wait-for-api`** 超时默认 **exit 1**；仅警告请 **`SKIP_STRICT_API_WAIT=1`**。结尾 **`pause`** 可 **`NO_PAUSE=1`** 或 **`TRAVELTRUST_NO_PAUSE=1`** 跳过。**`frontend/.env.local`** 同步仍见 **`sync-frontend-env-local-from-root.*`**。Linux/Mac 可选 **`TRAVELTRUST_DOCKER_UP=1 ./scripts/start_dev.sh`** 在本脚本内 **`docker compose up -d`** 并等 Postgres/MinIO，且在同条件下强制 **`DATABASE_URL`**（除非 **`USE_ENV_DATABASE_URL=1`**）；**`start_dev`** 默认 **`wait-for-api`** 超时亦 **exit 1**，**`SKIP_STRICT_API_WAIT=1`** 恢复仅警告。测试网全链路透仍依赖钱包、Gas、Runbook，**无法**由单一仓库脚本保证。

**93 母表 vs `frontend/app` 路由（可选门禁）**：`python scripts/check-spec93-routes-vs-app.py` — 对比 **`docs/spec/93`** **§5** 中出现的 **`/path`** 与 **`page.tsx`** 推断路由（**§5 父路径**如 **`/market`** **视为覆盖** **`/market/...`** 子路径）；默认 **WARN**；设置 **`ENFORCE=1`** 时未覆盖则 **exit 1**。与 **`run-check-04-routes`**（**04 契约**）互补；汇总 JSON 见 **docs/spec/R-001**。

**回归 `report.json` 机读闸（R-001 / R-002）**：`python scripts/validate-regression-report.py evidence/GO_YYYYMMDD/report.json` — 校验 **R-001** 必填字段；**`--fail-on-no-go`**、**`--require-go`** 用于 CI/发布；**`--validate-orchestration`** 校验可选 **`orchestration`**（**96-15 Tier** 侧车，见 **R-001 §7**）。**执行闭环**见 **docs/spec/R-002**、**go-live-checklist §0.3**。**首次 staging A+B 真跑**见 **docs/spec/R-003**；**GitHub Actions**：**`.github/workflows/regression-report-validate.yml`**（**`workflow_dispatch`**）。**TT-9627 段 3 · R-002**：**`bash scripts/gates/vertical-slice-tt9627-segment3-r002-prereport-chain.sh`** — 委托 **`local-verify-r002-prereport-chain.sh`**（**`gen-r002-iss007-prereport.py`** + **`validate-regression-report.py`**；无 **`DATABASE_URL`** 时软校验，见脚本头）。**仅校验已有 `report.json`**：**`bash scripts/gates/vertical-slice-tt9627-segment3-r002-validate.sh path/to/report.json`**（可选 **`R002_FAIL_ON_NO_GO=1`** 等；**argv1** / **`REPORT_JSON`** / **`TRAVELTRUST_R002_REPORT_PATH`** 优先级见 **[TT-9628 §0.0.3](../docs/runbook/TT-9628-main-line-vs-branch-lines-delivery.md#tt-9628-report-json-path-convention)**）；**`TT9627_SEGMENT3_R002_VALIDATE=1 REPORT_JSON=…`** 可与 **`ci-local-delivery-minimum.sh`** 同跑（**[TT-9627 §3.1～§3.3](../docs/runbook/TT-9627-delivery-order-spine-then-full-site.md)**、**[TT-LOCAL §2](../docs/runbook/TT-LOCAL-CI-DELIVERY-GATE-001.md)**）。**ISS-007 窄切片**：**`gen-r002-iss007-prereport.py`** 在 43 锚全 **PASS** 时仍写 **`release_gate: PARTIAL_GO`**（**不**冒充 staging 全矩阵 **GO**）；对该 **`report.json`** 机读请 **`--fail-on-no-go`**，**勿**单独 **`--require-go`**；分轨说明 **`evidence/GO_local_r002_verify/README.md`**。

**96-18 G15 窄域 R-001 留痕（不冒充全站 GO）**：**`python scripts/gen-r002-onboarding-96-18-prereport.py`** — 默认写 **`evidence/GO_YYYYMMDD_r002_onboarding_96_18_prereport/report.json`**（或 **`TRAVELTRUST_R002_REPORT_PARENT`** 子目录 **`r002_onboarding_96_18_prereport/`**，与 **`gen-r002-iss007-prereport.py`** 同形）；**`G15-ONB-CARGO`** = **`cargo test -p traveltrust-api onboarding`**；**`G15-ONB-E2E-SHELL`** 由 **`PLAYWRIGHT_ONBOARDING_G15_OUTCOME=PASS|FAIL`** 回填；未设时读 **`PLAYWRIGHT_E2E_STEP_OUTCOME`**（**`build.yml` / `production-gate.yml`** 与 **ISS-007** 同传 **`steps.e2e_playwright.outcome`**：**success→PASS**）；再否则 **NOT_RUN**（Playwright：**`frontend/e2e/me-onboarding-96-18-shell.spec.ts`**；**筛子** **`-g "503 compliance"`** **见** **[TT-9618 §4](../docs/runbook/TT-9618-onboarding-local-testnet.md)** **命令块**）。**可选 Vitest（无浏览器）**：**`cd frontend && npx vitest run lib/apiClient/onboarding.http.test.ts`**、**`lib/mapApiReadError.test.ts`**（与 **§4** **同源**）。**`release_gate`** 为 **PARTIAL_GO**（cargo 失败时为 **NO_GO**）。随后可 **`python scripts/validate-regression-report.py …/report.json`**。

**`#9618`（96-18 准入费 · ② 机读一键 / 台账）：** **`bash scripts/gates/tt-9618-onboarding-pg-evidence.sh`**（头注释 **`#9618-one-page-priority`** / **`#9618-cmd-cheatsheet`**）— **[TT-9618 §3.5.3](../docs/runbook/TT-9618-onboarding-local-testnet.md#tt-9618-pg-evidence-one-shot)**（**步 5** **`cargo test … matrix_93_admin_onb`** **子串** **已含** **`matrix_93_admin_onb_031_*`** **`GET …/admin/jobs?queue_name=onboarding_webhook`**）；**backlog / 命令筛子** **[96-18-未完成 §0～§4](../docs/spec/96-18-未完成清单与多维检查.md#9618-one-page-priority)** **v1.0.125+**；**批次台账** **[`#9618-batches` · §1](../docs/spec/96-18-未完成清单与多维检查.md#9618-batches)**。**批次 W** **台账** **见** **该文件 §1**。

**TT-9627 拆线 · 机读闸总表**：**[TT-9628 §0.0.2a](../docs/runbook/TT-9628-main-line-vs-branch-lines-delivery.md#tt-9628-tt9627-gates-index)**（竖切脚本 × 段 × **`ci-local`**）。

**TT-9627 段 1 竖切机读（须 API 已监听）**：**`bash scripts/gates/vertical-slice-01-guides-catalog.sh`**（**[TT-9623](../docs/runbook/TT-9623-vertical-slice-01-guides-catalog.md)** · meta + guides）；**`bash scripts/gates/vertical-slice-02-main-spine.sh`**（**[TT-9625 §3](../docs/runbook/TT-9625-golden-path-system-spine.md)** · 公开半脊：**`/health`** + **`/meta`** + **`/meta/build`** + **`GET /api/v1/discover/orders`**；**不**替代注册→创单→托管 **E2E/手点**）。**`/meta` 等慢路径遇 408（TimeoutLayer）**：**`scripts/gates/_http_smoke_retry.sh`** 提供 **`http_get_with_retry`**；可调 **`HTTP_SMOKE_RETRIES`**（默认 **5**）/**`HTTP_SMOKE_RETRY_DELAY_SECS`**（默认 **2**）/**`HTTP_SMOKE_CURL_MAX_TIME_SECS`**（默认 **180**；**`curl --max-time`**，**`0`** 表示不设上限）。**编排**：**`bash scripts/gates/vertical-slice-tt9627-segment1-api-smoke.sh`**（先 **02**，经 **`VS02_CHAIN_OFF_STATE_FILE`** 读 **`chain_off_mounted`**，**不**再 **`GET /meta`**；为 **true** 时再跑 **01**）。**本地三连** 可选串跑： **`TT9627_SEGMENT1_API_SMOKE=1`** / **`TT9627_SEGMENT2_API_SMOKE=1`**（可同开）/ **`TT9627_SEGMENT3_R002_VALIDATE=1 REPORT_JSON=…`** / **`TT9627_SEGMENT456_SPEC_PRESENCE=1`**（**或** **`SEGMENT4`/`5`/`6`**；**`ci-local`** 下 **456** 优先、个体尾段自动跳过） **`bash scripts/gates/ci-local-delivery-minimum.sh`**（见 **[TT-LOCAL-CI-DELIVERY-GATE-001 §2](../docs/runbook/TT-LOCAL-CI-DELIVERY-GATE-001.md)**）。勾选节奏见 **[TT-9627 段 1.6](../docs/runbook/TT-9627-delivery-order-spine-then-full-site.md)**。

**TT-9627 段 2 · `/market` 公开读面（须 API 已监听）**：**`bash scripts/gates/vertical-slice-03-market-hub-public-smoke.sh`** — 串 **竖切 02** + **`GET /api/v1/guides`**（与 **`frontend/components/market/useMarketPage.ts`** **`getDiscoverOrders`/`getGuides`** 对齐）；**不**二次 **`GET /meta`**（**`chain_off_mounted`** 已由 **02** 输出）。**不**替代 **96-20** 全矩阵 / 抽屉 / 登录态。见 **[TT-9627 段 2.2](../docs/runbook/TT-9627-delivery-order-spine-then-full-site.md)**。

**`/` + `/market` 四页数据链（① · 相对 A+B / 竖切 03 正交）**：**`bash scripts/dev/run-web3-itinerary-l5-green.sh`**（Vitest 绿集 · **`landingItinerarySession` = `localStorage`** · **`useMarketPage` 300ms debounce** · 收藏 **`localStorage` + F-020 best-effort**）+ 可选 **`bash scripts/dev/smoke-web3-itinerary-full-chain-local.sh`**（Hero→预览解锁→**`/escrow/[id]`** 走廊；须 API+DB）。**SSOT** **[LANDING-MARKET-PAGES-CODE-SSOT](../frontend/evidence/GO_local_web3_pages_closure/LANDING-MARKET-PAGES-CODE-SSOT.md)** · **[dev-local-smoke-baseline §10](../docs/dev-local-smoke-baseline.md)** · **[solo-dev-rhythm §6.5](../docs/solo-dev-rhythm.md)**；**非 ②③ GO**。

**TT-9627 段 2 · `/community/explore` 公开读面（须 API 已监听）**：**`bash scripts/gates/vertical-slice-04-community-explore-public-smoke.sh`** — **`GET /api/v1/community/feed`** + **`GET /api/v1/community/stats/posts-by-tag`**（与 **`frontend/app/community/explore/page.tsx`** **`getFeed`** 及话题入口同源）；**不**替代 **96-20** 社区全矩阵 / **mode=follow**。见 **[TT-9627 段 2.2-b](../docs/runbook/TT-9627-delivery-order-spine-then-full-site.md)**。

**TT-9627 段 2 · 编排一键（market + community，须 API 已监听）**：**`bash scripts/gates/vertical-slice-tt9627-segment2-hub-public-smoke.sh`** — 串 **竖切 03** + **竖切 04**。**本地三连** 可选：**`TT9627_SEGMENT2_API_SMOKE=1 bash scripts/gates/ci-local-delivery-minimum.sh`**（与 **`TT9627_SEGMENT1_API_SMOKE`** **独立**；见 **[TT-LOCAL-CI-DELIVERY-GATE-001 §2](../docs/runbook/TT-LOCAL-CI-DELIVERY-GATE-001.md)**、**[TT-9627 段 2.2-c](../docs/runbook/TT-9627-delivery-order-spine-then-full-site.md)**）。

**TT-9627 · ② 测试网 / 远端 API — 段 1～2 一键**：**`bash scripts/gates/tt-9627-testnet-segment12-smoke-pack.sh`** — 须 **`BASE=https://…`**（或 **`API_BASE_URL`**）；串 **segment1 + segment2**；可选 **`RUN_TT9618_PG=1 DATABASE_URL=…`** 串 **[TT-9618](../docs/runbook/TT-9618-onboarding-local-testnet.md)** §3.5.3。**① 回环**须 **`TRAVELTRUST_ALLOW_LOCAL_BASE=1`**；**http 非回环**须 **`TRAVELTRUST_ALLOW_INSECURE_HTTP_BASE=1`**。见脚本头注释。

**TT-9627 段 4 · 母表文件在位（不启服务）**：**`bash scripts/gates/vertical-slice-tt9627-segment4-spec-presence.sh`** — 断言 **96-13 / 96-16 / 29** 路径存在（**①**）；**不**替代 **96-13** 走查与 **96-20** 矩阵。**`TT9627_SEGMENT4_SPEC_PRESENCE=1`** 可与 **`ci-local-delivery-minimum.sh`** 同跑（**[TT-9627 段 4.0](../docs/runbook/TT-9627-delivery-order-spine-then-full-site.md)**、**[TT-LOCAL §2](../docs/runbook/TT-LOCAL-CI-DELIVERY-GATE-001.md)**）。

**TT-9627 段 5 · 闭环/规则真源在位（不启服务）**：**`bash scripts/gates/vertical-slice-tt9627-segment5-spec-presence.sh`** — **TT-9624** + **96-21** + **96-17** 路径存在（**①**）；**不**替代表内逐项勾选。**`TT9627_SEGMENT5_SPEC_PRESENCE=1`** 可与 **`ci-local-delivery-minimum.sh`** 同跑（**[TT-9627 段 5.0](../docs/runbook/TT-9627-delivery-order-spine-then-full-site.md)**、**[TT-LOCAL §2](../docs/runbook/TT-LOCAL-CI-DELIVERY-GATE-001.md)**）。

**TT-9627 段 6 · 发版 GO 真源在位（不启服务）**：**`bash scripts/gates/vertical-slice-tt9627-segment6-spec-presence.sh`** — **TT-9626**、**go-live**、**缺口总表**、**TT-MAINNET**、**96-15**、**`ops/RUNBOOK.md`** 路径存在（**①**）；**不**替代 **P0/go-live** 勾选与 **③** 证据。**`TT9627_SEGMENT6_SPEC_PRESENCE=1`** 可与 **`ci-local-delivery-minimum.sh`** 同跑（**[TT-9627 段 6.0](../docs/runbook/TT-9627-delivery-order-spine-then-full-site.md)**、**[TT-LOCAL §2](../docs/runbook/TT-LOCAL-CI-DELIVERY-GATE-001.md)**）。

**TT-9627 段 4～6 编排一键（不启服务）**：**`bash scripts/gates/vertical-slice-tt9627-segments-456-spec-presence.sh`** — 串 **段 4.0 + 5.0 + 6.0**；**`TT9627_SEGMENT456_SPEC_PRESENCE=1`** 可与 **`ci-local-delivery-minimum.sh`** 同跑。**`ci-local`** 同开 **456** + **4/5/6** 时仅跑编排一次（stderr **note**）；见 **[TT-9627 · 段 4～6 编排](../docs/runbook/TT-9627-delivery-order-spine-then-full-site.md#tt-9627-segments-456-orchestration)**、**[TT-LOCAL §2](../docs/runbook/TT-LOCAL-CI-DELIVERY-GATE-001.md)**。

**96-15 机读编排（Tier A + 默认 Tier B/C 机读）**：一键 **`bash scripts/release/run_96_tier_a_p0_full_chain.sh`**（**`gen_tier_a_evidence_bundle.py`** → **`verify_96_booklets_registry.py`**（逐册 **PASS/FAIL** + 占位门）→ **`run_96_15_orchestration.py`** → **`go_state_machine.py`**）。编排器：**`run_96_15_orchestration.py`** — **A1/A2** 半自动；**A3** **`b421_doclink_gate.py`**；**B/C** 默认 **`tier_bc_machine_gates.py`**（**`--manual-tier-bc`** 仍产占位步骤，但写入 **`release_orchestration.json`** 前默认 **`TT_96_V1_MAP_MANUAL_TO_FAIL=1`** 将 **`MANUAL_REQUIRED`** 改为 **`FAIL`**，避免机读链出现人工态）。**`summary`**：**`tier_a_all_pass`**、**`tier_bc_all_pass`**、**`tier_bc_scope_manual_only`**。CLI：**`--require-tier-bc-all-pass`** 等；环境变量 **`TT_TIER_A1_README`**、**`TT_TIER_A2_MARKDOWN`**。侧车：**`merge_orchestration_into_report.py`** → **`go_state_machine.py`**。CI：**`.github/workflows/96-15-orchestration-gate.yml`**。

**96 全量机读 v2（无 MANUAL_REQUIRED 状态）**：**`python scripts/release/run_96_full_automation.py`** 产出 **`release_orchestration.json`**（**`schema_version`:** **`2`**，**`kind`:** **`traveltrust.release_orchestration.v2`**）；注册表 **`scripts/release/data/96_full_automation_registry.v1.json`**；Schema **`scripts/schemas/release_orchestration_report.v2.schema.json`**；说明 **`scripts/release/README_96_FULL_AUTO.md`**。**`go_state_machine.py --policy tri_state_v2`** 给出 **NO_GO / SOFT_GO / PRODUCTION_GO**（需 **`report.json`** **`release_gate`** 等条件）。**`validate-regression-report.py --validate-orchestration`** 已支持 **v1/v2**。**`TT_96_LIGHT`**（含跳过外部 **`env_path_or_fail`** / **`r002_report_validate`**）与 **`TT_96_SCOPE_BOOKLETS`**、**`TT_96_REPORT_JSON`**、**`95_f_row_alignment.v1.json`** 见 **`scripts/release/README_96_FULL_AUTO.md`**。

**本地发布流水线（不依赖 GitHub CI）**：**`bash scripts/ci/run_local_ci.sh`** — 串行 **95**（`cargo test -p traveltrust-api` 可选跳过、**04** 路由、`verify_96_booklets_registry`）→ **93**（**`gen-r002-iss007-prereport.py`**）→ **96-15**（**`run_96_full_automation.py`** 默认 v2 / 或 **`TT_LOCAL_CI_ORCH_MODE=v1`** 走 **`run_96_15_orchestration.py`**）→ **merge** → **R-002**（**`validate-regression-report.py --validate-orchestration`**）→ **`go_state_machine.py`**；产出 **`evidence/.../report.json`**、**`release_orchestration.json`**、**`go_state_suggestion.json`**。说明：**[`scripts/ci/README.md`](ci/README.md)**。

**96 Hub 本地验证收口（TT-9600）**：**`docs/runbook/TT-9600-96-HUB-LOCAL-VERIFICATION-PACK.md`** — **96-01～96-17** 与 Hub 的 **P0/P1/P2** 索引、**`evidence/`** 目录模板、**B-421** / **`validate-regression-report.py`** / **Tier-A** 脚本矩阵、**NO-GO** 归因与 **95 / 93 / R-002 / go-live** 边界（**不依赖** GitHub Actions）。

**96-16 全路由机读矩阵（D5–D7 / D1–D12）**：在 **`frontend/`** 下 **`npm run matrix:96-16:all`**（写 **`evidence/GO_96_16_*_coverage_matrix_v1.json`** 等），**`npm run check:96-16-matrices`** 与已提交 JSON 比漂移；**`frontend/scripts/matrix-96-16-shared.mjs`** 为路由规则 **SSOT**。见 **TT-9600 §3.1**、**`docs/spec/96-16-全页面UI-UX优化方案总册.md` §5**；**`frontend/package.json`** 已登记上述脚本（**CI 接回后**可再挂 **`build.yml`**）。

**04/14 HTTP 机读路由表冻结 · 93 真实执行**：**04 §3.4** / **14** 路由表及 **`run-check-04-routes`** 门禁锚点默认不再单独改版式 PR；迭代验收优先跑 **[spec/93](../docs/spec/93-全站功能验证矩阵-域别回归清单.md)** 各批，将 **BLOCKED** 在目标环境收敛为 **PASS/FAIL** 并按 **93 §0.5** 留痕 — 见根目录 **[CONTRIBUTING.md](../CONTRIBUTING.md)**「路由与契约」与 **[`docs/runbook/93-matrix-batch-tracker.md`](../docs/runbook/93-matrix-batch-tracker.md)**。

**Next.js 与 07 §二 Phase 4（前端）**：当前 **Next 15**；`npm run dev` 经 **`frontend/scripts/run-dev.mjs`**：**Windows 默认 Webpack** `dev -p 3012`（减轻 Turbopack 下 `app-build-manifest.json` / `_buildManifest.js.tmp.*` ENOENT）；**macOS/Linux 默认 Turbopack**（先 `ensure-turbo-dev.mjs`）。**勿**在 CMD 里单独输入 `dev`。**仍要 Turbopack（Windows）**：`npm run dev:turbopack` 或 `TRAVELTRUST_DEV_TURBO=1 npm run dev`。显式 Webpack（任意系统）：`npm run dev:webpack`。`npm run start` 会先执行 `ensure-next-start.mjs`，端口 **3012**。`next.config.js` 中 **turbopack.resolveAlias** 与 **webpack resolve.fallback** 对齐。若 **`/market` 404** 且 **`/_next/static/chunks/*.js` 大量 404**：`frontend` 下 **`npm run clean && npm run dev`**，或 `scripts\frontend-clean-dev.bat` / **`.\scripts\frontend-clean-dev.ps1`** / `./scripts/frontend-clean-dev.sh`。

**Prometheus（API 索引器指标）**：可复制告警规则见 **`ops/monitoring/prometheus-alerts-indexer.example.yml`**（须已 scrape **`GET /metrics`**；说明见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)）。**96-18 / 96-09 准入 webhook 队列**：**`ops/monitoring/prometheus-alerts-onboarding-webhook-queue.example.yml`**（**[TT-9618 §3.6.1–§3.6.3](../docs/runbook/TT-9618-onboarding-local-testnet.md#tt-9618-prometheus-onboarding-rules-merge)**：积压/DLQ **§3.6.1–§3.6.2**、**`rule_files` / on-call** **§3.6.3**（**清单 + 可复制工单模板** **`#tt-9618-onboarding-on-call-template`**））。**内网 internal Ingress / 可选 mTLS / NetworkPolicy 骨架**：**[`ops/kubernetes/README.md`](../ops/kubernetes/README.md)**（与 **[TT-9618 §3.5](../docs/runbook/TT-9618-onboarding-local-testnet.md#35-测试网--ingress--mtls--私网入口运维选修--批次-a)**、**[04-附录 §1](../docs/spec/04-附录-商家主理人准入费HTTP契约草案-配96-18.md)** 对读）。**可选机读**：**`bash scripts/gates/check-ops-monitoring-prometheus-examples.sh`**（已装 **`promtool`** 时对上述两文件 **`promtool check rules`**；未装则 **skip**；**`PROMTOOL_REQUIRED=1`** 时无工具 **exit 2**）；**`tt-9618-onboarding-pg-evidence.sh`**（**§3.1** **步 5–7**：**`matrix_93_admin_onb`** / **`006`** / **`005_f036_ext`** + **`017`**（**`charge.refunded`**）+ **`014`/`016`**（**`charge.dispute.funds_withdrawn`** **`paid→revoked`** **与终态审计**）+ **§3.6** **`matrix_93_d_onb_009`–`012`**；**Admin** **`029`/`030`** **已含于** **`matrix_93_admin_onb`**）在 **`promtool`** 可用时会自动串跑。**Next 生产构建（批次 M）**：**`bash scripts/gates/check-frontend-npm-build.sh`**（默认无 **node** 则 **skip**；CI 要求构建时设 **`STRICT_FRONTEND_BUILD=1`**）；**`CHECK_FRONTEND_NPM_BUILD=1`** 时由 **`tt-9618-onboarding-pg-evidence.sh`** 尾段串跑。**Prometheus 规则合入勾选（批次 N）**：**[`ops/monitoring/PROMETHEUS_ONBOARDING_RULE_MERGE_CHECKLIST.md`](../ops/monitoring/PROMETHEUS_ONBOARDING_RULE_MERGE_CHECKLIST.md)**。**`#9618` 命令筛子** **见** **[96-18-未完成 §4](../docs/spec/96-18-未完成清单与多维检查.md#9618-cmd-cheatsheet)** **（与根 README `#9618-one-page-priority` / `#9618-cmd-cheatsheet` 同源）**。**贡献者入口** **[CONTRIBUTING.md](../CONTRIBUTING.md)** **「96-18 准入费 · PG 机读一键」** **↔** **[TT-9618 §3.5.3](../docs/runbook/TT-9618-onboarding-local-testnet.md#tt-9618-pg-evidence-one-shot)**。

**DB 对账探针（internal）**：**`scripts/indexer-reconcile-probe.sh`**（或 **`./scripts/internal-indexer-ops.sh probe`**）对 **`GET …/internal/indexer-status?live_reconcile=1`** 做 **`projection_reconcile_clean`** 门禁式校验（须 **jq**、**`INTERNAL_API_SECRET`**、内网）；退出码见脚本头注释；**`--ops-artifact`** → **Epic D-09** **`traveltrust.ops_artifact.v1`** **`probe`**（**`gate_workflow_checks_total_expected`** 与 **indexer-reconcile-gate** **`checks_total`** 同锚）。见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)。**Admin 只读（浏览器）**：与上列 **internal** 写路径 / 探针**对照**时，值班可在已登录 **Admin** 的前端核对 **`/admin/indexer`**、**`/admin/indexer/reconcile-reports`**、**`/admin/indexer/reconcile/[id]`**、**`/admin/observability`**、**`/admin/finance`**；正文互证见 [Runbook §2.55「Admin 只读 UI」](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[13-1 表 1 · `/admin/*`](../docs/spec/13-1-UI产品级SSOT与页面规范.md)。**下表 `indexer-public-snapshot.sh`** 之 **`ADMIN_BEARER_TOKEN`** 分支与上述 **Admin GET** 同源摘要。**合约部署顺序（Anvil→测试网→主网）** → **[Runbook §2.56](../ops/RUNBOOK.md)**（与 **[contracts/README](../contracts/README.md)**、[governance-token/02 §1.3](../docs/spec/governance-token/02-对内技术规格-草案.md)、**[07 §二 Phase 3](../docs/spec/07-开发流程与顺序.md)**、**[14 §6](../docs/spec/14-合约-API-ABI-前后端对齐.md)** 同序）。**发版 / 值班最小顺序（含留痕）** → **[Runbook §12.5](../ops/RUNBOOK.md)**。**GitHub Actions / Kubernetes 定时示例**：**`ops/monitoring/`**（**`README.md`**、**`github-actions-indexer-probe.example.yml`**、**`k8s-indexer-reconcile-probe.cronjob.example.yaml`**）。**Grafana**：**`grafana-dashboard-traveltrust-indexer.example.json`**（**`/metrics`** 索引器面板）。

**合并 JSON / `snapshot_provenance` 契约三角**（与上表 **`indexer-public-snapshot.sh`**/**`write-indexer-evidence.*`** 同读）：**L2 段落 SSOT** → **[04 §3.4](../docs/spec/04-后端与API.md)**（读前摘要「索引器运维 JSON 快照」+ **`internal` API 总述**）；**ABI·观测边界（非 §2.1 表内 HTTP）** → **[14 §2.1](../docs/spec/14-合约-API-ABI-前后端对齐.md)**「运维 JSON 快照」；**流程与 CI 锚点** → **[110 §3.1.2](../docs/spec/110-阶段开发链上索引器与事件同步器.md)**；**链级 dry-run 证据（只读）** → **`internal-indexer-ops.sh reconcile --chain-scope-dry-run`**（及 **`--event-log-scope-dry-run`** 等）+ **[110 §3.1.4](../docs/spec/110-阶段开发链上索引器与事件同步器.md)** + **[evidence/README](../evidence/README.md)** + **[Runbook §12.5](../ops/RUNBOOK.md)** **步骤 6**；工程台账 → **[07 §六 6.4](../docs/spec/07-开发流程与顺序.md)**。

### 开卡前检查清单（Pre-TT Gate）

人工或脚本化前置核对（与 **[04 · 零、SSOT Gate](../docs/spec/04-后端与API.md#ssot-gate-pre-tt)**、**[AI 任务卡索引 · 开卡总规则](../docs/AI任务卡索引.md)** 对读）：

- [ ] 已从母表 / 缺口总表 / 各 spec **Target·Partial** 句生成候选
- [ ] 已完成**信息价值** / **双源风险**评估（**可否决**、可仅登记母表评估行）
- [ ] [任务母表.md](../docs/任务母表.md) 已有对应 **B-xxx** 行
- [ ] [AI任务卡索引.md](../docs/AI任务卡索引.md) 已创建 **TT**（**未封口**）
- [ ] 已明确是否影响**公开 API**
- [ ] 已明确是否进入 **meta / admin / reconcile / ops**
- [ ] （**push / 合并前**）**单人开发元数据门禁（B-145 + B-147）**：已同批改 **`docs/任务母表.md`** 或 **`docs/AI任务卡索引.md`**（若 diff 含 **`crates/**`** 或 **须登记的 **`contracts/**`** — 豁免见 **`scripts/gates/check-pr-crates-needs-metadata.sh`** 头注释）；**须**本地 **`bash scripts/check-pr-crates-needs-metadata.sh main HEAD`**（直推无 PR 时 **仅此**；**非** PR gate）；若走 **GitHub PR** 另有 **`.github/workflows/ssot-crates-metadata-hint.yml`**（**B-148**：job 内 **`CRATES_METADATA_GATE_REQUIRE_REFS=1`**，`BASE`/`HEAD` **不可解析则 job 失败**）

**`docs/handbook` 文首机读（与 Build CI 同源）**：**`bash scripts/check-handbook-frontmatter.sh`** → **`scripts/gates/check-handbook-frontmatter.sh`**（依次尝试 **`python3` / `python` / `py -3`** 调用 **`check-handbook-frontmatter.py`**；**Linux CI** 仍以 **`python3`** 为主）— 校验 **`docs/handbook`** 下 **`engineering|product-manager|learn` 的 `NN-*.md`**、**`corpus/REG-*.md`**、各 **`README.md`**、**根 `00-*.md`** 的 **Version / 与 spec 关系** 规则；说明见 **[handbook/engineering/05-本地环境与常用门禁速查.md](../docs/handbook/engineering/05-本地环境与常用门禁速查.md)**。**CI**：**[build.yml](../.github/workflows/build.yml)** **`Handbook frontmatter`** 步（直接 **`python3 …`**）。

**`engineering` 域篇内容卫生（与 Build CI 同源）**：**`bash scripts/check-handbook-engineering-content.sh`** → **`scripts/gates/check-handbook-engineering-content.sh`** — 校验 **`docs/handbook/engineering/*.md`** 内 **`](../../spec/*.md)`** 链接目标存在；并要求 **`NN≥10`** 域文正文出现 **`cargo test` / `run-check-04-routes` / `bash scripts/`** 之一（工程验证段）；**`EVIDENCE-*-cluster-verified.md`** 须含 **22** 母版链、**`不替代`**、**`## V-1`～`V-3`** 与 **`-v1/-v2/-v3` HTML 锚**（与 **[engineering/22](../docs/handbook/engineering/22-横切-簇级verified证据模板.md)** 对拍；失败码 **`HBOOK-ENG-EVIDENCE`**）。**CI**：**[build.yml](../.github/workflows/build.yml)** **`Handbook engineering content hygiene (HBOOK-ENG-EVIDENCE; spec links + domain hints)`** 步。

**各 `EVIDENCE-*-cluster-verified.md` §V-1 命令并集（自动化复跑）**：**`bash scripts/run-handbook-cluster-evidence-v1.sh`**（实现 **`scripts/gates/run-handbook-cluster-evidence-v1.sh`**）— 串 **07 三元组**、**wave-phase**、**handbook 两门禁**、**55-S13**、**run-check-04-routes**、**indexer-reorg-recovery status** 与 **各簇 V-1 所列 `cargo test`/`--no-run`**；可选 **`TT_CLUSTER_EVIDENCE_V1_SKIP_CARGO`** / **`…_SKIP_AUTH`** / **`…_SKIP_INDEXER_REORG_STATUS`**。**CI**：**[handbook-cluster-evidence-v1.yml](../.github/workflows/handbook-cluster-evidence-v1.yml)**（**`workflow_dispatch`** + 变更 **`EVIDENCE-*`** 或本脚本时的 **PR**）。**验收入口契约**：对上述 **文档 / 脚本 / workflow** 的改动，以本脚本 **exit 0** 与末行 **`TT_EVIDENCE_V1_SUMMARY:`** 为**唯一收口线**；该行前缀与 **`OK`/`FAIL`** 格式须**长期稳定可 `grep`**，且 union 步骤须与各 **`EVIDENCE-*` `## V-1`** **语义一致** — 否则视为**未完成**（与 **`check-handbook-engineering-content.sh`** 互补）。**日志速查**：脚本末行（失败时亦会输出一行）为固定前缀 **`TT_EVIDENCE_V1_SUMMARY:`**（**`OK steps=…`** / **`FAIL step=… exit=…`**）；本地或 CI 全量 log 可用 **`grep TT_EVIDENCE_V1_SUMMARY`** 或 **`rg TT_EVIDENCE_V1_SUMMARY`**（或对保存的 **`*.log`**）快速定位结果行。

**`engineering` 相对路径断链（可选 · I5）**：**`bash scripts/check-handbook-engineering-local-md-links.sh`** — 校验 **`docs/handbook/engineering/*.md`** 内 **`](...)`** 相对文件目标存在（含 **`contracts/`**、**`corpus/`** 等；**不**替代上条 **spec** 外链门禁）。**CI**：默认**不**跑；见 **[engineering/09 · §8 I5](../docs/handbook/engineering/09-文档迁移覆盖审计报告.md#audit-issues)**。

**AI 任务卡索引一览（硬性 · 凡改 `docs/AI任务卡索引.md` 须在提交 / 推远程前本地通过）**：项目根优先 **`bash scripts/check-ai-task-card-index-overview.sh`**（参数透传；**Win/Git Bash** 内依次 **`python3` → `py -3` → `python`**，缓解 **商店** **`python3`** **占位体** **exit 49** — 与 **`scripts/gates/check-invariants.sh`** **≈L35** **同源**）；或直接 **`py -3 scripts/check-ai-task-card-index-overview.py docs/AI任务卡索引.md`** / **`python3 …`**（**严格** 序号 **1..n**，**勿** 传 **`--allow-seq-gaps`** 作为提交前门槛）。排障时可临时 **`--allow-seq-gaps`**，**不得** 依赖其合并 **main**。失败时 **stderr** 机读：**`RULE=`**（`A-SEQ` / `E-ID` / `B-STATUS` / `C-BODY` / `D-STASH` / `Z-META`）**`seq=`** **`id=`** **`msg=`**（**msg** 为最后一字段）。**CI**：[check-ai-task-card-index-overview.yml](../.github/workflows/check-ai-task-card-index-overview.yml)（workflow 名 **`AI task card index overview`**，job **`check`** → 分支保护勾选 **`AI task card index overview / check`**）；**Build** [build.yml](../.github/workflows/build.yml) 内同脚本一步（与上同源）。**维护者**：将 **`AI task card index overview / check`** 设为 **`main`** 必过见 [CONTRIBUTING.md](../CONTRIBUTING.md) **「main · AI 任务卡索引一览门禁」**。**`ci-local`/`dev-preflight`** 三连后经 **`gates/maybe-run-ai-task-card-index-overview-on-diff.sh`** 条件复用同上（**TT-LOCAL §2**、**`SKIP_AI_TASK_CARD_INDEX_OVERVIEW`** / **`CI_LOCAL_SKIP_AI_TASK_CARD_INDEX`**）。**`docs/AI任务卡索引.from-stash.md`**：与 **`.py`** 同一 **A/E/B/C/D** 规则；改后本地 **`bash scripts/check-ai-task-card-index-overview.sh docs/AI任务卡索引.from-stash.md`**；一览 **状态列** 与 **`### TT-…`** 正文易漂移时，可先审 **`python scripts/patch-from-stash-ai-index-overview-status.py`** 产出再复跑校验（**不**替代人工 **封口** 语义）。

**提交前自检三连（单人默认，与 [04 · 零、](../docs/spec/04-后端与API.md#ssot-gate-pre-tt) 一致）**：

```bash
cargo test -p traveltrust-api
bash scripts/run-check-04-routes.sh
bash scripts/check-pr-crates-needs-metadata.sh main HEAD
```

**一键（可选）**：**`bash scripts/dev-preflight.sh`** — 先 **`check-strict-ssot-local-prereqs`**，再与上三行同序（**cargo** → **run-check-04-routes** → **元数据**；**1～2** 任一失败 **exit 1**；第 **3** 条默认 **exit 0**）；**unset** **`SKIP_AI_TASK_CARD_INDEX_OVERVIEW`** 且 **unset** **`CI_LOCAL_SKIP_AI_TASK_CARD_INDEX`**（默认）且 **`docs/AI任务卡索引.md`** **或** **`docs/AI任务卡索引.from-stash.md`** **相对 `HEAD` 或 `main..HEAD` 有变** 时，**三连后自动** **`bash scripts/gates/maybe-run-ai-task-card-index-overview-on-diff.sh`**（与上段 **AI 一览** 同核；跳过：**`SKIP_AI_TASK_CARD_INDEX_OVERVIEW=1`** 或 **`CI_LOCAL_SKIP_AI_TASK_CARD_INDEX=1`**）。**GitHub-hosted** **不可** **用时** **的** **最小** **交付** **三连** **（** **同** **上** **三** **行** **、** **薄** **封装** **）** **：** **`bash scripts/ci-local-delivery-minimum.sh`** — **同一** **`maybe-run-…`** **逻辑**（**无** **`check-strict`** 前置）；编排见 **[TT-LOCAL-CI-DELIVERY-GATE-001](../docs/runbook/TT-LOCAL-CI-DELIVERY-GATE-001.md)**。

**Playwright（可选）· 94 子站 + 31 社区 + 49 A 自定义行程**：**`cd frontend && npm run e2e:market-community`**（`package.json` **`e2e:market-community`** → **`node ./scripts/run-e2e-default.mjs`** 两 spec；本地默认 **`PLAYWRIGHT_FULL_STACK=1`**；与 **`e2e-stability-probe.sh`** **`E2E_MARKET_COMMUNITY_SLICE=1`** 尾段同源）；**Vitest**：**`npx vitest run components/community/PublishDrawer/constants.test.ts components/community/PublishDrawer/usePublishForm.video-duration.test.ts`**。**`build.yml` `e2e`** 已含全量 **`npm run e2e`**（两 spec 在内）。**本地扩充一键**（三连 + 上述前端 + Vitest + 条件 **`e2e:market-community`**）：**`bash scripts/gates/local-delivery-expanded.sh`**（根 **`bash scripts/local-delivery-expanded.sh`**；**`DATABASE_URL` / `CI_LOCAL_*`** 见 **[TT-LOCAL §2.2](../docs/runbook/TT-LOCAL-CI-DELIVERY-GATE-001.md)**）。编排与 **①** 口径见 **[TT-LOCAL-CI-DELIVERY-GATE-001 · §2.1](../docs/runbook/TT-LOCAL-CI-DELIVERY-GATE-001.md)**（扩展覆盖登记 **§2.1.1**）、**[TT-9600 · §3.2](../docs/runbook/TT-9600-96-HUB-LOCAL-VERIFICATION-PACK.md)**。

**`check-pr-crates-needs-metadata.sh` 环境变量**（**B-145** / **B-146** / **B-147**，详见 **`scripts/gates/check-pr-crates-needs-metadata.sh`** 头与 **[母表 B-146](../docs/任务母表.md)**）：**`CRATES_METADATA_GATE_FAIL=1`** — diff 含 **`crates/**`** 或 **须登记的 **`contracts/**`** 但未同批母表/索引时 **exit 1**；**`CRATES_METADATA_GATE_REQUIRE_REFS=1`** — **`git rev-parse` 失败**时 **exit 2**（**默认 unset** 仍为 **WARN + exit 0**）。

第三条若 **stderr 有提醒**：**`crates/**`** 或 **须登记的 **`contracts/**`** 已进 **`main..HEAD`**，但母表/索引**未同批** — **先补齐再 commit**（**B-147** 豁免路径见脚本头）。

---

## 一、日常开发

| 脚本 | 平台 | 说明 |
|------|------|------|
| **start-api-with-seed.bat** | Windows | 一键：**Step 0a** **`resolve-dev-stack-ports.ps1`** → **Step 1** SQLx 前缀 → **Step 1b** **55-S13**（可 **`SKIP_ABI_GATE=1`**）→ 可选 **`TRAVELTRUST_ABI_FORGE_VERIFY=1`** **forge multiset** → **Step 1c** **`run-check-04-routes.ps1`**（须 **Python**；可 **`SKIP_ROUTES_GATE=1`**）→ **Step 1d** **`scripts/gates/check-auth-email-resend-gate.ps1`**（可 **`SKIP_AUTH_EMAIL_RESEND_GATE=1`**）→ 停旧进程 → **Docker `up -d`** → **wait-for-postgres**（可 **`SKIP_WAIT_POSTGRES=1`**）→ **编译** → 起 API（**`PORT`=解析端口**、**`SEED_TEST_ACCOUNTS=1`**、默认 **`DATABASE_URL`**；默认子进程未定义时注入 **`API_RATE_LIMIT_PER_MINUTE=0`** / **`CRITICAL_WRITE_RATE_LIMIT_PER_MINUTE=0`**（与 **`start-api-for-playwright.*`** 同源）；**`TRAVELTRUST_STRICT_API_RATE_LIMIT=1`** 关闭注入）→ **`wait-for-api`** → **`POST /auth/seed-test-accounts`**（**Step 6b**：优先 **curl**，无 **curl** 时用 **PowerShell**；可 **`SKIP_POST_SEED_TEST_ACCOUNTS=1`**；与 **E2E** **`seedTestAccounts`** / **96-18** **PG** 资格幂等同源）→ 可选 **Step 6c** **`GET /meta`**（**`TRAVELTRUST_POST_START_META_CHECK=1`**）→ 同步 **`frontend/.env.local`**（可 **`SKIP_FRONTEND_ENV_SYNC=1`**）→ 起 Next（**`FRONTEND_PORT`/`TRAVELTRUST_FRONTEND_PORT`** → **`run-dev.mjs`**）；收尾可 **`TRAVELTRUST_OPEN_BROWSER=1`** 自动打开登录页；控制台打印 **UI 手测** 与 **`docs/spec/96-13-UI-UX-i18n-a11y-性能走查.md`** 路径。**`SKIP_*`**、**`NO_PAUSE`** 等见 bat 头注释。 |
| **stop-all.bat** | Windows | 停后端+前端（不关 Docker）；**`resolve-dev-stack-ports.ps1`** 解析端口（与一键/e2e 同源），可用 **`API_PORT`/`FRONTEND_PORT`** 覆盖；**`NO_PAUSE`** / **`TRAVELTRUST_NO_PAUSE`** 跳过末尾 pause。 |
| **stop-api-thorough.bat** | Windows | **单独**彻底清 API：结束 **traveltrust-api** 与 **8080** 上所有 **LISTENING** 进程（PowerShell **Get-NetTCPConnection**），释放 **`target\debug\traveltrust-api.exe`** 锁；可选传 **`-AlsoFrontend`**。`start-api-with-seed.bat` 启动前亦调用同一逻辑。 |
| **run-frontend.bat** | Windows | 仅启动前端（**`npm run dev`** → **`run-dev.mjs`**）；默认端口 **3012**，可由环境变量 **`FRONTEND_PORT`** / **`TRAVELTRUST_FRONTEND_PORT`** 覆盖。 |
| **run-frontend-webpack.bat** | Windows | Webpack dev（`npm run dev:webpack`，含 `ensure-dev-next`）。Turbopack 异常时改用。 |
| **run-frontend-prod.bat** | Windows | `npm run build` → `npm run start`（生产，端口 3012，含 `ensure-next-start`）。 |
| **frontend-clean-dev.bat** | Windows | `npm run clean && npm run dev`，修复 dev/build `.next` 混用导致的 404。 |
| **frontend-clean-dev.sh** | Linux/Mac | 与上等价；项目根 `./scripts/frontend-clean-dev.sh`。 |
| **frontend-clean-dev.ps1** | Windows | 委托 **`bash scripts/frontend-clean-dev.sh`**（须 **Git Bash**）；日常亦可 **`.bat`**。 |
| **check-3000-and-start.bat** | Windows | 检查 3012 是否监听；未监听则新窗口启动前端。适合「只起前端」时用。（文件名保留兼容，端口为 3012。） |
| **build-api.bat** | Windows | 仅编译后端（先跑 `check-sqlx-migration-prefixes.ps1`，再 `cargo build -p traveltrust-api`）。编译后需重启 API 或再运行 start-api-with-seed.bat。 |
| **dev-preflight.sh** | Linux/Mac/Git Bash | **`check-strict-ssot-local-prereqs`** → **`cargo test -p traveltrust-api`** → **`run-check-04-routes.sh`** → **`check-pr-crates-needs-metadata.sh main HEAD`** → **`gates/maybe-run-ai-task-card-index-overview-on-diff.sh`**（与 **[04 零、](../docs/spec/04-后端与API.md#ssot-gate-pre-tt)**、上表 **AI 一览** 段同源；第 3 条默认 **exit 0**；**`CRATES_METADATA_GATE_REQUIRE_REFS=1`** 见上）。项目根 **`bash scripts/dev-preflight.sh`**。 |
| **gh-l4-run-inspect.sh** | Linux/Mac/Git Bash | **`gh`** 查看 **`.github/workflows/l4-parallel-ci.yml`** 最近（或指定 **run id**）一次 run 的 **job 级** 结论、**JSON** 与 **per-job** `gh run view --job=…`（**Annotations** / 计费未启动）。可选环境变量 **`GH_BRANCH`**。根 **`bash scripts/gh-l4-run-inspect.sh`**；实现 **`scripts/dev/gh-l4-run-inspect.sh`**；Runbook **[TT-L4-PARALLEL-CI-001](../docs/runbook/TT-L4-PARALLEL-CI-001.md)**。 |
| **gh-l4-run-inspect.ps1** | Windows | 转发至 **`dev/gh-l4-run-inspect.sh`**（须 **Git `bash.exe` 在 PATH**）。项目根 **`powershell -File scripts/gh-l4-run-inspect.ps1`**。 |
| **check-pr-crates-needs-metadata.sh** | Unix / Git Bash | **单人开发元数据门禁（B-145 + B-147）**：**`BASE..HEAD`** 与 **`crates/**`**、**须登记的 **`contracts/**`** / 母表·索引；**`CRATES_METADATA_GATE_FAIL`**、**`CRATES_METADATA_GATE_REQUIRE_REFS`** 见上表 **Pre-TT** 段与 **[B-146](../docs/任务母表.md)**。 |
| **check-sqlx-migration-prefixes.ps1** / **check-sqlx-migration-prefixes.sh** | Windows / Unix | 校验 `crates/api/migrations/*.sql` 文件名数字前缀唯一；`start-api-with-seed.bat`、`build-api.bat`、`start_dev.sh` 会调用。 |
| **dev-preflight.ps1** | Windows | 检查 Docker、npm、cargo、`.env`；项目根 `powershell -ExecutionPolicy Bypass -File scripts\dev-preflight.ps1`。 |
| **e2e-verify.bat** | Windows | 验证：数据库容器、API **`/health`**、前端根与 **`/market`**、种子账号登录。未设 **`API_PORT`/`FRONTEND_PORT`** 时由 **`resolve-dev-stack-ports.ps1`** 与根 **`.env`** 对齐（与一键脚本同源）。启动约 1 分钟后运行。 |
| **smoke-ab-core-chain.sh** | Unix / Git Bash | **本地/可控环境 A+B 主链烟测**（注册→登录→`/me`→`/discover/orders`→下单→订单→消息；**`DATABASE_URL` 已设时**用 **`psql`** 或 **`docker exec traveltrust-postgres psql`** 抽检落库，无需本机安装 psql）。**不涉及 GO/NO_GO**。项目根 **`bash scripts/smoke-ab-core-chain.sh`**（须已起 API，建议 **`SEED_TEST_ACCOUNTS=1`**）。**成功标准与一键复现** → **[dev-local-smoke-baseline.md](../docs/dev-local-smoke-baseline.md)**；与 staging 真跑对照 → **[R-003 · #r003-ab-core-acceptance](../docs/spec/R-003-Staging首次完整回归-A-B域-执行Runbook.md#r003-ab-core-acceptance)**。 |
| **dev/smoke-provider-onboarding-local.sh** | Unix / Git Bash | **① 商家入驻全链 API 烟测**（注册→钱包→资质→96-18 准入费→role-confirm→Admin 审核→**POST/GET market listings**；**非 ②③**）。须 **`DATABASE_URL`** · **`INTERNAL_API_SECRET`** · **`SEED_TEST_ACCOUNTS=1`**。**SSOT** → **[`frontend/app/provider/register/README.md`](../frontend/app/provider/register/README.md)** · **[TT-9618 §2.1](../docs/runbook/TT-9618-onboarding-local-testnet.md)**。Windows：**`scripts/smoke-provider-onboarding-local.bat`**。 |
| **dev/smoke-acquisition-pd009-local.sh** | Unix / Git Bash | **① 旅行收购 PD-009 垂直切片烟测**（绑钱包→发布 bond→**`POST …/market/acquisition/listings`** 门闸→目录/草稿→创单→可选 Admin suspend；**非 ②③ GO**）。须 **`DATABASE_URL`** · **`SEED_TEST_ACCOUNTS=1`**。**SSOT** → **[`frontend/app/market/acquisition/README.md`](../frontend/app/market/acquisition/README.md)** · **[acquisition-publish-trust-rules §8.1](../docs/spec/artifacts/acquisition-publish-trust-rules.v1.md#81-第一阶段--本地--closed2026-05-27)** · **93 §2.1b**。 |
| **dev/smoke-admin-pages-local.sh** | Unix / Git Bash | **① Admin 控制台 HTTP 面探针（M-03）**（须 API **:8080** + FE **:3012** + admin 会话；探针 **`/admin/*`** 子路由与关键 **GET** 不 5xx）。**非 ②③ GO**。**SSOT** → **[`frontend/app/admin/README.md`](../frontend/app/admin/README.md)** · **[70 §3.0.2](../docs/spec/70-管理员系统开发文档.md)**。 |
| **dev/smoke-admin-rbac-matrix-local.sh** | Unix / Git Bash | **① Admin RBAC / console_role 矩阵烟测（M-01）**（须 **`DATABASE_URL`** · **`SEED_TEST_ACCOUNTS=1`** · **`POST /auth/seed-test-accounts` promote + 重登**；校验 **`GET …/capabilities`** · **`GET …/rbac/route-matrix`** · 路由 deny 预览）。**非 ②③ GO**。**SSOT** → **[`frontend/app/admin/README.md`](../frontend/app/admin/README.md)** · **[70 §3.0.2](../docs/spec/70-管理员系统开发文档.md)**。 |
| **dev/smoke-admin-adm-u02-local.sh** | Unix / Git Bash | **① ADM-U02 控制台角色审批 + 2FA + 审计烟测（M-02）**（**`POST …/console-role-change-request`** → **`POST …/approvals/:id/approve`**；**`TRAVELTRUST_ADMIN_CONSOLE_ROLE_DIRECT=1`** 时跳过直写 **409** 探针）。成功末行 **`TT_ADM_U02_LOCAL: PASS`**。**非 ②③ GO**。**Runbook** → **[ADM-U02](../docs/runbook/ADM-U02-admin-permissions-2fa-approval.md)** · **SSOT** **[`frontend/app/admin/README.md`](../frontend/app/admin/README.md)**。 |
| **dev/run-admin-l5-green.sh** | Unix / Git Bash | **① Admin 工作台 L5 Vitest 绿集**（**`node scripts/dev/run-admin-l5-green.mjs`**；含系统概况 · L5 confirm · SWR · 审计抽样）。项目根 **`bash scripts/dev/run-admin-l5-green.sh`**。**SSOT** → **[`frontend/app/admin/README.md`](../frontend/app/admin/README.md)** · **[70 §3.0.2](../docs/spec/70-管理员系统开发文档.md)** · **[ADMIN-L5-PHASE-GAP-TASK-LIST](../frontend/evidence/GO_local_admin_workspace_closure/ADMIN-L5-PHASE-GAP-TASK-LIST.md)**；**非 ②③ GO**。 |
| **dev/verify-admin-audit-closure.sh** | Unix / Git Bash | **① Admin 审计批次一键验收**（**`check-admin-capabilities-route.sh`** + **`run-admin-l5-green.sh`** + **`smoke-admin-pages-local.sh`**；可选 M-01）。**SSOT** → **[`frontend/app/admin/README.md`](../frontend/app/admin/README.md)**。 |
| **dev/run-admin-remaining-local-prep.sh** | Unix / Git Bash | **① Admin 剩余项本地预备**（L5 + RBAC + ADM-U02 + Shell 预览矩阵 · **非 ② GO**）。 |
| **dev/run-admin-adm-u01-local-prep.sh** | Unix / Git Bash | **① ADM-U01** 子集预备（session 预览 · 顶栏快切）。 |
| **dev/run-admin-adm-u02-local-prep.sh** | Unix / Git Bash | **① ADM-U02** 子集预备（**`smoke-admin-adm-u02-local.sh`**；可选 Playwright **`ADM_U02_LOCAL_PREP=1`**）。 |
| **dev/run-admin-web3-l5-green.sh** | Unix / Git Bash | **① Admin L5 + Web3  itinerary L5 合并绿集**（非 ②③ GO）。 |
| **dev/check-admin-capabilities-route.sh** | Unix / Git Bash | **8080** 上 **`GET …/admin/capabilities`** 须 **401**（非 404）；旧 API 进程排查探针。 |
| **dev/check-admin-phase2-prep-toolchain.sh** | Unix / Git Bash | **② Admin 脚本链自检**（**`--with-l5`** 可选；不触 Staging）。 |
| **dev/run-admin-phase1-closure.sh** | Unix / Git Bash | **① Admin Phase1 正式收口闸**（绿集 + 预备脚本组合 · **非 ②③ GO**）。 |
| **dev/run-web3-itinerary-l5-green.sh** | Unix / Git Bash | **① Web3 旅行首页 + 自由市场数据链 Vitest 绿集**（**`/`** **1×POST** · **`landingItinerarySession` localStorage** · **`/market` debounce/收藏** · Escrow 草稿 freeze 等）。项目根 **`bash scripts/dev/run-web3-itinerary-l5-green.sh`**。**SSOT** → **[LANDING-MARKET-PAGES-CODE-SSOT](../frontend/evidence/GO_local_web3_pages_closure/LANDING-MARKET-PAGES-CODE-SSOT.md)** · **[dev-local-smoke-baseline §10](../docs/dev-local-smoke-baseline.md)**；**非 ②③ GO**。 |
| **dev/smoke-web3-itinerary-full-chain-local.sh** | Unix / Git Bash | **① 创新行程走廊 API 烟测**（Hero 创单→预览 **`getOrder`**→**`/escrow/[id]`**；须 API+DB）。项目根 **`bash scripts/dev/smoke-web3-itinerary-full-chain-local.sh`**。与 **`run-web3-itinerary-l5-green.sh`** 同 **CODE SSOT**；**非 ②③ GO**。 |
| **start_dev.sh** | Linux/Mac | 一键：**编译后端** → 启动后端(8080)+前端。前端：`ensure-turbo-dev` + `next dev --turbopack -p $FRONTEND_PORT`（默认 **3012**，Next 15）。`./scripts/start_dev.sh`。设 **SKIP_API_BUILD=1** 可跳过编译。 |
| **start_dev.ps1** | Windows | 委托 **`bash scripts/start_dev.sh`**（须 **Git Bash**）；含 Docker/DB 一键见 **start-api-with-seed.bat**。 |
| **stop_dev.sh** | Linux/Mac | 停止 start_dev 启动的进程。 |
| **stop_dev.ps1** | Windows | 委托 **`bash scripts/stop_dev.sh`**；亦可 **stop-all.bat**。 |
| **internal-indexer-ops.sh** | Linux/Mac/Git Bash | 本机/内网 **`POST …/internal/indexer-tick|indexer-replay|indexer-reconcile`**、**`GET …/internal/indexer-status`**（**`status --ops-artifact`** → **`traveltrust.ops_artifact.v1`** / **Epic D-03**（**`artifact_version`：`v1`**）；**`status --live-reconcile --ops-artifact`** → **Epic D-04** / **`live_orders_projection_reconcile`**；**`reconcile --ops-artifact`** → **Epic D-05** / **`artifact_type:reconcile`**（**无** **`--persist`**）；**`reconcile --chain-scope-dry-run --ops-artifact`** → **Epic D-06** / **`dry_run_chain`**；**`reconcile --event-log-scope-dry-run --ops-artifact`** → **Epic D-07** / **`dry_run_event_log`**；**`reconcile --correction-executor-scope-dry-run --ops-artifact`** → **Epic D-08** / **`dry_run_correction_executor`**（**禁** **`--persist`** / **execute**；**`--chain-scope-dry-run` / `--event-log-scope-dry-run` / `--correction-executor-scope-dry-run`** 不得互叠）；**`probe --ops-artifact`** → **Epic D-09** / **`artifact_type:probe`**；**`status --live-reconcile`** 单独 → 裸 **`?live_reconcile=1`**）、**`probe`**（调用 **`indexer-reconcile-probe.sh`**）、**`recover`**（调用 **`indexer-reorg-recovery.sh`**）、**`evidence`** / **`evidence-bundle`**（**`write-indexer-evidence.sh`**；可选 **`--skip-internal-reconcile`**/**`--with-indexer-tick`**；bundle 含 manifest + zip）（须非公网）；`reconcile` 可选 **`--include-chain-tip`**（**`include_chain_tip`** / **`chain_observation`**，见 **110**）、**`--include-event-log-escrow-coverage`**（**`event_log_escrow_coverage`** / **`110-EVENT-LOG-ESCROW-COVERAGE`**）；`API_BASE_URL`、`INTERNAL_API_SECRET` 与 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读) 一致。例：`./scripts/internal-indexer-ops.sh status`、`./scripts/internal-indexer-ops.sh probe`、`./scripts/internal-indexer-ops.sh recover status`、`./scripts/internal-indexer-ops.sh evidence`、`./scripts/internal-indexer-ops.sh evidence-bundle`、`./scripts/internal-indexer-ops.sh reconcile --persist --rpc 3`、`./scripts/internal-indexer-ops.sh reconcile --include-chain-tip`、`./scripts/internal-indexer-ops.sh reconcile --include-event-log-escrow-coverage`。 |
| **internal-indexer-ops.ps1** | Windows | **evidence** / **evidence-bundle** → **`write-indexer-evidence.ps1`**（可选 **`--skip-internal-reconcile`**/**`--with-indexer-tick`**）；**tick / replay / reconcile / status / probe / recover** → **`bash scripts/internal-indexer-ops.sh`**（须 **Git Bash**）。例：`.\scripts\internal-indexer-ops.ps1 evidence`。 |
| **indexer-reconcile-probe.sh** | Linux/Mac/Git Bash | **`GET …/internal/indexer-status?live_reconcile=1`**；**退出码 0** 当且仅当 **`projection_reconcile_clean`**（须 **jq**、**`INTERNAL_API_SECRET`**）；**`--ops-artifact`** → **Epic D-09** **`traveltrust.ops_artifact.v1`** **`probe`**；供 cron / 值班；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)。 |
| **indexer-reconcile-probe.ps1** | Windows | 委托 **`bash scripts/indexer-reconcile-probe.sh`**（须 **Git Bash**）。 |
| **orders-deadline-ssot-ops-check.sh** | Linux/Mac/Git Bash | **`GET …/admin/observability/overview`**；校验 **`overview.orders_deadline_ssot_ops_check`** 之 **`overall`****/**`exit_code_hint`**（须 **jq**、**`ADMIN_BEARER_TOKEN`**）；与 **internal** 对账探针正交；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[04 §3.4](../docs/spec/04-后端与API.md)。**可选 CI**：**`.github/workflows/orders-deadline-ssot-ops-staging.yml`**（**`Orders deadline SSOT ops (staging)`**）仅在配置了 **`STAGING_ORDERS_DEADLINE_OPS_ADMIN_BEARER_TOKEN`** + **`STAGING_ORDERS_DEADLINE_OPS_API_BASE_URL`** 时跑脚本；无 token **跳过**；**不**进入默认 **Build** PR 流程。 |
| **auth-audit-alerts.sh** | Linux/Mac/Git Bash | 认证审计告警快检：统计 **`auth_audit_events`** 近窗口内 **`auth_session_invalidated_401`** / **`password_reset_requested`** / 同 IP 高频；阈值由 **`AUTH_AUDIT_ALERT_*`** 控制（默认 15 分钟、50/40/12），超阈值 **exit 1**。须 **`DATABASE_URL`** + **`psql`**。可选通知：配置 **`AUTH_AUDIT_ALERT_WEBHOOK_URL`** 后，超阈值时 POST JSON 到 webhook（超时由 **`AUTH_AUDIT_ALERT_WEBHOOK_TIMEOUT_SECS`** 控制，默认 5 秒，发送失败仅 WARN 不改变脚本退出码）。可选 CI：**`.github/workflows/auth-audit-alerts-staging.yml`**（每 15 分钟 + 手动触发；仅配置 **`STAGING_AUTH_AUDIT_ALERTS_DATABASE_URL`** 时执行，否则跳过，不挂默认 Build）。 |
| **auth-audit-retention.sh** | Linux/Mac/Git Bash | 认证审计保留清理：按 **`AUTH_AUDIT_RETENTION_DAYS`**（默认 90）统计并删除历史 `auth_audit_events`；**`AUTH_AUDIT_RETENTION_DRY_RUN=1`** 时仅统计不删除。须 **`DATABASE_URL`** + **`psql`**。可选 CI：**`.github/workflows/auth-audit-retention-staging.yml`**（每日定时 + 手动触发；仅配置 **`STAGING_AUTH_AUDIT_RETENTION_DATABASE_URL`** 时执行，否则跳过）。 |
| **user-security-notifications-dispatch.sh** | Linux/Mac/Git Bash | 用户安全通知派发占位器：读取 `user_security_notifications` 的 `pending` 行并批量流转状态（默认 `pending -> sent`，`attempts+1`；可用 **`USER_SECURITY_NOTIFICATION_DISPATCH_FORCE_FAIL=1`** 演练 `pending -> failed` + `last_error`）。支持 **`USER_SECURITY_NOTIFICATION_DISPATCH_DRY_RUN=1`** 仅预览。须 **`DATABASE_URL`** + **`psql`**。可选 CI：**`.github/workflows/user-security-notifications-dispatch-staging.yml`**（每 10 分钟 + 手动触发；仅配置 **`STAGING_USER_SECURITY_NOTIFICATIONS_DATABASE_URL`** 时执行，否则跳过）。 |
| **user-security-notifications-retry.sh** | Linux/Mac/Git Bash | 用户安全通知失败重试：将 `failed` 且 `attempts < USER_SECURITY_NOTIFICATION_RETRY_MAX_ATTEMPTS` 的记录批量重置为 `pending`，供下轮派发重试。支持 **`USER_SECURITY_NOTIFICATION_RETRY_DRY_RUN=1`**。须 **`DATABASE_URL`** + **`psql`**。可选 CI：**`.github/workflows/user-security-notifications-retry-staging.yml`**（每 20 分钟 + 手动触发；仅配置 **`STAGING_USER_SECURITY_NOTIFICATIONS_RETRY_DATABASE_URL`** 时执行，否则跳过）。 |
| **user-security-notifications-retention.sh** | Linux/Mac/Git Bash | 用户安全通知保留清理：按 **`USER_SECURITY_NOTIFICATION_RETENTION_DAYS`**（默认 180）统计并删除历史 `user_security_notifications`；**`USER_SECURITY_NOTIFICATION_RETENTION_DRY_RUN=1`** 时仅统计不删除。须 **`DATABASE_URL`** + **`psql`**。可选 CI：**`.github/workflows/user-security-notifications-retention-staging.yml`**（每日定时 + 手动触发；仅配置 **`STAGING_USER_SECURITY_NOTIFICATIONS_RETENTION_DATABASE_URL`** 时执行，否则跳过）。 |
| **orders-deadline-ssot-ops-check.ps1** | Windows | 委托 **`bash scripts/orders-deadline-ssot-ops-check.sh`**（须 **Git Bash**）。 |
| **governance-governor-view-params-ssot-ops-check.sh** | Linux/Mac/Git Bash | **`GET …/admin/observability/overview`**；校验 **`overview.governor_view_params_ssot_ops_check`**（**TT-B110-SEQ5**；须 **jq**、**`ADMIN_BEARER_TOKEN`**）；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[04 §3.4](../docs/spec/04-后端与API.md)。 |
| **governance-governor-view-params-ssot-ops-check.ps1** | Windows | 委托 **`bash scripts/governance-governor-view-params-ssot-ops-check.sh`**（须 **Git Bash**）。 |
| **governance-timelock-delay-ssot-ops-check.sh** | Linux/Mac/Git Bash | **`GET …/admin/observability/overview`**；校验 **`overview.timelock_delay_ssot_ops_check`**（**TT-B110-SEQ6**；须 **jq**、**`ADMIN_BEARER_TOKEN`**）；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[04 §3.4](../docs/spec/04-后端与API.md)。 |
| **governance-timelock-delay-ssot-ops-check.ps1** | Windows | 委托 **`bash scripts/governance-timelock-delay-ssot-ops-check.sh`**（须 **Git Bash**）。 |
| **governance-governor-proposal-count-ssot-ops-check.sh** | Linux/Mac/Git Bash | **`GET …/admin/observability/overview`**；校验 **`overview.governor_proposal_count_ssot_ops_check`**（**TT-B110-SEQ10**；须 **jq**、**`ADMIN_BEARER_TOKEN`**）；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[04 §3.4](../docs/spec/04-后端与API.md)。 |
| **governance-governor-proposal-count-ssot-ops-check.ps1** | Windows | 委托 **`bash scripts/governance-governor-proposal-count-ssot-ops-check.sh`**（须 **Git Bash**）。 |
| **ops/b381-governance-pool-drift-reconcile-admin-overview-smoke.sh** | Linux/Mac/Git Bash | **`POST …/internal/indexer-reconcile`**（**`persist:true`** + **`include_governance_pool_db_vs_chain_balance_drift_observability:true`**）与 **`GET …/admin/observability/overview`** **`overview.governance_pool_db_vs_chain_balance_drift_observability`** **JSON 深相等**（**TT-B382** / **B-382**；须 **`INTERNAL_API_SECRET`**、**`ADMIN_BEARER_TOKEN`**、**`jq`**）；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[`docs/runbook/TT-B382-GOVERNANCE-POOL-DRIFT-REAL-LINK-VERIFY-001.md`](../docs/runbook/TT-B382-GOVERNANCE-POOL-DRIFT-REAL-LINK-VERIFY-001.md)。 |
| **ops/b430-gov-post-exec-reconcile-overview-bundle.sh** | Linux/Mac/Git Bash | **单** **`POST …/internal/indexer-reconcile`**（**`persist:true`** **+** **B-381/B-383/B-384** **三** **`include_*`**；**B-415** **无** **单独** **开关**）再 **`GET …/admin/observability/overview`**；**四** **键** **reconcile** **根级** **与** **`overview.*`** **JSON** **深相等** **+** **B-381** **根** **与** **B-415** **内嵌** **`governance_pool_db_vs_chain_balance`** **自洽**（**TT-B430** / **B-430**；须 **`INTERNAL_API_SECRET`**、**`ADMIN_BEARER_TOKEN`**、**`jq`**；可选 **`B430_OUT_DIR`**、**`B430_WRITE_CLOSEOUT_PACK=1`**、**`B430_POST_INDEXER_TICK=1`**、**`B430_PROPOSAL_ID`**、**`B430_FETCH_PUBLIC_POOL`**）；落盘 **`b430-closeout-record.json`** **等** **见** **Runbook** **§3.1**；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[`docs/runbook/TT-B430-GOV-POST-EXEC-RECONCILE-OVERVIEW-BUNDLE-001.md`](../docs/runbook/TT-B430-GOV-POST-EXEC-RECONCILE-OVERVIEW-BUNDLE-001.md)、[`evidence/README.md`](../evidence/README.md#b430-gov-post-exec-reconcile-overview)。 |
| **ops/b431-gov-execute-foundry-closeout.sh** | Linux/Mac/Git Bash | **B-431** **Foundry SSOT**：**`forge test --match-test test_B431_governor_execute_chain_reads_match_payload_and_timelock_operation`**；**tee** **`forge_b431.log`** **+** **`b431-closeout-record.json`** **（** **默认** **`evidence/b431_gov_execute_chain_read/run_<UTC>/`** **）** **；** **不** **替代** **B-417** **真链** **。** **见** [`docs/runbook/TT-B431-GOV-EXECUTE-CHAIN-READ-PAYLOAD-ALIGN-001.md`](../docs/runbook/TT-B431-GOV-EXECUTE-CHAIN-READ-PAYLOAD-ALIGN-001.md)、[`docs/verification-evidence/B-431-gov-execute-chain-read-payload-align-ENTRY.md`](../docs/verification-evidence/B-431-gov-execute-chain-read-payload-align-ENTRY.md)、[`evidence/README.md`](../evidence/README.md#b431-gov-execute-chain-read-payload-align)。 |
| **ops/b409-order-state-primary-acceptance.sh** | Linux/Mac/Git Bash | **`cargo test -p traveltrust-api`**：**`tt_b409_chain_off_http_order_created_accepted_escrowed`** + **`p21_order_create_accept_mock_pay_confirm`**（**TT-B409** / **B-409** 主成功链；须 **Rust** **工具链**）；见 [`docs/runbook/TT-B409-ORDER-STATE-MACHINE-CHAIN-OFF-53-001.md`](../docs/runbook/TT-B409-ORDER-STATE-MACHINE-CHAIN-OFF-53-001.md) **§2.2**。 |
| **ops/b409-order-state-exception-acceptance.sh** | Linux/Mac/Git Bash | **`cargo test -p traveltrust-api`**：**`p21_order_cancel_created`**（**TT-B409** 异常链 **`created→cancelled`**；可扩支付超时等）；同上 Runbook。 |
| **ops/b410-user-flow-e2e-gate.sh** | Linux/Mac/Git Bash | **B-410**：串联 **`b409-order-state-primary-acceptance.sh`** + **`b409-order-state-exception-acceptance.sh`**；可选 **`B410_RUN_PLAYWRIGHT=1`** 时 **`frontend`** **`npm run e2e`**；见 [`docs/runbook/TT-B410-USER-FLOW-E2E-ORDER-STATE-UNIFIED-001.md`](../docs/runbook/TT-B410-USER-FLOW-E2E-ORDER-STATE-UNIFIED-001.md)。 |
| **ops/b383-fee-router-platform-fee-routed-log-count-reconcile-admin-overview-smoke.sh** | Linux/Mac/Git Bash | **`POST …/internal/indexer-reconcile`**（**`persist:true`** + **`include_fee_router_platform_fee_routed_log_count_chain_vs_db_observability:true`**）与 **`GET …/admin/observability/overview`** **`overview.fee_router_platform_fee_routed_log_count_chain_vs_db_observability`** **JSON 深相等**（**TT-B383** / **B-383**；须 **`INTERNAL_API_SECRET`**、**`ADMIN_BEARER_TOKEN`**、**`jq`**）；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[`docs/runbook/TT-B383-FEE-ROUTER-PLATFORM-FEE-ROUTED-LOG-COUNT-CHAIN-VS-DB-VERIFY-001.md`](../docs/runbook/TT-B383-FEE-ROUTER-PLATFORM-FEE-ROUTED-LOG-COUNT-CHAIN-VS-DB-VERIFY-001.md)。 |
| **ops/b416-fee-router-write-path-b415-preflight.sh** | Linux/Mac/Git Bash | **B-416 L0**：**`POST …/internal/indexer-reconcile`**（**`persist:false`**）与 **`GET …/admin/observability/overview`** **`overview.fee_router_governance_fact_stream_observability`** **JSON 深相等**（**B-415** **锚** **`415-FEE-ROUTER-GOVERNANCE-FACT-STREAM-OBS-V1`**；**链上** **写** **前** **门禁**）；可选 **`B416_REQUIRE_DISTRIBUTE_UNPAUSED=1`**；须 **`INTERNAL_API_SECRET`**、**`ADMIN_BEARER_TOKEN`**、**`jq`**；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[`docs/runbook/TT-B416-FEE-ROUTER-WRITE-PATH-TESTNET-ADMIN-001.md`](../docs/runbook/TT-B416-FEE-ROUTER-WRITE-PATH-TESTNET-ADMIN-001.md)。 |
| **ops/b416-print-distribute-cast-template.sh** | Linux/Mac/Git Bash | **B-416 L1 辅助**：打印 **`cast send`** / **`cast calldata`** **`distribute(address,uint256)`** **模板**（**不** **执行**）；须 **`FEE_ROUTER_ADDRESS`** **或** **`B416_FEE_ROUTER`**、**`B416_TOKEN`**、**`B416_AMOUNT_WEI`**；见 [`docs/runbook/TT-B416-FEE-ROUTER-WRITE-PATH-TESTNET-ADMIN-001.md`](../docs/runbook/TT-B416-FEE-ROUTER-WRITE-PATH-TESTNET-ADMIN-001.md) **§6**。 |
| **ops/b416-testnet-closeout-evidence.sh** | Linux/Mac/Git Bash | **B-416 封口** **：** **L0** **预检** **+** **L1** **清单** **attestation** **+** **L2** **`b407-exec`** **或** **手工** **tx** **hash** **+** **可选** **tick** **/** **b383** **/** **b402** **→** **`b416-closeout-record.json`** **；** **见** [`evidence/b416_fee_router_write_path_testnet/README.md`](../evidence/b416_fee_router_write_path_testnet/README.md) **、** [Runbook §8](../docs/runbook/TT-B416-FEE-ROUTER-WRITE-PATH-TESTNET-ADMIN-001.md) **。** |
| **ops/b417-governance-execution-automation.sh** | Linux/Mac/Git Bash | **B-417**：**queue→execute→`b417-governance-execution-report.json`→validate**；**`B417_CHAIN_MODE=1`** **时** **默认** **hook** **`b417-governor-queue-testnet.sh`** **/** **`b417-governor-execute-testnet.sh`**（**`cast send`** **TravelTrustGovernor** **queue/execute**）；**须** **`CHAIN_RPC_URL`**、**`GOVERNOR_ADDRESS`**、**`B417_PROPOSAL_ID`**、**`B417_PRIVATE_KEY`**；**`B417_CHAIN_MODE=1`** **时** **默认** **先** **`b417-sepolia-preflight.sh`**（**`B417_SKIP_PREFLIGHT`** **/** **`B417_NO_AUTOLOAD_ENV`** **可覆写**）；**证据目录** **见** [`evidence/b417_governance_execution_runs/README.md`](../evidence/b417_governance_execution_runs/README.md) **。** |
| **ops/b417-sepolia-preflight.sh** | Linux/Mac/Git Bash | **B-417**：**Sepolia** **前置** — **四变量非空**、**可选** **`CHAIN_ID`** **与** **RPC** **对拍**、**默认** **`state==4`**、**执行地址余额非 0**；**`B417_ALLOW_QUEUED_PREFLIGHT=1`** **时** **接受** **`state==4`** **或** **`5`** **（** **已** **queue** **复跑** **）** **；** **exit** **2/3/4/5/12** **见脚本头**；**主脚本** **`B417_CHAIN_MODE=1`** **默认调用**。 |
| **ops/b417-list-proposal-states.sh** | Linux/Mac/Git Bash | **B-417** **辅助**：**仅** **`CHAIN_RPC_URL`** **+** **`GOVERNOR_ADDRESS`** **枚举** **`proposalCount`** **与各** **`state()`** **（** **无** **私钥** **）** **，** **便于选** **`B417_PROPOSAL_ID`** **（** **Succeeded=4** **）** **。** |
| **ops/b417-sepolia-propose-vote-succeeded.sh** | Linux/Mac/Git Bash | **B-417** **Sepolia** **首个提案**：**`GovernanceVotesToken`（TTG）** **无** **`delegate`** **（默认跳过自委托）**；**`B417_DELEGATE_CAST=1`** **时** **`cast`** **`delegate(self)`** **（** **ERC20Votes** **外链** **）**；**`SepoliaProposeMinimal`** **→** **`SepoliaCastVote`** **→** **`state()`**；**须** **`GOVERNANCE_TOKEN_ADDRESS`** **（** **与** **API** **/** **`.env.example`** **七键** **同源** **）** **或** **旧名** **`GOVERNANCE_VOTES_TOKEN_ADDRESS`** **+** **gas** **私钥**；**`SepoliaProposeMinimal`** **内** **已** **`GOVERNANCE_TOKEN_ADDRESS`** **优先** **。** **成功后** **`b417-list-proposal-states.sh`** **选** **state=4** **填** **`B417_PROPOSAL_ID`** **再跑** **gap-check** **/** **preflight** **/** **`b417-run-onchain-evidence`** **。** |
| **ops/b417-sepolia-treasury-spend-propose-vote-succeeded.sh** | Linux/Mac/Git Bash | **Treasury.spend / spendETH** **：** **`SepoliaProposeTreasurySpend`** **→** **`SepoliaCastVote`** **→** **`state=4`**；**须** **`TREASURY_ADDRESS`**、**`TREASURY_SPEND_TO`**、**`TREASURY_SPEND_AMOUNT`**、**`GOVERNOR_ADDRESS`**、**`CHAIN_RPC_URL`**、**`PRIVATE_KEY`**；ERC20 时 **`GOVERNANCE_TOKEN_ADDRESS`**；可选 **`TREASURY_SPEND_MODE=ETH`**、**`TREASURY_SPEND_SKIP_PREFUND=1`**。见 [`docs/runbook/TT-TREASURY-SPEND-MINI-EVIDENCE-001.md`](../docs/runbook/TT-TREASURY-SPEND-MINI-EVIDENCE-001.md)。 |
| **ops/b417-run-onchain-evidence.sh** | Linux/Mac/Git Bash | **B-417**：**`B417_CHAIN_MODE=1`** **+** **默认** **`evidence/b417_governance_execution_runs/run_<UTC>/`** **，** **调用** **`b417-governance-execution-automation.sh`** **（** **内含** **preflight** **）** **；** **须** **`.env`** **已填** **Governor** **/** **Succeeded** **提案** **/** **gas** **私钥** **。** |
| **ops/b417-env-gap-check.sh** | Linux/Mac/Git Bash | **B-417** **企业** **：** **`.env`** **必填键** **+** **cast** **/** **jq** **/** **Python** **；** **未设** **`GOVERNANCE_TOKEN_ADDRESS`** **（** **TTG** **）** **时** **[WARN]** **（** **非** **Governor** **）** **；** **不** **打印私钥** **、** **不** **发交易** **；** **可选** **`B417_GAP_CHAIN_PROBE=1`** **只读** **RPC** **。** |
| **ops/ttg-econ-align-read.sh** | Linux/Mac/Git Bash | **TT-GOV-TOKEN-ECON-ALIGN-001** **§3**：**纯 `cast`** **只读** **TTG** **经济快照** **`chain_reads.json`** **+** **`compare.md`** **→** **`evidence/ttg_econ_align/run_<UTC>/`** **（** **不** **依赖** **API** **/** **`/meta`** **）** **；** **见** [`docs/runbook/TT-GOV-TOKEN-ECON-ALIGN-001.md`](../docs/runbook/TT-GOV-TOKEN-ECON-ALIGN-001.md) **。** |
| **ops/ttg-econ-align-compare.sh** | Linux/Mac/Git Bash | **§3.9 N2**：**`chain_reads.json`** **锚点** **下** **`GET /meta?ttg_econ_anchor_block=…`** **对拍** **`total_supply` / `treasury_balance`** **（** **可选** **`/governance/pool`** **、** **`governance_pool` DB** **）** **→** **`api_db_compare.json`** **；** **与** **`GET …/internal/ttg-econ-anchor`** **同投影** **；** **须** **`curl`** **+** **`jq`** **；** **见** [`docs/runbook/TT-GOV-TOKEN-ECON-ALIGN-001.md`](../docs/runbook/TT-GOV-TOKEN-ECON-ALIGN-001.md) **§3.9** **。** |
| **ops/ttg-transfer-team-15pct-single-address.example.sh** | Linux/Mac/Git Bash | **TTG** **测试** **：** **`cast`** **`totalSupply`** **→** **15%** **`transfer`** **到** **`TTG_TEAM_ALLOCATION_RECIPIENT`** **（** **默认** **单地址** **承接** **团队桶** **）** **；** **须** **`PRIVATE_KEY`** **（** **部署者** **）** **。** **见** **[`docs/spec/82-治理币-文档总览.md`](../docs/spec/82-治理币-文档总览.md)** **§三之二** **。** |
| **ops/runtime-chain-ssot-cast-verify.sh** | Linux/Mac/Git Bash | **Runtime Chain SSOT** **：** **只读** **`cast`** **核验** **`.env`** **中** **Governor** **/** **Timelock** **/** **治理币** **/** **FeeRouter.owner** **与** **链上** **接线** **是否** **一致** **（** **`CHAIN_RPC_URL`** **）** **；** **不** **替代** **Explorer** **对** **「** **最新** **部署** **」** **的** **人工** **确认** **。** **见** **[`evidence/GO_FINAL_20260416/RUNTIME_CHAIN_SSOT_CHECKLIST.md`](../evidence/GO_FINAL_20260416/RUNTIME_CHAIN_SSOT_CHECKLIST.md)** **§5** **。** |
| **ops/b435-sepolia-stake-first-payment.example.sh** | Linux/Mac/Git Bash | **TT-B435 §3.3**：自动 **`source`** **仓库根** **`.env`**；**`cast send --async`** **`approve`** **+** **`stake`**；若存在 **`evidence/b435_fullstack_fund_testnet_closeout/run_20260416T122500Z/tx_hashes.json`** **且** **已装** **`jq`**，**自动** **写入** **`first_payment`**（**或** **`B435_TX_HASHES_JSON=`** **指向** **JSON**）。**须** **`PRIVATE_KEY`** **/** **`CHAIN_RPC_URL`** **/** **`GUIDE_STAKING_ADDRESS`** **等**；**勿** **泄露** **私钥**。**见** [`docs/runbook/TT-B435-FULLSTACK-FUND-TESTNET-RELEASE-CHAIN-001.md`](../docs/runbook/TT-B435-FULLSTACK-FUND-TESTNET-RELEASE-CHAIN-001.md) **§3.3** **、** [`evidence/b435_fullstack_fund_testnet_closeout/README.md`](../evidence/b435_fullstack_fund_testnet_closeout/README.md) **。** |
| **ops/b435-merge-first-payment-tx.example.sh** | Linux/Mac/Git Bash | **TT-B435 §3.3** **（** **替代** **stake** **）** **：** **`B435_FIRST_PAYMENT_TX=0x…`** **合并** **进** **`tx_hashes.json`** **→** **`first_payment`** **（** **托管** **/** **FeeRouter** **等** **真实** **tx** **）** **。** |
| **ops/b435-seal-run-bundle.example.sh** | Linux/Mac/Git Bash | **TT-B435** **封口** **：** **串联** **merge** **`first_payment`** **+** **`b435-evidence-internal-curls`** **（** **须** **`B435_FIRST_PAYMENT_TX`** **+** **admin** **/** **mint** **条件** **）** **。** |
| **ops/tt-testnet-fullstack-new-run-dir.sh** | Linux/Mac/Git Bash | **TT-TESTNET** **：** **新建** **`evidence/b435_fullstack_fund_testnet_closeout/run_<UTC>/`** **（** **`README.md`** **+** **`tx_hashes.json`** **模板** **）** **；** **可选** **`TT_TESTNET_RUN_ID=run_…Z`** **。** |
| **ops/tt-testnet-fullstack-seal.sh** | Linux/Mac/Git Bash | **TT-TESTNET** **/** **TT-B435** **：** **`merge`** **`first_payment`** **+** **`runtime-chain-ssot-cast-verify`** **→** **`ssot.txt`** **+** **`b435-evidence-internal-curls`** **+** **可选** **复制** **`broadcast/`** **；** **91** **：** **`GET /api/v1/meta`** **→** **`meta.json`** **；** **可选** **`fee_router_ops_snapshot.json`** **（** **`FEE_ROUTER_ADDRESS`** **）** **；** **`evidence_sha256_manifest.json`** **+** **`.sha256`** **（** **`TT_TESTNET_SKIP_*`** **可跳过** **）** **。** |
| **ops/evidence-run-sha256-manifest.sh** / **`evidence_run_sha256_manifest.py`** | Linux/Mac/Git Bash + Python 3 | **91 §8.2**：对 **`run_<UTC>/`** **生成** **/** **校验** **`evidence_sha256_manifest.json`** **（** **全文件** **SHA256** **）** **。** |
| **gates/check-evidence-run-insurance-gate.sh** | Linux/Mac/Git Bash | **91**：**`verify`** **manifest** **；** **若** **`meta.json`** **存在** **则** **`jq`** **比对** **`.chain.chain_id`** **与** **`CHAIN_ID`** **（** **`CHECK_EVIDENCE_RUN_STRICT=1`** **强制** **manifest** **）** **。** |
| **gates/check-auth-email-resend-gate.sh** / **gates/check-auth-email-resend-gate.ps1** | Linux/Mac/Git Bash / **Windows PowerShell** | **`TRAVELTRUST_EMAIL_TRANSPORT=resend`** 时须 **`TRAVELTRUST_RESEND_API_KEY`** + **`TRAVELTRUST_RESEND_FROM`**（可选 **source** 根 **`.env`**）；否则 **exit 0** **skip**。**`.ps1`** 与 **`.sh`** 同规则，供 **`start-api-with-seed.bat`** **Step 1d**（避免 **`bash.exe`** 走 **WSL** 占位失败）。**Production gate** **`verify-gate-config`**、**`run-production-gate-local.sh`**、**`enterprise-preflight.sh`** 仍串联 **`.sh`**；根 **`scripts/check-auth-email-resend-gate.sh`** 薄转发。 |
| **gates/check-auth-email-repo-doc-contract.sh** | Linux/Mac/Git Bash | **`cargo test -p traveltrust-api`** 子集：锁定 **`.env.example` / `.env.preprod.local.example`** 与 **`read_email_transport`（`smtp`→Off）**、**`TRAVELTRUST_RESEND_FROM`≠`TRAVELTRUST_EMAIL_FROM`**；不触网。根 **`scripts/check-auth-email-repo-doc-contract.sh`** 薄转发。 |
| **ops/fee-router-ops-snapshot.sh** | Linux/Mac/Git Bash + **cast** | **91 §八**：只读 **`distributePaused`** **/** **`owner`** **→** **`fee_router_ops_snapshot.json`** **。** |
| **ops/b435-evidence-internal-curls.example.sh** | Linux/Mac/Git Bash | **TT-B435 §3.4**：**自动** **`source`** **根** **`.env`**；**`INTERNAL_API_SECRET`** **+** **`ADMIN_BEARER_TOKEN`** **（或** **`B435_AUTO_ADMIN_BEARER_MINT=1`** **且** **API** **`TRAVELTRUST_TESTNET_ADMIN_BEARER_MINT=1`** **走** **mint）**；写 **`indexer_tick.json`** **/** **`reconcile.json`** **/** **`overview.json`** **到** **`B435_EVIDENCE_RUN_DIR`** **。** |
| **ops/b417-evidence-pack-verify.sh** | Linux/Mac/Git Bash | **B-417** **企业** **：** **某次** **`run_*`** **目录** **链上** **GO** **证据包** **（** **侧车** **+** **report** **+** **schema** **）** **合规** **；** **stub** **路径** **预期** **FAIL** **。** |
| **ops/b384-region-vault-forwarded-log-count-reconcile-admin-overview-smoke.sh** | Linux/Mac/Git Bash | **`POST …/internal/indexer-reconcile`**（**`persist:true`** + **`include_region_vault_forwarded_log_count_chain_vs_db_observability:true`**）与 **`GET …/admin/observability/overview`** **`overview.region_vault_forwarded_log_count_chain_vs_db_observability`** **JSON 深相等**（**TT-B384** / **B-384**；须 **`INTERNAL_API_SECRET`**、**`ADMIN_BEARER_TOKEN`**、**`jq`**）；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[`docs/runbook/TT-B384-REGION-VAULT-FORWARDED-LOG-COUNT-CHAIN-VS-DB-VERIFY-001.md`](../docs/runbook/TT-B384-REGION-VAULT-FORWARDED-LOG-COUNT-CHAIN-VS-DB-VERIFY-001.md)。 |
| **ops/b385-p5-country-ledger-credited-log-count-reconcile-admin-overview-smoke.sh** | Linux/Mac/Git Bash | **`POST …/internal/indexer-reconcile`**（**`persist:true`** + **`include_p5_country_ledger_credited_log_count_chain_vs_db_observability:true`**）与 **`GET …/admin/observability/overview`** **`overview.p5_country_ledger_credited_log_count_chain_vs_db_observability`** **JSON 深相等**（**TT-B385** / **B-385**；须 **`INTERNAL_API_SECRET`**、**`ADMIN_BEARER_TOKEN`**、**`jq`**）；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[`docs/runbook/TT-B385-P5-COUNTRY-LEDGER-CREDITED-LOG-COUNT-CHAIN-VS-DB-VERIFY-001.md`](../docs/runbook/TT-B385-P5-COUNTRY-LEDGER-CREDITED-LOG-COUNT-CHAIN-VS-DB-VERIFY-001.md)。 |
| **ops/b386-revenue-pipeline-log-count-bundle-reconcile-admin-overview-smoke.sh** | Linux/Mac/Git Bash | **`POST …/internal/indexer-reconcile`**（**`persist:true`** + **`include_revenue_pipeline_log_count_chain_vs_db_bundle_observability:true`**）与 **`GET …/admin/observability/overview`** **`overview.revenue_pipeline_log_count_chain_vs_db_bundle_observability`** **JSON 深相等**（**TT-B386** / **B-386**；须 **`INTERNAL_API_SECRET`**、**`ADMIN_BEARER_TOKEN`**、**`jq`**）；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[`docs/runbook/TT-B386-REVENUE-PIPELINE-LOG-COUNT-BUNDLE-CHAIN-VS-DB-VERIFY-001.md`](../docs/runbook/TT-B386-REVENUE-PIPELINE-LOG-COUNT-BUNDLE-CHAIN-VS-DB-VERIFY-001.md)。 |
| **ops/b387-revenue-pipeline-bundle-cross-snapshot-reconcile-admin-overview-smoke.sh** | Linux/Mac/Git Bash | **两步** **`POST …/internal/indexer-reconcile`**（先 **B-386** **bundle** **`persist`**，再 **`persist:true`** + **`include_revenue_pipeline_bundle_cross_snapshot_observability:true`**）与 **`GET …/admin/observability/overview`** **`overview.revenue_pipeline_bundle_cross_snapshot_drift_observability`** **JSON 深相等**（**TT-B387** / **B-387**；须 **`INTERNAL_API_SECRET`**、**`ADMIN_BEARER_TOKEN`**、**`jq`**）；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[`docs/runbook/TT-B387-REVENUE-PIPELINE-BUNDLE-CROSS-SNAPSHOT-DRIFT-VERIFY-001.md`](../docs/runbook/TT-B387-REVENUE-PIPELINE-BUNDLE-CROSS-SNAPSHOT-DRIFT-VERIFY-001.md)。 |
| **ops/b388-revenue-pipeline-cross-snapshot-history-trend-reconcile-admin-overview-smoke.sh** | Linux/Mac/Git Bash | **三步** **`POST …/internal/indexer-reconcile`**（**B-386** **bundle** **`persist`** → **B-387** **`cross`** → **`persist:true`** + **`include_revenue_pipeline_bundle_cross_snapshot_observability:true`** + **`include_revenue_pipeline_cross_snapshot_history_trend_observability:true`**）与 **`GET …/admin/observability/overview`** **`overview.revenue_pipeline_cross_snapshot_history_trend_observability`** **JSON 深相等**（**TT-B388** / **B-388**；须 **`INTERNAL_API_SECRET`**、**`ADMIN_BEARER_TOKEN`**、**`jq`**）；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[`docs/runbook/TT-B388-REVENUE-PIPELINE-CROSS-SNAPSHOT-HISTORY-TREND-OBS-001.md`](../docs/runbook/TT-B388-REVENUE-PIPELINE-CROSS-SNAPSHOT-HISTORY-TREND-OBS-001.md)。 |
| **ops/b389-revenue-pipeline-latest-persist-freshness-reconcile-admin-overview-smoke.sh** | Linux/Mac/Git Bash | **`POST …/internal/indexer-reconcile`**（**`persist:true`** + **`include_revenue_pipeline_latest_persist_freshness_observability:true`**）与 **`GET …/admin/observability/overview`** **`overview.revenue_pipeline_latest_persist_freshness_observability`** **JSON 深相等**（**TT-B389** / **B-389**；**DB-only** **新鲜度**；须 **`INTERNAL_API_SECRET`**、**`ADMIN_BEARER_TOKEN`**、**`jq`**）；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[`docs/runbook/TT-B389-REVENUE-PIPELINE-LATEST-PERSIST-FRESHNESS-OBS-001.md`](../docs/runbook/TT-B389-REVENUE-PIPELINE-LATEST-PERSIST-FRESHNESS-OBS-001.md)。 |
| **ops/b390-revenue-pipeline-freshness-drift-suspect-reconcile-admin-overview-smoke.sh** | Linux/Mac/Git Bash | **`POST …/internal/indexer-reconcile`**（**`persist:true`** + **`include_revenue_pipeline_log_count_chain_vs_db_bundle_observability:true`** + **`include_revenue_pipeline_freshness_drift_suspect_observability:true`**）与 **`GET …/admin/observability/overview`** **`overview.revenue_pipeline_freshness_drift_suspect_observability`** **JSON 深相等**（**TT-B390** / **B-390**；**freshness** **→** **suspect**；须 **`INTERNAL_API_SECRET`**、**`ADMIN_BEARER_TOKEN`**、**`jq`**）；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[`docs/runbook/TT-B390-REVENUE-PIPELINE-FRESHNESS-DRIFT-SUSPECT-OBS-001.md`](../docs/runbook/TT-B390-REVENUE-PIPELINE-FRESHNESS-DRIFT-SUSPECT-OBS-001.md)。 |
| **ops/b391-revenue-pipeline-union-max-projection-block-vs-indexer-checkpoint-reconcile-admin-overview-smoke.sh** | Linux/Mac/Git Bash | **`POST …/internal/indexer-reconcile`**（**`persist:true`** + **`include_revenue_pipeline_union_max_projection_block_vs_indexer_checkpoint_observability:true`**）与 **`GET …/admin/observability/overview`** **`overview.revenue_pipeline_union_max_projection_block_vs_indexer_checkpoint_observability`** **JSON 深相等**（**TT-B391** / **B-391**；须 **`INTERNAL_API_SECRET`**、**`ADMIN_BEARER_TOKEN`**、**`jq`**）；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[`docs/runbook/TT-B391-REVENUE-PIPELINE-UNION-MAX-PROJECTION-BLOCK-VS-INDEXER-CHECKPOINT-OBS-001.md`](../docs/runbook/TT-B391-REVENUE-PIPELINE-UNION-MAX-PROJECTION-BLOCK-VS-INDEXER-CHECKPOINT-OBS-001.md)。 |
| **ops/b392-revenue-pipeline-union-max-vs-event-log-max-block-tail-coverage-reconcile-admin-overview-smoke.sh** | Linux/Mac/Git Bash | **`POST …/internal/indexer-reconcile`**（**`persist:true`** + **`include_revenue_pipeline_union_max_vs_event_log_max_block_tail_coverage_observability:true`**）与 **`GET …/admin/observability/overview`** **`overview.revenue_pipeline_union_max_vs_event_log_max_block_tail_coverage_observability`** **JSON 深相等**（**TT-B392** / **B-392**；须 **`INTERNAL_API_SECRET`**、**`ADMIN_BEARER_TOKEN`**、**`jq`**）；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[`docs/runbook/TT-B392-REVENUE-PIPELINE-UNION-MAX-VS-EVENT-LOG-MAX-BLOCK-TAIL-COVERAGE-OBS-001.md`](../docs/runbook/TT-B392-REVENUE-PIPELINE-UNION-MAX-VS-EVENT-LOG-MAX-BLOCK-TAIL-COVERAGE-OBS-001.md)。 |
| **ops/b393-revenue-pipeline-union-max-checkpoint-vs-event-log-tail-bundle-reconcile-admin-overview-smoke.sh** | Linux/Mac/Git Bash | **`POST …/internal/indexer-reconcile`**（**`persist:true`** + **`include_revenue_pipeline_union_max_checkpoint_vs_event_log_tail_bundle_observability:true`**）与 **`GET …/admin/observability/overview`** **`overview.revenue_pipeline_union_max_checkpoint_vs_event_log_tail_bundle_observability`** **JSON 深相等**（**TT-B393** / **B-393**；**rollup.marker**；须 **`INTERNAL_API_SECRET`**、**`ADMIN_BEARER_TOKEN`**、**`jq`**）；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[`docs/runbook/TT-B393-REVENUE-PIPELINE-UNION-MAX-CHECKPOINT-VS-EVENT-LOG-TAIL-BUNDLE-OBS-001.md`](../docs/runbook/TT-B393-REVENUE-PIPELINE-UNION-MAX-CHECKPOINT-VS-EVENT-LOG-TAIL-BUNDLE-OBS-001.md)。 |
| **ops/b394-revenue-pipeline-per-leg-projection-max-block-spread-reconcile-admin-overview-smoke.sh** | Linux/Mac/Git Bash | **`POST …/internal/indexer-reconcile`**（**`persist:true`** + **`include_revenue_pipeline_per_leg_projection_max_block_spread_observability:true`**）与 **`GET …/admin/observability/overview`** **`overview.revenue_pipeline_per_leg_projection_max_block_spread_observability`** **JSON 深相等**（**TT-B394** / **B-394**；**三腿** **`max(block_number)`** **spread**；须 **`INTERNAL_API_SECRET`**、**`ADMIN_BEARER_TOKEN`**、**`jq`**）；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[`docs/runbook/TT-B394-REVENUE-PIPELINE-PER-LEG-PROJECTION-MAX-BLOCK-SPREAD-OBS-001.md`](../docs/runbook/TT-B394-REVENUE-PIPELINE-PER-LEG-PROJECTION-MAX-BLOCK-SPREAD-OBS-001.md)。 |
| **ops/b395-revenue-pipeline-spread-stratified-reconcile-admin-overview-smoke.sh** | Linux/Mac/Git Bash | **`POST …/internal/indexer-reconcile`**（**`persist:true`** + **`include_revenue_pipeline_spread_stratified_observability:true`**）与 **`GET …/admin/observability/overview`** **`overview.revenue_pipeline_spread_stratified_observability`** **JSON 深相等**（**TT-B395** / **B-395**；**`spread_anomaly_layer`** **/** **`inter_leg_drift`**；须 **`INTERNAL_API_SECRET`**、**`ADMIN_BEARER_TOKEN`**、**`jq`**）；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[`docs/runbook/TT-B395-REVENUE-PIPELINE-SPREAD-STRATIFIED-OBS-001.md`](../docs/runbook/TT-B395-REVENUE-PIPELINE-SPREAD-STRATIFIED-OBS-001.md)。 |
| **ops/b396-revenue-pipeline-spread-vs-union-indexer-gap-reconcile-admin-overview-smoke.sh** | Linux/Mac/Git Bash | **`POST …/internal/indexer-reconcile`**（**`persist:true`** + **`include_revenue_pipeline_spread_vs_union_indexer_gap_observability:true`**）与 **`GET …/admin/observability/overview`** **`overview.revenue_pipeline_spread_vs_union_indexer_gap_observability`** **JSON 深相等**（**TT-B396** / **B-396**；**`dominance_signal`** **/** **`gap_blocks`**；须 **`INTERNAL_API_SECRET`**、**`ADMIN_BEARER_TOKEN`**、**`jq`**）；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[`docs/runbook/TT-B396-REVENUE-PIPELINE-SPREAD-VS-UNION-INDEXER-GAP-OBS-001.md`](../docs/runbook/TT-B396-REVENUE-PIPELINE-SPREAD-VS-UNION-INDEXER-GAP-OBS-001.md)。 |
| **ops/b397-revenue-pipeline-spread-vs-event-log-tail-reconcile-admin-overview-smoke.sh** | Linux/Mac/Git Bash | **`POST …/internal/indexer-reconcile`**（**`persist:true`** + **`include_revenue_pipeline_spread_vs_event_log_tail_observability:true`**）与 **`GET …/admin/observability/overview`** **`overview.revenue_pipeline_spread_vs_event_log_tail_observability`** **JSON 深相等**（**TT-B397** / **B-397**；**`dominance_signal`** **/** **`tail_slack_blocks`**；须 **`INTERNAL_API_SECRET`**、**`ADMIN_BEARER_TOKEN`**、**`jq`**）；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[`docs/runbook/TT-B397-REVENUE-PIPELINE-SPREAD-VS-EVENT-LOG-TAIL-OBS-001.md`](../docs/runbook/TT-B397-REVENUE-PIPELINE-SPREAD-VS-EVENT-LOG-TAIL-OBS-001.md)。 |
| **ops/b398-revenue-pipeline-spread-dual-slack-triangulation-reconcile-admin-overview-smoke.sh** | Linux/Mac/Git Bash | **`POST …/internal/indexer-reconcile`**（**`persist:true`** + **`include_revenue_pipeline_spread_dual_slack_triangulation_observability:true`**）与 **`GET …/admin/observability/overview`** **`overview.revenue_pipeline_spread_dual_slack_triangulation_observability`** **JSON 深相等**（**TT-B398** / **B-398**；**`triangulation_signal`** **/** **`gap_blocks`** **/** **`tail_slack_blocks`**；须 **`INTERNAL_API_SECRET`**、**`ADMIN_BEARER_TOKEN`**、**`jq`**）；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[`docs/runbook/TT-B398-REVENUE-PIPELINE-SPREAD-DUAL-SLACK-TRIANGULATION-OBS-001.md`](../docs/runbook/TT-B398-REVENUE-PIPELINE-SPREAD-DUAL-SLACK-TRIANGULATION-OBS-001.md)。 |
| **ops/_local_b387_b388_smoke_orchestrator.sh** | Linux/Mac/Git Bash | **可选**：本地 **一次** **拉起** **`PORT=8080`** **（** **`SMOKE_API_PORT`** **可覆写** **）** **+** **补** **`CHAIN_RPC_URL`** **/** **`CHAIN_ID`** **+** **`INTERNAL_API_SECRET`** **+** **admin** **会话** **后** **连跑** **B-387** **与** **B-388** **§3** **smoke**（**勿** **提交** **密钥** **；** **`/auth/*`** **路径** **与** **公开** **路由** **一致** **无** **`/api/v1`** **前缀** **）。 |
| **governance-governor-token-timelock-ssot-ops-check.sh** | Linux/Mac/Git Bash | **`GET …/admin/observability/overview`**；校验 **`overview.governor_token_timelock_ssot_ops_check`**（**TT-B110-SEQ11**；须 **jq**、**`ADMIN_BEARER_TOKEN`**）；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、[04 §3.4](../docs/spec/04-后端与API.md)。 |
| **governance-governor-token-timelock-ssot-ops-check.ps1** | Windows | 委托 **`bash scripts/governance-governor-token-timelock-ssot-ops-check.sh`**（须 **Git Bash**）。 |
| **indexer-reorg-recovery.sh** | Linux/Mac/Git Bash | **reorg** 恢复：**`status|hint|replay|reconcile|rewind|all`**（**`rewind`** 须 **`REWIND_FROM_BLOCK`**，可选 **`FORCE_REWIND=1`**；**curl** + **jq**；**`INTERNAL_API_SECRET`**）；与 **`GET …/internal/indexer-status`** 的 **`reorg_recovery`**（**`110-REORG-RECOVERY-HINT`**）配套；见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、**110 §3.1.3**。 |
| **indexer-reorg-recovery.ps1** | Windows | 委托 **`bash scripts/indexer-reorg-recovery.sh`**（须 **Git Bash**）。例：`.\scripts\indexer-reorg-recovery.ps1 status`。 |
| **verify-reconcile-export-ed25519.sh** | Linux/Mac/Git Bash | 对 **`GET …/reconcile-reports/export`** 保存的**原始响应体** + 头 **`x-traveltrust-reconcile-export-ed25519`** + **`GET /meta` → `admin_exports.reconcile_ed25519_public_key_hex`** 做 **Ed25519** 离线验签（**OpenSSL 3+**；**`xxd -r -p`**）；**`--self-test`** 临时密钥自验（CI）。见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)。 |
| **vault-forwarded-export-fetch.sh** | Linux/Mac/Git Bash | **P5-2-C1**：只读拉取 **`GET …/admin/region-vault/forwarded-events/export`**（**须 `ADMIN_BEARER_TOKEN`**，与 **`indexer-public-snapshot.sh`** 同形）；落盘 **`region-vault-forwarded-events.{csv,json}`**、**`.export.headers.txt`**、**`.export.sha256`**（与头 **`x-traveltrust-reconcile-export-sha256`** 一致）；若存在则 **`.export.ed25519`** / **`.export.truncated`**。可选 **`API_BASE_URL`**、**`VAULT_EXPORT_FORMAT`**、**`VAULT_EXPORT_CHAIN_ID`**、**`VAULT_EXPORT_LIMIT`**、**`VAULT_EXPORT_OUT_DIR`**。见 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)。 |
| **vault-forwarded-export-fetch.ps1** | Windows | 委托 **`bash scripts/vault-forwarded-export-fetch.sh`**（须 **Git Bash**）；环境变量与 **.sh** 相同。 |
| **finance-readonly-smoke.sh** | Linux/Mac/Git Bash | **Epic E-10**：对 **`GET …/admin/finance/summary`**、**`cross-check`**、**`drift-summary`** 分别 **curl** + **jq** 校验**单路**成功体形状；**禁止**跨接口对账；**exit** 不表达 drift 结论。须 **`ADMIN_BEARER_TOKEN`**、**`jq`**；**`FINANCE_READONLY_SMOKE_SKIP=1`** → 跳过并 **exit 0**。可选参数：本地 **`epic_d_go_bundle_closure.json`** 路径 → 仅 **jq** **`.bundle_closure`** 四键结构。见 [Epic-E-finance-readonly-ladder.md](../docs/runbook/Epic-E-finance-readonly-ladder.md)、[evidence/GO_EPIC_E_FINANCE_READONLY_CLOSE.md](../evidence/GO_EPIC_E_FINANCE_READONLY_CLOSE.md)。 |
| **check-e2e-three-pack-evidence.sh** | Linux/Mac/Git Bash | **Epic F-06**：校验 **`EVIDENCE_GO_DIR`** 下 **`artifacts/e2e-*.md`** 三文件存在；**不**读正文、**不**对账。**`jq`** 仅在 **`E2E_THREE_PACK_CHECK_MANIFEST=1`** 时必需。**`E2E_THREE_PACK_CHECK_SKIP=1`** → **exit 0**。**`E2E_THREE_PACK_CHECK_MANIFEST=1`** → 额外检查 **`manifest.json`** · **`artifacts[]`** 是否登记三条 **`path`**。见 [Epic-F-e2e-three-pack-ladder · F-06](../docs/runbook/Epic-F-e2e-three-pack-ladder.md#epic-f-f06-check-脚本)。 |
| **indexer-public-snapshot.sh** | Linux/Mac/Git Bash | **`GET /health`** + **`GET /meta`**；可选 **`ADMIN_BEARER_TOKEN`** → admin **indexer health** / **observability overview**；可选 **`INTERNAL_API_SECRET`** → **`GET /api/v1/internal/indexer-status`**（**`internal_indexer_status`**；可选 **`SNAPSHOT_INTERNAL_STATUS_LIVE_RECONCILE=1`** → **`?live_reconcile=1`**）+ 默认 **`POST /api/v1/internal/indexer-reconcile`**（**`persist:false`**；**`SNAPSHOT_INTERNAL_SKIP_RECONCILE=1`** 时**不**调用，**`internal_indexer_reconcile`**·**`snapshot_skipped`**，且 **`snapshot_options`** 内 **`snapshot_internal_reconcile_rpc`**/**`…_include_chain_tip`**/**`…_include_event_log_escrow_coverage`** 强制 **`null`**；可选 **`SNAPSHOT_INTERNAL_RECONCILE_RPC=1～10`** → body **`rpc_escrow_samples`**；可选 **`SNAPSHOT_INTERNAL_RECONCILE_INCLUDE_CHAIN_TIP=1`** → **`include_chain_tip`** / **`chain_observation`**，见 **110**；可选 **`SNAPSHOT_INTERNAL_RECONCILE_INCLUDE_EVENT_LOG_ESCROW_COVERAGE=1`** → **`include_event_log_escrow_coverage`** / **`event_log_escrow_coverage`**（**DB 已索引**）；**`internal_indexer_reconcile`**）；可选 **`SNAPSHOT_INTERNAL_INDEXER_TICK=1`** → **`POST /api/v1/internal/indexer-tick`**（**`internal_indexer_tick`**；**`logs_fetch_skipped`** 等；**会推进**索引，**慎用**）；合并 JSON 含 **`snapshot_provenance`**（**`script`**/**`script_semver`**（当前 **`1.3.0`**）/**`host_git_commit`**/**`host_git_branch`**/**`host_repo_dirty`**）+ **`snapshot_options`**（含 **`snapshot_internal_skip_reconcile`**）；密钥与 token **勿**入库；须 **内网** 调 internal；`API_BASE_URL` 默认 `http://127.0.0.1:8080`；须 **jq**。 |
| **indexer-public-snapshot.ps1** | Windows | 委托 **`bash scripts/indexer-public-snapshot.sh`**（须 **Git Bash** + **jq**）。 |
| **write-indexer-evidence.sh** | Linux/Mac/Git Bash | 调用 **`indexer-public-snapshot.sh`** 并将 stdout 写入 **`evidence/GO_YYYYMMDD/indexer_public_snapshot_*.json`**（**`EVIDENCE_ROOT`** / **`EVIDENCE_DAY_GO`** / **`EVIDENCE_GO_DIR`**）；**`INDEXER_EVIDENCE_WRITE_MANIFEST=1`** 或 **`INDEXER_EVIDENCE_BUNDLE_ZIP=1`** → **`manifest.json`**（与 **`indexer_public_snapshot_manifest.json`** 同形）、**`manifest.sha256`**、**`epic_d_go_bundle_closure.json`**（**Epic D-10** **`traveltrust.ops_artifact.v1`** **`bundle`**）、可选 **`artifacts/epic_d_d0*.json`**（**`INTERNAL_API_SECRET`**）、可选 **`.zip`**（**jq**、**sha256sum/shasum**、**zip**）；与 [RUNBOOK §2.55](../ops/RUNBOOK.md#255-indexer-tick重放与对账internal--admin-只读)、**110**、**`evidence/README`**、**[Epic-D D-10](../docs/runbook/Epic-D-indexer-ops-readonly-ladder.md)** 一致。 |
| **write-indexer-evidence.ps1** | Windows | 与 **.sh** 等价：**bash** 写快照；**manifest** 由 PS **`Get-FileHash`** + **`ConvertTo-Json`**；**`--epic-d10-post`** 委托 **bash** **`write-indexer-evidence.sh`** 写 **sha256** + **closure**；**`.zip`** 用 **`Compress-Archive`**。 |

**修改后端 CORS 或 .env 后**：须重新编译并重启 API 才生效。一键脚本默认会先编译，可避免 `/api/v1/me`、`/api/v1/community/*` 等 404；若遇 404 请勿设 SKIP_API_BUILD=1，或手动执行 `cargo build -p traveltrust-api` 后重启 API。

详见 [docs/测试账号与本地联调.md](../docs/测试账号与本地联调.md)、[README 快速开始](../README.md#快速开始)。

---

## 二、CI 门禁

### SSOT Guard 统一索引（TT-SSOT-GUARD-INDEX-017）

**触发入口（统一）**：仓库根执行 **`bash scripts/check-invariants.sh`**（Windows 可用 **`scripts/check-invariants.ps1`**，与 **`.sh`** 同源）；CI 上由 **[`.github/workflows/build.yml`](../.github/workflows/build.yml)** 的 **Build** job **首步**调用；**`pre-release-automation`** 亦串联同一 **`check-invariants`** 链路。

**Build 新红链首诊**（**`build.yml` 红 / 部分红**）：先 **regression hint** → **`go_no_go_status`** → **首个失败 job** → **再开卡** — 固定顺序与可复制模板见 **[evidence/GO_20260408_BUILD_CI_CLOSURE.md · 新红链首诊流程](../evidence/GO_20260408_BUILD_CI_CLOSURE.md#new-red-chain-triage-flow)**（与 **`scripts/ci-triage.sh`** / **`ci-triage-hint`** job 配套）。

**横向扩展（必须）**：若在**其它 HTTP 端点**引入与下表同类的**根级链读 SSOT**（新 `m.insert` 键族或新 JSON 根键），须**单独开立 TT**，并**新增独立 guard 脚本**或**扩展 allowlist / 并列校验**；**禁止**不经 TT 与脚本评审把新键并入既有 Escrow / B-110 默认范围。

| Guard | 脚本 | 覆盖范围（摘要） |
|------|------|------------------|
| **B-097** 订单详情 **Escrow** 四套根级链读 | **`ssot-guard-escrow-orders-detail.py`**（**`.ps1`** 包装） | **`escrow_*` SSOT `m.insert` 仅** `crates/api/src/routes/orders/mod.rs`；**`merge_escrow_*`** 内**禁止**回填 **`order.*`**；**`data_source` / `is_chain_ssot`** 字面 **`chain_read` / `true`**；**002** 类聚合体排除 **12** 键（与 **`TT-ESCROW-SSOT-ORDER-STATE-AGGREGATE-EXCLUDE-002`** 同口径） |
| **B-110** 治理池 **四根级**链上 SSOT | **`ssot-guard-b110-pool-ssot.py`**（**`.ps1`** 包装） | **`GET …/governance/pool`**：**`pool_balance`** 与顶层 **`data_source` / `is_chain_ssot`**；**`country_pool*` / `treasury_pool*` / `treasury_erc20_pool*`** 的 **`m.insert` 落点、`chain_read` / `true`、不写 0**；**`build_fee_pool_aggregate_body`（fee-pool-aggregates Σ）** **不得**含上述三池**根级**键（与 **`TT-SSOT-GUARD-B110-POOL-016`** 同链） |

**母表交叉索引**（任务溯源）：**[docs/任务母表.md](../docs/任务母表.md)** 内 **「SSOT Guard 门禁索引」** 段（检索 **TT-SSOT-GUARD-INDEX-017**）。

**Evidence 总览**（覆盖 / CI / 典型回归 / 扩展规则一条读）：**[evidence/GO_20260407_SSOT_GUARDS.md](../evidence/GO_20260407_SSOT_GUARDS.md)**（**TT-SSOT-GUARD-GO-SUMMARY-018**）；**evidence 目录锚点**：[evidence/README.md · SSOT Guard 总览](../evidence/README.md#ssot-guards-ci-summary)。

**CI Gate v2（编排）**：**`check-invariants.sh` / `check-invariants.ps1`** 调用 **`ssot-guard-ci-v2.py`** — 顺序执行 **B-097 静态**、**B-110 静态**、**`ssot-guard-response-contract.py`**（**`scripts/gates/ssot-guard-fixtures/v2/*.snapshot.json`** 运行时契约快照）；成功/失败均写 **`target/ssot-guard-ci-v2-report.json`**。失败字段说明：**[gates/templates/SSOT_GUARD_FAILURE_REPORT.md](gates/templates/SSOT_GUARD_FAILURE_REPORT.md)**。新端点接入流程：**[SSOT_GUARD_NEW_ENDPOINT.md](SSOT_GUARD_NEW_ENDPOINT.md)**（**TT + allowlist**）。共享常量：**`gates/ssot_guard_v2_constants.py`**（根路径 **`scripts/ssot-guard-ci-v2.py`** 等同调用）。

| 脚本 / Workflow | 说明 |
|------|------|
| **Contract ABI Gate**（`.github/workflows/contract-abi-gate.yml`） | `forge test` + **`bash scripts/run-verify-abi-forge.sh`**；**仅当**变更命中 `contracts/**`、`frontend/dapp/abis/**`、相关 `scripts/*` 或本 workflow 时自动跑（省 CI）；**Actions → Contract ABI Gate → Run workflow** 可手动全量跑。 |
| **ABI 同步有序清单** | 改 **`contracts/src`** 或 ABI 消费路径后的**最小顺序**与验收句 → **[Runbook §12.4](../ops/RUNBOOK.md)**（与 **[14 §1.2](../docs/spec/14-合约-API-ABI-前后端对齐.md)**、下表 **sync-abi-from-forge** / **check-55-s13** / **run-verify-abi-forge** 配套）。 |
| **run-verify-abi-forge.sh** | 优先 `python3`、否则 `python` 调用 **verify-abi-forge.py**；项目根 `bash scripts/run-verify-abi-forge.sh`。 |
| **run-verify-abi-forge.ps1** | Windows PowerShell 与上等价；项目根 `.\scripts\run-verify-abi-forge.ps1`。 |
| **verify-abi-forge.py** | ABI multiset 校验实现；通常经上一行或 CI 调用（须 `forge`、建议已 `forge build`）。 |
| **check-invariants.sh** | 校验 rust-toolchain、frontend、**`frontend/package-lock.json`**（存在性 + **B-158** **`verify-frontend-package-lock-sync.mjs`**：`package.json` 直接依赖 vs lock **`packages`**）、API 安全头；并联 **`ssot-guard-ci-v2.py`**（**CI Gate v2**）：内含 **`ssot-guard-escrow-orders-detail.py`**（**B-097**）、**`ssot-guard-b110-pool-ssot.py`**（**B-110**）、**`ssot-guard-response-contract.py`**（**快照契约**）；报告 **`target/ssot-guard-ci-v2-report.json`**，模板 **[gates/templates/SSOT_GUARD_FAILURE_REPORT.md](gates/templates/SSOT_GUARD_FAILURE_REPORT.md)**。**文首读前摘要**入口 **[07](../docs/spec/07-开发流程与顺序.md)**「**工台基线 · 依赖审计（invariants · audit-deps）**」行；**[00-文档治理总册 §8.3](../docs/spec/00-文档治理总册.md#doc-audit-gates-ssot)**；**pre-release-automation** 首段同源。 |
| **ssot-guard-escrow-orders-detail.py** | **CI / PR**（经 **check-invariants.sh** → **Build** workflow）：静态校验 **`escrow_*` SSOT `m.insert` 仅见于 `routes/orders/mod.rs`**；两路 **`merge_escrow_*`** 内**禁止 `order.*`**；**`chain_read` / `true`** 与 **002** 聚合排除 **12** 键。订单**列表**若将来需链读展示：**单开 TT**，**扩脚本或并列 guard**，**不**并入 **B-097** 默认范围。项目根 `python3 scripts/ssot-guard-escrow-orders-detail.py`。 |
| **ssot-guard-escrow-orders-detail.ps1** | Windows：委托 **`python`** 运行 **`ssot-guard-escrow-orders-detail.py`**（与上表 **.py** 同链）。 |
| **ssot-guard-b110-pool-ssot.py** | **CI / PR**（经 **check-invariants.sh**）：**B-110** `GET …/governance/pool` 四池根级链上 SSOT 静态门禁（见上表 **check-invariants** 行）。项目根 `python3 scripts/ssot-guard-b110-pool-ssot.py`。 |
| **ssot-guard-b110-pool-ssot.ps1** | Windows：委托 **`python`** 运行 **`ssot-guard-b110-pool-ssot.py`**。 |
| **ssot-guard-ci-v2.py** | **CI Gate v2 编排器**（由 **check-invariants** 调用）：串联 **B-097** / **B-110** 静态脚本 + **`ssot-guard-response-contract.py`**；写 **`target/ssot-guard-ci-v2-report.json`**。 |
| **ssot-guard-response-contract.py** | **快照契约**：校验 **`scripts/gates/ssot-guard-fixtures/v2/*.snapshot.json`**（与 **B-097 / B-110** 根级不变量对齐）。 |
| **audit-deps.sh** | 前端 npm audit、后端 cargo audit（CI 中 continue-on-error）。**文首读前摘要**入口 **[07](../docs/spec/07-开发流程与顺序.md)**「**工台基线 · 依赖审计（invariants · audit-deps）**」行；**[00-文档治理总册 §8.3](../docs/spec/00-文档治理总册.md#doc-audit-gates-ssot)**。 |
| **audit-deps.ps1** | Windows：委托 **`bash scripts/audit-deps.sh`**（须 **Git Bash**；与上表 **audit-deps.sh** 行同文档链）。 |
| **check-08-consistency.sh** | 08-3/08-4 **W-PDP-SSOT-CONSISTENCY**；`./scripts/check-08-consistency.sh [BASE_REF]`（默认 **main**）。Workflow：[check-08-consistency.yml](../.github/workflows/check-08-consistency.yml)。**文首读前摘要**入口 **[07](../docs/spec/07-开发流程与顺序.md)**「**08-3/08-4 机读预检（一致性 · evidence 指针）**」行。**W-GATE-CROSS-CHECK** 机读辅助之一，**不替代** **08-2 审查二** — **[Runbook §12.7](../ops/RUNBOOK.md)**；**[00-文档治理总册 §8.3](../docs/spec/00-文档治理总册.md#doc-audit-gates-ssot)**。 |
| **check-08-consistency.ps1** | Windows：委托 **`bash scripts/check-08-consistency.sh`**；可选首参 **`BASE_REF`**（与上表 **.sh** 行同文档链）。 |
| **check-08-evidence-pointer.sh** | 08-3 **evidence_pointer** 最小校验。Workflow：[check-08-evidence-pointer.yml](../.github/workflows/check-08-evidence-pointer.yml)。**文首读前摘要**入口 **[07](../docs/spec/07-开发流程与顺序.md)**「**08-3/08-4 机读预检（一致性 · evidence 指针）**」行；**[00-文档治理总册 §8.3](../docs/spec/00-文档治理总册.md#doc-audit-gates-ssot)**。 |
| **validate_evidence_manifest.py** | **IMP-EV-001**：唯一基线 **`evidence/GO_20260409`**；**`scripts/gates/imp-ev-001-evidence-bundles.sh`** 对基线 + **diff 触及的**含 **`manifest.json`** 的 **`evidence/*/`** 跑 **`--emit-summary --verify-artifact-files`** + **`jq`**（**`validate_summary.v1_1`**）。**`self-test`**；PR/push 冻结基线路径。与 **Build** 解耦、**不设 **`continue-on-error`**；未绿灯 **禁止合并**。Workflow：[evidence-manifest-validate.yml](../.github/workflows/evidence-manifest-validate.yml)；**`main`** **须** **required status check**；**[docs/runbook/evidence-gate.md](../docs/runbook/evidence-gate.md)**。实现见 **§三** **`validate-evidence-manifest.sh`**、**[evidence/README.md](../evidence/README.md)**。 |
| **validate_dual_score_signoff.py** / **check-dual-score-gate.sh** | **Check-G（Runbook §2.7.4）**：校验 **`dual_score_signoff.v1.json`**（七维 × 分阶段阈值 **7/8/9** + **`risk_acceptances[]`**）。**`bash scripts/check-dual-score-gate.sh`** **`path/to/dual_score_signoff.v1.json`**；**`self-test`**；**`--emit-summary`** → stdout **`traveltrust.dual_score_validate.v1`**。Schema：**[evidence/schemas/dual_score_signoff.v1.schema.json](../evidence/schemas/dual_score_signoff.v1.schema.json)**；模板：**[GO_YYYYMMDD_template/…/dual_score_signoff.v1.example.json](../evidence/GO_YYYYMMDD_template/artifacts/dual_score_signoff.v1.example.json)**。**不**校验 **G1** 的 **%** 阈值（仍人工）；**不**并入 **Build** 默认同源（发版 / evidence 包手动跑）。**Runbook**：[§2.7.4](../ops/RUNBOOK.md#check-g-dual-score-gate)。 |
| **check-08-evidence-pointer.ps1** | Windows：委托 **`bash scripts/check-08-evidence-pointer.sh`**（与上表 **.sh** 行同文档链）。 |
| **check-governance-doc-linkage.sh** | `82` / **`83` / `84`** / `governance-token`（含 `03`）与 00-索引、00-总表、[00-文档体系与阅读串联（兼容壳）](../docs/spec/00-文档体系与阅读串联.md)、**[07-开发流程与顺序 §二 2.4](../docs/spec/07-开发流程与顺序.md)**、**08-4-附录** 等**静态联动**校验；项目根执行 `bash scripts/check-governance-doc-linkage.sh`。Workflow：[governance-doc-linkage-gate.yml](../.github/workflows/governance-doc-linkage-gate.yml)（PR 与 `main` push 触发；同 workflow 另跑 **`check-07-version-triple.sh`**）。**文首读前摘要**入口 **[07](../docs/spec/07-开发流程与顺序.md)**「**治理文档联动（CI）**」行。**串联正文 SSOT** 在 **07 §零、§五**；详见 [00-文档治理总册 §8.3](../docs/spec/00-文档治理总册.md#doc-audit-gates-ssot)。**W-GATE** 机读辅助 — **[Runbook §12.7](../ops/RUNBOOK.md)**。 |
| **check-governance-doc-linkage.ps1** | Windows：委托 **`bash scripts/check-governance-doc-linkage.sh`**（与上表 **.sh** 行同文档链）。 |
| **check-07-version-triple.sh** | **[07](../docs/spec/07-开发流程与顺序.md)** 文首 **`Version:`**、**[00-文档索引](../docs/spec/00-文档索引.md)** 版本表 **`07-开发流程与顺序`** 行、**07 §六 6.5** 变更表**首条数据行**须同号。项目根 `bash scripts/check-07-version-triple.sh`。Workflow：与 **`governance-doc-linkage-gate.yml`** 第二步同批。**维护**：**07 §六 6.1 #4**、**6.2**、**§二 2.4** **CI 基线**；[00-文档治理总册 §8.3](../docs/spec/00-文档治理总册.md#doc-audit-gates-ssot)。 |
| **check-07-version-triple.ps1** | Windows：委托 **`bash scripts/check-07-version-triple.sh`**（须 **Git Bash**；与上表 **.sh** 行同文档链）。 |
| **check-mainnet-launch-precheck-gate.sh** | **TT-MAINNET** **G0～G6 + SL**（**`MAINNET_EVIDENCE_RUN_DIR/shadow_go_no_go.json`** **`shadow_launch_verdict=="GO"`** **+** **chain** **1**）：**`CHAIN_ID=1`** **且**（**`TT_MAINNET_GATE_ENFORCE=1`** **或** **`MAINNET_LAUNCH_PRECHECK=1`**）**时** **runtime SSOT** **+** **bytecode** **+** **Timelock** **+** **证据** **/** **G2** **/** **G6**；**否则** **SKIP** **exit 0**。**CI** **`broadcast-batch-blockers`** **`TT-MAINNET G0–G6+SL`**：**无** **`MAINNET_CHAIN_RPC_URL`** **→** **job** **fail**（**P0** **禁假绿**），**除非** **`MAINNET_GATE_ALLOW_ABSENT_RPC`**。**Runbook**：[TT-MAINNET §7](../docs/runbook/TT-MAINNET-LAUNCH-PRECHECK-AFTER-B435-001.md#7-ci-机读门禁g0g6)。 |
| **check-ssot-triangle-gate.sh** | **SSOT 三角门禁**（**短路由序**）：**`check-07-version-triple.sh`** → **`check-governance-doc-linkage.sh`** → **`run-check-04-routes.sh`**；**优先** **`python`** **再** **`python3`** **（** **`export PYTHON`** **）**；**统一** **`GO`/`NO_GO`** **与** **exit**（首败即停，**exit** **=** **子脚本** **码**）。**`--json`**：stdout **单行** **`traveltrust.ssot_triangle_gate.v1`**。项目根 **`bash scripts/check-ssot-triangle-gate.sh`**。**Runbook**：[TT-B419-GO-SSOT-PR-TRIANGLE-CI-001.md](../docs/runbook/TT-B419-GO-SSOT-PR-TRIANGLE-CI-001.md)。 |
| **check-w-gate-prerelease.sh** | **W-GATE 发版前机读聚合**（**B-420**）：**`check-invariants`** → **`check-55-s13`** → **`check-ssot-triangle-gate`** → **`check-08-consistency`** → **`validate-evidence-manifest`**（默认 **`evidence/GO_20260409`**）；**`W_GATE_SKIP_08_CONSISTENCY`** **/** **`W_GATE_SKIP_EVIDENCE_MANIFEST`** **可选跳过**；**`--json`**：**`traveltrust.w_gate_prerelease.v1`**。项目根 **`bash scripts/check-w-gate-prerelease.sh`**。**Runbook**：[TT-B420-GO-W-GATE-PRERELEASE-001.md](../docs/runbook/TT-B420-GO-W-GATE-PRERELEASE-001.md)。 |
| **auth-contract-gate.sh** | Linux/Mac/Git Bash | 认证契约回归门禁：运行后端认证关键测试切片（`auth_logout_api_tests`、`auth_register_login_logout_db_api_tests`、`auth_session_contract_b446`），用于拦截登录/注册/登出/会话契约回退。可选 CI：**`.github/workflows/auth-contract-gate.yml`**（auth 相关路径变更触发，且 main push 触发）。 |
| **doc-enterprise-audit-machine-phases.sh** | **TT-DOC 全仓文档企业审计清单 · 机读聚合**：**`check-07-version-triple`** → **`check-ai-task-card-index-overview.py`**（**先后** **`docs/AI任务卡索引.md`** **与** **`docs/AI任务卡索引.from-stash.md`** **—** **与** **`scripts/gates/doc-enterprise-audit-machine-phases.sh`** **同源**）→ **`gates/check-doc-markdown-relative-links.py`**（**`docs/`** 下相对 **`[](path)`**；**目标** **含** **空格** **或** **` "title"`** **尾** **时** **按** **Markdown** **惯例** **解析** **；** **默认排除** **`spec/27-archived`** **与** **`AI任务卡索引.from-stash.md`**；**`DOC_AUDIT_LINKS_ENFORCE=1`** 时断链 **exit 1**，否则 **warn-only**）；**`DOC_AUDIT_FULL=1`** 再跑 **`check-55-s13`** + **`run-check-04-routes`**。**跳过子步**：**`DOC_AUDIT_SKIP_07`** **/** **`DOC_AUDIT_SKIP_AI`** **/** **`DOC_AUDIT_SKIP_LINKS`** **=1**。项目根 **`bash scripts/doc-enterprise-audit-machine-phases.sh`**（实现 **`scripts/gates/…`**）。**Runbook**：[TT-DOC-ENTERPRISE-AUDIT-CHECKLIST-001.md](../docs/runbook/TT-DOC-ENTERPRISE-AUDIT-CHECKLIST-001.md)。 |
| **check-data-reconcile-projection-gov-gate.sh** | **B-422 数据闭环**：**`indexer-reconcile-probe`** → **`b402-min-revenue-e2e-reconcile-smoke`**（**`B422_SKIP_REVENUE_E2E_SMOKE=1`** **可跳过**）→ **`governance-governor-proposal-count-ssot-ops-check`**；**`--json`**：**`traveltrust.data_reconcile_projection_gov_gate.v1`**。须 **`INTERNAL_API_SECRET`**、**`ADMIN_BEARER_TOKEN`**、**jq**。项目根 **`bash scripts/check-data-reconcile-projection-gov-gate.sh`**。**Runbook**：[TT-B422-GO-DATA-RECONCILE-PROJECTION-GOV-001.md](../docs/runbook/TT-B422-GO-DATA-RECONCILE-PROJECTION-GOV-001.md)。 |
| **check-8384-snapshot-country-smoke-gate.sh** | **B-423 83/84 分域烟测**：**`b384-region-vault-forwarded-…-smoke.sh`** → **`b385-p5-country-ledger-credited-…-smoke.sh`**；**`B423_SKIP_REGION_VAULT_SMOKE`** **/** **`B423_SKIP_COUNTRY_LEDGER_SMOKE`** **可跳过**；**`--json`**：**`traveltrust.data_8384_snapshot_country_smoke_gate.v1`**。须 **`INTERNAL_API_SECRET`**、**`ADMIN_BEARER_TOKEN`**、**jq**。项目根 **`bash scripts/check-8384-snapshot-country-smoke-gate.sh`**。**Runbook**：[TT-B423-GO-DATA-8384-SNAPSHOT-COUNTRY-SMOKE-001.md](../docs/runbook/TT-B423-GO-DATA-8384-SNAPSHOT-COUNTRY-SMOKE-001.md)。 |
| **check-meta-overview-deepeq-gate.sh** | **B-424** **`/meta`** **↔** **`/api/v1/admin/observability/overview`**：**Leg 1** **`build`** **同源**；**Leg 2** **`obs_mirror_core`**（**finality_n**、**indexer.checkpoint**、**chain_id**、**strip** **后** **`rate_limits`**）；**`--json`**：**`traveltrust.meta_overview_deepeq_gate.v1`**。须 **`ADMIN_BEARER_TOKEN`**、**jq**；可选 **`API_BASE_URL`**。项目根 **`bash scripts/check-meta-overview-deepeq-gate.sh`**。**Runbook**：[TT-B424-GO-OBS-META-OVERVIEW-DEEPEQ-001.md](../docs/runbook/TT-B424-GO-OBS-META-OVERVIEW-DEEPEQ-001.md)。 |
| **check-indexer-lag-locate-gate.sh** | **B-425** **`lag_core`** **三源** **同源**：**`GET /meta.indexer`** **=** **`GET …/internal/indexer-status.state`** **=** **`GET …/admin/indexer/health.health`** **（** **七键** **）** **；** **根** **`finality_n`** **↔** **`indexer.finality_n`** **；** **`--json`**：**`traveltrust.indexer_lag_locate_gate.v1`** **+** **`lag_snapshot`** **（** **chain_tip** **等** **）** **。** 须 **`ADMIN_BEARER_TOKEN`**、**jq**；若 **internal** **启** **密钥** **门禁** **则** **`INTERNAL_API_SECRET`** **。** 项目根 **`bash scripts/check-indexer-lag-locate-gate.sh`**。**Runbook**：[TT-B425-GO-OBS-INDEXER-LAG-LOCATE-001.md](../docs/runbook/TT-B425-GO-OBS-INDEXER-LAG-LOCATE-001.md)。 |
| **check-revenue-suspect-runbook-gate.sh** | **B-426** **`ops/RUNBOOK.md`** **锚** **`B-426-REVENUE-SUSPECT-TRIAGE`** **+** **TT** **全名** **；** **`GET …/admin/observability/overview`** **须** **含** **四** **suspect/triangulation** **键**（**B-390/B-398/B-400/B-401** **overview** **字段**）；**`--json`**：**`traveltrust.revenue_suspect_runbook_gate.v1`** **+** **`suspect_attribution_hints`** **。** **`B426_SKIP_OVERVIEW=1`** **仅** **跑** **Runbook** **腿** **。** 须 **`jq`** **；** **全** **腿** **须** **`ADMIN_BEARER_TOKEN`** **。** 项目根 **`bash scripts/check-revenue-suspect-runbook-gate.sh`**。**Runbook**：[TT-B426-GO-OBS-REVENUE-SUSPECT-RUNBOOK-001.md](../docs/runbook/TT-B426-GO-OBS-REVENUE-SUSPECT-RUNBOOK-001.md)。 |
| **check-order-e2e-01353-gate.sh** | **B-427** **`ops/RUNBOOK.md`** **锚** **`B-427-ORDER-E2E-01353-GATE`** **+** **TT** **全名** **；** **串联** **`ops/b409-order-state-primary-acceptance.sh`** **+** **`ops/b409-order-state-exception-acceptance.sh`** **（** **TT-B409** **；** **与** **`b410`** **主路径** **`b409`** **一致** **，** **默认** **不** **跑** **Playwright** **）** **。** **`--json`**：**`traveltrust.order_e2e_01353_gate.v1`** **。** **`B427_SKIP_CARGO=1`** **仅** **Runbook** **腿** **。** 项目根 **`bash scripts/check-order-e2e-01353-gate.sh`**。**Runbook**：[TT-B427-GO-BIZ-ORDER-E2E-01353-001.md](../docs/runbook/TT-B427-GO-BIZ-ORDER-E2E-01353-001.md)。 |
| **check-runbook-golive-doclink-gate.sh** | **B-421** **六向** **文档** **锚** **`B-421-RUNBOOK-GOLIVE-DOCLINK-GATE`** **+** **`TT-B421-GO-RUNBOOK-GOLIVE-DOCLINK-001`** **`ops/RUNBOOK.md`** **`docs/go-live-checklist.md`** **`docs/spec/00-文档索引.md`** **`docs/spec/缺口与待补-官方总表.md`** **`docs/spec/15-多维度文档与技术检查报告.md`** **`docs/runbook/TT-B421-GO-RUNBOOK-GOLIVE-DOCLINK-001.md`** **。** **`--json`**：**`traveltrust.runbook_golive_doclink_gate.v1`** **。** 项目根 **`bash scripts/check-runbook-golive-doclink-gate.sh`**。**Runbook**：[TT-B421-GO-RUNBOOK-GOLIVE-DOCLINK-001.md](../docs/runbook/TT-B421-GO-RUNBOOK-GOLIVE-DOCLINK-001.md)。 |
| **run-environment-go.sh** | **Environment GO** **编排**：**Phase A** **`check-w-gate-prerelease`** **→** **`check-runbook-golive-doclink-gate`** **→** **`check-order-e2e-01353-gate`** **；** **Phase B**（**须** **`ADMIN_BEARER_TOKEN`** **+** **`GET …/meta` = 200**）**B-424** **/** **B-425** **/** **B-426** **。** **`ENV_GO_SKIP_API_GATES=1`** **仅** **Phase A** **。** 项目根 **`bash scripts/run-environment-go.sh`** **（** **`--json`** **与各** **子** **gate** **一致** **）** **。** |
| **check-did-rank-no-escrow-prefetch.sh** | **`frontend/components/did-rank`****/**`frontend/app/did-rank`**** **内** **不得** 出现 **`stashEscrow`****/**`orderEscrowPrefetch`**（DID 榜 **`trackDidRank*`**** **仅** **analytics** **侧效**，与托管 session 预填解耦）。项目根 `bash scripts/check-did-rank-no-escrow-prefetch.sh`。**文档配套**：[87 §11.1.1](../docs/spec/87-TravelTrust-角色体系技术文档-融合架构版.md)、**[07 §六 6.4](../docs/spec/07-开发流程与顺序.md)** **637** 批、**[缺口官方总表](../docs/spec/缺口与待补-官方总表.md)** **P2**。 |
| **check-did-rank-no-escrow-prefetch.ps1** | Windows：委托 **`bash scripts/check-did-rank-no-escrow-prefetch.sh`**（须 **Git Bash**；与上表 **.sh** 行同文档链）。 |
| **check-wave-phase-files.sh** | 核对 **00 索引主表**登记的 **90～550** 阶段规格是否在 `docs/spec/` 下各有 `NNN-*.md`（规划空号 **280/290/300/310** 允许缺失）。项目根 `bash scripts/check-wave-phase-files.sh`。Workflow：[check-wave-phase-files.yml](../.github/workflows/check-wave-phase-files.yml)（`docs/spec/**` 变更或 `main` push 触发）。**文档配套**：[07 读前摘要「阶段规格文件存在性」行](../docs/spec/07-开发流程与顺序.md)、**§零 0.4**、**[00-文档治理总册 §8.3](../docs/spec/00-文档治理总册.md#doc-audit-gates-ssot)**、根 **[CONTRIBUTING.md](../CONTRIBUTING.md)** 阶段文义务、**07 §二 2.3** **[推送前本地检查](../CONTRIBUTING.md#pre-push-local)**。 |
| **check-wave-phase-files.ps1** | Windows：委托 **`bash scripts/check-wave-phase-files.sh`**（与上表 **.sh** 行同文档链）。 |
| **check-ai-task-card-index-overview.py** | **`docs/AI任务卡索引.md`** **或** **`docs/AI任务卡索引.from-stash.md`** **一览**（**argv** 路径）：**A** 序号唯一连续、**E** **TT** **ID** 唯一、**B** **状态** 合法（**已封口** 族 / **登记（未封）**）、**C** **已封口** 须有 **`### TT-…`** 正文标题（宽松 **`^###\s+(TT-[A-Z0-9-]+)\b`**）、**D** **登记（未封）** 摘要须含 **`from-stash`** 路径三选一。默认**严格**；**`--allow-seq-gaps`** 仅放宽 **A**（**非** **main** 提交前门槛）。项目根 **`bash scripts/check-ai-task-card-index-overview.sh`**（推荐）或 **`py -3`** / **`python3`** / **`python`** 直调 **`.py`**。**失败**：**stderr** **`RULE=`**…**`seq=`**…**`id=`**…**`msg=`**…。Workflow：[check-ai-task-card-index-overview.yml](../.github/workflows/check-ai-task-card-index-overview.yml)（必过检查名 **`AI task card index overview / check`**）；**Build** [build.yml](../.github/workflows/build.yml) 同脚本一步。 |
| **check-ai-task-card-index-overview.sh** | **Git Bash**：依次 **`python3` → `py -3` → `python`** 调上表 **.py**；参数透传（含 **`--allow-seq-gaps`**）；**缓解** **Win** **商店** **`python3`** **占位体**。 |
| **patch-from-stash-ai-index-overview-status.py** | 将 **`docs/AI任务卡索引.from-stash.md`** **「## 任务卡一览」首张表** 的 **状态列** 规范为 **`已封口…` / `登记（未封）`**，并为 **登记** 行补 **`./AI任务卡索引.from-stash.md`** 等指针，使 **`check-ai-task-card-index-overview.py`** **B/C/D** 可 **`exit 0`**。**须审 diff**；**不**替代业务 **封口** 决策。项目根 **`python scripts/patch-from-stash-ai-index-overview-status.py`**。 |
| **sync-from-stash-overview-with-main-index.py** | 在 **序号 + ID + 阶段** 与 **`docs/AI任务卡索引.md`** **一致** 时，将 **from-stash** 一览 **状态/摘要** 与 **主索引** 对齐；**主索引为「已封口」** 且 **from-stash 无** **`### TT-…`** 正文时 **跳过**（如 **127～128**）。**不**改 **ID/阶段** 已分叉的尾部行。**`--report-divergence`**：**只读** 列出 **两文件同序号** **但** **ID** **或** **阶段** **不同** 的行 **+** **仅** **一侧** **有** **序号** **的** **计数** **（** **不写盘** **，** **末行** **`TT_AI_INDEX_DIVERGENCE:`** **）**。项目根 **`python scripts/sync-from-stash-overview-with-main-index.py`** **/** **`python scripts/sync-from-stash-overview-with-main-index.py --report-divergence`**。 |
| **dev/extract_93_case_inventory.py** | 从 **`docs/spec/93-全站功能验证矩阵-域别回归清单.md`** 抽 **用例 ID + 自动化列** 为 TSV；**93 改版**后与历史 diff，配合 **[`docs/runbook/93-matrix-batch-tracker.md`](../docs/runbook/93-matrix-batch-tracker.md)**。 |
| **dev/sync-spec00-handbook-docs-version-column.py** | 将 **[`docs/spec/00-文档索引.md`](../docs/spec/00-文档索引.md)** **§文档版本与最后更新** 表中 **`handbook/*`**、**`docs/*`**（键名去掉 **`（…）`** 后缀后映射到 **`docs/*.md`**）的 **Version** 列与磁盘 frontmatter **`Version:`** 对拍并回写（**不改日期列**）。项目根 **`python scripts/dev/sync-spec00-handbook-docs-version-column.py`**；**`--dry-run`** 仅打印将改行。 |
| **check-48-line-count.sh** | **50-O-B2**：`crates/api/src` 下 **`.rs`** 单文件行数 ≤500（**`STRICT=1`** 时 ≤400）。**文首读前摘要**入口 **[07](../docs/spec/07-开发流程与顺序.md)**「**API 单文件行数 · 27-archived 链（50-O-B2 · 工具）**」行；**[00-文档治理总册 §8.3](../docs/spec/00-文档治理总册.md#doc-audit-gates-ssot)**；**[48 §1.1](../docs/spec/48-后端模块化拆分与落地清单.md)**。 |
| **check-48-line-count.ps1** | Windows：委托 **`bash scripts/check-48-line-count.sh`**（与上表 **.sh** 行同文档链）。 |
| **fix_27_archived_links.sh** | 批量修正 **docs/spec/27-archived** 内 Markdown 相对链（**perl**）。**文首读前摘要**入口 **[07](../docs/spec/07-开发流程与顺序.md)**「**API 单文件行数 · 27-archived 链（50-O-B2 · 工具）**」行；**[00-文档治理总册 §8.3](../docs/spec/00-文档治理总册.md#doc-audit-gates-ssot)**；**[27-archived/README](../docs/spec/27-archived/README.md)**。 |
| **fix_27_archived_links.ps1** | Windows：委托 **`bash scripts/fix_27_archived_links.sh`**（与上表 **.sh** 行同文档链）。 |

---

## 三、验收清单与发版前检查

| 文件 | 平台 | 说明 |
|------|------|------|
| **checklist-17.md** | — | 01 §10 发版前 17 条，逐条勾选。 |
| **export_deployment_params.sh** | Linux/Mac/Git Bash | 17 条 #5：forge build + 合约 bytecode 长度摘要；可选再跑 slither；`./scripts/export_deployment_params.sh [out.txt]`。**有序清单** → **[Runbook §12.8](../ops/RUNBOOK.md)**。 |
| **export_deployment_params.ps1** | Windows | 与 **.sh** 等价；产出文件为 **UTF-8 无 BOM**；`.\scripts\export_deployment_params.ps1 [out.txt]`；**§12.8** 见上表 **.sh** 行。 |
| **gen-frontend-manifest.sh** | Linux/Mac/Git Bash | **08-4 第 7 章** 可验证发布：先 `cd frontend && npm run build`，再在项目根 `./scripts/gen-frontend-manifest.sh` → `frontend/.next/build-manifest.json`。无 **BUILD_ID/static** 时 **`artifacts`** 为 `[]`；可选 **`EVIDENCE_GO_DIR=evidence/GO_YYYYMMDD`** 同步 **`frontend-build-manifest.json`** + **`.sha256`**。**有序清单** → **[Runbook §12.6](../ops/RUNBOOK.md)** §A（**`pre-release-automation` 不替代** 本脚本）。 |
| **gen-frontend-manifest.ps1** | Windows | 与上等价；**`$env:EVIDENCE_GO_DIR`**（可相对仓库根）；**§12.6** 见上表 **gen-frontend-manifest.sh** 行。 |
| **validate-evidence-manifest.sh** | Linux/Mac/Git Bash | **IMP-EV-001**：校验 **`evidence/GO_YYYYMMDD/manifest.json`** 必填字段（**`gate`/`date`/`artifacts`/`sign_off`**）；若存在 **`manifest.sha256`** 则核对 **`manifest.json`** 本体哈希。可选 **`validate … DIR --verify-artifact-files`** 核对 **artifacts** 文件与登记 **sha256**。**`self-test`** 内置正负例。实现：**`scripts/dev/validate_evidence_manifest.py`**（**Python 3**）。详见 **[evidence/README.md](../evidence/README.md)** **manifest 格式**、**[CONTRIBUTING.md](../CONTRIBUTING.md)** **Gate-5 / evidence 校验**段。 |
| **check-55-s13.sh** | Linux/Mac | 55-S13 发版前 API/ABI/端口核对；项目根执行 `./scripts/check-55-s13.sh`。**文档配套**：[14 §1.2](../docs/spec/14-合约-API-ABI-前后端对齐.md)（双目录字节一致、**Escrow** 关键字）、**[07 §二 2.3](../docs/spec/07-开发流程与顺序.md)** 脚本索引表（**ABI / Foundry** 行）与**读前摘要**「**ABI·55-S13**」行、根 **[CONTRIBUTING.md](../CONTRIBUTING.md)** 合约段、**[Runbook §12.4](../ops/RUNBOOK.md)** 与下行 **sync-abi-from-forge**。 |
| **check-04-routes-vs-code.py** / **run-check-04-routes.sh** / **run-check-04-routes.ps1** | 须 Python 3 | **PR 默认建议**：触及 **`crates/api/src/routes/**`**、**`frontend/lib/api.ts`** 或 **`frontend/lib/apiClient/**`** 之 **`/api/v1/*`** 时，**合并前**跑本串联（与 **Build** CI、**`dev-preflight.sh`** 同源）。**`run-check-04-routes`** 串联：**本脚本**（§3.4 **API 主表** vs `crates/api` **`.route`**）、**`check-04-frontend-routes-vs-app.py`**、**`check-04-api-ts-routes-vs-doc-34.py`**（**`frontend/lib/api.ts`** **`routes`** vs **04 §3.4～`## 四、` 前** 含 **§3.5 Admin** 主表）、**`check-13-1-table1-routes-vs-app.py`**、**`check-13-1-routes-covered-by-04-frontend-table.py`**、**`check-b432-governance-ui-ssot-surface.py`**（**B-432** / **TT-B432**，治理闭环 UI 面钉死；**不**替代 04 全量表）。`DOC_ONLY_SCHEDULED` 预留「仅文档登记、代码待挂载」豁免（当前为空）。**Build CI 与 run-check-04-routes 默认 `STRICT_WARNINGS=1`**（未登记的公开 `/api/v1/*` 失败）；仅排障时可设 `STRICT_WARNINGS=0`。**治理 P5-4**：**`/governance/distribution-accruals`**（及 **`[id]`**）、**`/governance/distribution-claim`** 须登记于 **04 §3.4** 前端表并与 **`frontend/app/**/page.tsx`** 一致（总卷 **[GO_P5_4_CLOSE.md](../evidence/GO_P5_4_CLOSE.md)**）。**文档侧配套**：**[59](docs/spec/59-企业级全域检查清单与文档补充计划.md)** 篇首「API / ABI 与代码对齐」、**[14](docs/spec/14-合约-API-ABI-前后端对齐.md)** **§2.1**（**`api_router()` 20×`merge`** 路由域 SSOT，与 `crates/api/src/routes/mod.rs` 同源）；改 **04 §3.4** 前端表或 **13-1** 表 1 须按 **[07 §二 2.4](docs/spec/07-开发流程与顺序.md)** 复核 **59** **A1** 等行。 |
| **check-b432-governance-ui-ssot-surface.py** | 须 Python 3 | **B-432**：钉死 **B-428** 演示路径相关 **`frontend/app/governance/*`**、**`/staking`**、**`GovernanceProposalImpactPanel`**、**`GovernanceTargetNotice`** 与 **`governance_b428_closeloop_doc_pointer`** i18n；由 **`run-check-04-routes`** 末步串联；**Build** job 亦单独一步。**前端**：**`npm run test:b432`**（`components/governance` Vitest 切片）。 |

| **check-04-frontend-routes-vs-app.py** | 须 Python 3 | 解析 **04 §3.4「前端页面路由」**表（至 API 主表之前）：首列路径须对应 `frontend/app/**/page.tsx`（**`/`** → **`app/(home)/page.tsx`**；**`:id`** → **`[id]`**；**`/auth/*`** 至少 **login/register**；**`/community/*`**、**`/admin/*`** 须存在目录）。由 **`run-check-04-routes`** 在 **`check-04-routes-vs-code.py`** 成功后串联执行；**Build** CI 单独一步。 |
| **check-13-1-table1-routes-vs-app.py** | 须 Python 3 | 解析 **13-1 §二 表 1** 页面地图 + **「个人中心与 TT 社区·我」互通**子表：全角括号路径、**`/a 或 /b` 择一**、主 IA 表 **Landing/Discover/OrderFlow/…** 命名行映射、裸路径单元格（如 **`/community/me`**）；路径 token 用 **`(?<![0-9A-Za-z])/`** 避免 **TTG/Target** 误匹配；**`/api…`** 前缀视为 **REST 引用**（非 `app` 页面）并跳过。由 **`run-check-04-routes`** 串联；**Build** CI 单独一步。 |
| **check-13-1-routes-covered-by-04-frontend-table.py** | 须 Python 3 | **13-1 表 1** 抽取路径须被 **04 §3.4 前端页面路由**首列某 pattern 覆盖（**`/*`**、**`:id`**、**或** 组择一）；`importlib` 复用前两脚本解析逻辑；纯文档、不读 `frontend/app`。由 **`run-check-04-routes`** 串联；**Build** CI 单独一步。 |
| **check-55-s13.ps1** | Windows | 与上等价；项目根执行 `.\scripts\check-55-s13.ps1`。 |
| **sync-abi-from-forge.sh** | Linux/Mac/Git Bash | 合约变更后：在已安装 **Foundry** 且 `contracts` 已 `forge build` 的前提下，将 `Escrow`/`EscrowFactory`/`GuideIdentityStakingPool`/`ProviderIdentityStakingPool`/`Registry`/`FeeRouter`/…/`GovernanceVotesToken`/`TravelTrustGovernor` 等 ABI 写入 `contracts/abi/*.json`；再按脚本末尾提示复制 **55-S13 子集** 至 `frontend/dapp/abis/`（治理两 JSON **仅** **canonical** **于** **`contracts/abi/`**），并跑 `check-55-s13.sh`。 |
| **sync-abi-from-forge.ps1** | Windows PowerShell | 与上等价；项目根 `.\scripts\sync-abi-from-forge.ps1`，随后 `.\scripts\check-55-s13.ps1`。 |
| **check-55-quick-verify.sh** | Linux/Mac | 55 阶段运行时快速验收：**/health**、**/meta**、**/metrics**、discover/orders、**`/api/v1/community/stats/posts-by-tag?tag=smoke`**（**31 / 04 §3.4**）、**did-rank/itineraries**、**did-rank/guides**、**did-rank/travelers**（各端点 200 且 **jq** 存在时：`?period=week` → **guides** **`rank_basis`**=`guide_reception_gross_total_then_completed_count`、`guides[]`；**travelers** **`rank_basis`**=`tourist_completed_orders_in_window`、`travelers[]`；**itineraries** **`rank_basis`** ∈ `order_completed_at` \| `itinerary_created_at_fallback` \| `itinerary_created_at_proxy`、`itineraries[]`）；**discover 为 200** 时再验 **`/api/v1/discover/orders?limit=1`**（**55-S12**）；**jq** 存在时校验 **/meta** `.service`、**`.dual_write`**（`failure_policy`、`strict_db_write_any`）、**`.indexer.checkpoint`**（**`source`**∈`runtime`\|`startup_snapshot`，**`block_number`/`log_index`** 为 number）、分页 JSON、**community stats**（`.status` / `.post_count`）。未设 `BASE_URL` 时用 **`PORT`（默认 8080）** 拼 `http://localhost:$PORT`。 |
| **check-55-quick-verify.ps1** | Windows | 与上等价；**/meta**、**/metrics** 须 200；**meta** `.service`、**`.dual_write`**、**`.indexer.checkpoint`**（与 **sh** 同口径）；**community stats** `?tag=smoke`；discover 200 时校验 **`?limit=1`**；did-rank **itineraries** / **guides** / **travelers** 200 时校验 **`?period=week`** 与各榜 **`rank_basis`**（**itineraries** 三值枚举 + **`itineraries[]`**）；**`$env:PORT`**。 |
| **smoke-api-public-routes.sh** | Linux/Mac/Git Bash | API 已启动后：**/health**、**/meta**、**/metrics**、`/api/v1/discover/orders`、`/api/v1/discover/orders?limit=1`、**`/api/v1/did-rank/itineraries`**、**`/api/v1/did-rank/guides`**、**`/api/v1/did-rank/travelers`**、**`/api/v1/community/stats/posts-by-tag?tag=smoke`** 须均为 **200**；**jq** 存在时校验 **/meta**（`.service`、`.strict_mode`、**`.dual_write`**、**`.indexer.checkpoint`** 同 **55-quick**）、**itineraries / guides / travelers `?period=week` `rank_basis`** 与列表数组键、discover 分页（**55-S12**）、**community stats** JSON。默认 `http://127.0.0.1:${PORT}`。用于联调留痕，**不替代** 01 §9 E2E 三项。 |
| **smoke-api-public-routes.ps1** | Windows | 与 **smoke-api-public-routes.sh** 等价（**discover** 须 **200**，非 55-quick 之 503 容忍；含 **did-rank/itineraries**、**did-rank/guides**、**did-rank/travelers** 与 **`?period=week` `rank_basis`**；**/meta** **`.indexer.checkpoint`**）；**`$env:API_BASE_URL`** 或 **`$env:PORT`**（默认 **127.0.0.1**）。 |
| **pre-release-automation.sh** | Linux/Mac/Git Bash | 发版前机器项聚合：`check-invariants.sh`、`check-55-s13.sh`、**`run-check-04-routes.sh`**（四步与 **Build** CI 同源；无可用 **Python 3** 时退出码 **2** 跳过本段）；本机有 `forge` 时 `forge build` + `run-verify-abi-forge.sh`。无 forge 设 `SKIP_FORGE_VERIFY=1`。不替代 P0/15 附录〇/53 确认表/55 §八附续.9 人工勾选。**不替代** **`gen-frontend-manifest`**（须 **`npm run build` 后**单独跑）— **[Runbook §12.6](../ops/RUNBOOK.md)**。日常改路由/API 表另见根目录 **[CONTRIBUTING.md](../CONTRIBUTING.md)**「路由与契约」。**分层留痕（推荐）**：**`EVIDENCE_PRE_RELEASE_LOG_DIR=evidence/GO_YYYYMMDD`** 时由**根** **`scripts/pre-release-automation.sh`** 将 stdout **`tee`** 为 **`${EVIDENCE_PRE_RELEASE_LOG_BASENAME:-pre-release-automation-run}.full.txt`**（根 **`.gitignore`** 排除）并生成同基名 **`.summary.txt`**（**`scripts/gates/extract-pre-release-summary.py`**，可提交、体积小）。**可选（Epic F-10）**：**`CHECK_E2E_THREE_PACK=1`** 且 **`EVIDENCE_GO_DIR=evidence/GO_YYYYMMDD`** 时于末尾调用 **`check-e2e-three-pack-evidence.sh`**；可选 **`CHECK_E2E_THREE_PACK_MANIFEST=1`**（须 **jq**）。**不设上述变量则与历史行为一致**。失败语义见 **[Runbook §12.6](../ops/RUNBOOK.md)** **§A 步骤 4**、**[Epic-F ladder · F-10](../docs/runbook/Epic-F-e2e-three-pack-ladder.md#epic-f-f10-pre-release-hook)**。 |
| **check-invariants.ps1** | Windows | 与 **check-invariants.sh** 等价（**rust-toolchain.toml**、**frontend/package-lock.json**、**security_headers_layer** / **x-content-type-options**）；与 **§二** **check-invariants.sh** 行同文档链。 |
| **pre-release-automation.ps1** | Windows | 与 **pre-release-automation.sh** 等价（含 **§12.6** 与 **`gen-frontend-manifest`** 关系）：`check-invariants.ps1`、`check-55-s13.ps1`、`run-check-04-routes.ps1`（无 Python 时退出码 2 跳过，与 Bash 一致）；本机有 `forge` 时 `forge build` + **run-verify-abi-forge.ps1**。无 forge 设 **`$env:SKIP_FORGE_VERIFY='1'`**。**分层留痕**：若 **`$env:EVIDENCE_PRE_RELEASE_LOG_DIR`** 已设，本入口**改调 Git Bash** 执行根 **`pre-release-automation.sh`**（与上表 **.sh** 同源）；无 **bash** 时报错退出。**Epic F-10** 可选 **`$env:CHECK_E2E_THREE_PACK='1'`** + **`$env:EVIDENCE_GO_DIR`**；见上表 **.sh** 行与 **Runbook §12.6**。 |

---

*发版/evidence/08 等流程已精简，按 ops/RUNBOOK.md、evidence/README.md 手工执行。**说明**：原 **`p34_pre_release_checks.sh`** 汇总脚本**已移除**；发版前 08 相关机读请分项使用上表 **`check-08-consistency.sh`**、**`check-08-evidence-pointer.sh`** 等（与 **[07 §四 4.3](../docs/spec/07-开发流程与顺序.md)**、**data/README** 留痕示例一致）。*
