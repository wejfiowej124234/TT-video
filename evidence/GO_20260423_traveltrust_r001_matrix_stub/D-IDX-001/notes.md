# D-IDX-001 · indexer-status (stub evidence)

This directory is referenced from `../report.json` for **R-001** shape only.

**Effective automation (SSOT):** `cargo test -p traveltrust-api internal_indexer_admin_db_api_tests` with **`DATABASE_URL`** set and migrations applied — tests named **`matrix_93_d_idx_001_f029_*`** (see **95 · §8.2 · F-029**).

This stub **does not** replace **ISS-007** closure for **full 93 matrix** or **`build.yml` · `e2e`**.
