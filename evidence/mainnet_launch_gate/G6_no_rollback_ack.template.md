# G6 · 合约层无「链上回滚」口径签收（模板）

**复制本文件** 为 **`G6_no_rollback_ack.md`**（**同目录或运维指定路径**），**填写日期 / 角色 / 签名或工单链接** 后，**`MAINNET_G6_FILE`** **指向该文件** **供** **`check-mainnet-launch-precheck-gate.sh`** **G6** **通过**。

**强制句**（与 **TT-MAINNET §6.2** 一致）：

- 合约层**不存在**「回滚链上状态」；**只存在**：**pause**、**经 Timelock 的治理修复**、**状态对账与补偿**。

| 字段 | 值 |
|------|-----|
| 日期（UTC） | |
| 签收人 / 角色 | |
| 证据（邮件 / 工单 URL，可选） | |
