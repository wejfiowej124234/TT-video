# GO_95 · §11.2 — 00 索引行 ↔ 95 台账机读旁证（2026-04-22）

## 1. 目的与边界

- **目的**：按 **[95 §11.2](../../docs/spec/95-全链路生产就绪检查清单与完成度矩阵.md)**「**95 ↔ 00**」「**00 索引 · 95 行对拍**」做一次**可重复机读**核对（Version、§0.2 关键派生数、文首可数快照在 **00** 行内是否出现）。
- **诚实边界**：
  - **不**等价「**全量 spec 对拍**」「**snapshots/27-archived 全文审计**」「**缺口官方总表 ↔ §9 已闭**」。
  - **不**替代 **§10.1** 各键填实 / **R-001** 主链。
  - **时点稿 `merge` 叙事**：本包仅**登记漂移**（见 §3）；**`snapshots/58-…`** 已于 **v1.4.121** 单篇纠 **20→21**（**`evidence/GO_95_20260422_section11_2_snapshot58_merge/README.md`**）；**`snapshots/`** 目录其余篇**未**扫。

## 2. 命令与输出摘要（仓库根）

**95 `Version:`**

```bash
grep '^\*\*Version:\*\*' docs/spec/95-全链路生产就绪检查清单与完成度矩阵.md
```

**00 表 95 行首段 `v*` 与行尾 `总完成度 %`（单行截断展示）**

```bash
grep '^\| 95 \|' docs/spec/00-文档索引.md | head -c 220 && echo …
grep '^\| 95 \|' docs/spec/00-文档索引.md | grep -o '总完成度 %[^|]*' | head -1
```

**核对结论（本包执行时快照）**

- **`grep '^\*\*Version:\*\*' docs/spec/95-…md`** → 以 **95** 文首 **`Version:`** 为准（本包起草时曾为 **1.4.119** → **1.4.120**；**v1.4.121** 见 **`…section11_2_snapshot58_merge/`**）。
- **`grep -c '\.merge(' crates/api/src/routes/mod.rs`** → **`21`**。
- **`python -c "x=(15/33)+(43/78)+(0/33)+(10/22); print(round(100*x/4))"`** → **`37`**（与 **95 §0.2** 表 **总完成度 %** 一致）。
- **00** 表 **95** 行内可见 **§0.2** 同源片段：**`R=15`**、**`C=43/78`**、**开放 ISS=4**、**`P=10`**、**`K=10/22`**、**`总完成度 %` `37`**、**`api_router()` 21×`merge`**，与 **95 §0.2**/**文首**/**`routes/mod.rs`** 互证。

## 3. `api_router()` `.merge` 真值 vs 时点稿

**代码（`pub fn api_router()` 扇面）**

```bash
grep -c '\.merge(' crates/api/src/routes/mod.rs
# 期望：21（health_meta … internal）
```

**时点稿漂移（示例 · 已由 v1.4.121 单篇纠）**

- **`docs/spec/snapshots/58-企业级检查-完成证明-20260306.md`** 曾写 **20** 次 `merge`；**v1.4.121** 已改为 **21**（见 **`evidence/GO_95_20260422_section11_2_snapshot58_merge/README.md`**）。**`snapshots/`** 其余篇**未**在本轮扫。

## 4. 与 95 文档版本

- **v1.4.120**：本包支撑 **00↔95** 台账机读；**§11.2** 对拍两条标 **`[x]`**。
- **v1.4.121**：**`snapshots/58`** **merge** 纠偏旁证见 **`…section11_2_snapshot58_merge/`**；**§11.2** 时点稿子条 **`[x]`**。
