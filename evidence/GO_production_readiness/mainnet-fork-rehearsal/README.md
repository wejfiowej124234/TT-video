# Mainnet fork rehearsal evidence

```bash
export MAINNET_FORK_RPC_URL="https://…"
bash scripts/dev/rehearse-mainnet-cutover-fork.sh
```

Never `forge --broadcast` to live `chain_id=1` from this harness.
