# §12.1 · S-1 补充读通（根入口三角 · 非主行闭证 · v1.4.162）

**日期**：2026-04-22  
**范围（有界）**：**根** **`README.md`**/**`CONTRIBUTING.md`** 篇首～必读表 + **`CONTRIBUTING.md`** **`api_router()` `.merge` 共 21 次** 句；**根** **`.env.example`** **`wc -l` 真值**；**不**重轮 **00/01/04/07/08-5/14** 全文（**v1.4.150** **`…section12_1_s1_readthrough_batch/`** 已登记）；**不**将 **§12.1 · S-1** 主表 **`[ ]`** 改为 **`[x]`**。

## 1. 闸门（exit 0）

| 命令 | 结果 |
|------|------|
| `bash scripts/check-07-version-triple.sh` | **OK**（07 **1.0.858**） |
| `bash scripts/run-check-04-routes.sh` | **exit 0** |

## 2. 机读（仓库根）

| 指标 | 命令 | 结果 |
|------|------|------|
| 根 `.env.example` 行数 | `wc -l .env.example` | **475** |
| `README.md` | `wc -l README.md` | **203** |
| `CONTRIBUTING.md` | `wc -l CONTRIBUTING.md` | **196** |
| `api_router` `.merge` 次数 | `grep -c '\.merge(' crates/api/src/routes/mod.rs` | **21** |

## 3. 有界读通摘要

- **`CONTRIBUTING.md`** **「路由与契约」** 段：**`api_router()`** **`merge` 共 21 次** 与 **`routes/mod.rs`** 机读 **21** 一致；域序 **`health_meta`→`internal`** 与 **07 §零 0.6**/**14 §2.1**/**95 §12.3** 互指链同源（**S-1** 主批次仍须 **00/01/04/…** 全文闭证）。
- **`.env.example`**：**`wc -l`→475** — 纠 **95** 内 **§11 清点快照**/**§12 清点基线**/**§12.1.1·§10.1（子证）**/**§12.4·「§10.1（子证）」行** 仍写 **约 441**/**`≈441`** 之漂移（与 **文首 ~475**/**§10.1·v1.4.115** **`…doc_alignment_baseline/`** 真值对齐）。
- **`README.md`**：内部协作表链 **07**/**`CONTRIBUTING.md`** 与 **S-1** 入口叙事一致（未扫全文）。

## 4. 诚实边界

- **未**重跑 **`docs/spec/00-文档索引.md`** 主表 **95** 行以外之 **00** 全表对拍。
- **未**将 **§12.1.1** **「S-1 子证」**（**`api_router` merge 枚举**）与本轮合并为 **S-1** 主行闭证。

## 5. 互证

- **95**：**Version 1.4.162** + **文首审计日期** + **§11 清点快照**/**§12 清点基线**/**§12.1.1**/§12.4 **`.env.example` 行数** + **§0.2**/**§6** + **[00-文档索引](../../docs/spec/00-文档索引.md)** 表 **95** 行（同批摘要前缀）。
