# 草稿 / 前序登记（已由最终封口包取代）

**说明**：本目录保留 **2026-04-16** 起登记的 **`tx_hashes.json`**（链上哈希草稿）。**TT-B435 最终封口证据包**（`tx_hashes.json` 与 `indexer_tick.json` / `reconcile.json` / `overview.json` **同目录**）以 **`../run_20260417T003342Z/`** 为母表 **B-435** 引用真源。
