/** 与 `crates/api` `CHAIN_SYNC_MINIMAL_BODY_NOTE` / `GET /meta` `minimal_body_note_stable` 对读（714）。 */
export const ORDER_CHAIN_SYNC_MINIMAL_BODY_NOTE =
  "minimal runtime snapshot when order projection backend is unavailable";
