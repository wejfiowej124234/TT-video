"use client";



import Link from "next/link";

import type { ReactNode } from "react";



/** 社区子域列表页顶栏：页内操作 / 交叉链（可观测见 subtitle `AdminCommunityRelatedLinks` · batch59）。 */

export function AdminCommunityListHeaderAside({ children }: { children?: ReactNode }) {

  return <>{children}</>;

}

