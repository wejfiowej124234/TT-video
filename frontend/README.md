# TravelTrust 前端（Next.js + DApp + 3D）

- **技术栈（旅游项目前端）**：Next.js + React + TypeScript + wagmi + viem + WalletConnect v2 + R3F + drei + Framer Motion。**旅游项目 DApp**：Next.js / React / TS（见 [06-DApp架构总览](../docs/spec/06-DApp架构总览.md)、[09-技术架构总览](../docs/spec/09-技术架构总览-v1.0.md)）。
- **运行**：在 **`frontend` 目录**执行 `npm install` 后 **`npm run dev`**（勿在 CMD 里单独输入 `dev`，否则会报 `'dev' is not recognized`）。pnpm 用户：`pnpm run dev`。后端 API 基地址通过 `NEXT_PUBLIC_API_BASE_URL` 配置。
- **`npm install` / `prepare`**：每次安装后会执行 **`npm run clean`**（删 `.next`）再跑 **`scripts/prepare-optional.mjs`**。若 lock 与 `node_modules` 严重不一致，可 **`FRONTEND_PREPARE_NPM_CI=1 npm install`** 在安装末尾强制 **`npm ci`**（较慢、须已有 **`package-lock.json`**）。根目录启用 **`STRICT_SSOT=1`** 时，可在 **`frontend`** 下执行 **`npm run check:strict-ssot-env`**（需 Git Bash 或 WSL 以运行仓库根预检脚本）核对根 `.env`。
- **开发模式耗时（读日志）**：`next dev` 是**按路由懒编译**的。日志里 **`○ Compiling /traveltrust …`** / **`✓ Compiled … (5792 modules)`** 是**第一次**点到该路由时 Webpack 在扫依赖；紧接着的 **`GET /traveltrust 200 in 2061ms`** 往往把**编译 + 渲染**算在一起，所以会出现 **2～8 秒**，**不等于**线上 TTFB。同一路由**第二次**访问通常只有 **100～250ms** 量级（依赖已进 `.next/cache/webpack`）。**换路由又变慢**：每个**尚未编译过**的 `app/.../page` 都会再走一轮「Compiling …」。**可做的**：① 测真实速度用 **`npm run build && npm run start`**；② Windows 上若可接受 Turbopack 偶发 manifest 问题，用 **`npm run dev:turbopack`** 或 **`TRAVELTRUST_DEV_TURBO=1 npm run dev`**；③ dev 起来后在**另一终端**执行 **`npm run dev:warm`**（`scripts/warm-dev-routes.mjs`），预先 GET 常用路径，减少第一次手动点顶栏的等待。`next.config.js` 里 **`experimental.optimizePackageImports`** 含 framer-motion、TanStack、**wagmi、viem**；**`experimental.staleTimes`**（dynamic/static）延长客户端路由缓存复用；**`middlewarePrefetch: 'flexible'`** 略增预取积极性。**运行时**：`Providers` 内 **`RoutePrefetcher`** 在 `requestIdleCallback` 后对主路由 `router.prefetch`；顶栏 **`NavLink`** 在 **`pointerenter`** 时再预取目标。若需关掉空闲预取（弱网/调试）：**`NEXT_PUBLIC_DISABLE_IDLE_PREFETCH=1`**。
- **故障**：若出现 **`middleware-manifest.json` MODULE_NOT_FOUND**、**`routes-manifest.json` ENOENT**（访问 `/admin` 等直接 **500**）、**`app-build-manifest.json` ENOENT** 或 **`_buildManifest.js.tmp.*` ENOENT**（Windows 上 Turbopack 长时间 HMR 易发）：关掉**所有**占用 3012 的 `node`/`next` 窗口，只保留一个 dev；在 **`frontend`** 执行 **`npm run clean`** 后 **`npm run dev`**（`scripts/ensure-dev-next.mjs` 会在缺 manifest 时自动删 `.next`）。**建议**将 **`frontend/.next`** 加入杀毒软件「实时扫描排除」，避免 manifest 被扫断。Windows 默认 **`npm run dev` 走 Webpack**；需要 Turbopack 时用 **`npm run dev:turbopack`** 或 **`TRAVELTRUST_DEV_TURBO=1`**。勿同时开两个 dev 抢同一 `frontend/.next`。
- **WalletConnect v2**：在 [cloud.walletconnect.com](https://cloud.walletconnect.com) 创建项目，将 Project ID 写入 `NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID`（见 `.env.example`）；不设置则仅使用注入型钱包（如 MetaMask）。
- **链 ID**：`NEXT_PUBLIC_CHAIN_ID` 用于 Escrow 页与钱包组件链校验，需与后端一致（如 Polygon 137、Amoy 80002）；见 `.env.example`。
- **FeeRouter（07 §五 5.2A）**：`NEXT_PUBLIC_FEE_ROUTER_ADDRESS` 须与 Escrow 创建时的 `platformFeeRecipient`、后端 `FEE_ROUTER_ADDRESS` 一致；读取辅助见 `lib/feeRouterEnv.ts`。
- **顶栏五主路由 · ① 代码真源（企业级 · 2026-06-03）**：**[`FIVE-PAGES-ENTERPRISE-CODE-AUDIT-20260603.md`](evidence/GO_local_marketing_front_closure/FIVE-PAGES-ENTERPRISE-CODE-AUDIT-20260603.md)**（十维矩阵 · 五路由功能表 · **AF-01～13**）· **UI 壳冻结** **[`FIVE-MAIN-ROUTES-PHASE1-FREEZE.md`](evidence/GO_local_marketing_front_closure/FIVE-MAIN-ROUTES-PHASE1-FREEZE.md)**（**`/`** · **`/traveltrust`** · **`/market`** · **`/did-rank`** · **`/community/*`**；**仅**数据链路 + 治理层 · **禁止**结构/视觉回流）；各 `app/*/README.md` · **88 §一**。**禁止** ① 冒充 ②③ GO；**非** `archive/ui-v1`。
- **商家入驻 · ① UI 壳冻结（2026-05-26 · 非五主）**：**`/auth/register?role=provider`** · **`/provider/register`** — **[PROVIDER-REGISTER-UI-FREEZE.md](evidence/GO_local_provider_register_closure/PROVIDER-REGISTER-UI-FREEZE.md)** · 全链 SSOT **[`app/provider/register/README.md`](app/provider/register/README.md)**；**仅允许**数据链路 / i18n / 校验 / Admin·market 门闸；**禁止** L5 结构回流；**① 绿集** **`providerRegisterValidation` + `providerRegisterL5.contract`**；全链 **`bash scripts/dev/smoke-provider-onboarding-local.sh`**（**非 ②③ GO**）。
- **旅行收购 PD-009 · ① 数据链已闭（非五主 · 非第五 `users.role`）**：**`/me/identities`** 收购 **「进入子站」** → **`/market/acquisition`** — **[`app/market/acquisition/README.md`](app/market/acquisition/README.md)** · **[`app/me/identities/README.md`](app/me/identities/README.md)** · **[acquisition-publish-trust-rules §8.1](../docs/spec/artifacts/acquisition-publish-trust-rules.v1.md#81-第一阶段--本地--closed2026-05-27)**；**Hub UI 冻结** **[ME-IDENTITIES-UI-FREEZE.md](evidence/GO_local_auth_l5/ME-IDENTITIES-UI-FREEZE.md)**；**仅允许**数据链路 / trust / bond / listing 门闸 / Admin suspend；**禁止** Hub layout lock 回流；**① 绿集** **`acquisitionL5` + `acquisitionL5FullScore` + `meTrust` + `meIdentitiesPage.contract`**；全链 **`bash scripts/dev/smoke-acquisition-pd009-local.sh`**（**非 ②③ GO**）。
- **订单页 `/escrow/[id]` · ① 收口（2026-05-28 · 非五主）**：创新行程解锁后 **Experience 暖壳**（`experienceDraft`）— **[ESCROW-ORDER-PAGE-PHASE1-CLOSURE.md](evidence/GO_local_web3_itinerary_l5/ESCROW-ORDER-PAGE-PHASE1-CLOSURE.md)** · UI 硬闸 **[ESCROW-DRAFT-EXPERIENCE-FREEZE.md](evidence/GO_local_web3_itinerary_l5/ESCROW-DRAFT-EXPERIENCE-FREEZE.md)** · 组件 **[`components/escrow/EscrowDetail/README.md`](components/escrow/EscrowDetail/README.md)** · 路由 **[`app/escrow/[id]/README.md`](app/escrow/[id]/README.md)**；**仅** bugfix / 数据链路 / i18n；**禁止** 旅行者默认 FeeRouter / cyan DID；高级区仅 **`NEXT_PUBLIC_ESCROW_DEV_TOOLS=1`**；**① 绿集** **`bash scripts/dev/run-web3-itinerary-l5-green.sh`**（含 `escrowDraftExperienceUiFreeze`）；**已上链协议 DID 壳未收口**。
- **入口**：首页 **`/`**（L0 **Home** 深条；**Web3旅行** 仅 **`pathname === "/"`** 激活）。**`/traveltrust`**：字标 → 本页（**不在** **`<nav>`** 重复）；L0 **Cinematic** 深条；**L1** **`TravelTrustHomeLandingNavSlot`**（**portal → body** · z-280）+ **`TravelTrustLandingChrome`** / **`TravelTrustPulseTicker` inline**（**CSS** 公告跑马灯 · **`globals.css`**）；页身 **layout lock**（**无** `#overview`）。**`/market`/`/did-rank`/`/community`**：L0 **Dark** 深条 + 四链暖金。**Console**（**`/orders`** 等）：L0 **Light** 浅条 + 暖棕激活。真值 **[86 §6.0](../docs/spec/86-UI-双系统未来风-风格与动效技术规格.md)**、**[88 §一](../docs/spec/88-五主路由页身实现快照与UX缺口审计-20260330.md)**。**`<nav>`** 四链：**`/`**、**`/market`**、**`/did-rank`**、**`/community`**（`header_*` i18n）。**`/market`**：**`WarmRouteFieldBackdrop`** + **`MarketAmbientBackdrop`**。**`/did-rank`** / **`/community/*`**：暖场 + **静态** **`bg-scifi-gradient-static`**（壳层**无** Three）。**`/did-rank` 榜单主架**：竖脊 **五签**（旅行者/向导/行程/商家/收购）+ **`framer-motion` 内页翻页** + **`?board=`**（含 **`itinerary`**）；[`app/did-rank/README.md`](app/did-rank/README.md) · **[30 §1/§6](../docs/spec/30-DID排行榜-页面规范.md)**。**`/` 代码 SSOT**：[`app/(home)/README.md`](app/(home)/README.md)（**无** `app/page.tsx`）。**① 数据链**：**1×** `postItineraryCreate` · **`ITINERARY_CARD_COUNT=1`** · **`landingItinerarySession`**（**`localStorage`** · 跨 tab · **`marketFavoritesStorage.ts`**) · **`UnlockModal`→`getOrder`** → **`/escrow/[id]`** — **[LANDING-MARKET-PAGES-CODE-SSOT](evidence/GO_local_web3_pages_closure/LANDING-MARKET-PAGES-CODE-SSOT.md)** §2 · 走廊 **[GO_local_web3_itinerary_l5](evidence/GO_local_web3_itinerary_l5/README.md)** · 订单页 **[ESCROW-ORDER-PAGE-PHASE1-CLOSURE](evidence/GO_local_web3_itinerary_l5/ESCROW-ORDER-PAGE-PHASE1-CLOSURE.md)**。**`/market`**：**`useMarketPage`** **300ms debounce** · 收藏 **localStorage**（**F-020 best-effort 已接线（①）· ② SLA**）— **[`app/market/README.md`](app/market/README.md)** · **CODE SSOT** §3。

## 质量

- **Lint**：`npm run lint`（Next.js ESLint）
- **类型**：`npx tsc --noEmit`
- **单测**：`npm run test`（Vitest，含 `lib/api`、`lib/apiClient`、`lib/didRankUtils`、`dapp/hooks/useEscrowActions`）
- **E2E**：`npx playwright install` 后 `npm run e2e`（需先 `npm run dev` 或已启动的本地服务）
- **Epic A（治理 proposals 执行态只读 UX · A-10 收口）**：验收命令见 **[docs/runbook/Epic-A-governance-execution-ux-ladder.md](../docs/runbook/Epic-A-governance-execution-ux-ladder.md)**（§ 前端验收命令）；证据指针 **[evidence/GO_EPIC_A_GOVERNANCE_EXEC_UX_CLOSE.md](../evidence/GO_EPIC_A_GOVERNANCE_EXEC_UX_CLOSE.md)**。

## 目录

- `app/`：页面与路由（App Router）
- `components/`：通用组件与 DApp 用 Providers（Wagmi + React Query）
- `lib/`：API 客户端、auth、me、orders 等（与 04 对接）
- `dapp/`：钱包、EIP-712、txMachine、viem watchContractEvent 等（与 06 一致）
- 3D / 动效：**R3F + drei** 用于 **`/traveltrust`** cinematic 3D（**`modules/traveltrust-home`** · layout lock；**非** legacy **`TravelTrustNetworkParticles`** 页内树）；**`MePageBackground`** 用于 **`/me`**、**`/guide`** 等（**`/community/me`** 仅用社区壳，**88 §二**）。**`/`** 为 **Ken Burns + 点阵 + 玻璃 + 1 预览卡数据链 + 冷灰页脚**（**[(home)/README](app/(home)/README.md)** · **[GO_local_web3_itinerary_l5](evidence/GO_local_web3_itinerary_l5/README.md)** · **FIVE-MAIN-ROUTES** · **88** · **86 §6.1**）。Framer Motion 用于页面/组件过渡。可选：`r3f-perf`、WalletConnect UI 等（**09** §2.5）。
