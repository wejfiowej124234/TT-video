/**
 * Default on-chain settlement token symbol for orders/cards/API mocks.
 * User-visible copy uses `order_defaultSettlementToken` in locales.
 */
export const DEFAULT_SETTLEMENT_CURRENCY_CODE = "USDC" as const;
