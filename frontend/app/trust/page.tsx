"use client";

import TrustTransparencyHub from "@/components/trust/TrustTransparencyHub";

/** P-UX1 / P-UX2：信任中心 — 用户路径入口 `/trust` */
export default function TrustPage() {
  return <TrustTransparencyHub />;
}
