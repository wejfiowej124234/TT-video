/**
 * @deprecated 已替换为 `OrdersListFilterRail`（纯 CSS sticky）。
 * 保留此垫片，避免 dev 热更新仍解析已删文件时加载旧 IntersectionObserver 实现。
 */
export { OrdersListFilterRail as OrdersListStickyFilterDock } from "./OrdersListFilterRail";
