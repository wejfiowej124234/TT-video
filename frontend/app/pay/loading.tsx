"use client";

import { PayRouteSuspenseFallback } from "@/components/pay/PayRouteSuspense";

/** 与 `PayRouteSuspense` 同暖色壳骨架 */
export default function PayLoading() {
  return <PayRouteSuspenseFallback />;
}
