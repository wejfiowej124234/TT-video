---
description: TravelTrust AI 协作减负与边界（详版见 docs/AI协作话术-减负与边界.md）
alwaysApply: true
---

# TravelTrust · AI 协作边界

- **规划方向**：默认 **先本地 + 测试网跑通**，**主网真链 / Production GO** **另闸**（根 **README**「工程规划方向」、**TT-9618**、**96-索引**、**TT-9600**、**go-live-checklist#go-decision-entry-point**；**Hub / 分册 Version** 以 **00#文档版本与最后更新** 为准，**Declaration** 见 **96-索引#95-96-execution-linkage-declaration**；**`#9618-*`** **真源** **`docs/spec/96-18-未完成清单与多维检查.md`**（**`#9618-one-page-priority`** **/** **`#9618-cmd-cheatsheet`**）**与** **README** **两锚** **同源**）；**不**把主网触链当每轮默认完成标准，除非用户本轮写明。
- **进度口径（①②③，防跑阶）**：任何「进度 / 完成度 / 已闭 / GO」**必须**标明落在 **① 本地**、**② 测试网** 或 **③ 公网·生产真实链路**（与根 **README**「工程规划方向」、**TT-9618**、**96-18** / **96-18-未完成**「验收顺序三阶」同源）；**须顺序递进，禁止跳阶**；**分层口诀（写死）**：**先本地跑通** → **再测试网跑通** → **再在公网（生产）查问题并收口**（与 **`docs/00-文档索引.md`** **「工程验收分层」** **同句**）；**禁止**用 **①②** 冒充 **③**（主网真链、生产 PSP、`go-live` / **Production GO** 等）除非用户**本轮写明**验收 **③**。
- **读任意 `docs/` 时**：先判断任务属于 **① / ② / ③**；**未完成当前阶可验证目标前，不**规划或宣称下一阶已完成；**禁止跳阶**；与根 **`AGENTS.md`**、**`CONTRIBUTING.md`** 篇首阶次纪律同源。
- **handbook / ADR / Wave 词**：程序员入口 [`docs/handbook/README.md`](docs/handbook/README.md)；**ADR（Why）** [`docs/adr/README.md`](docs/adr/README.md) + [`engineering/07`](docs/handbook/engineering/07-架构决策记录ADR规范.md)；**`engineering` 内 Wave/阶段消费词** [`32 §0`](docs/handbook/engineering/32-横切-Wave与阶段体系导读.md#x32-0-terminology)；与 [`spec/00` 读前摘要](docs/spec/00-文档索引.md) 表体对读；**`handbook/engineering*` 文档版本母表边界**（**主序 `00～50` + `engineering/README` + `engineering/adr/README`**；**`EVIDENCE-*`/`_TEMPLATE` 不逐行入表**）见 [`spec/00` · 文档版本与最后更新](docs/spec/00-文档索引.md#文档版本与最后更新) 节前说明。
- **engineering 主序三处对拍**：增删改 **`docs/handbook/engineering/`** 下 **`00～50` 的 `NN-*.md`** 时，须 **手册 00 §3**、**`engineering/README`**、磁盘 **同序同号**；勾选项 **[`engineering/02` · §4a](docs/handbook/engineering/02-生产级文档约束与合入门禁.md#hb-prod-doc-triple-sync)**（与根 **`README`**「**handbook 工程主序**」行、**`CONTRIBUTING.md`**、**[`engineering/08`](docs/handbook/engineering/08-文档与spec迁移台账.md)** 文首同条）。**`engineering/NN-*` / `EVIDENCE-*`** 文首 **`SSOT（必读）`** 见 **[手册 00 · §2.2](docs/handbook/00-手册总览与编制规范.md#hb-00-frontmatter-min)**（与 **`engineering/README`** 段首、**`CONTRIBUTING.md`**、**[`spec/00` 读前 · handbook 与 spec 边界](docs/spec/00-文档索引.md)** 同键：**`engineering/`** **仅为导读，不替代 spec**；真源 **04 §3.4**、**93**、**14**、**代码与脚本**；**HTTP/ABI** **同 PR** 闭包见 **04 §1a**/**50 §5**）。
- **`EVIDENCE-*`（engineering）**：改 **`docs/handbook/engineering/EVIDENCE-*-cluster-verified.md`** 须 **`bash scripts/check-handbook-engineering-content.sh`**（**`HBOOK-ENG-EVIDENCE`**）；母版 **[`engineering/22 §2`](docs/handbook/engineering/22-横切-簇级verified证据模板.md#vtpl-2-naming)**；与 **`CONTRIBUTING.md`**（**必读入口** / **「提 PR 前」**）同源。**§V-1 并集验收入口**：动 **`EVIDENCE-*` `## V-1`**、**`scripts/run-handbook-cluster-evidence-v1.sh`** / **`scripts/gates/…`**、或 **`handbook-cluster-evidence-v1.yml`** 时，须 **`bash scripts/run-handbook-cluster-evidence-v1.sh` exit 0**；末行 **`TT_EVIDENCE_V1_SUMMARY:`** **稳定可 grep** 且与各 **`## V-1`** **语义一致**，否则**未完成** — **`CONTRIBUTING`**、**`scripts/README.md`**、**[`engineering/22` §3](docs/handbook/engineering/22-横切-簇级verified证据模板.md#vtpl-3-v1)**。
- **`docs/spec` 路径依赖**：凡是**新增、删除或改动** **`docs/spec` 路径依赖**（硬编码消费方见 **`CONTRIBUTING`**），**须同步**更新 **[盘点 `docs/spec-path-dependency-migration-inventory.md`](docs/spec-path-dependency-migration-inventory.md)** 与 **`registry/spec-path-dependencies.v1.yaml`**。**提交前**本地依次 **`python registry/validate-spec-path-dependencies-registry.py`**、**`bash scripts/check-handbook-frontmatter.sh`**、**`bash scripts/check-handbook-engineering-content.sh`** **exit 0**；**不以** PR / Actions 为收口；**`registry-spec-path-dependencies-validate.yml`** 为 **CI 恢复后可选**。**不**借此迁移或删除 **`docs/spec`**，**除非单独立项**；**不**改 **`build.yml`** 默认 Build 必过主链 — **`CONTRIBUTING`**、**[`registry/README.md`](registry/README.md)**。
- **先计划再读**：动手前列出拟读取文件路径，**≤8**；优先 `grep`/`semantic search`，勿无目标通读超大 spec（**07**/**04**/**tail**）。
- **范围**：只改用户给定路径或本轮明确模块；**禁止**无关重构、删注释、顺手格式化全文件。
- **文档**：默认只改 **04**（§3.4 / §三 契约句）。**07**、**00** 版本表、**缺口官方总表**、**27-archived tail** 仅当用户写明 **「台账同批」** 或 **「合 07 门禁」** 时同批修改；否则不动 6.4 长表。
- **版本三线**：若 bump **07** `Version:`，必须同批 **00** 中 07 行与 **§六 6.5** 表顶，并跑 `bash scripts/check-07-version-triple.sh`。
- **测试**：默认 `cargo test -p traveltrust-api`；勿擅自 `cargo test --workspace` 或 `npm test`，除非用户要求全量。
- **迭代期 vs 发版**：功能迭代期**不以** full production gate 作为每日完成标准；日常以快速检查 + 受影响 E2E + `scripts/gates/e2e-stability-probe.sh` 为主；**冻结后再跑** full production gate。话术与三层门禁见 [docs/AI协作话术-减负与边界.md](docs/AI协作话术-减负与边界.md) §12。
- **完成度 %**：无新业务代码合入的纯文档轮，**勿改** **07** 文首（1）（2）百分比（见 **07 §6.6** 复审注）。
- **可复制话术全集**：[docs/AI协作话术-减负与边界.md](docs/AI协作话术-减负与边界.md)
