# 数据库相关：缺口与需补充清单

**用途**：集中列出当前**缺口**、**问题**与**需补充项**，便于接库前与迭代中逐项闭合。与 [04-数据库架构与表分类](spec/04-数据库架构与表分类.md)、[04-业务逻辑与数据库支持清单](spec/04-业务逻辑与数据库支持清单.md)、[41-后端数据库接库与落地清单](spec/41-后端数据库接库与落地清单.md)、[数据库表与UI功能对照检查](数据库表与UI功能对照检查.md) 配套。

**更新**：接库或实现完成后请在本清单中勾选或更新状态。

---

## 一、代码缺口（实现层）

| # | 缺口 | 说明 | 优先级 | 状态 |
|---|------|------|--------|------|
| 1 | **itineraries 的 db 层与双写** | 迁移 000006 已建表；`db.rs` 已实现 insert_itinerary、list_itineraries、update_itinerary_snapshot_hash；chain_off 双写 + main 启动 hydrate | P0 | ✅ 已做 |
| 2 | **order_messages 的 db 层与双写** | 迁移 000007 已建表；`db.rs` 已实现 insert_order_message、list_order_messages_all；chain_off 双写 + main 启动 hydrate | P0 | ✅ 已做 |
| 3 | **幂等键使用** | 迁移 **20250308000013** 已建表；中间件 **55-S8** 在有 `DATABASE_URL` 时读写 `idempotency_keys`；`REQUIRE_IDEMPOTENCY_KEY=1` 时缺键 400 | P2 生产前 | ✅ 已做（按需开启强制） |

**闭合方式**：完成 1、2 后行程与聊天接库即可持久化；**3** 已具备表+中间件，生产按策略开启 `REQUIRE_IDEMPOTENCY_KEY` 并监控。

---

## 二、迁移 / DDL 缺口（表或列缺失）

| # | 缺口 | 说明 | 优先级 | 状态 |
|---|------|------|--------|------|
| 4 | **orders 表缺列** | 04 附录 §9.4 有 trip_end_at、snapshot_hash；当前迁移 000003 无。对账/审计或确认最终方案落库时需要 | P1 可选 | ☐ 未做 |
| 5 | **users 表缺列** | 04 §2.1 有 email_verified_at；当前迁移 000001 无。若做邮箱验证流程需补 | P1 可选 | ☐ 未做 |
| 6 | **idempotency_keys 表** | 04 附录 §7；迁移 **20250308000013** 已建 | P2 | ✅ 已建 |
| 7 | **guide_slot 表** | 04 附录 §9.9；档期占位。当前可由 orders 查询「该 guide 非终态订单」替代，非必须 | P2 可选 | ☐ 未做 |
| 8 | **evidence_receipts 表** | 04 附录 §9.7；迁移 **20250304000009** 已建表；全路径写入是否覆盖见 API/产品 | P2 可选 | ✅ 表已建 |
| 9 | **stakes 表** | 04 §2.2 有 stakes；当前以 guides.stake_amount 满足 MVP。若需质押历史、locked_until、slashed_amount 可建 | P2 可选 | ☐ 未做 |

**闭合方式**：按需新增迁移（如 000008_orders_optional_columns.sql、000009_idempotency_keys.sql）或 ALTER TABLE；并在 04-附录-DDL 与 04-数据库架构 中同步说明。

---

## 三、文档缺口（说明或清单未补齐）

| # | 缺口 | 说明 | 优先级 | 状态 |
|---|------|------|--------|------|
| 10 | **索引与约束一览** | 企业级审计建议「现有索引/约束一览与复合索引建议」；便于性能与门禁核对 | P1 | ✅ 已补（见 04-数据库架构与表分类 §七） |
| 11 | **敏感字段与访问策略** | 架构文档已有一句（password_hash、passport_number_hash、token；日志脱敏）；若需展开可单独小节 | P1 可选 | ☐ 已有一句；可展开 |
| 12 | **备份与恢复策略** | Runbook 有「外部备份策略由法务/运维另行规定」；业务库（PostgreSQL/CockroachDB）的日备/恢复演练未单独成节 | P2 | ☐ 建议 Runbook 或 41 增「业务库备份与恢复」小节 |
| 13 | **reviews 唯一约束** | 若产品要求「一单一评」或「一单每角色一评」，需在 reviews 上增加 UNIQUE(order_id, reviewer_id) 或等价约束；当前未写死 | P2 可选 | ☐ 待产品定稿 |

**闭合方式**：10 已在 04-数据库架构与表分类 中补 §七；11～13 按需在 Runbook、41 或 04-附录 中补充。

**已补齐资料**：开工前「按页面×功能×API×表」全方位对照已落档 [数据库与UI-全方位多维度深度检查报告](数据库与UI-全方位多维度深度检查报告.md)，与本文、对照检查、41、04-业务逻辑 形成闭环。

---

## 四、产品 / 后续（增量能力）

| # | 项 | 说明 | 状态 |
|---|-----|------|------|
| 14 | **TT社区（社区域）** | 核心表与 `/api/v1/community/*` 已具备（有 DB 时）；**举报/审核工单/推荐审计** 等见 **160** Target | ⚠ 核心 ✅；160 增量待产品 |
| 15 | **Governance（治理域）** | **governance_pool / governance_reward_records** 与 `GET /api/v1/governance/pool|rewards` 已具备；**提案/投票** 全链路仍待产品 | ⚠ 池/奖励 ✅；完整治理页待迭代 |

---

## 五、问题与风险（需注意）

| # | 问题 | 影响 | 建议 |
|---|------|------|------|
| 16 | ~~接库后行程/聊天仍仅内存~~ | 已实现 #1、#2；接上 DATABASE_URL 并执行迁移后行程与聊天持久化 | 已闭合 |
| 17 | **orders 与 04 附录字段不一致** | trip_end_at、snapshot_hash 缺失；若对账或审计依赖这两列，报表需补 | 按需做清单 #4 |
| 18 | **生产环境幂等** | 未开启 `REQUIRE_IDEMPOTENCY_KEY` 时，部分写路径仍依赖客户端自觉传键 | 生产前按需开启并观测；表与中间件见 #3、#6 |

---

## 六、快速对照（按优先级）

| 优先级 | 项（编号） | 建议动作 |
|--------|------------|----------|
| **P0** | 1、2 | ✅ 已完成（itineraries、order_messages 的 db.rs + chain_off 双写 + main hydrate） |
| **P1** | 4、5、10 | 可选：orders/users 补列；索引一览已补 §七 |
| **P2** | 3、6、7、8、9、12、13 | 生产前：确认幂等策略与 `REQUIRE_IDEMPOTENCY_KEY`；可选：guide_slot、stakes、备份小节、reviews 约束；evidence_receipts 写入覆盖 |
| **后续** | 14、15 | 社区 **160**、治理完整提案/投票由产品迭代 |

---

**开工前多维度检查**：按页面逐项核对 UI→API→表、缺表/缺列/待实现汇总见 [数据库与UI-全方位多维度深度检查报告](数据库与UI-全方位多维度深度检查报告.md)。

*与 [04-数据库架构与表分类](spec/04-数据库架构与表分类.md)、[04-业务逻辑与数据库支持清单](spec/04-业务逻辑与数据库支持清单.md)、[41-后端数据库接库与落地清单](spec/41-后端数据库接库与落地清单.md)、[数据库表与UI功能对照检查](数据库表与UI功能对照检查.md) 配套。*
