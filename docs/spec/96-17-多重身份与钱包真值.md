# 96-17 · 多重身份与钱包真值（执行核对分册）

<a id="identity-multi-wallet-ssot"></a>

**Version:** 1.0.21  
**Status:** Living — **96 执行分册**（发版前抽检与跨文档对读）；**本分册为「多重身份」执行层主入口**（**96 主**；**64 / 04 / 30 / 87 / 97** 等为辅助真源）。**条款真值**仍以 **[64 §五](64-申请向导-行业标准与DID检查清单.md#五钱包与账号体系多钱包--换绑--向导-vs-旅行者)** 与 **[04](04-后端与API.md)** 为 SSOT，**不**替代 **95 §3/§8.2** 功能矩阵。  
**Parent:** [96-索引-全链路外生产验收分册.md](96-索引-全链路外生产验收分册.md)  
**最近更新：** 2026-05-27 — **v1.0.21**：**§0.3.3 商家行** **93** 互指 **B-MKT-004/005**（修正旧 **005～012**）；增 **96-20**。**承** **v1.0.20**。  

---

## §0 读前摘要

| 你要找什么 | 单源 / 入口（**主 = 本分册 §；辅 = 外链**） |
|------------|-------------|
| **DID 排行榜：当前四类脊签身份 + 将来扩展的工程留口** | **§0.2**（主）；产品/视觉细规 **→ [30](30-DID排行榜-页面规范.md)**；`?board=` / `rank_basis` **→ [04-附录-did-rank](04-附录-did-rank对接说明.md)** |
| **产品口径：账号 vs 向导 DID、换绑、显式不支持** | **§2** + **辅 [64 §五](64-申请向导-行业标准与DID检查清单.md#五钱包与账号体系多钱包--换绑--向导-vs-旅行者)** |
| **HTTP 契约（Me、auth、did-rank）** | **辅 [04 §三 / §3.2](04-后端与API.md)**（含 **`GET /me`** **`identity_slots`**）、**[04-附录-did-rank](04-附录-did-rank对接说明.md)** |
| **回归矩阵（A 域）** | **辅 [93 §1](93-全站功能验证矩阵-域别回归清单.md#1-a-域--账号--注册--登录--me--密码--邮箱)** |
| **功能行对位** | **辅 [95](95-全链路生产就绪检查清单与完成度矩阵.md)**（如 **F-005**、**F-023**） |
| **登录 / 找回 / 会话与链上投影边界** | **辅 [97](97-登录找回密码钱包态企业级审计清单.md)** |
| **链上身份质押 / 订单押金（≠ 排行榜脊签）** | **辅 [81](81-经济模型-向导质押与订单押金.md)**；治理代币 **→ [82](82-治理币-文档总览.md)**、**[governance-token/02](governance-token/02-对内技术规格-草案.md)** |
| **主理人 TTG 质押 bps / 辖区 cap（协议层 · ≠ 96-18 准入费）** | **辅 [protocol-ssot.v1.md](governance-token/protocol-ssot.v1.md)**；**`region_steward` 开通** 仍以 **96-18 + `/me/onboarding`** 为准；**API** **`GET /api/v1/governance/protocol-reference`** |
| **协议四类角色用语（与脊签叙事对读）** | **辅 [87](87-TravelTrust-角色体系技术文档-融合架构版.md)** **§1.1·§1.3·§4**（四类与 Shop/**`provider`**）；**§1.4**（旅行收购产品扩展） |
| **商家/主理人准入费 · `provider`/`region_steward` 与 `/me/onboarding`** | **辅 [96-18](96-18-商家与主理人准入费用与治理币兑换设计.md)**、**[04 §3.4](04-后端与API.md)** **`/me/onboarding`**；**①② Runbook / PG 机读** **→ [TT-9618](../runbook/TT-9618-onboarding-local-testnet.md)**；**GAP 台账** **→ [96-18-未完成](96-18-未完成清单与多维检查.md#9618-one-page-priority)** **v1.0.118+**（**与** **`[00]`** **表体** **对拍**） |
| **四脊签 · 权限与申请标准（人读矩阵）** | **§0.3**（总框 **[`#identity-four-roles-framework`](#identity-four-roles-framework)**；主表 **[`#identity-four-spine-prod-matrix`](#identity-four-spine-prod-matrix)**）；**身份×文档速查表** **[§0.3.0](#identity-classification-doc-map)** |
| **总则里与本分册关系** | **[96-01](96-01-总则与95边界和执行顺序.md)** **§0** / **§0.3**（**P0 一束** 含 **96-17** 行）；锚 **`identity-multi-wallet-ssot`** 位于本文文首 |

**本分册定位（升级项目起点）**：把「**账号—钱包—向导 DID**」与「**排行榜脊签身份维（当前四类、可扩展）**」从散落在 **93/95/97/30** 的互指，收敛为 **96** 侧**可勾选、可留痕**的执行核对面；后续 **EPIC** 在本分册 **§5** 登记，**不**在本文写假实现。

### §0.1 范围与边界（与其它 96 分册同构）

| 项 | 说明 |
|----|------|
| **本册职责** | **执行核对**：**§0.2** 脊签身份枚举与扩展契约；**§0.3** 四脊签 **权限 + 申请与开通**（**§0.3.3** **[生产核对矩阵](#identity-four-spine-prod-matrix)** 正文已闭合；**GO** 仍以 **93 PASS + 95 §8.2** 证据为准）；**§2** 字段语义与混读风险；认证对 DB 列的影响；发版 **§4** P0 抽检；升级 **§5** backlog 指针。 |
| **与 95** | **不**新开 **F** 行；功能完成度仍以 **95 §3 / §8.2** 为准。 |
| **与 96-01** | **P0 一束** 见 **[96-01 §0.3](96-01-总则与95边界和执行顺序.md)**「**身份 · 钱包真值**」行。 |
| **Hub 本地验证** | **[TT-9600](../runbook/TT-9600-96-HUB-LOCAL-VERIFICATION-PACK.md)** **§3** 总表索引；**不依赖** GitHub Actions。 |

### §0.2 DID 排行榜 · 脊签「身份维」（**当前四类 · 闭合枚举**；**须留扩展口**）

**路由真值**：`/did-rank`（与 **[30 §5～§6](30-DID排行榜-页面规范.md)**、**Header**「DID排行榜」一致）。**书脊切换**与 URL 查询参数 **`?board=`** 绑定；**页内同一时间只展示一榜**。

#### §0.2.1 当前闭合集（仅下列四类；产品对外叙事以此为准）

| 序号 | 脊签（产品文案） | **`?board=`**（URL 真值） | TypeScript **`DidRankBoardTab`**（实现） | UI 入口块（实现） | 辅助规范 |
|:----:|------------------|---------------------------|---------------------------------------------|---------------------|----------|
| 1 | **旅行者** | **`traveler`**（缺省；非法或未知见 **§0.2.3**） | `traveler` | `TravelerRankBlock` | **30 §2** |
| 2 | **向导** | **`guide`** | `guide` | `GuideRankBlock` | **30 §3** |
| 3 | **商家** | **`provider`**（别名 **`merchant`** 解析到同一 Tab，见 **`parseDidRankBoardParam`**） | `provider` | **`ProviderRankBlock`**（**① UI 壳已挂载** + **`getDidRankProviders`**；**②** **`rank_basis`/产品真值 Partial** — **30 §3.1** · **[FIVE-MAIN-ROUTES](../../frontend/evidence/GO_local_marketing_front_closure/FIVE-MAIN-ROUTES-PHASE1-FREEZE.md)**） | **30 §3.1** |
| 4 | **旅行收购** | **`acquisition`** | `acquisition` | **`AcquisitionRankBlock`**（**① UI 壳已挂载** + **`getDidRankAcquisitions`**；**②** 产品排序 **Partial** — **30 §3.2** · **87 §1.4**） | **30 §3.2**、**87 §1.4** |

**实现真值（机读旁证，非契约正文）**：`frontend/lib/didRankUtils.ts`（**`parseDidRankBoardParam`**、**`DidRankBoardTab`**）；**`frontend/app/did-rank/page.tsx`**。

#### §0.2.2 扩展第 5 类及以后的**强制清单**（生产级 · 文档与门禁同批）

新增脊签身份时，**须同批**完成下列事项（**缺一不得宣称该榜已发布**）：

1. **类型与 URL**：扩展 **`DidRankBoardTab`**；在 **`parseDidRankBoardParam`** 中**显式**解析新字面量；**禁止**依赖「未列字面量悄悄归并到 `traveler`」冒充上线（若产品改为未知 `board` **显式 4xx** 或专用占位页，须在 **30 / 04 附录 / 本 §** 同批写明）。  
2. **契约**：在 **[04-附录-did-rank](04-附录-did-rank对接说明.md)** 登记 HTTP 路径（若适用）、**`rank_basis`**、**`period` / `since` / `limit`** 等与现有榜同形元数据；**04 §3.4** 路由总表 + **`bash scripts/run-check-04-routes.sh` exit 0**。  
3. **产品规范**：**[30](30-DID排行榜-页面规范.md)** 增补脊签节、色键、**§7.1** 台账行；与 **[87](87-TravelTrust-角色体系技术文档-融合架构版.md)** 角色用语对读。  
4. **回归证据**：**93** 矩阵或等价用例 +（若触及 **`page.tsx`** 大块结构）**96-16** 路由母表 / 证据链。  
5. **i18n / a11y**：脊签 **`role="tablist"` / `tab` / `tabpanel`** 与新文案键（见 **30 §7** 无障碍约定）。

#### §0.2.3 未知 `?board=` 的**当前实现口径**（评审点）

**当前代码**：非法或未知 `?board=` → **`parseDidRankBoardParam` 回退 `traveler`**（fail-open）。**扩展枚举时**须由 **Release Owner** 决定：维持 fail-open、改为 **显式错误态**、或 **独立占位路由**；决定须写入 **30** 与 **本条** 同版本 **changelog**，并在 **§4** 抽检中可查一句。

#### §0.2.4 与其它「身份」措辞的分轨（**禁止混读**）

| 概念 | 含义 | **主文档** |
|------|------|------------|
| **本 §0.2 脊签身份** | 排行榜 **UI / URL / 产品榜种** 维度的四类（可增） | **96-17 §0.2** + **30** |
| **账号默认钱包 vs 向导 DID** | **同一 user** 下 `users.default_wallet_address` 与 `guides.wallet_address` 可能不同 | **96-17 §2** + **64 §五** |
| **链上身份质押池** | Guide / Provider **IdentityStakingPool** 系、接单签名经济边界 | **81**、**14**、**95 §7.1 · 域 J**（`/staking` 等） |
| **did-rank HTTP / PG 排行** | 与脊签数据同轨的 API 真值（**≠** 脊签 UI 文案本身） | **95 §7.1 · 域 K**、**04 附录**、**30** |
| **治理持币与投票权** | **TTG**、`/governance` 控制台；**不以**「榜种脊签」替代 **计票真值** | **82**、**governance-token/02**、**89** |

<a id="identity-four-roles-framework"></a>

### §0.3 四脊签身份 · **权限与申请标准**（**生产核对矩阵**）

本节与 **§0.2.1** 一一对应；**§0.3.3** 为 **「权限 / 申请与开通标准 / 全链路挂钩」** 的**人读主表**（正文已闭合）。**HTTP 机读真值**仍以 **[04 §3.4](04-后端与API.md)** 为准；**字段与账号/向导 DID 边界**仍以 **64 §五** 为准。扩展新脊签须走 **§0.2.2** 并与 **87**、**04 / 04 附录**、**93 / 95** **同批**修订。

<a id="identity-classification-doc-map"></a>

#### §0.3.0 身份分类总表与文档地图（速查）

**用途**：一眼区分 **脊签 / `?board=` / `identity_slots[].id` / 资质·经济·UI 各去哪份文档**；**不**替代 **64 / 81 / 04 / 30** 正文 SSOT。

| 脊签（产品） | **`?board=`**（§0.2.1） | **`identity_slots[].id`** | 账号侧默认/说明 | 资质与申请条款 | 质押与经济 | UI / 入口（示例） |
|-------------|-------------------------|----------------------------|-----------------|----------------|------------|-------------------|
| **旅行者** | `traveler`（缺省） | `traveler` | 注册完成即 **旅行者侧**；与 **`users_role_is_traveler_side`**（`chain_off/me.rs`）同源 | **64 §五**（钱包 vs 向导 DID） | 非 **81** 身份池主对象（见 **81 §1** 边界） | 个人中心、下单；Header **多重身份** |
| **向导** | `guide` | `guide` | **`POST /api/v1/guides`** 审核通过后槽位 **active**；接单/托管签名以 **`guides.wallet_address`** 为准（**≠** `users.default_wallet_address`） | **64**（**§一** + **§1.1** 证据包）**、87** | **81**（Guide 池；**可选质押与信任展示**见 **81 附录 D**） | **`/guide/register`**；**申请向导**；**向导质押** |
| **商家** | `provider`（`merchant` 别名） | `merchant` | **`users.role=provider`** 等与槽位对表以 **04 / 41** 为准；勿与 URL 字面 **`merchant`** 与 DB **`provider`** 混读 | 入驻与合规模块 | **81** Provider 池 | **`/market/provider/*`**（以 **04 §3.4** 为准） |
| **旅行收购** | `acquisition` | `acquisition` | **① 已对齐**：**`active`** = PD-009 **`publish_eligible`**；**`inactive`** = 未开通；**`restricted`** = trust 阻塞或 Admin suspend（**`chain_off/me.rs`** + **`acquisition_trust_snapshot`**） | **94**、**`market_listings.variant=acquisition`**、**[acquisition-publish-trust-rules v1](artifacts/acquisition-publish-trust-rules.v1.md)** | **发布/履约保证金**（**81 附录 E**）；**`order_kind=acquisition_listing`** | **30 §3.2** · **`/market/acquisition`** · **Hub「进入子站」** |
| **区域主理人** | *（无 `?board=` 脊签）* | **`region_steward`** | **`users.role=region_steward`** 时槽位 **active**；注册 **`role`** 路径见 **04 §三**、**87** | **87**、**04** | 与 **FeeRouter / 治理** 叙事分路径（**81 §1** 边界） | Header / 个人中心矩阵；**不计入** **§0.2.1** 四榜 |

**说明**：上表为 **导航**；**权限 HTTP 级** 仍以 **§0.3.3** 行 + **04** 为准。**§0.3.3** 四脊签行「全链路挂钩」已与 **93 / 95 / 04 / 94 / 30 / 04-附录-did-rank** 等对位；若 **93** 表号（**B-MKT-***）或 **95**（**F-0**）重编号，须 **§0.3.3 与 93/95 同批**修订以免断链。

#### §0.3.1 填写约定（生产级）

**§0.3.3「状态」列**：**DOC-CLOSED** = 矩阵正文与 **93 / 95 / 04** 等 **文档侧** 已对齐；**不**等同于 **发布 GO**（**GO** 仍以 **93 PASS**、**95 §8.2**、**P0 十二项** / **单人多角色自签** 等为硬门槛）。**SIGNED** = **Product** 书面签字后由维护者显式改写 **Owner/状态** 之目标占位。

1. **权限**：建议写清 **允许**的 **HTTP 方法 + 路径前缀** 或 **页面路由**；**显式禁止**与 **403/401 语义**（与 **04** 错误码表同口径）。  
2. **申请与开通标准**：写清 **前置条件**（账号状态、KYC、`users.role`、`guides.status`、**`market_listings`** 等）、**人工/自动**审核、**生效与失效**规则。  
3. **全链路挂钩**：**§0.3.3 四脊签主表** 已与 **93** 行号、**95 §8.2**、**04 §3.4** 等对位；**新增/改编号** 时须 **96-17 / 93 / 95 同批**以免断链。  
4. **版本**：矩阵或 **93/95** 锚变更时，**本条**与 **87/04** 相关节 **同批 bump** 并在 **§6** 留一行摘要。

#### §0.3.2 `GET /api/v1/me` · `identity_slots[].id` 与脊签产品名（**实现挂钩，非权限本身**）

| **`identity_slots[].id`（JSON）** | 对应 **§0.2.1 脊签（产品）** | 备注 |
|----------------------------------|------------------------------|------|
| **`traveler`** | 旅行者 | 与 **`users.role`** 旅行者侧判定同源（见 **`chain_off/me.rs`** **`users_role_is_traveler_side`**）。 |
| **`guide`** | 向导 | 与 **`guides`** 行及 **`users.role == guide`** 等分支同源。 |
| **`merchant`** | 商家（脊签文案） | URL 为 **`?board=provider`**；**API slot id 仍为 `merchant`**，勿与 DB **`users.role`** 字面 **`provider`** 混读。 |
| **`acquisition`** | 旅行收购 | **① 已对齐（PD-009）**：**`active`** = **`publish_eligible`**（钱包 + 押金/免押 + 未 suspend + 未 trust 阻塞）；**`inactive`** = 默认或未满足开通；**`restricted`** = **`identity_status=restricted`** / **`risk_level=high`** / Admin suspend。**权限** **[acquisition-publish-trust-rules v1](artifacts/acquisition-publish-trust-rules.v1.md)** · **`acquisition_publish_gate.rs`**（**非** **`region_steward`**）。**`stake_display`** = 发布 bond 镜像。 |
| **`region_steward`** | *（不映射 DID 榜脊签）* | **个人中心 / 注册矩阵**用第五槽；**`users.role == region_steward`** 时 **`state`** 可为 **`active`**。与 **§0.2.1 四脊签** **正交**；**勿**与 **`/did-rank`** 榜种混读。 |

<a id="identity-four-spine-prod-matrix"></a>

#### §0.3.3 主表 · **权限 + 申请与开通标准**（**四脊签行：矩阵与 93/95/04 已挂钩**）

| 脊签（产品） | **`?board=`** | **权限（允许 / 禁止 / 数据域）** | **申请与开通标准** | **全链路挂钩** | **Owner** | **状态** |
|-------------|----------------|-----------------------------------|---------------------|-----------------|-----------|----------|
| **旅行者** | `traveler` | **允许**：旅行者侧下单、评价、个人中心、**`PUT /api/v1/me`**（含 `default_wallet_address` 换绑，与 **04** 一致）；**禁止**：以向导身份调用接单/向导后台 API（以 **`users.role`** / 路由守卫为准，细则 **04**）。 | **注册成功默认**，无额外「开通旅行者」申请。 | **[93 §1](93-全站功能验证矩阵-域别回归清单.md#1-a-域--账号--注册--登录--me--密码--邮箱)**（**A-ME-001/002**、**A-LOG-001/002**、文内「多重身份」摘要）；**[95](95-全链路生产就绪检查清单与完成度矩阵.md)** **§8.2** **F-002**、**F-004**、**F-005**；**[04 §3.2](04-后端与API.md)** **`identity_slots`**；**[04 §3.4](04-后端与API.md)** **`GET`/`PUT /api/v1/me`** | Product | **DOC-CLOSED**（**GO** 以 **93/95** 证据为准；**Product 签字**后可标 **SIGNED**） |
| **向导** | `guide` | **允许**：审核通过后 **`guides`** 行有效；接待/履约/托管签名语义以 **`guides.wallet_address`** 为 DID；具体路由与 403 边界 **04**。**禁止**：未过审调用需 `guide` 门禁的写操作（与 **`guides.status`** 一致）。 | **申请**：**`/guide/register`**（**`POST /api/v1/guides`** + 证件 **`upload-doc`**）；**跨国资质证据包**见 **[64 §1.1](64-申请向导-行业标准与DID检查清单.md#guide-evidence-pack)**；链上 DID 说明见 **64 §五**。 | **[93 §1](93-全站功能验证矩阵-域别回归清单.md#1-a-域--账号--注册--登录--me--密码--邮箱)**；**[93 · §2 B 域](93-全站功能验证矩阵-域别回归清单.md)** 表 **B-GDE-001～003**；**[95](95-全链路生产就绪检查清单与完成度矩阵.md)** **§8.2** **F-023**、**F-024**；**[04 §3.3](04-后端与API.md)** **`POST /api/v1/guides`** / **`upload-doc`** / **`…/stake`**；**[04 §3.4](04-后端与API.md)** **`/guide/register`**；**[81 附录 D](81-经济模型-向导质押与订单押金.md#appendix-d-guide-trust-display)** | Product | **DOC-CLOSED**（**GO** 以 **93/95** 证据为准；**Product 签字**后可标 **SIGNED**） |
| **商家** | `provider`（`merchant`） | **允许**：**`users.role=provider`** 时 **`identity_slots`** **`id=merchant`** **`state=active`**（与 **`chain_off/me.rs`** 一致）；**`GET`/`POST /api/v1/market/provider/*`**、**`/market/provider/*`** 页面及 **`market_listings.variant=provider`** 契约内写路径。**禁止**：无 **provider** 角色时调用须商家鉴权之写接口（**04**）。**`POST …/market/provider/listings`** 另须 **PG paid entitlement + approved 申请**（**`market_merchant_gate.rs`**；**403** **`merchant_role_required`** / **`provider_application_not_approved`**，**400** **`onboarding_entitlement_required`**）。 | **① 全链（代码 SSOT）：** **`/auth/register?role=provider`**（注册 **落库 `traveler`**，FE **不传 role**）→ **`/provider/register`**（钱包 + **`POST …/provider-applications`**）→ **`/me/onboarding`**（96-18 quote → payment-intent → webhook → **`role-confirm`**）→ **`/admin/provider-applications`** → **`/admin/users/[id]`** 审核 → **`/market/provider`** 发 listing。**本地锚：** **[`frontend/app/provider/register/README.md`](../../frontend/app/provider/register/README.md)**；**① API 烟测：** **`bash scripts/dev/smoke-provider-onboarding-local.sh`**。**`GET /me.role`** = **chain_off 内存**（**role-confirm** 后 **`sync_user_role_in_memory_when_pg_matches`**）。 | **[93 · §2.1 / §2.1a](93-全站功能验证矩阵-域别回归清单.md)** **B-MKT-004**、**B-MKT-005**；**[95](95-全链路生产就绪检查清单与完成度矩阵.md)** **§8.2** **F-021**；**[04 §3.4](04-后端与API.md)** **`/market/provider`**、**`GET|POST …/market/provider/*`**、**商家入驻 ① 实现真源**段；**[94](94-自由市场-商家橱窗与旅行收购-链上托管技术规格.md)**；**[identity-unified-model §3.3](artifacts/identity-unified-model.v1.md)**；**[96-20 §5.5/§5.6](96-20-前后端页面对齐与UI生产级审计报告.md)** | Product | **DOC-CLOSED**（**GO** 以 **93/95** 证据为准；**Product 签字**后可标 **SIGNED**） |
| **旅行收购** | `acquisition` | **① 已对齐（PD-009）**：**允许** 旅行者侧 + **主钱包** + **发布保证金/信用免押** + **`agree_escrow_copy`** 后 **`POST …/market/acquisition/listings*`**；**`identity_slots.acquisition=active`** = 发布能力已开通（**`acquisition_publish_gate.rs`** · **非** **`region_steward`** / **96-18 准入费**）。**① 已闭（2026-05-27）** — **[acquisition-publish-trust-rules §8.1](artifacts/acquisition-publish-trust-rules.v1.md#81-第一阶段--本地--closed2026-05-27)**；**②③ backlog** 见同文 **§8.2/§8.3**。**禁止**：**②③** 将收购榜宣称 **Production 真排行/GO**（**30 §3.2** **Partial**）。 | **无** 商家式 KYB；**开通** = **`/me/identities`** **「进入子站」** → **`/market/acquisition`** 内 **绑主钱包 + 发布押金（或信用免押）**；**mock 押金** **`POST …/me/acquisition/publish-bond`**。**代码 SSOT** **[`frontend/app/me/identities/README.md`](../../frontend/app/me/identities/README.md)** → **[`frontend/app/market/acquisition/README.md`](../../frontend/app/market/acquisition/README.md)**；**① 烟测** **`bash scripts/dev/smoke-acquisition-pd009-local.sh`**。 | **[93 · §2.1b](93-全站功能验证矩阵-域别回归清单.md#93-21b-acquisition-pd009)** **B-MKT-006～008**、**010**、**012**；**[95](95-全链路生产就绪检查清单与完成度矩阵.md)** **§8.2** **F-022**；**[04 §3.4](04-后端与API.md)** **`/market/acquisition`**；**[94 §1.4](94-自由市场-商家橱窗与旅行收购-链上托管技术规格.md)**；**[30 §3.2](30-DID排行榜-页面规范.md)**；**[04-附录-did-rank](04-附录-did-rank对接说明.md)** | Product | **RULES-LOCKED**（**PD-009**；**① CLOSED** · **②③** 见 **acquisition-publish-trust-rules §8**） |

#### §0.3.4 扩展位（第 5 脊签及以后）

| 拟定脊签名 | 拟定 **`?board=`** | **权限** | **申请与开通标准** | **全链路挂钩** | **Owner** | **状态** |
|-----------|---------------------|---------|---------------------|----------------|-----------|----------|
| *（预留行）* | *（预留）* | *（待填写）* | *（待填写）* | *（须先完成 §0.2.2）* | *（待填写）* | **RESERVED** |

<a id="header-identity-ring-tiers"></a>

#### §0.3.5 全站 Header 头像「身份环」档位列（**Target**）

**信号**：以 **`GET /api/v1/me`** 返回的 **`identity_slots`** 中 **`state === "active"`** 的条数计档（实现见 **`crates/api/src/chain_off/me.rs`** **`identity_slots_json`**）。JSON 键为 **`state`**（**`active`****/**`inactive`****/**`pending`****/**`restricted`**），**不是** `status`。前端 **禁止**与后端不一致的自算档级。

**与第五槽 `region_steward`**：数组含 **`id: "region_steward"`**。若产品规定头像环 **仅统计四脊签对应四槽**，须在后端给出派生字段或在 **04** 写明过滤规则，**禁止** 前端各自为政。

| 档位数 | 产品语义（摘要） | 外圈色（**Target**） | **a11y / 披露** |
|-------:|------------------|----------------------|----------------|
| **1** | 仅旅行者（默认） | **细中性环 / 浅灰白**（避免浅顶栏上纯白不可见） | **`aria-label` / Tooltip** 明示「已开通：旅行者」 |
| **2** | 含向导等 **第二** 身份 | **绿色系** 环 | 文案列明已开通身份名；**不得**仅靠色相传达 |
| **3** | 三槽 active | **蓝色系** 环 | 同上 |
| **4** | 四槽 active | **紫色系** 环 | 同上 |

**生产注意（`active` 计数与五槽）**：**`traveler`**、**`guide`**、**`merchant`**、**`region_steward`**、**`acquisition`** 槽位 **独立**；**`acquisition`** 在 **PD-009 ①** 下可在 **`publish_eligible`** 时为 **`active`**（**与 `users.role` 无关**）。**`state === "active"`** 条数可达 **5**；上表 **§0.3.5** 四档色为历史 UI 留口 — **出现 5 条 active 时** 须 **UI + 产品** 扩充档级或后端派生字段（与 **「与第五槽 `region_steward`」** 段同决策），**禁止** 前端硬编码只认四档而截断。

**视觉定稿与对比度**：以 **[86](86-UI-双系统未来风-风格与动效技术规格.md)**、**[05 补充 · Header](code-maps/05-补充-前端实现细节与代码映射-20260306.md)** 与 **`Header.tsx`** 同批验收；**30** 读前摘要互指本分节。

---

## §1 真值分层（防双 SSOT）

1. **业务与字段定义** → **64 §五**（向导申请、DID 说明、不支持项）。  
2. **API 请求/响应与错误码** → **04**（含 **did-rank 附录**）。  
3. **是否「做完」**（矩阵行、Rust/E2E 绑定）→ **95 / 93**。  
4. **认证与恢复对会话、DB 列的影响** → **97** + **`crates/api`** 实现（**`auth_reset_password`**、**`put_me_password`**、**`PUT /api/v1/me`** 等）。  
5. **排行榜脊签清单与扩展契约** → **本文 §0.2**（**96 主**）并联 **30 / 04 附录**。  
6. **四身份权限与申请标准（产品 Owner）** → **本文 §0.3**；填毕后与 **87 / 04 / 93 / 95** 对拍。

**96-17** 只做：**执行核对表 + 发版抽检 + 升级 backlog 指针**。

---

## §2 账号与链上地址模型（与 §0.2「榜种身份」**正交**）

| 概念 | 持久化 / 行为要点 |
|------|-------------------|
| **账号主体** | 一邮箱一 `user_id`；会话绑定该主体（**97**、**93·A-LOG-\***）。 |
| **账号级默认链上地址** | `users.default_wallet_address`；**`PUT /api/v1/me`**（**93·A-ME-002**、**95·F-005**）。 |
| **向导链上 DID** | `guides.wallet_address`；申请向导时写入；接单 / 托管签名语义以之为准；**可与** `default_wallet_address` **不同**。 |
| **显式不支持（当前实现）** | 同一账号多默认钱包、同一向导多 DID；向导 DID **无**页内换绑 API（**64 §5.1**）。 |

---

## §3 与认证链路的边界（审计必对）

- **登录成功**：颁发会话，绑定 **`user_id`**；与 **`GET /api/v1/me`** 同源载体（Cookie / Bearer，见 **93 §0.7**）。  
- **修改密码 / 忘记密码重置成功**：**吊销该用户全部会话**；**不**自动修改 **`users.default_wallet_address`** 或 **`guides.wallet_address`**（与 **97** 读前摘要一致）。  
- **「钱包态」广义**：链上投影、Escrow 状态机等仍以 **97 §2.3** 与 **04** 为准；**本分册**聚焦 **「账号展示钱包」≠「向导接单钱包」** 之混读风险；**亦不**把 **§0.2** 脊签切换误认为「切换默认链上付款地址」。  

---

<a id="sec4-p0-checklist"></a>

## §4 发版前 P0 抽检（本束最小证据）

在 **`evidence/GO_96_bundle_<YYYYMMDD>/README.md`**（或 **96-01 §0.3** 一束表之 **96-17** 行）中留一行路径或勾选：

- [ ] **Me 与 PG 一致**：`GET /api/v1/me` 之 **`default_wallet_address`**（或契约等价字段）与 **`users`** 行一致（抽检 1 个非种子账号即可）。  
- [ ] **向导 DID 与公开列表一致**：已知向导之 **`guides.wallet_address`** 与 **接单 / stake** 文档叙事一致（可与 **93·B-GDE-\*** 或手工 **GET …/guides/:id** 对拍）。  
- [ ] **改密 / 重置后会话失效**：重置或改密后，旧 **Bearer** 调 **`GET /me`** 为 **401**（与 **97**、**93·A-PWD-\*** 一致）。  
- [ ] **`/did-rank` 四脊签与 `?board=` 一致**：手工或使用 E2E：`traveler` / `guide` / `provider`（及别名 **`merchant`→provider**）/ `acquisition` 与 **§0.2.1** 表一致；脊签 **`tablist`/`tab`/`tabpanel`** 可见配对（**30 §7**）。**若本轮未触及排行榜**，本行标 **N/A** 并写「本轮不含 `/did-rank` 变更」。  
- [ ] **互指可解析**：**64 / 04 / 04附录 / 30 / 93 §1 / 95 / 97 / 96-01** 指向本分册之链接在本仓库内可打开（**B-421** 并联习惯）。  

**与 95 正交**：上列 **☑** **不**替代 **95 §8.2** 行完成或 **ISS** 关闭。

---

## §5 升级路线（项目演进 · Backlog）

以下为 **产品/架构升级 EPIC** 占位，**落地前**须另开 **04** 契约变更、**95** F 行 / **93** 用例及 **01** 订单归属约束评审。

| 方向 | 说明 |
|------|------|
| **第五脊签及以后（榜种扩展）** | **须**走 **§0.2.2** 强制清单；优先在 **本表** 登记 **EPIC / Owner / 拟定 `?board=` 字面量** 再拆 **04+30+93**。 |
| **向导 DID 可审计换绑** | 须新 API、审计事件、与「已支付订单签名钱包不可变」等约束（**64 §5.1**、**01** 缝对齐）。 |
| **账号级多钱包** | 若产品需要「多地址簿」，须独立数据模型与 UI，**禁止** silent 覆盖 **`default_wallet_address`** 语义。 |
| **KYC / DID 可验证凭证** | 与 **64 §六**、**90** 身份与信任系统对读；链上锚定不在本分册范围。 |

---

## §6 修订记录

| Version | 日期 | 摘要 |
|---------|------|------|
| 1.0.21 | 2026-05-27 | **§0.3.3 商家行**：**93** 回归用例 **B-MKT-004/005**（删旧 **005～012** 误引）；增 **96-20** 互指。**承** **1.0.20**。 |
| 1.0.20 | 2026-05-27 | **§0.3.3 商家行**：**① 商家入驻全链**、**`GET /me` 内存 role**、**市场三门闸**、**烟测** 与 **provider/register README** / **identity-unified-model §3.3** 对拍。 |
| 1.0.18 | 2026-04-28 | **批次 AU**：**§0** **`GAP 台账`** **伴侣字面** **`#9618-one-page-priority` v1.0.116+`**（**96-18-未完成 1.0.116** **/** **§0** **脚注** **现行伴侣** **/** **`spec/00`** **对拍**）。**承** **v1.0.17**。 |
| 1.0.17 | 2026-04-28 | **批次 AT**：**§0** **`GAP 台账`** **伴侣字面** **`#9618-one-page-priority` v1.0.115+`**（**96-18-未完成 1.0.115** **/** **`spec/00`** **对拍**）。**承** **v1.0.16**。 |
| 1.0.16 | 2026-04-28 | **批次 AS**：**§0** **`GAP 台账`** **伴侣字面** **`#9618-one-page-priority` v1.0.114+`**（**96-18-未完成 1.0.114** **对拍**）。**承** **v1.0.15**。 |
| 1.0.15 | 2026-04-28 | **批次 U**：**§0** **`GAP 台账`** **伴侣字面** **`#9618-one-page-priority` v1.0.90+**（**`[00]`** **96-18-未完成 1.0.91** **同批**）。**承** **v1.0.14**。 |
| 1.0.14 | 2026-04-28 | **批次 R**：**§0** **`GAP 台账`** **`#9618-one-page-priority`** **v1.0.87+**（**04** **读前** / **TT-9618** / **96-18** **§0** **对拍**）。**承** **v1.0.13**。 |
| 1.0.13 | 2026-04-28 | **§0** **`GAP 台账`** **`#9618-one-page-priority`** **字面** **v1.0.86+**（**承** **批次 P/Q** **与** **`[00]`** **96-18-未完成** **行**）。 |
| 1.0.9 | 2026-04-27 | **生产级收口**：锚 **`#identity-four-spine-prod-matrix`**（**§0.3.3**）；**§0.3**/**§0.3.1**（**DOC-CLOSED**/**SIGNED** 语义）/**§0.3.2** 与「矩阵已闭合、GO 以 93/95 为准」对齐；主表 **状态** 改 **DOC-CLOSED**；**04/93/95** 读前或互指补链。 |
| 1.0.8 | 2026-04-27 | **§0.3.0** 说明：与已填 **§0.3.3** 对齐（四脊签「全链路挂钩」已闭合；**93/95** 重编号须同批）；读前 **87** 锚改为 **§1.1·§1.3·§4·§1.4**；**商家** 行 **87** 用语锚同上。 |
| 1.0.7 | 2026-04-27 | **§0.3.3**：**商家 / 旅行收购** 行补权限、开通与 **93/95/04/94/30/附录** 挂钩；**04 §3.4** **`GET /me`** 行互指 **`identity_slots`**（见 **§三 3.2**）。 |
| 1.0.6 | 2026-04-27 | **§0.3.5**：补 **五槽 `active` 计数可达 5** 之生产注意；**§0** 读前 **04** 句补 **`identity_slots`**；**§0.3.3** 不变。 |
| 1.0.5 | 2026-04-27 | **§0.3.3**：**旅行者 / 向导** 行 **全链路挂钩** 补 **93 / 95 / 04** 可点击锚；**Owner** 列 **Product**。 |
| 1.0.4 | 2026-04-27 | **§0.3.2** 补 **`region_steward`** 槽位说明；**§0.3.0** 补 **区域主理人** 行；**§0.3.5** 与实现对齐：**`state`**（非 `status`）、**`region_steward`** 计档留口。 |
| 1.0.3 | 2026-04-27 | **§0.3.0** 身份分类总表与文档地图；**§0.3.3** 充实 **旅行者 / 向导** 摘要列；**§0.3.5** Header 头像身份环 **Target**；锚 **`#identity-classification-doc-map`**。 |
| 1.0.2 | 2026-04-27 | **§0.3**：四脊签 **权限 + 申请标准** **Owner 填写框架**（主表 + **`identity_slots`** 挂钩 + 扩展预留行）；锚 **`#identity-four-roles-framework`**。 |
| 1.0.1 | 2026-04-27 | **§0.2**：DID 排行榜 **四类脊签身份** + **扩展契约** + 与 **81/82/§2** 分轨；**§4** 增 **`/did-rank`** 抽检行；**§5** 第五脊签登记口径。 |
| 1.0.0 | 2026-04-27 | 初版：自 **96-01** 迁出「多重身份」执行核对；锚 **`identity-multi-wallet-ssot`**；§4 抽检 + §5 升级 backlog。 |
