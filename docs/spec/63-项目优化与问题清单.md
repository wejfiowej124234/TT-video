# 项目优化与问题清单

### 读前摘要

| 你要找什么 | 单源 |
|------------|------|
| **问题与优化项** | **§一** 起 |
| **发版/质量** | **[07](07-开发流程与顺序.md)**、**[缺口与待补-官方总表](缺口与待补-官方总表.md)** |

**目的**：集中记录当前已知问题与后续可优化项，便于排期与验收。  
**范围**：前端、后端、合约、文档、CI/质量。

---

## 一、已处理（本轮）

| 项 | 说明 |
|------|------|
| **跨页表单占位与 Me 标签** | **`ui_placeholder_https_full` / `ui_placeholder_https_prefix` / `ui_placeholder_numeric_zero`**、**`me_avatar_url_label`**、**`auth_register_idPhotoHint`**、**`admin_placeholder_uuid` / `admin_placeholder_json_empty`**：**`MeProfileSection`**、市场向导报价、**`PublishDrawer`**、**`SetEscrowAddressBlock`**、**`WalletStatusMini`**、向导注册护照提示、Admin 申诉/处罚筛选 |
| **Landing 稳定币 / 表单占位 / 默认结算常量** | **`didRank_badge_stablecoins`** 替代 **`STABLECOIN_LABEL`**；**`DEFAULT_SETTLEMENT_CURRENCY_CODE`**（**`lib/defaultSettlementCurrency`**）统一 mock 与行程弹窗订单卡；**`/orders/new`**、**`/pay`**、**`RegisterGuideForm`** 占位与默认法币走 **`locales`** |
| **CustomItinerary / 订单抽屉 / 协议折叠** | **`ui_currency_suffix_usdc_per_person`**；**`CustomItineraryModal`** 分项与跨城人均价 **`t()`**；**`OrderDetailDrawer`**、**`/orders`**、向导时薪缺省币、**`UnifiedItineraryList`** 默认展示币；**`AgreementSummaryAccordion`** **`agree_label_*`**；**`TrustInfraWall`** 墙标 **`didRank_badge_polygon` / `order_defaultSettlementToken` / `trust_infra_wall_audited`** |
| **币种后缀 / 结算 token / DID 榜 pill** | **`ui_currency_suffix_usd|usdc|usdt`**、**`order_defaultSettlementToken`**、**`trust_supported_token_pill_label`**、**`didRank_badge_polygon`**、**`didRank_badge_stablecoins`**：**Landing** 预算行、**`OrderCard`** / **`/itinerary/new`**、**`MeStatsSection`**、**did-rank**（榜/弹窗/页头 pill）、**`SupportedTokensPill`**、**`GuideCard`** DID 可见文案 **`t()`** |
| **Landing 结果卡 / 创建 notes / 向导页** | **`landing_total_price_label`**、**`landing_unlocked_price_usd_suffix`**、**`landing_notes_attraction_types`**；**`useLandingPage`** 提交 **`notes`** 随界面语言；向导详情 DID 与头像占位 **`t()`** |
| **按天行程摘要 i18n** | **`landing_results_day_segment` / `landing_results_day_joiner`**：`getDailyItineraryOutline`、`getDaySummary(..., t)`、**`OrderChatContextCard`**（5 天）与 **Landing** 结果卡（10 天）中英一致 |
| **Staking / Landing `ui_em_dash`** | **Staking** 四面板与 **Landing** `ItineraryResultsSection`（占位骨架、`getDaySummary` 的 **`dash`** + **`t()`**）已接 **`t("ui_em_dash")`** |
| **前台 `ui_em_dash`** | 非 Admin 页面缺失占位由 **`frontend/locales` `ui_em_dash`** 驱动（市场、社区、消息、托管、DID 榜、`StatusBadge`、`me` 资料等），与 **`admin_em_dash`** 分键同形 |
| **EscrowDetail ApiErrorAlert** | 已补 `import ApiErrorAlert`，消除 TS 报错 |
| **frontend .env.example** | 已补充 `NEXT_PUBLIC_CHAIN_ID` 说明（Escrow/钱包链校验用） |
| **frontend ESLint** | 已新增 `.eslintrc.json` 继承 `next/core-web-vitals`，`npm run lint` 可非交互运行 |
| **Admin `loading` main / 404** | **`AdminSubpageRouteLoading.mainAriaLabelKey`**；**`/admin`** 根段 **`admin_workspace_title`**；各 **`app/admin/**/loading`** 与页级 **`h1`** 同 **`locales` 键**；根 **`not-found`** **`main`** **`aria-labelledby`** **`not-found-title`** |
| **`/admin` 首页 i18n** | **`AdminHomeClient`**：**`admin_workspace_title` / `admin_workspace_subtitle` / `admin_home_modules_aria`**；入口卡片 **`h2`** 复用各子页 **`admin_*_title`**，说明用 **`admin_home_desc_*`**；烟雾 **`管理后台首页可访问（middleware 占位 Cookie）`** |
| **`/admin` error i18n** | **`app/admin/error.tsx`** 眉标与返回 **`/admin`** 链文案由 **`admin_workspace_title`** 驱动（与首页 **`h1`** / **`loading`** 一致） |
| **Admin 表区 `aria-label` 字面量清零** | **orders～users** 等 **`admin_*_table_aria`**；续 **community/reports**、**api-versions**、**fee-router**（摘要 **`admin_fee_router_summary_aria`**、事件表 **`admin_fee_router_events_table_aria`**）；**`app/admin/**/*.tsx`** 无 **`aria-label="…"`** 硬编码英文 |
| **Admin 台账表 `aria-label` 全覆盖** | **flags / ranking·snapshots / config·releases / compliance·requests+events / scheduler·jobs / policies / risk-signals / tenants·scopes / penalties / appeals / policy-change-logs / jobs / internal-tools·audits / moderation·cases / lifecycle / secrets·metadata** 等白底横向滚动表 **`section`** 统一 **`admin_*_table_aria`**；**`/orders`** 面包屑 **`nav`** → **`common_nav_breadcrumb`**；**全仓 `*.tsx` 无 `aria-label="…"` 字面量** |
| **Admin JSON/摘要区 `section` 地标** | **finance** 摘要栅格、**observability** overview、**schema** 迁移桶、**indexer** 健康与对账跳转卡、**audit/operations**、**alerts/incidents** 与 **`[id]`**、**indexer/reconcile/[id]** 主内容 **`section`** 增 **`aria-label`**（**`locales`** `*_aria` / 复用 **`admin_indexer_reconcile_sectionTitle`**） |
| **Admin 筛选条 / 写表单 `section` aria** | 灰底筛选区统一 **`aria-label={t("admin_*_filters")}`**（含 **approvals、jobs 摘要 `admin_jobs_summary_aria`**）；**comments/visibility、abuse-policy、appeals/review、compliance/update** 表单区 **`admin_*_form_aria`**；**`locales`** **`admin_*_filters` / `admin_jobs_summary_aria`** 等中英补齐 |
| **53 八步步骤条 i18n 键名** | **`OrderFlowSteps`** 步骤文案改为 **`order_steps_step_draft`** … **`order_steps_step_release`**（与 **53 §4.6.1 / 37 §2.4** 一致）；**`/admin/fee-router`** 链上事件表外包由 **`div`** 改为 **`section`**（与台账页白表地标一致） |
| **53 订单状态展示 i18n（§4.6.1）** | 新增 **`lib/orderStatusI18n`** **`orderStateToStatusLabelKey`**；**`order_status_*`** 键（草稿、待向导确认、双边、已确认待付、托管付款中、评分、终态等）中英补齐；**`/orders`、市场 `OrderCard` / `OrderDetailDrawer`、托管 `EscrowDetail` `OrderFlowSteps` `statusLabel`** 统一走映射；**`OrderListItem` / `OrderDetailItem`** 可选 **`sub_status`** |
| **托管复制摘要 + 状态徽章 i18n** | **`EscrowDetail` `handleCopySummary`** 剪贴板正文行（订单号、目的地、日期、天数、合计、分项 **`order_copySummary_*` / 复用 `order_*` 费用键**、行程标题与日行）随 **`t()`** 与当前语言一致；**`StatusBadge`** 支持 **`sub_status`**，展示文案走 **`orderStateToStatusLabelKey`**（与 **`EscrowDetailHeader`** 订单行一致） |
| **订单写错误 i18n + 评分上传 a11y** | **`lib/mapOrderWriteError`**：含 **`itinerary` / `version_conflict` / `message_db` / `dispute_open` / `evidence_db` / `dispute_resolve_db`** 等 **`parseResponse`** 码→**`order_error_*` / `escrow_*` / `dispute_*` / `common_*`**；**`/orders/new`**、**`/orders` 列表/分页/取消**、**`EscrowDetail`** PATCH/删单（**`role="alert"`**）、**`ConfirmFinalPlanBlock`**、**`SetEscrowAddressBlock`**、**`CreateOnChainEscrowBlock`** 回写、**`ChatBlock`**、**`OrderActionsBlock`**（REST）、**`BilateralConfirmBlock`**、**`/escrow/:id/rate`**、**市场 `OrderDetailDrawer` 接单**、**争议详情** 证据/裁决（**`only_arbitrator_*`** 仍页内分支；EIP-712 仍 **`mapIntentError`**）；**`mapReviewSubmitError`** 委托同码表；**`rate_uploadInput_aria`**（**53 §4.6.2 / 37 §2.4**） |

---

## 二、前端

### 2.1 质量与工具

| 项 | 说明 | 优先级 |
|------|------|--------|
| **ESLint 全量通过** | 已修掉全部告警（含 `no-img-element` → `next/image`） | ✅ 已做 |
| **单元测试覆盖** | `lib/api.test.ts`、`apiClient.test.ts`、`useEscrowActions.test.ts`；**DID**：`lib/didRankUtils.test.ts`（分页、period 解析），页面已改用 didRankUtils | ✅ 已补 |
| **E2E** | `playwright.config.ts` + `e2e/smoke.spec.ts`：**`/`**（**`landing_hero_title`** **`main`** **`aria-label`**）、**`/did-rank`**（**`didRank_title`** **`main`** **`aria-label`**；页内 **`header`** 不再重复地标名）、**`/market`**、**`/discover`**（**`waitForURL` → `/market`**，与 29 §10 一致）、**`/guides`**、**`/me`**（未登录 **`/auth/login`**，已登录 **Profile / 个人中心**）、**`/orders`**（未登录回登录，已登录 **My orders / 我的订单**）、**`/pay`**（**Pay & escrow / 支付与托管**）、**`/itinerary/new`**（**Generate itinerary / 行程生成**）、**`/orders/new`**（**Create order / 创建订单**）、**`/disputes`**（**Disputes / 争议列表**）、**`/governance`**（**Governance / 治理**）、**`/governance/proposals`**、**`/governance/params`**、**`/governance/fee-routes`**、**`/staking`**（**Guide staking / 向导质押**）、**`/traveltrust`**（**85 Partial 壳**）、**`/escrow/:id`**（占位 UUID；**`EscrowDetailSection`** **`dynamic` `loading`** 与 **`Suspense` `fallback`** 为 **`main`** **`escrow_detailAria`**；**`EscrowDetail`** 骨架/错误/成功 **`main`** **`escrow_detailAria`**）、**`/escrow/:id/rate`**（**`rate_pageTitle`** **`main`**；成功 **`aria-labelledby`** **`rate-page-h1`**；加载骨架 **`sr-only` `h1`**；拉单失败 **`escrow_loadFailed`** **`sr-only` `h1`**）、**`/guide/register`**、**`/me/password`**、**`/community/user/:id`**（合法 UUID **`community_user_main_aria`**；非法 id **`community_user_not_found`** **`main`**；烟雾含非法 id）、**`/community/messages/:id`**（**`community_conversation_thread_aria`** **`main`**；空 id **`community_conversation_not_found`**）、**`/community`**（**`CommunityFeedMain`** **`main`** **`aria-label`** **`community_tab_feed`**；含 **`?post=`** 分享深链占位 UUID，与 31 §2.2 **`stripPostParam`** 一致）、**`/community/explore`**（**`main`** **`community_explore_title`**）、**`/community/activity`**（**`main`** **`community_activity_title`**）、**`/community/tt`**（**`main`** **`community_tt_title`**）、**`/community/topic/…`**（与 Feed 同源 **`main`** **`community_tab_feed`**）、**`/community/friends`**（**`main`** **`community_tab_friends`**）、**`/community/messages`**（**`main`** **`community_tab_messages`**）、**`/community/feedback`**（**`main`** **`community_feedback_title`**）、**`/community/me`**、**`/community/me/collects`**、**`/community/me/posts`**、**`/community/me/reports`**、**`/community/me/reports/:id`**（未登录 **`community_report_ticket_title`** **`main`** + 登录提示；已登录走 **`GET …/community/reports/:id`**，见 **04 §3.4**）、**`/terms`**、**`/terms/community-guidelines`**、**`/privacy`**、**`/help`**、**`/auth/login`**、**`/auth/register`**、**`/auth/forgot-password`**、**`/auth/reset-password`**（**04 §三 3.1** stub 页）、**`/auth/verify-email`**（**`auth_verify_title`** **`main`** **`aria-label`**）、**`/disputes/:id`**（加载/错误/成功 **`main`**：**`dispute_detailTitle`**；错误 **`dispute_notFound`** **`sr-only` `h1`**；成功 **`aria-label`** 与 **`h1`** 同前缀+短 id）、**`/guides/:id`**（加载/错误 **`main`** **`aria-label`** **`guideDetail_title`**；错误 **`guideDetail_notFound`** **`sr-only` `h1`**；成功 **`main`** **`aria-labelledby`** **`guide-hero-name`**）、**`/admin`**（**middleware** 要求 **`traveltrust_user_id`** Cookie；烟雾写入占位值后断言 **`main`/`h1`** 与 **`admin_workspace_title`** 一致，并含 **Orders** 入口链）可访问；`npm run e2e`（需先 `npx playwright install`）。`tsconfig` 与 `vitest` 已排除 e2e，避免 tsc/单测误跑 Playwright 用例 | ✅ 占位已做 |

### 2.2 DID 排行榜（文档 30 §8 剩余）

| 项 | 说明 | 优先级 |
|------|------|--------|
| **Loading 状态** | 首屏与切换 period 已加骨架屏；接入 API 后数据返回前保持 loading | ✅ 已做 |
| **奖金池** | 页面顶部奖金池区块，每月奖励前 10 名治理币，风格与 §4 一致 | ✅ 已做 |
| **结构化数据** | 前 10 旅行者/向导已输出 ItemList JSON-LD（SEO） | ✅ 已做 |
| **页面接真实 API** | **`/did-rank` 页**调用 `getDidRankTravelers` / `getDidRankGuides`；失败或空则回退 mock；**`getDidRankItineraries`** 保留在 client，**当前页不调用**（**30 §0.1**） | ✅ 已做 |
| **旅行者详情入口** | UUID 用户链至 **`/community/user/:id`**：旅行者榜昵称、战绩弹窗、向导榜昵称/弹窗；mock 非 UUID 无链 | ✅ 已做 |

### 2.3 其他页面与体验

| 项 | 说明 | 优先级 |
|------|------|--------|
| **规范 28 一致性** | 已检查：未发现 text-gray-* / text-blue-* / rounded-md；Experience 区玻璃用 bg-white/xx 为 28 允许 | ✅ 已核对 |
| **错误边界** | `app/error.tsx`、`app/did-rank/error.tsx`（**`main`** **`aria-labelledby`** **`did-rank-error-h1`**）；**已加** `app/escrow/error.tsx`、`app/market/error.tsx`（28 风格） | ✅ 已做 |
| **图片优化** | 已用 `next/image` 替代全仓 `<img>`；`next.config.js` 已配 `images.remotePatterns`（unsplash、github）；市场/首页/DID 头像与卡片图已优化 | ✅ 已做 |

---

## 三、后端与数据

| 项 | 说明 | 优先级 |
|------|------|--------|
| **DID 排名 API** | 已实现 `GET /api/v1/did-rank/travelers?period=`、`/guides?period=`、`/itineraries?period=`，返回与 mock 同构结构 | ✅ 已做 |
| **「我的排名」与登录** | **已落地**：`GET /api/v1/did-rank/*` 在已鉴权时返回 `is_me`；前端 `getDidRank*` 带 `getAuthHeaders()`；did-rank 页支持 `?me=traveler-`/`guide-`+UUID 与 `is_me` 高亮；行程卡展示「我的排名」标签 | ✅ 已做 |
| **CORS_ORIGINS** | 生产必须设置；文档与 Runbook 已强调 | P1 |

---

## 四、合约与链

| 项 | 说明 | 优先级 |
|------|------|--------|
| **合约实现状态** | 以 `contracts/README.md` 与 01/02 为准；实现进度见仓库与 evidence | P1 |
| **ABI 与前端同步** | 前端仅用 `dapp/abis/*.json`，与 14 对齐；合约变更后需重新生成并拷贝 | P2 |

---

## 五、文档与规范

| 项 | 说明 | 优先级 |
|------|------|--------|
| **30 文档 §6 落点** | 文档中「竖版（1～100 或 11～100）」已统一为「竖版 11～100」与实现一致 | ✅ 已做 |
| **08-3 / Runbook** | 参数、门禁、部署前校验按现有文档执行 | P1 |

---

## 六、CI 与协作

| 项 | 说明 | 优先级 |
|------|------|--------|
| **PR/主分支** | `.github/workflows/build.yml` 已含 **frontend** job：`npm ci` → `npm run lint`、`npx tsc --noEmit`、`npm run test -- --run`；Rust 仍为 build 主 job | ✅ 已做 |
| **发版前本地检查** | 仓库根执行 `./scripts/frontend_pre_deploy.sh` 可跑 lint+tsc+单测，与 CI 一致 | ✅ 已做 |
| **.env 与密钥** | `.env` 勿提交；根与 **frontend/.env.example** 已交叉说明 CORS/鉴权；仍须发版前核对生产密钥不入库 | P1 |

---

## 七、小结

- **当前无阻塞性问题**：TypeScript 与 Lint 检查通过；已知 2 个错误已修复（ApiErrorAlert、ESLint 配置）。
- **DID 页**：Loading 骨架、奖金池、战绩/向导弹窗、结构化数据、弹窗焦点与埋点已做；已接后端 did-rank API；UUID 用户已链 **`/community/user/:id`**（见 §2.2）。
- **建议优先**：生产 CORS（Runbook 已写部署前必检）、合约与 ABI 同步；E2E 占位已加、规范 28 已核对。ESLint 全量通过、图片已统一为 next/image。
- **批量优化（本轮）**：next 关闭 `X-Powered-By`、增加安全头（X-Frame-Options、X-Content-Type-Options、Referrer-Policy）；根 layout 增加 openGraph/twitter 与 viewport/themeColor；新增 `scripts/frontend_pre_deploy.sh` 发版前一键检查。
- **社区壳层（2026-03-26）**：`CommunityAuthProvider` 上提包裹全布局；Tab 未读角标用的 `getConversations` 仅在 **已登录且 getMe 完成** 后启用（`react-query` `enabled`），匿名浏览社区不再反复请求会话列表。
- **社区发现 / 离线（2026-03-27）**：**`/community/explore`** 瀑布流区块使用 **`useInfiniteQuery`** + **`community_explore_load_more`**（游标 `next_cursor`）；**`CommunityExplorePhotoMasonry`** 支持 **`maxThumbs`**（随页增至封顶 120）。发帖/评论在 **`navigator.onLine === false`** 时提示 **`community_publish_offline`** / **`community_comment_offline`**，不入乐观队列。
- **社区离线续（2026-03-27）**：点赞/收藏/关注、Feed **游标加载更多**、**举报提交**、**私信发送** 统一离线提示（**`community_interaction_offline`** / **`community_messages_offline`**）；话题 Hero **全站帖子数** API 失败时 **`community_topic_server_post_total_error`** + 重试。
- **社区离线再续（2026-03-27）**：**`useCommunityPostLikeCollect`**（作者页/我的帖子/收藏等）；**`/community/user/:id`** 关注、评论、删帖、可见性；**`/community/friends`** 取关、加好友、收到申请接受/拒绝；好友关注空态链 **Explore**。
- **社区反馈 / 发现 / 消息空态（2026-03-27）**：**`/community/feedback`** 提交前离线走本地草稿；**`/community/explore`** 瀑布流 **IntersectionObserver** 自动下一页（离线跳过）；**`/community/messages`** 空态 CTA 含 **Explore**；E2E 增 **`/community/feedback`**。
- **社区我的 · 推荐关注（2026-03-27）**：**`/community/me`**「你可能感兴趣的人」调用 **`postUserFollow`**，成功 **`invalidateQueries`**（**`meFollowing`**、**`feedSuggest`**）；未登录 → **`/auth/login?returnUrl=/community/me`**；离线 toast；E2E 增 **`/community/me`**。同批：**`getFeed` / `getUserPosts` / `getMyPosts`** 返回类型与 JSON cast 补齐 **`author_*`** 等字段；我的页推荐作者用 **`suggestedAuthorsFromPosts`** 与侧栏同源展示。
- **社区 API 类型 / 活动中心 CTA / E2E（2026-03-27）**：**`getPostById`** 的 **`post`** 与 cast 补齐 **`author_*` / `cover_url` / `evidence_anchored` / `visibility_status`**（与 **`mapApiPostToCommunityPost`** 一致）；**`getMeCollects`** 由 **`unknown`** 改为显式返回类型；**`/community/activity`** 底部 CTA 增加 **`/community/explore`**；烟雾 **`/community/me/collects`**。
- **社区关系列表 API 类型 / E2E me/posts（2026-03-27）**：导出 **`CommunityPublicUserRow`**、**`CommunityFriendRequestReceivedRow`**、**`CommunityFriendRequestSentRow`**；**`getMeFollowing` / `getMeFollowers` / `getFriendsList` / `getFriendsRequests` / `getFriendsRequestsSent`** 显式返回类型 + JSON cast；好友页、Feed、探索、我的、用户主页去掉冗余 **`as`**；烟雾 **`/community/me/posts`**。
- **私信会话 API 类型 / E2E messages（2026-03-27）**：**`CommunityConversationRow`**、**`CommunityDmMessageRow`**；**`getConversations` / `getConversationMessages`** 显式返回类型 + cast；消息列表/会话详情/布局未读/分享选会话/好友 **`convByPeer`** 等去掉大段断言；**`apiClient/index`** 重导出类型；烟雾 **`/community/messages`**（heading **Messages / 消息**）。
- **社区写操作与评论列表类型（2026-03-27）**：**`CommunityWriteJsonResponse`**（点赞/取消赞/收藏/关注/好友请求处理/发评论等 **`communityJsonBody`**）；**`CommunityCommentListRow`** + **`getPostComments`** 显式返回；**`useCommunityFeed`**、用户主页/我的帖子/我的收藏 评论加载去掉重复 **`as`**；**`index`** 导出 **`CommunityWriteJsonResponse`**、**`CommunityCommentListRow`**。
- **社区举报 API 类型 / E2E me/reports（2026-03-27）**：**`CommunityReportTicketRow`**、**`CommunityGetMyReportsResponse`**、**`CommunityGetReportDetailResponse`**、**`CommunityReportAppealResponse`**；**`postCommunityReport` / `getMyCommunityReports` / `getCommunityReport` / `postCommunityReportAppeal`** 显式返回；**`postFeedback`** 与 **`CommunityWriteJsonResponse`** 交叉；举报列表/详情、**`useCommunityPostReport`** 去冗余断言；烟雾 **`/community/me/reports`**。
- **删帖 / 可见性 PATCH 类型（2026-03-27）**：**`deletePost`** 改为 **`Promise<CommunityWriteJsonResponse | null>`**（4xx/5xx 仍解析 JSON）；**`patchPostVisibility`** → **`CommunityPatchPostVisibilityResponse | null`**；**`/community/me/posts`**、**`/community/user/[id]`**（本人）失败文案走 **`messageForCommunityActionResponse`**；**`index`** 导出 **`CommunityPatchPostVisibilityResponse`**。
- **帖子详情 API 类型 / 社区规范 E2E（2026-03-27）**：**`CommunityApiPostDetailRow`**、**`CommunityGetPostByIdResponse`**（含 **`post: null`** / **`note`** / **`message`**，对齐 **`get_post_detail`**）；**`getPostById`** 返回类型与 **`index`** 导出；**`/terms/community-guidelines`** **`main`** 增 **`aria-label`**；烟雾 **`/terms/community-guidelines`**（**main** + **heading** 匹配 **Community guidelines / 社区规范**）。
- **条款与隐私 E2E / a11y（2026-03-27）**：**`/terms`**、**`/privacy`** **`main`** 增 **`aria-label`**（**`terms_title`** / **`privacy_title`**）；烟雾 **`服务条款页可访问`**、**`隐私政策页可访问`**（**main** + **heading** 中英匹配）。
- **帮助与认证入口 E2E / a11y（2026-03-27）**：**`/help`** **`main`** **`aria-label`**（**`help_title`**）；**`/auth/login`**（**`auth_login_title`**）；**`/auth/register`** 选择步与子表单 **`main`**（**`auth_register_title`** / **`auth_register_traveler`** / **`auth_register_guide`** / **`auth_register_provider`** / **`auth_register_steward`**）；选择区 **`role="group"`** + **`aria-labelledby`**（**05-补充 §二点五**）；烟雾 **`帮助中心可访问`**、**`登录页可访问`**、**`注册入口页可访问`**。
- **自由市场与发现重定向 E2E / a11y（2026-03-27）**：**`/market`** **`main`** **`aria-label`**（**`market_hero_title`**）；**`/discover`** 过渡页 **`main`** **`aria-label`**（**`discover_page_a11y`**，与 **`locales`** 一致；**非** 已废弃的 **`nav_discover`**）；烟雾 **`自由市场页可访问`**、**`发现页重定向至自由市场`**（**`waitForURL`** **`/market`** + **Market / 自由市场** **main** + **heading**）。
- **向导列表与个人中心 E2E / a11y（2026-03-27）**：**`/guides`** 加载/错误/成功三态 **`main`** **`aria-label`**（**`guides_title`**）；错误态 **`sr-only` `h1`**；**`/me`** 与 **`MePageSkeleton`**、错误态改 **`main`** + **`aria-label`**（**`me_title`**），去掉 **`header`** 重复 **`aria-label`**；错误态 **`sr-only` `h1`**；烟雾 **`向导列表页可访问`**、**`我的页可访问（未登录回登录或已登录留个人中心）`**。
- **订单与支付入口 E2E / a11y（2026-03-27）**：**`/orders`** 加载/错误/成功 **`main`** **`aria-label`**（**`orders_myOrders`**）；错误态 **`sr-only` `h1`**；**`/pay`** **`main`** + **`Suspense`** **`PayPageSuspenseFallback`** **`aria-label`**（**`pay_pageTitle`**）；烟雾 **`我的订单页可访问（未登录回登录或已登录留订单列表）`**、**`支付与托管页可访问`**。
- **行程·建单·争议列表 E2E / a11y（2026-03-27）**：**`/itinerary/new`** **`main`** **`aria-label`**（**`itin_title`**）；**`/orders/new`** 表单/成功两态 **`main`**（**`orders_createTitle`** / **`orders_created`**）+ 成功态 **`sr-only` `h1`**；**`/disputes`** 加载/错误/列表 **`main`**（**`disputes_listTitle`**）+ 错误态 **`sr-only` `h1`**；烟雾 **`新建行程页可访问`**、**`创建订单页可访问`**、**`争议列表页可访问`**。
- **治理·质押·社区深链 E2E / a11y（2026-03-27）**：**`/governance`** **`main`** **`aria-labelledby`** **`governance-heading`**（与 **`governance_title`** 一致）；**`/staking`** **`main`** **`aria-label`**（**`staking_pageTitle`**）；**`/community?post=`** 烟雾（占位 UUID，**`main`** **`community_tab_feed`** + **`community_title` `h1`**）；烟雾 **`治理页可访问`**、**`向导质押页可访问`**、**`社区 Feed 带 post 查询可访问`**。
- **治理子页·85 壳·托管详情 E2E / a11y（2026-03-27）**：**`/governance/proposals|params|fee-routes`** 沿用 **`aria-labelledby`** **`h1`**；**`/traveltrust`** **`main`** **`aria-label`**（**`traveltrust_title`**）；**`EscrowDetail`** 加载失败分支由 **`div`** 改为 **`main`** + **`aria-label`**（**`escrow_detailAria`**）+ **`sr-only` `h1`**（**`escrow_errorTitle`**）；烟雾 **`治理提案页可访问`**、**`治理参数页可访问`**、**`治理费用路由页可访问`**、**`TravelTrust 网络落地页可访问`**、**`托管详情页可访问（占位订单 UUID）`**（仅断言 **`main`**，覆盖骨架/错误/成功）。
- **向导注册·改密·社区用户主页 E2E / a11y（2026-03-27）**：**`/guide/register`** 三态 **`main`** **`aria-label`**（**`guideRegister_title`** / **`guideRegister_doneMessage`** / **`guideRegister_alreadyGuideTitle`**）；**`/me/password`** 表单与成功 **`main`**（**`mePassword_title`**）；**`locales`** 增 **`community_user_main_aria`**；**`/community/user/[id]`** 合法 UUID 内容区改 **`main`**；非法 id **`main`** + **`sr-only` `h1`**（**`community_user_not_found`**）；烟雾 **`向导注册页可访问`**、**`修改密码页可访问`**、**`社区用户主页可访问（占位 UUID）`**。
- **社区用户非法 id·私信会话 E2E / a11y（2026-03-27）**：**`locales`** 增 **`community_conversation_thread_aria`**；**`/community/messages/[id]`** 外层改 **`main`**（有 id：**`community_conversation_thread_aria`**；无 id：**`community_conversation_not_found`** + **`sr-only` `h1`**）；烟雾 **`社区用户主页非法 id 可访问`**、**`社区私信会话页可访问（占位会话 id）`**。
- **认证找回密码·举报工单详情 E2E / a11y（2026-03-27）**：**`/auth/forgot-password`**、**`/auth/reset-password`** 表单/成功态 **`main`** **`aria-label`**（**`auth_forgot_title`** / **`auth_reset_title`**）；**`/community/me/reports/[id]`** 未登录分支 **`main`** **`aria-label`** + **`sr-only` `h1`**（**`community_report_ticket_title`**）；烟雾 **`忘记密码页可访问`**、**`重置密码页可访问`**、**`社区举报工单详情可访问（未登录提示）`**。
- **邮箱验证·争议详情·向导详情 E2E / a11y（2026-03-27）**：**`/auth/verify-email`** 两路 **`main`** **`aria-label`**（**`auth_verify_title`**）；**`/disputes/[id]`** 三态 **`main`**（**`dispute_detailTitle`**；错误 **`sr-only` `h1`** **`dispute_notFound`**）；**`/guides/[id]`** 加载/错误 **`main`** **`aria-label`**（**`guideDetail_title`**）+ 错误 **`sr-only` `h1`**（**`guideDetail_notFound`**）；成功 **`main`** **`aria-labelledby`** **`guide-hero-name`**；烟雾 **`邮箱验证页可访问`**、**`争议详情页可访问（占位 UUID）`**、**`向导详情页可访问（占位 UUID）`**。
- **首页·DID 榜·行程评分 E2E / a11y（2026-03-27）**：**`/`** **`main`** **`aria-label`**（**`landing_hero_title`**，与 Hero **`h1`** 一致）；**`/did-rank`** 根容器改 **`main`** **`aria-label`**（**`didRank_title`**）；**`DidRankHeader`** 去掉与页级重复的 **`header`** **`aria-label`**；**`/escrow/[id]/rate`** 无 id / 加载 / 拉单失败 / 成功 四路 **`main`**（**`rate_pageTitle`** / **`aria-labelledby`** **`rate-page-h1`**）+ 骨架与错误态 **`sr-only` `h1`**；烟雾 **`首页可访问`** 增 **`main`**、**`DID 排行榜可访问`** 增 **`main`**、**`行程评分页可访问（占位订单 UUID）`**。
- **社区子页壳层 main（2026-03-27）**：**`/community/friends`**、**`/community/messages`**、**`/community/activity`**、**`/community/feedback`**、**`/community/tt`** 根容器由 **`div`** 改为 **`main`**，**`aria-label`** 分别对应 **`community_tab_friends`** / **`community_tab_messages`** / **`community_activity_title`** / **`community_feedback_title`** / **`community_tt_title`**（与可见 **`h1`** 一致）；烟雾 **Feed / 发现 / 活动中心 / TT / 话题 / 好友 / 消息 / 反馈** 增 **`main`** 断言（**Feed|动态**、**Explore|发现** 等与 **`locales`** 一致）。
- **托管动态加载·DID 榜错误边界 main（2026-03-27）**：**`app/escrow/[id]/EscrowDetailSection`** 的 **`next/dynamic` `loading`** 与 **`Suspense` `fallback`** 使用 **`EscrowDetailRouteLoading`**（**`main`** **`aria-label`** **`escrow_detailAria`** + **`sr-only` `h1`**），与 **`EscrowDetail`** 内 **`main`** 一致，避免首屏无地标；**`app/did-rank/error.tsx`** 外层改 **`main`** **`aria-labelledby`** **`did-rank-error-h1`**，内层卡片保留 **`role="alert"`**。
- **路由 `loading.tsx` 地标与页级对齐（2026-03-27）**：非 Admin 前台各段 **`loading.tsx`** 的 **`main`** **`aria-label`**（及 **`AuthRouteLoading` 的 `mainAriaLabelKey`**）改为与同路由 **`page.tsx` `main`** 一致的 **`locales` 键**（如 **`landing_hero_title`**、**`community_tab_feed`**、**`market_hero_title`**、**`auth_login_title`**、**`rate_pageTitle`**、**`community_tab_friends`** / **`community_user_main_aria`** 等）；**`/did-rank`** 段 **`loading`** 根节点改 **`main`** **`didRank_title`**；**`/escrow/[id]/rate/loading`** 根节点改 **`main`** **`rate_pageTitle`**；**`/` 首页 `loading`** 增补 **`sr-only` `h1`**（**`landing_hero_title`**）。**`app/loading.tsx`** 仍为顶栏进度条，不占用页级 **`main`**。
- **Admin 子路由 loading / 404 main（2026-03-27）**：**`AdminSubpageRouteLoading`** 支持 **`mainAriaLabelKey`**（默认 **`common_loading`**）；**`/admin/loading`** 用 **`admin_workspace_title`**；**`app/admin/**/loading.tsx`**（除根段）均传入与各页 **`h1`** 一致的 **`admin_*_title`** 等键；根 **`not-found`** **`main`** **`aria-labelledby`** 与 **`h1` `id`**（**`not-found-title`**）对齐。
- **`/admin` 首页 i18n + E2E（2026-03-27）**：**`components/admin/AdminHomeClient`** 承接原 **`page.tsx`** 栅格；**`locales`** 增副标题、区块 **`aria-label`**、**`admin_home_desc_*`**；卡片标题与各子路由 **`admin_*_title`** 一致；**`e2e/smoke.spec.ts`** 用占位 **`traveltrust_user_id`** Cookie 过 **middleware**，断言 **`main`/`h1`**（**Admin Workspace / 管理后台**）及 **Orders** 链。
- **`/admin` error 文案 i18n + 63 E2E 表（2026-03-27）**：**`app/admin/error.tsx`** 顶部眉标与底栏 **Admin home** 链统一 **`t("admin_workspace_title")`**；**§二 2.1 E2E** 长表补 **`/admin`** 烟雾与 Cookie 说明。
- **Admin 列表 `section` aria（2026-03-27）**：**`/admin/orders|disputes|reviews|audit|approvals|users`** 表格外壳 **`aria-label`** 走 **`admin_orders_table_aria`** 等 6 键，**`locales`** 中英补齐。
- **Admin 列表地标续（fee-router / reports / api-versions）**：**`/admin/fee-router`** 摘要 **`section`** 与事件表外包 **`div`**；**`/admin/community/reports`**、**`/admin/api-versions`** 表 **`section`** — 硬编码英文 **`aria-label`** 清零，统走 **`locales`**；**`app/admin` 下已无 `aria-label="…"` 字面量**。
- **Admin 台账表 aria 全覆盖 + 订单面包屑（2026-03-27）**：余下 **flags / 社区子页台账 / compliance / scheduler / policies / tenants / jobs / internal-tools / lifecycle / secrets** 等 **`overflow-x-auto` 白表 `section`** 全部 **`t("admin_*_table_aria")`**；**`locales`** 增 **`common_nav_breadcrumb`**，**`/orders`** 面包屑 **`nav`** 接入；**`frontend` 下 `*.tsx` 已无 `aria-label="…"` 字面量**（与 37 地标 i18n 一致）。
- **Admin 非表页主内容 `section` aria（2026-03-27）**：**finance / observability / schema / indexer（健康 + 对账表单区）/ audit·operations / alerts·incidents·hub+detail / indexer·reconcile·detail** 主 **`section`** 使用 **`admin_finance_summary_aria`**、**`admin_observability_overview_aria`**、**`admin_schema_migrations_panel_aria`**、**`admin_indexer_health_panel_aria`**、**`admin_indexer_reconcile_sectionTitle`**（对账跳转卡）、**`admin_audit_ops_panel_aria`**、**`admin_alert_incident_*_panel_aria`**、**`admin_indexer_reconcile_payload_aria`** 等键。
- **P1 .env（2026-03-26）**：`frontend/.env.example` 篇首说明与根 `.env` 分工、**CORS_ORIGINS** 须含前端 origin、**STRICT_SESSION_GATE** 与 Bearer；补 **NEXT_PUBLIC_DEV_USER_ID** / **NEXT_PUBLIC_STAKING_ADDRESS** / **NEXT_PUBLIC_REGISTRY_ADDRESS** 注释行；根 `.env.example` 在 CORS 段补 **STRICT_SSOT 联用须非空 CORS**。
- **Admin（2026-03-26）**：`/admin/indexer` 只读页已对接 **`GET /api/v1/admin/indexer/health`**（`routes.admin.indexerHealth`）；**`/admin/indexer/reconcile/[id]`** 对接 **`GET …/indexer/reconcile-report/:id`**（`routes.admin.indexerReconcileReport`）；Indexer 页含报告 ID 表单跳转；完整对账导出仍按 110/200 排期。
- **Admin 160 / 340（2026-03-26）**：**`/admin/community/reports`** 列表 + `limit`/`status` + 行内 **Modal** **`PATCH …/moderation/:id`**（`routes.admin.communityModeration`，`writeRequestHeaders`）；**`/admin/api-versions`** 列表 + `limit`，对接 **`routes.admin.apiVersions`**（须 DB）；API 退役治理完整 UI 仍迭代。
- **Admin 160 / 350（2026-03-26）**：**`/admin/community/moderation/cases`**（`communityModerationCases`）、**`/admin/community/risk-signals`**（`communityRiskSignals`）、**`/admin/community/policy-change-logs`**（`communityPolicyChangeLogs`）、**`/admin/community/ranking/snapshots`**（`communityRankingSnapshots`）、**`/admin/community/penalties`**（`communityPenalties` + **Modal POST** `communityPenaltyCreate`）；**`/admin/lifecycle`**（`lifecycleStateMachines`）；举报页顶栏链到社区子页；**`/admin` 首页** 增加入口卡片。
- **Admin 160 写路径（2026-03-26）**：**`/admin/community/comments/visibility`** → **`PATCH …/comments/:id`**（`communityCommentVisibility`）；**`/admin/community/abuse-policy`** → **`PATCH …/abuse-policy`**（`communityAbusePolicy`，**super_admin**）；策略审计页顶栏链到滥用策略表单。
- **Admin 160 申诉（2026-03-26）**：**`/admin/community/appeals`** → **`GET …/admin/community/appeals`**（`routes.admin.communityAppeals`，`limit` / `report_id` / `status`）；**`/admin/community/appeals/review`** → **`POST …/appeals/:id/review`**（**super_admin**，query 预填 `appeal_id` + `expected_version`）；**`/admin`** 与举报工单页顶栏入口。
- **Admin 120 观测占位只读（2026-03-26）**：**`/admin/audit/operations`** → **`GET …/audit/operations`**（`auditOperations`）；**`/admin/alerts/incidents`** + **`[id]`** → **`GET …/alerts/incidents/:id`**（`alertIncident`）；**`/admin/observability`** 顶栏链出；**`/admin` 首页** 卡片。
- **Admin 70 用户角色变更申请（2026-03-26）**：**`/admin/users`** 列表 **`GET …/admin/users`**；行内 **Modal** **`POST …/users/:id/role-change-request`**（`routes.admin.userRoleChangeRequest`，`writeRequestHeaders`）；成功提示链至 **`/admin/approvals`**。
- **Admin 70 契约与审批写头（2026-03-26）**：**[14](14-合约-API-ABI-前后端对齐.md)** 110 段表补 **`GET …/orders|disputes|reviews|audit-logs|approvals`** 与 **`POST …/approvals/:id/approve`** 及状态说明；**`/admin/approvals`** 批准动作用 **`writeRequestHeaders`**；**04 §3.5** 证据长行、**13-1** 表 2、**00** Admin 口径长行已同步。
- **Admin 70 i18n + 14 可观测路径（2026-03-26）**：**`/admin/orders|disputes|reviews|audit|approvals`** 页 UI 文案接入 **`locales/zh.ts` / `en.ts`**（`admin_orders_*`、`admin_disputes_*`、`admin_reviews_*`、`admin_audit_list_*`、`admin_approvals_*` 等）；**[14](14-合约-API-ABI-前后端对齐.md)** **`GET …/observability/overview`** 行补 **`frontend/app/admin/observability`**、`routes.admin.observabilityOverview`。
- **Admin 70 finance / fee-router / observability i18n（2026-03-26）**：**`/admin/finance`**（`admin_finance_*`）、**`/admin/fee-router`**（`admin_fee_router_*`，请求头 **try/catch** 与其它 Admin 读页一致）、**`/admin/observability`** 空态 **`admin_observability_noData`**；**[13-1](13-1-UI产品级SSOT与页面规范.md)** 表 2 增 **fee-router / observability** 行并扩 **finance**；**[00](00-文档索引.md)** Admin 口径长行含 **finance、fee-router**。
- **Admin 70 `admin_em_dash`（2026-03-26）**：**`frontend/locales`** 增通用 **`admin_em_dash`**；**approvals / fee-router / observability / api-versions / lifecycle / orders / users / reviews** 表格与摘要占位统一 **`t("admin_em_dash")`**，移除 **`admin_approvals_dash`**；**13-1** 表 2-补充、**63**、**00/07**。
- **Admin 70 `admin_em_dash` 续 + 顶栏（2026-03-26）**：**`/admin/orders`**（游客/向导/创建时间）、**`/admin/disputes`**、**`/admin/audit`**、**`/admin/alerts/incidents/[id]`** 余下 **`"-"` / `"—"`** 占位改 **`t("admin_em_dash")`**；**`/admin/audit/operations`**、**`/admin/alerts/incidents`** 与 **`[id]`** 顶栏「返回 Admin 首页」改 **`admin_schema_back`**（与 **`/admin/community/*`** 保留 **`admin_community_reports_back`** 区分）。
- **Admin 70 `admin_em_dash` 批量（社区 + 运维台账）（2026-03-26）**：**`/admin/community/*`**（**reports、appeals、policy-change-logs、penalties、ranking/snapshots、risk-signals、moderation/cases**）与 **`/admin/jobs`**、**`/admin/scheduler/jobs`**、**`/admin/policies`**、**`/admin/flags`**、**`/admin/compliance/requests`**、**`/admin/compliance/requests/[requestId]/events`**、**`/admin/tenants/scopes`**、**`/admin/secrets/metadata`**、**`/admin/config/releases`**、**`/admin/internal-tools/audits`** 列表单元格及 **preview/trunc** helper 全部 **`t("admin_em_dash")`**；**`frontend/app/admin/**/*.tsx`** 内字面 **`"—"`** 清零；**07/13-1/00/63** 版本双写。
- **Admin 70 写表单单选 i18n（2026-03-26）**：**`/admin/community/comments/visibility`** **visibility_status**、**`/admin/community/appeals/review`** **decision**、**`/admin/compliance/requests/[requestId]/update`** **status** 下拉的 **zh/en** 展示文案（**`admin_comment_vis_opt_*`**、**`admin_appeal_review_opt_*`**、**`admin_compliance_update_status_*`**）；成功提示中的状态亦用译名；**07/13-1/00/63** 版本双写。
- **Admin 70 / 450 / 220（2026-03-26）**：**`/admin/policies`**（`policies` + **`policyPublish`** Modal）、**`/admin/internal-tools/audits`**（`internalToolAudits`）、**`/admin/flags`**（`flags` + **`flagPublish`** Modal）；**`/admin` 首页** 卡片已述写路径；完整治理台仍迭代。
- **Admin 250 / 260 / 220 / 70（2026-03-26）**：**`/admin/jobs`**（`routes.admin.jobs`，含 `summary`）、**`/admin/config/releases`**（`configReleases`）、**`/admin/secrets/metadata`**（`secretsMetadata`）、**`/admin/scheduler/jobs`**（`schedulerJobs` + 行内 **`schedulerJobRerun`**）；**`/admin` 首页** 增加入口；密钥轮换等仍迭代。
- **Admin 320 / 500 / 70（2026-03-26）**：**`/admin/tenants/scopes`**（`tenantScopes` + 列表 **Modal** **`POST …/publish`**，`tenantScopePublish`）；**`/admin/compliance/requests`**（`complianceDataRequests`）、**`…/[requestId]/events`**、**`…/[requestId]/update`**（**`POST …/update`**，`complianceDataRequestUpdate`）；**`/admin` 首页** 卡片；完整导出与租户治理台仍迭代。
- 本清单随实现进展更新；具体页面级待办以各文档（如 30 §8）为准。

---

## 八、当前还能做啥（按优先级）

| 优先级 | 项 | 说明 |
|--------|------|------|
| **P1** | CORS_ORIGINS | 生产环境必须配置；见 Runbook 部署前必检 |
| **P1** | .env 与密钥 | 根目录与 **frontend/.env.example** 已补 **CORS 在 API 侧**、与 **STRICT_SESSION_GATE** / **NEXT_PUBLIC_DEV_USER_ID** 互斥说明；勿提交真实密钥 |
| **P1** | 合约与 Runbook | 按 01/02、08-3 与 Runbook 执行与校验 |
| **P2** | DID 排名 API | ✅ 已实现并入 04；前端已对接；持续对齐 `rank_basis` / 口径即可 |
| **P2** | ABI 与前端同步 | 合约变更后重新生成 ABI 并更新 `dapp/abis/` |
| **P3** | 旅行者详情入口 | ✅ 已链 **`/community/user/:id`**（UUID）；见 §2.2 |
