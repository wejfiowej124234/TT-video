# 62-补充-03：Escrow路由逐文件代码映射（2026-03-06）

### 读前摘要

| 你要找什么 | 单源 |
|------------|------|
| **Escrow 逐文件映射** | **正文** |
| **总表** | **[62-UI-UX前端100%代码映射总表-20260306](62-UI-UX前端100%代码映射总表-20260306.md)** |
| **域规范** | **[35-DApp与钱包流程实现与验收清单](../35-DApp与钱包流程实现与验收清单.md)**、**[14](../14-合约-API-ABI-前后端对齐.md)** |

Status: Code-driven deep audit (Round 3)
Scope: `/escrow` 全域（详情 + 评分）
Source of truth: `frontend/app/escrow/*`, `frontend/components/escrow/*`
Related docs: 13, 13-1, 23, 35, 37, 39, 53, 62

---

## 1. 审计结论

本轮对 Escrow 路由进行了逐文件级核对：

1. 路由结构完整：详情页与评分页链路齐全，且包含 `layout/loading/error` 路由壳层。
2. 协议流程完整：8 步状态机（草稿->向导确认->双边确认->确认->付款->完成->评分->释放）在 UI 有可视化步骤条与状态映射。
3. 链上能力完整：deposit/release/refund/dispute 操作、交易状态机、终局性展示、Reorg 提醒、风险提示均已落位。
4. i18n 覆盖完整：Escrow 范围键位检查 `total_keys=179`，`missing_zh=0`，`missing_en=0`。
5. 测试基础优于前两轮：已存在 4 个 escrow 相关测试文件，但仍有关键交互链路需补齐。
6. 13 宪法风险点仍在：`order-protocol` 资金控制区当前采用 DID 赛博风（cyan glow + blur），与 13 中“资金区银行级克制”边界存在冲突，需要后续收口。

统计快照：

- `escrow_app_files=5`
- `escrow_component_files=28`

---

## 2. 路由层映射（app/escrow）

| 文件 | 代码职责 | 对应规范 | 状态 |
|---|---|---|---|
| `frontend/app/escrow/layout.tsx` | Escrow 子域 metadata 与路由壳层 | 33, 37 | 已对齐 |
| `frontend/app/escrow/loading.tsx` | Escrow 路由加载反馈（读屏可播报） | 30, 37, 39 | 已对齐 |
| `frontend/app/escrow/error.tsx` | Escrow 路由错误边界（重试/返回首页） | 30, 37, 39 | 已对齐 |
| `frontend/app/escrow/[id]/page.tsx` | Escrow 详情页入口，动态加载 `EscrowDetail` | 13-1, 33, 53 | 已对齐 |
| `frontend/app/escrow/[id]/rate/page.tsx` | 双边评分页（上传、审核、确认、释放阶段） | 53, 35, 37 | 已对齐 |

---

## 3. 组件层映射（components/escrow）

| 文件/目录 | 代码职责 | 对应规范 | 状态 |
|---|---|---|---|
| `frontend/components/escrow/EscrowDetail/index.tsx` | Escrow 主编排：流程条、金额区、确认区、链上操作、风险区、打印与摘要复制 | 13, 13-1, 53 | 已对齐（含风格冲突待收口） |
| `frontend/components/escrow/EscrowDetail/useEscrowDetail.ts` | 详情数据聚合：订单/行程/钱包链状态/交易状态机 | 35, 53 | 已对齐 |
| `frontend/components/escrow/OrderFlowSteps.tsx` | 8 步流程条与状态->步骤映射（含 did 变体） | 13-1, 53 | 已对齐 |
| `frontend/components/escrow/EscrowDetail/BilateralConfirmBlock.tsx` | 双边确认区（旅行者/向导确认） | 53 | 已对齐 |
| `frontend/components/escrow/EscrowDetail/EscrowOnChainActions.tsx` | deposit/release/refund/dispute 四类链上动作与 Tx 状态反馈 | 35, 53 | 已对齐 |
| `frontend/components/escrow/EscrowDetail/EscrowTxModal.tsx` | 链上交易确认弹窗与状态提示 | 35 | 已对齐 |
| `frontend/components/escrow/EscrowDetail/SetEscrowAddressBlock.tsx` | Escrow 地址设置区块 | 53 | 已对齐 |
| `frontend/components/escrow/EscrowDetail/OrderActionsBlock.tsx` | 订单动作区块（状态推进） | 53 | 已对齐 |
| `frontend/components/escrow/EscrowDetail/ReviewBlock.tsx` | 完成后评价区块 | 53 | 已对齐 |
| `frontend/components/escrow/EscrowDetail/EscrowRiskNotice.tsx` | 争议窗口与不可逆风险提示 | 13, 35 | 已对齐 |
| `frontend/components/escrow/EscrowDetail/ReorgBanner.tsx` | Reorg 风险提醒与刷新动作 | 10, 35 | 已对齐 |
| `frontend/components/escrow/OnchainEventTimeline.tsx` | 链上事件时间线 | 35 | 已对齐 |
| `frontend/components/escrow/FinalityBadge.tsx` | Finality 块确认展示 | 35 | 已对齐 |
| `frontend/components/escrow/TxMachineStatus.tsx` | 交易状态机可视化反馈 | 35, 37 | 已对齐 |
| `frontend/components/escrow/EscrowDetail/OrderMessageLink.tsx` | 从 Escrow 跳转社区消息（`/community/messages?orderId=`） | 31, 53 | 已对齐 |

---

## 4. 关键约束核对

### 4.1 53 阶段流程一致性

1. `OrderFlowSteps` 已实装 8 步流程并维护状态映射。
2. 详情页覆盖双边确认、付款、评分入口、资金释放与评价闭环。
3. `rate/page.tsx` 建立评分上传与确认流程，和订单状态回刷联动。

### 4.2 35 DApp/链上流程一致性

1. `useEscrowDetail` 连接 wagmi 链状态并执行 escrow actions hooks。
2. 链上四动作（deposit/release/refund/dispute）均提供 pending/success/failed 可感知反馈。
3. `FinalityBadge`、`OnchainEventTimeline`、`ReorgBanner` 提供链不确定性可见证据。

### 4.3 13 宪法边界冲突（重点）

1. `EscrowDetail/index.tsx` 中 `order-protocol` 区使用深色赛博面板（`bg-slate-950` + `border-cyan` + `backdrop-blur`）。
2. `OrderFlowSteps`、`BilateralConfirmBlock`、`OrderMessageLink` 均启用 `did`/`variantDid` 视觉。
3. 与 13 的“资金区银行级克制”存在视觉边界冲突，应在后续轮次形成明确收口方案（保留流程结构，替换视觉变体）。

### 4.4 37 i18n/a11y 约束

1. Escrow 范围翻译键覆盖：
   - 抽取范围：`app/escrow`, `components/escrow`
   - 结果：`total_keys=179`, `missing_zh=0`, `missing_en=0`
2. a11y 证据：
   - `loading.tsx` 使用 `role="status"`
   - `error.tsx` 使用 `role="alert"`
   - 关键按钮带 `aria-busy`
   - 关键状态区使用 `role="status"`/`aria-live`

---

## 5. 测试覆盖现状

已存在测试：

1. `frontend/components/escrow/StatusBadge.test.tsx`
2. `frontend/components/escrow/OrderFlowSteps.test.ts`
3. `frontend/components/escrow/OrderFlowSteps.integration.test.tsx`
4. `frontend/components/escrow/EscrowDetail/BilateralConfirmBlock.test.tsx`

当前缺口（建议补充）：

1. `EscrowOnChainActions` 四动作禁用态与错误态（含 allowance hint）测试。
2. `EscrowTxModal` 确认流程与关闭逻辑测试。
3. `EscrowDetail/index.tsx` 关键路径集成测试（草稿->双边确认->评分入口显示）。
4. `app/escrow/[id]/rate/page.tsx` 上传/确认/错误状态与无障碍属性测试。

---

## 6. 结论与下一步

Escrow 路由已完成 `62` 系列第 03 个逐文件映射闭环，流程能力、链上能力与 i18n/a11y 证据均可追溯。

下一步建议：

1. 进入 `Auth + Me` 路由域（账号与个人中心）逐文件映射。
2. 对 Escrow 资金区执行 13 宪法风格收口专项（保持信息架构，替换为银行级克制视觉 token）。
3. 先补 Escrow 关键交互测试，再进入下一路由域，避免回归风险累积。
