# 29-补充：自由市场撮合控制台代码实现映射（2026-03-06）

### 读前摘要

| 你要找什么 | 单源 |
|------------|------|
| **29 主规范** | **[29-自由市场-撮合控制台规范](29-自由市场-撮合控制台规范.md)** |
| **执行摘要与评分** | **§1** |
| **路由/组件映射** | **§2** 起 |
| **风格** | **[86](86-UI-双系统未来风-风格与动效技术规格.md)** **Business 降级**、**[22 §一点五](22-Design-Tokens-旅游Web3融合体系-v1.0.md)**、**[28](28-Cinematic-Glassmorphism-Web3融合规范.md)**（叙事/信息分层）、**[13-1](13-1-UI产品级SSOT与页面规范.md)**；读序 **[07 §五 5.3](07-开发流程与顺序.md)** |

Status: Code-driven supplement
Scope: 对齐 `29-自由市场-撮合控制台规范.md` 与 `frontend/app/market` 真实实现
Related: **86**, **22**, 28, 29, 05, 13-1, **07**, 60, 61

**文档版本**：1.0.2（2026-03-30；与 [00-文档索引](00-文档索引.md) 版本表一致）

---

## 1. 执行摘要

`/market` 已不是普通列表页，而是完整的撮合控制台实现。核心规范项已基本落地：

- 双栏/单栏视图切换（Split/Orders/Guides）已实现。
- Sticky 筛选条（国家/城市/语言/服务）与多选逻辑已实现。
- 订单/向导卡片 + 详情抽屉 + 预约向导弹窗已实现。
- `/discover` 已重定向 `/market`，避免双入口分流。
- Web3元素保持克制露出（徽章/状态），资金动作不在 market 主流程。

当前评分（29维度）:
- 页面结构落地: 95/100
- 组件完整度: 93/100
- 业务流程一致性: 90/100
- 综合: 93/100

---

## 2. 页面结构映射（规范 vs 代码）

规范项: 单页撮合控制台（Split 7/5 + 可切换）
- 代码: `frontend/app/market/page.tsx`
  - Hero + StickyFilterBar + MarketContent + Footer
  - `OrderDetailDrawer`, `GuideDetailDrawer`, `BookGuideModal`
  - `CustomItineraryModal` 使用动态加载

规范项: Discover 与 Market 统一
- 代码: `frontend/app/discover/page.tsx` 执行 `router.replace("/market")`

规范项: 玻璃态 + 克制Web3
- 代码: 页面容器、FilterBar、内容卡区大量 `bg-white/5` / `border-white/25` / `backdrop-blur-*`

---

## 3. 关键组件证据

### 3.1 Hero 区

- `frontend/components/market/MarketPageHero.tsx`
  - 标题、副标题、三个功能 pill（match/escrow/dispute）
  - 自定义行程 CTA，含 focus-visible ring

### 3.2 StickyFilterBar

- `frontend/components/market/StickyFilterBar.tsx`
  - 国家单选 + 城市联动
  - 语言多选（按国家动态）+ 服务类型多选
  - 高级筛选折叠区（ARIA：group/expanded/controls）

### 3.3 ViewSwitcher

- `frontend/components/market/ViewSwitcher.tsx`
  - 三态切换：`split|orders|guides`
  - `role="tablist"` + `role="tab"` + `aria-selected`

### 3.4 MarketContent

- `frontend/components/market/MarketContent.tsx`
  - 左订单、右向导分区与响应式栅格
  - API error alert + retry + dismiss
  - 空态与骨架屏
  - 排序按钮（latest/priceDesc/priceAsc）
  - 订单卡/向导卡均支持收藏与详情动作

---

## 4. 29 核心约束核查

### 约束 A: 市场页只做撮合，不做支付

- 代码状态: 基本符合。
- 证据: `market/page.tsx` 仅包含匹配、详情、预约、自定义行程，不含链上支付动作入口。

### 约束 B: Web3克制嵌入

- 代码状态: 符合。
- 证据: 以 pill/badge/状态字样呈现，未出现重链上交互面板。

### 约束 C: 单一入口与职责清晰

- 代码状态: 符合。
- 证据: `/discover` 已重定向 `/market`。

### 约束 D: a11y 基础可用

- 代码状态: 部分符合。
- 证据:
  - 已有 ARIA 结构（tablist/group/alert/status）
  - 仍建议补齐更多 aria-label 与键盘操作覆盖测试。

---

## 5. 代码级缺口与建议

P0:
1. 补齐 Market 全链路 i18n key 完整度（错误态/校验态/空态细分）。
2. 增加 ViewSwitcher 与筛选条的键盘回归测试（Vitest + RTL）。

P1:
1. 在 29 主文档增加“OrderCard glass 变体”说明（已在 28-补充提及，需回链）。
2. 将筛选参数与 URL 同步策略明确到实现级（country/city/language/service/view）。

P2:
1. 增加性能指标（首次可交互、筛选切换响应、抽屉开合耗时）并纳入发布门禁。

---

## 6. 结论

`29` 规范在当前代码中已经高度落地，属于可发版级页面骨架。
后续重点从“结构完成”转向“可验证质量闭环”（i18n、a11y、性能与测试证据）。
