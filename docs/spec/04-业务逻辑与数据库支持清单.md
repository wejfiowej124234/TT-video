# 04 业务逻辑与数据库支持清单

### 读前摘要

| 你要找什么 | 单源 |
|------------|------|
| **文档关系与使用顺序** | **§一** |
| **按域：业务逻辑 ↔ 表/列/索引** | **§二** 起 |
| **库表域划分** | **[04-数据库架构与表分类](04-数据库架构与表分类.md)** |
| **API 侧契约** | **[04-后端与API](04-后端与API.md)** |

**用途**：由 **UI 与展示功能** 推导 **业务逻辑**，再明确每条业务逻辑需要的 **数据库支持**（表、列、索引、约束）；便于接库前「文档数据库相关准备齐全」、实现时按单查表与落代码。

**依据**：[01-总库总览](01-总库总览.md)、[03-业务流程与风控](03-业务流程与风控.md)、[04-后端与API](04-后端与API.md)、[13-1-UI产品级SSOT与页面规范](13-1-UI产品级SSOT与页面规范.md)、[数据库表与UI功能对照检查](../数据库表与UI功能对照检查.md)、[04-数据库架构与表分类](04-数据库架构与表分类.md)。

---

## 一、文档关系（数据库相关准备齐全）

| 文档 | 职责 |
|------|------|
| **本文** | 业务逻辑 ↔ 数据库支持（表/列/索引）逐条对应；缺项与待实现标注 |
| [04-数据库架构与表分类](04-数据库架构与表分类.md) | 域划分、表分类、迁移目录、与合约/治理/订单/管理员关系 |
| [04-附录-DDL草案](04-附录-DDL草案.md) | 可执行 DDL、表结构定稿 |
| [数据库表与UI功能对照检查](../数据库表与UI功能对照检查.md) | UI 功能 ↔ API ↔ 表/持久化、全量清单与状态 |
| [41-后端数据库接库与落地清单](41-后端数据库接库与落地清单.md) | 接库勾选、迁移执行、hydrate、表完整性 |

**使用顺序**：先看本文知「要哪些业务逻辑、各需什么 DB」；再看 04-数据库架构 知表归属与迁移；接库时按 41 执行；对照 UI 时查数据库表与UI功能对照检查。

**55 阶段收口已同步**：本文与 [55-阶段-数据同步与数据库功能同步](55-阶段-数据同步与数据库功能同步.md) 已对齐；社区域/治理域/反馈表、orders.guide_id 可空、行程写回、订单与发现列表字段等见 55 §九附、§八附续。

**无 DB 时与生产必配（P2）**：与 [Runbook §2.5](../../ops/RUNBOOK.md)、[41-后端数据库接库与落地清单](41-后端数据库接库与落地清单.md) 一致。依赖 DB 的路由（社区、DID 榜、治理、反馈等）在**未配置 DATABASE_URL** 时返回 service unavailable 或列表为空；**生产/需持久化场景必须配置 DATABASE_URL**。发版前三处交叉核对并勾选（55 §八附续.1 P2）。

---

## 二、业务逻辑与数据库支持（按域）

### 2.1 账号与身份（identity）

| # | 业务逻辑 | 规则/校验来源 | 所需数据库支持 | 迁移/实现状态 |
|---|----------|----------------|----------------|----------------|
| 1 | 注册：创建用户、唯一邮箱 | 04 §3.1；防重复注册 | users（id, email UNIQUE, password_hash, role, kyc_status, nickname, avatar_url, default_wallet_address）；idx_users_email | ✅ 000001 |
| 2 | 登录：校验密码、创建会话 | 04 §3.1；token 与 user_id 绑定 | users（读 password_hash）；sessions（token PK, user_id, created_at）；idx_sessions_user | ✅ 000001 |
| 3 | 登出/刷新：使 token 失效或换 token | 04 §3.1 | sessions 删除或更新 | ✅ 已有 |
| 4 | 个人资料：查/改昵称、头像、钱包地址 | 04 §3.2 GET/PUT me | users 读/写 nickname, avatar_url, default_wallet_address, updated_at | ✅ 已有 |
| 5 | 修改密码 | 04 §3.2 PUT me/password | users 读 password_hash 校验旧密码、写新 hash | ✅ 已有 |
| 6 | 邮箱验证/忘记密码/重置密码 | 04 §3.1（可选） | users；若做链接/验证码可增 email_verified_at 或 password_reset_tokens 表 | 可选 email_verified_at 列 |

### 2.2 向导（guides）

| # | 业务逻辑 | 规则/校验来源 | 所需数据库支持 | 迁移/实现状态 |
|---|----------|----------------|----------------|----------------|
| 7 | 向导列表：按 city/language/service_type 筛选 | 04 §3.3 GET guides | guides 表；索引 idx_guides_user、idx_guides_status；按 city/languages/service_types 过滤 | ✅ 000002 |
| 8 | 向导详情：按 id 查，不暴露 passport 明文 | 04 §3.3 GET guides/:id | guides 单行；不返回 passport_number_hash 明文 | ✅ 已有 |
| 9 | 向导注册：一人一向导、必填 city/证件 | 03、04；REQUIRE_ID_PHOTO 等 | guides 插入（user_id UNIQUE）；校验 users 角色与是否已有 guide | ✅ 已有 |
| 10 | 证件上传：落盘 + 回写 URL | 04 §3.3 upload-doc | 文件系统 data/guide_uploads；guides 更新 id_photo_url/language_cert_url/guide_license_url | ✅ 已有 |
| 11 | 质押：更新金额、pending→active 条件 | 01 §4、04 §3.3 stake | guides.stake_amount、guides.status；可选 stakes 表记历史 | ✅ 已有（guides 字段） |
| 12 | 档期：一向导同一时段仅一单（Accepted/Escrowed） | 01 §5、03 §1.3 | 可选 guide_slot 表（guide_id, order_id）；或由 orders 查询「该 guide 非终态订单」 | 未建表；逻辑可由 orders 查询实现 |

### 2.3 订单与状态机（orders）

| # | 业务逻辑 | 规则/校验来源 | 所需数据库支持 | 迁移/实现状态 |
|---|----------|----------------|----------------|----------------|
| 13 | 创建订单：Draft 或 Created，关联向导与旅行者；Draft 时 guide_id 可空（55-S1） | 01 §1、04 §3.3、55 | orders 插入（tourist_id, guide_id 可 NULL, amount, currency, status）；外键 users, guides | ✅ 000003、20250310000015 |
| 14 | 我的订单列表：按角色过滤（旅行者看己下单/向导看己接单） | 04 §3.3 GET orders | orders；idx_orders_tourist、idx_orders_guide、idx_orders_status | ✅ 已有 |
| 15 | 接单：Created→Accepted，可接单超时 | 01 §1、03 §1.1 | orders 更新 status, accepted_at；校验当前 status=Created、超时则不可接 | ✅ 已有 |
| 16 | 取消：按状态与角色允许取消，释放档期 | 01 §1、03 §1.2 | orders 更新 status=Cancelled；档期解档（若有 guide_slot） | ✅ 已有 |
| 17 | **链下**确认行程完成（`confirm-completion`）：推进业务进度，**非**单笔即 Completed/放款；**Completed** 与资金终态以链上 `Released` 及 04 §3.4、53 为准 | 01 §1、03、[04](04-后端与API.md) §3.4、53 | orders 更新进度字段（实现以迁移为准）；Escrowed 下可调；与 **release** 解耦 | ✅ 已有；语义与 14 对齐 |
| 18 | 订单状态机：仅合法迁移、资金态仅链事件驱动 | 01 §1、01 §3、03 | orders.status 与时间戳；链上对接时由 event_log/orders_projection 驱动 | ✅ 已有；接链后见 chain 域 |
| 19 | **自由市场**可浏览订单列表（**`GET /api/v1/discover/orders`**；前端主 UI **`/market`**，**`/discover`** 重定向）：可浏览 Draft/Created 等，按 country/city 筛 | 04 §3.4 GET discover/orders | orders 查询 status 与 city/country（或 itinerary 关联）；idx_orders_status | ✅ 已有 |
| 20 | 确认最终方案 / 设置托管地址 | 04 §3.3；P16 | orders 更新 snapshot_hash、escrow_address 等 | ✅ 已有 |
| 21 | 超时与争议窗口：dispute_deadline、auto_complete_at | 01 §5、03 §1.3 | orders.dispute_deadline_at、orders.auto_complete_at（当前迁移已有） | ✅ 000003 |

### 2.4 行程（itineraries）

| # | 业务逻辑 | 规则/校验来源 | 所需数据库支持 | 迁移/实现状态 |
|---|----------|----------------|----------------|----------------|
| 22 | 创建/更新行程包：关联 Draft 订单，每日行程+费用；PATCH 写回 DB（55-S2） | P15/17、04 §3.3、55 | itineraries（order_id UNIQUE, version, destination, city, days_json, amount_breakdown_json, snapshot_hash）；PATCH 更新 days_json/amount_breakdown/version 写回 DB | ✅ 000006；db 双写 + hydrate + PATCH 写回已实现 |
| 23 | 订单详情含行程：Draft 时返回 itinerary | 04 §3.3 GET orders/:id | itineraries 按 order_id 查（hydrate 后内存）；与 orders 联合返回 | ✅ 已有 |

### 2.5 订单聊天（order_messages）

| # | 业务逻辑 | 规则/校验来源 | 所需数据库支持 | 迁移/实现状态 |
|---|----------|----------------|----------------|----------------|
| 24 | 仅订单参与方可读/发消息 | 04 §3.3 P16 | order_messages（order_id, sender_id, content, created_at）；校验当前用户为 tourist 或 guide | ✅ 000007；db 双写 + hydrate 已实现 |
| 25 | 消息列表按时间序 | 04 §3.3 GET orders/:id/messages | order_messages；idx_order_messages_order、idx_order_messages_created（hydrate 后内存） | ✅ 已有 |

### 2.6 评价（reviews）

| # | 业务逻辑 | 规则/校验来源 | 所需数据库支持 | 迁移/实现状态 |
|---|----------|----------------|----------------|----------------|
| 26 | 仅资金终态可提交评价；一单一评（或按角色一旅行者一向导各一） | 01 §4、03 §2.1 | reviews（order_id, reviewer_id, reviewee_id, score, weight, comment）；校验 order 已 Completed/争议已关闭 | ✅ 000004 |
| 27 | 按订单/被评人查评价列表 | 04 §3.3 GET orders/:id/reviews | reviews；idx_reviews_order、idx_reviews_reviewee | ✅ 已有 |

### 2.7 争议与证据（disputes / audit）

| # | 业务逻辑 | 规则/校验来源 | 所需数据库支持 | 迁移/实现状态 |
|---|----------|----------------|----------------|----------------|
| 28 | 一单一争议；发起争议写 evidence_hashes | 01 §5、04 §3.3 | disputes（order_id UNIQUE, status, evidence_hashes, arbitrator_id, refund_ratio, slash_guide, resolved_at）；idx_disputes_order | ✅ 000004 |
| 29 | 裁决：仲裁员写结果、执行器幂等 | 01 §7、03 §3.2 | disputes 更新 status, arbitrator_id, refund_ratio, slash_guide, resolved_at；链上对接时 executor_executions | ✅ 已有；接链后 executor_executions 表 |
| 30 | 证据上传/列表：工单内可读、审计 | 01 §6、04 §3.3 | disputes.evidence_hashes；可选 evidence_receipts 表（order_id, content_hash, uploader_id） | 当前 disputes 存 hash；evidence_receipts 可选 |

### 2.8 统计与展示（聚合）

| # | 业务逻辑 | 规则/校验来源 | 所需数据库支持 | 迁移/实现状态 |
|---|----------|----------------|----------------|----------------|
| 31 | 个人统计：订单数/总消费/评价数/完成单数等 | 04 §3.2 me/stats | orders、reviews 聚合（按 tourist_id 或 guide_id）；无需新表 | ✅ 已有 |
| 32 | DID 排行榜：按周期聚合旅行者/向导榜 | 30、04 §3.4 did-rank | orders + reviews 聚合；或可选 did_rank_snapshot 缓存表 | 无专用表；聚合或 stub 可满足 |

### 2.9 社区域（TT社区，产品落地方）

| # | 业务逻辑 | 规则/校验来源 | 所需数据库支持 | 迁移/实现状态 |
|---|----------|----------------|----------------|----------------|
| 33 | Feed：发帖、点赞、评论、收藏、关注、私信 | 31、13-1 | community_posts, community_likes, community_comments, community_collects, community_follows, community_conversations, community_dm_messages 等表 | ✅ 已实现（20250306/07；55 阶段） |
| 33b | 反馈/建议（54-S19/55-S10） | 04、55 | community_feedback（id, user_id, category, content, status, official_reply） | ✅ 已实现（20250310000016；GET/POST /api/v1/community/feedback）；前端列表已展示 status、official_reply（55 1.6）；G4 官方回复 PATCH 已实现（内网 PATCH /api/v1/internal/community/feedback/:id），产品定稿后可扩展公网 |

### 2.10 治理域（Governance，产品落地方）

| # | 业务逻辑 | 规则/校验来源 | 所需数据库支持 | 迁移/实现状态 |
|---|----------|----------------|----------------|----------------|
| 34 | 参数展示、提案列表、投票 | 13-1 治理页 | governance_pool, governance_reward_records（池与奖励记录已建） | ✅ 池与奖励已实现（20250305）；proposals/votes 待产品定稿 |

---

## 三、数据库支持汇总表（表/列/索引）

| 表名 | 主要支撑业务逻辑（编号） | 迁移 | db 层/hydrate |
|------|--------------------------|------|----------------|
| users | 1～6 | 000001 | ✅ |
| sessions | 2～4 | 000001 | ✅ |
| guides | 7～12 | 000002、000005 | ✅ |
| orders | 13～21 | 000003 | ✅ |
| itineraries | 22～23 | 000006 | ✅ 已有（db 双写 + hydrate） |
| order_messages | 24～25 | 000007 | ✅ 已有（db 双写 + hydrate） |
| reviews | 26～27 | 000004 | ✅ |
| disputes | 28～30 | 000004 | ✅ |
| （聚合） | 31～32 | — | orders+reviews 查询 |
| （社区域） | 33、33b | 000011/000012/000016 | ✅ 已建（community_*、community_feedback；55 阶段） |
| （治理域） | 34 | 000010 | ✅ 池与奖励已建（governance_pool、governance_reward_records） |
| guide_slot（可选） | 12 | 未建 | 可由 orders 查询替代 |
| evidence_receipts（可选） | 30 | 000009 | ✅ 已建 |
| idempotency_keys | API 幂等（01 §10 #14） | 000013 | ✅ 55-S8 已打通（中间件读写该表，跨实例/重启幂等） |

---

## 四、待实现与可选（文档准备齐全后的落代码顺序）

| 优先级 | 项 | 说明 |
|--------|-----|------|
| P0 | itineraries、order_messages 的 db.rs + 双写 + hydrate | ✅ 已完成（insert/list、chain_off 双写、main hydrate） |
| P1 | orders 补列 trip_end_at、snapshot_hash（可选） | 04 附录 §9.4；对账/审计需要时 |
| P1 | users 补列 email_verified_at（可选） | 04 §2.1；做邮箱验证时 |
| P2 | idempotency_keys | ✅ 迁移 **000013** + **55-S8** 中间件；生产按需 `REQUIRE_IDEMPOTENCY_KEY=1` |
| P2 | guide_slot 或等效档期校验 | 若需「一向导一单」硬约束可建表；否则 orders 查询 |
| P2 | evidence_receipts 写入覆盖（可选） | 表 **000009** 已建；是否全路径写入见证据 API/产品 |
| P3 | 社区域增量（160）、治理提案/投票 | 核心社区与治理池已落库；**举报/审核**、**完整治理页**按产品与 160/04 迭代 |

---

*与 [04-数据库架构与表分类](04-数据库架构与表分类.md)、[数据库表与UI功能对照检查](../数据库表与UI功能对照检查.md)、[41-后端数据库接库与落地清单](41-后端数据库接库与落地清单.md)、[03-业务流程与风控](03-业务流程与风控.md) 配套。*
