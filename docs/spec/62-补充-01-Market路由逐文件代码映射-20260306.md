# 62-补充-01：Market路由逐文件代码映射（2026-03-06）

### 读前摘要

| 你要找什么 | 单源 |
|------------|------|
| **Market 逐文件映射** | **正文** |
| **总表** | **[62-UI-UX前端100%代码映射总表-20260306](62-UI-UX前端100%代码映射总表-20260306.md)** |
| **域规范** | **[29](29-自由市场-撮合控制台规范.md)**、**[28](28-Cinematic-Glassmorphism-Web3融合规范.md)** |

Status: Code-driven deep audit (Round 1)
Scope: `/market`（含 `/discover -> /market`）
Source of truth: `frontend/app/market/*`, `frontend/components/market/*`
Related docs: 29, 28, 13-1, 33, 34, 37, 39, 62

---

## 1. 审计结论

本轮对 Market 路由进行了逐文件级核对：

1. 信息架构与职责边界符合 29（撮合控制台，不承载支付）。
2. 视觉层符合 28（玻璃态用于 Experience 区，资金动作不在主面板直接执行）。
3. 交互与可访问性基础符合 13-1/37（tablist、alert、status、focus-visible、dialog）。
4. i18n 键在 Market 范围完成双语覆盖（`zh.ts`/`en.ts` 检查：缺失 0）。
5. 测试覆盖主要集中在 `CustomItineraryModal` 计算与表单，`ViewSwitcher/StickyFilterBar/Drawer` 仍需补集成测试。

---

## 2. 路由层映射（app）

| 文件 | 代码职责 | 对应规范 | 状态 |
|---|---|---|---|
| `frontend/app/market/page.tsx` | Market 主入口，组装 Hero/FilterBar/Content/Footer + 抽屉/弹窗 | 29, 28, 33 | 已对齐 |
| `frontend/app/market/layout.tsx` | 路由级 Suspense 壳与 metadata | 33, 37 | 已对齐 |
| `frontend/app/market/loading.tsx` | loading 态 + `LoadingText` + ARIA status | 30, 37 | 已对齐 |
| `frontend/app/market/error.tsx` | 路由错误边界，i18n + retry + back home | 30, 37, 39 | 已对齐 |
| `frontend/app/discover/page.tsx` | `router.replace("/market")` 统一入口 | 29 | 已对齐 |

---

## 3. 组件层映射（components/market）

| 文件 | 代码职责 | 对应规范 | 状态 |
|---|---|---|---|
| `frontend/components/market/useMarketPage.ts` | 核心状态机：URL query 同步、筛选、排序、收藏、API 拉取、接单回调 | 29, 33, 53 | 已对齐 |
| `frontend/components/market/MarketContent.tsx` | 双栏内容区，ViewSwitcher、排序、错误提示、骨架、空态、卡片列表 | 29, 13-1 | 已对齐 |
| `frontend/components/market/StickyFilterBar.tsx` | 国家/城市/语言/服务筛选，多选交互与 ARIA | 29, 37 | 已对齐 |
| `frontend/components/market/ViewSwitcher.tsx` | Split/Orders/Guides 视图切换（tablist） | 29, 37 | 已对齐 |
| `frontend/components/market/OrderCard.tsx` | 订单卡（状态、费用摘要、徽章、收藏、查看/抢单） | 29, 28, 34 | 已对齐 |
| `frontend/components/market/GuideCard.tsx` | 向导卡（资质、价格、标签、收藏、查看/预约） | 29, 28, 34 | 已对齐 |
| `frontend/components/market/OrderDetailDrawer.tsx` | 订单详情抽屉、预算拆分、统一行程表、接单确认 | 29, 52, 53 | 已对齐 |
| `frontend/components/market/GuideDetailDrawer.tsx` | 向导详情抽屉、能力与报价、预约入口 | 29 | 已对齐 |
| `frontend/components/market/BookGuideModal.tsx` | 预约向导弹窗（当前占位流程，导向订单） | 29 | 已对齐（占位逻辑） |
| `frontend/components/market/EmptyState.tsx` | no-orders/no-guides/no-matches 三态空页面 | 29, 37 | 已对齐 |
| `frontend/components/market/CustomItineraryModal/*` | 自定义行程创建、报价计算、表单提交流程 | 29, 42, 49A | 已对齐 |

---

## 4. 关键约束核对

### 4.1 29 核心约束

1. 单一入口：`/discover` 重定向 `/market`，无双入口分流。
2. 页面定位：Market 以撮合为主（订单/向导发现、详情、预约、接单），不直接承担支付执行。
3. 结构完整：Hero + StickyFilterBar + ViewSwitcher + 双栏内容 + Drawer/Modal + 空态/骨架。

### 4.2 28 风格约束

1. 玻璃态用于 Experience 区（市场主场景、筛选条、卡片容器）。
2. Web3 露出克制（Escrow badge/token pill），不在此处叠加重链上交互。

### 4.3 37 i18n/a11y 约束

1. Market 范围翻译键覆盖：
   - 抽取范围：`app/market`, `components/market`, `app/discover`
   - 结果：`total_keys=236`, `missing_zh=0`, `missing_en=0`
2. a11y 证据：
   - `ViewSwitcher` 使用 `role="tablist"/role="tab"/aria-selected`
   - `MarketContent` 错误提示 `role="alert"`
   - loading 和 toast 具备 `role="status"/aria-live`
   - Drawer/Modal 使用 `role="dialog"` + `aria-modal`

---

## 5. 测试覆盖现状

已存在测试（Market 域）：

1. `frontend/components/market/CustomItineraryModal/useQuoteCalculation.test.ts`
2. `frontend/components/market/CustomItineraryModal/useItineraryForm.test.tsx`

当前缺口（建议补充）：

1. `ViewSwitcher` 键盘与 ARIA 回归测试。
2. `StickyFilterBar` 多选联动与 URL 参数同步测试。
3. `MarketContent` 错误提示重试流、空态切换测试。
4. `OrderDetailDrawer` 与 `GuideDetailDrawer` 的 focus-trap/关闭逻辑测试。

---

## 6. 结论与下一步

Market 路由在规范层已达到高一致性，可作为 `62` 系列的第一个“逐个排查”闭环样板。

下一步建议（继续按 62 顺序执行）：

1. `Escrow (/escrow/[id])` 逐文件映射与 13 宪法冲突复核（资金区风格边界）。
2. `Community (/community)` 逐文件映射与 31 **L1** 壳、**88** 页身真值一致性复核（见 **62-补充-02**）。
3. 在每个路由补充文档后，同步更新 `62` 总表中的审计进度。
