# 参数与门禁表（SSOT — Single Source of Truth）

本文档为 TravelTrust **参数与门禁的单源真相**，与 [08-1-战略与合规风险检查清单](08-1-战略与合规风险检查清单.md)、[08-2-附录-闭合工单表](08-2-附录-闭合工单表.md) 配套。变更须走多签/版本，并更新 **evidence_pointer**；关键 key 变更须同步 [08-4](08-4-对外口径包.md) 对应章节（见 W-PDP-SSOT-CONSISTENCY）。**受众与用法**：产品、风控、架构、法务；定稿与变更评审用。**文档维护责任**：产品/风控/架构；变更须同步 01/03/04 及 08-4 对应章节。文档版本与最后更新见 [00-文档索引](00-文档索引.md)。

**内容与结构**：表头 → 26 key 表（全列）→ 权限层级 → key 与 08-4 章节映射 → 08-4 承诺可测对应 → 证据删除与司法协助 → 变更记录。

### 读前摘要

| 你要找什么 | 单源 |
|------------|------|
| **26 key 全表与四列填满规则** | **26 key** 表及 **审计闭合标准** |
| **key ↔ 08-4 章节映射** | **key 与 08-4 章节映射** 节 |
| **变更与证据** | **变更记录**、**证据删除与司法协助**（随正文） |
| **检查清单主文** | **[08-1](08-1-战略与合规风险检查清单.md)** |
| **CI 联动** | **[08-5](08-5-CI与一致性落地说明.md)**、**[08-2](08-2-附录-闭合工单表.md) W-DRIFT-CI** |

---

## ⛔ 审计闭合标准（P0）

**若 08-3 不能做到「每个关键 key 都有：谁能改、怎么改、怎么审计、证据指针是什么」，则 W-PARAM-ABUSE / W-DRIFT-CI 等机制将变成制度存在但无法执行。** 每个 key 须填满 **who_can_change / change_process / audit_requirement / evidence_pointer**（须注明 Runbook §N 或具体表名，不得裸写「见 Runbook」），否则不得视为 SSOT 闭合。

---

**表头（必须固定）**：

| param_key | description | scope | value | units | chain_or_backend | who_can_change | change_process | audit_requirement | evidence_pointer |
|-----------|--------------|--------|-------|-------|------------------|----------------|----------------|-------------------|------------------|
| （唯一键） | 参数含义 | PrePaid/PostPaid/DisputeOnly/Global | 当前值 | 单位 | Onchain/Backend/Both | Multisig/Timelock+Multisig/Governance/BackendConfig | 步骤+回滚 | 双人审批/演练 | 配置快照/txHash/版本号 |

---

## 建议优先入 SSOT 的 26 个 key（每 key 须填满四列：who_can_change / change_process / audit_requirement / evidence_pointer）

| param_key | description | scope | value | units | chain_or_backend | who_can_change | change_process | audit_requirement | evidence_pointer |
|-----------|--------------|--------|-------|-------|------------------|----------------|----------------|-------------------|------------------|
| finalityN | 链确认块数 | Global | 12 | block | Onchain | Multisig | 提案→Timelock→多签执行；回滚见 Runbook §10 | 双人审批；finalityN 变更后必须按 Runbook §11 执行“重放/校验”，checkpoint 必含 (blockNumber, logIndex) | Runbook §11（finalityN 变更重放/校验 + checkpoint/logIndex 要求）；artifact: `data/indexer_state.json`（或 `INDEXER_STATE_PATH`）包含 `checkpoint.log_index` 与 `last_seen_finality_n`；log: `data/indexer_audit.jsonl`（含 `replay_plan_applied`/`event_applied`/`event_skipped_duplicate`/`event_rejected_before_checkpoint` + `finality_n_used`）；replay 输出：日志行 `indexer_replay_plan: {...}`；离线输入验收：`traveltrust-api --indexer-validate-jsonl <events.jsonl>`（dup 统计）；回放证据：`traveltrust-api --indexer-ingest-jsonl <events.jsonl>` 追加 `data/indexer_events.jsonl`（raw event + dedupe_key）与 `data/indexer_seen_keys.json`（或 `INDEXER_SEEN_KEYS_PATH`）；门禁：非重复事件必须严格晚于 checkpoint（否则 fail-closed）；API 侧取证：`GET /meta` + `startup_snapshot` |
| disputeWindowMin | 争议窗口最小值 | DisputeOnly | 86400 | 秒 | Onchain | Multisig | 同上 | 双人审批 | 08-3 变更记录 |
| autoCompleteDelay | 自动完成延迟 | PostPaid | 604800 | 秒 | Onchain | Multisig | 同上 | 双人审批 | 08-3 变更记录 |
| arbFeeBase | 仲裁费基数 | DisputeOnly | 50 | USDC | Both | Multisig | 同上；同步 08-4 第 3/4 章 | 双人审批 | 08-3 变更记录 |
| arbFeeMultiplier | 重复争议倍数 | DisputeOnly | 1.5 | 倍 | Both | Multisig | 同上 | 双人审批 | 08-3 变更记录 |
| arbFeeCap | 仲裁费上限 | DisputeOnly | 500 | USDC | Both | Multisig | 同上；同步 08-4 第 3/4 章 | 双人审批 | 08-3 变更记录 |
| maxOrderAmount | 单笔订单金额上限 | PrePaid | 10000 | USDC | Onchain | Multisig | 同上 | 双人审批 | 08-3 变更记录 |
| maxDailySettlementAmount | 单日结算金额上限 | Global | 100000 | USDC | Backend | Multisig | Runbook §10 变更流程 | 双人审批 | 08-3 变更记录 / Runbook §10 |
| maxConcurrentOrdersPerGuide | 单向导并发订单上限 | PrePaid | 5 | 笔 | Backend | Multisig | Runbook §10 变更流程 | 双人审批 | 08-3 变更记录 |
| stakeTierThresholds | 质押档位阈值 | PrePaid | 500,2000,10000 | USDC | Onchain | Multisig | 同上 | 双人审批 | 08-3 变更记录 |
| stakeToOrderCapMap | 档位→接单上限映射 | PrePaid | 1,3,10 | 笔 | Onchain | Multisig | 同上 | 双人审批 | 08-3 变更记录 |
| pauseCooldown | Pause 冷却期 | Global | 86400 | 秒 | Backend | Multisig | Runbook §1、§6；同步 08-4 第 6 章 | 双人审批+演练 | 08-3 变更记录；Runbook §6 Pause 演练编号（含日期/操作者/触发原因） |
| pauseAllowlist | Pause 白名单（地址/角色） | Global | Runbook §1 表 RUNBOOK_PAUSE_ALLOWLIST | — | Onchain | Multisig | 同上；同步 08-4 第 6 章 | 双人审批 | Runbook §1 表 + 08-3 变更记录 |
| kycAmountThresholds | KYC 触发金额（游客/向导分级） | PrePaid | 1000,500 | USDC | Backend | 风控（法务审批） | Runbook §10 或治理文档 | 双人审批 | 08-3 变更记录 |
| **ofacHitPolicy** | 制裁命中策略（只读冻结/允许退出禁新增/允许存量禁提现） | Global | 只读冻结 | — | Backend | 法务 | 定稿→同步 08-4 第 8 章 | 法务审批 | 08-4 第 8 章定稿；W-A2-OFAC |
| evidenceMaxSize | 证据单文件大小上限 | DisputeOnly | 50 | MB | Backend | Multisig | Runbook §10；同步 08-4 第 3 章 | 双人审批 | 08-3 变更记录 |
| evidenceTypeAllowlist | 证据类型白名单 | DisputeOnly | image/*,application/pdf | MIME | Backend | Multisig | 同上 | 双人审批 | 08-3 变更记录 |
| evidenceRetentionDays | 证据保留天数 | DisputeOnly | 1095 | 天 | Backend | 法务 | 同上；同步 08-4 第 3 章 | 法务审批 | 08-3 变更记录；08-4 第 3 章 |
| timeSourcePolicy | 时间源（block/backend/hybrid） | Global | hybrid | — | Both | Multisig | Runbook §10 | 双人审批 | 08-3 变更记录；C2 SSOT |
| **serviceStartTimeSource** | 服务开始时间最终判定源（裁决用；防链时间与现实/时区/篡改冲突） | DisputeOnly | backend_evidence | — | Backend | Multisig | 同上；裁决以证据时间戳+服务器时间为准；链上 disputeWindow 仍以 block 为界 | 双人审批 | 08-3 变更记录；08-4 第 3 章；W-D2-EVID |
| fxDisplayPolicy | 汇率展示（USDC=1/法币/不展示） | PrePaid | chain_only | — | Backend | 产品（双人审批） | Runbook 变更流程；同步 08-4 第 5 章 | 双人审批 | 08-3 变更记录；上线就炸 #8 |
| chargebackPolicy | 法币拒付策略（若接法币） | PostPaid | 08-1 A6 段或 Runbook §10（具体策略写 Runbook/治理文档） | — | Backend | 法务 | Runbook §10 变更流程；同步 08-4 | 法务审批 | 08-1 A6、Runbook §10、08-3 变更记录 |
| **paramChangeMaxPer30d** | 30 天内参数修改次数上限（超则触发冻结期） | Global | 3 | 次 | Backend | 产品（风控审批） | 同上；同步 08-4 公告 | 双人审批 | 08-3 变更记录；W-PARAM-ABUSE |
| **paramFreezeDays** | 参数冻结期时长（触发后 N 天内仅紧急多签可改） | Global | 7 | 天 | Backend | 产品（风控审批） | 同上 | 双人审批 | 08-3 变更记录；W-PARAM-ABUSE |
| **freezeDisputePolicy** | 稳定币冻结期间：争议是否暂停、SLA 是否暂停、默认裁决是否自动触发（三问合一的策略 key） | Global | 争议不暂停；SLA 暂停计算；默认裁决不自动触发 | — | Backend | 风控（法务审批） | 同上；同步 08-4 第 5 章 | 法务审批 | 08-3 变更记录；08-4 第 5 章；W-FREEZE-DISPUTE |
| **minArbitratorCount** | 仲裁员最低人数阈值（低于则暂停新争议或降级） | DisputeOnly | 3 | 人 | Backend | 运营（法务审批） | Runbook §8；同步 08-4 第 7 章 | 双人审批 | Runbook §8 + 08-3 变更记录 |

### 向导订单质押与 [81-经济模型-向导质押与订单押金](81-经济模型-向导质押与订单押金.md) 对齐（设计 SSOT）

**目的**：将 **81 §4～§4.3、§7** 的推荐模型与 **合约 / 后端** 对齐；上表 **26 key** 中的 **stakeTierThresholds**、**stakeToOrderCapMap** 继续代表**档位与接单规模**，本节补充**订单质押比例与 Cap** 的设计三角，避免「文档写 10～15%、实现仍按未文档化默认」。

| 项 | MVP 定稿（与 81 / 03 一致） |
|----|------------------------------|
| **向导订单质押比例** | **默认区间 10%～15%**（普通/高风险档见 **81 §4.2**）；**单笔不得超过 20%**。**链上 bps** 以 `Staking`/合约部署值为终局；变更后须登记 **08-3 变更记录** 并核对与 **81** 无冲突。 |
| **订单质押 Cap** | **81** 示例：`min(订单金额×比例, 2000 USDC)`；正式 **Cap** 以合约为准。若未来将 Cap/bps 提升为可调治理参数，可新增 **param_key**（如 `guideOrderStakeBpsMax`、`guideOrderStakeCapUsdc`）并入上表 26 行并走 **W-PDP-SSOT-CONSISTENCY**。 |
| **身份押金与档位** | **81** 文中 **1000U** 等为**解释性示例**；上表 **stakeTierThresholds / stakeToOrderCapMap** 为**运营与路线图 SSOT**。当前 **`contracts/src/Staking.sol`** 仅强制 **`MIN_STAKE`**（见下行），**未**链上实现多档 tier — 接单档位逻辑若在 **后端**，须与上表 key 及 **04** 一致。 |
| **游客订单质押** | **不要求**（与 [03-业务流程与风控](03-业务流程与风控.md) **§一 1.2.1** 一致）；**不**在本表新增「游客押」key，直至 01/03/04/合约另行定稿。 |
| **当前仓库 `Staking.sol`（代码事实）** | 仅 **`MIN_STAKE = 1000e6`**（假设 token 为 **6 位小数 USDC**）作为**累计** `stakeOf` 下限；**无**按订单 **bps** / **Cap**。**`slash(user, amount)`** 仅 **`slasher`**（构造参数 `_slasher`，**immutable**）可调用；部署脚本默认 `slasher = deployer`，**测试网/主网宜改为多签或执行器**（见 **14**、**01/02**）。**81 §4.2 订单质押比例** 仍属设计先行；链上按单锁仓须另扩合约 + **14** + 回填本节与 **81**。 |

*（指本节 **26 key 主表**）上表 **value** 为写死值（定稿时由主责签字确认）；变更须走变更记录并同步 08-4。**who_can_change** 仅允许单一主责（括号内为审批/配合），不得写「A+B」联合 Owner。value 列不得为未具名「待填」；若引用 Runbook 须注明具体章节/表名（如 Runbook §1 表）。*

**53 评分材料（E3/N5）**：[53-阶段开发技术文档](53-阶段开发技术文档.md) 评分页上传照片/视频与 evidence **共用** **evidenceMaxSize**、**evidenceTypeAllowlist**（实现时可扩展 video/*）；53-S8 验收含上传限制与审核入口，见 53 附录、04。**53 评分材料保留期**与 **evidenceRetentionDays** 一致；**订单消息（聊天）保留期**与 evidence 或本表约定一致（可单独 key 或与 evidenceRetentionDays 同值），见 53 §3.5.2（R2）。

**定稿检查勾选**（全部满足后方可视为 SSOT 闭合）：□ 上表每行 value 已填实（无数值/枚举的须写 Runbook §N 或表名等可落点）□ 每行 evidence_pointer 已填 □ who_can_change 无「—」□ 08-4 对应承诺句与本表 key 一致 □ 变更记录表已启用且关键 key 变更已登记。

**SSOT 与链/DB 冲突时的优先级**（写死）：**链事件 > SSOT（08-3）> DB**。若 SSOT 与链状态冲突，**以链为准**；若 DB 与链/SSOT 冲突，以链与 SSOT 为准并修正 DB。运行冲突时不得现场裁量，须按此优先级执行；**冲突时自动停机策略**（链参数与 08-3 文档冲突且无法自动修正时拒绝执行或暂停相关结算）见 Runbook §11。写死于 Runbook §11。

**权限层级（紧急 vs 常规）**（无「终极钥匙」、可审计）：
- **常规多签**：改 08-3 参数须 Timelock + 双人审批；受 paramFreezeDays、paramChangeMaxPer30d 约束。
- **紧急多签**：仅限 Runbook §7 列明的场景（Pause、OFAC/监管执行、安全事件）；可**豁免** paramFreezeDays 与 pauseCooldown，**不可**改 ofacHitPolicy 等法务口径 key；每次紧急动作须事后留痕（evidence 或 08-3 变更记录）。**不存在**「超级多签」或单把「终极钥匙」— 紧急多签仍为多签门限，且权限范围写死于 Runbook。
- **paramFreezeDays 防绕开 / 全局变更指数**（写死）：paramChangeMaxPer30d 按**关键 key 变更次数**计；**不得**以「不同 key 轮流改」或**不同参数交替改动**绕过（如改 disputeWindow、改 stakeTier、改 arbFee 各一次—单项未超限但整体已重构系统，合计仍计次数）。**全局变更指数（可执行口径）**：**30 天内**以「08-3 变更记录表」中**日期**落在同一滚动 30 日窗口内的**记录条数**计；每条变更记录计 **1**（同一批次变更多 key 仍计 1 条）；若某条记录涉及多个 param_key，**仍计 1 条**，不得拆成多条规避。**指数 ≤ paramChangeMaxPer30d（3）** 方可执行下一次非紧急参数变更；超限则触发 paramFreezeDays 冻结期。执行处：变更前由产品/风控在 Runbook §10 或本表变更记录中核对「过去 30 日记录条数」；无此口径则频率限制可被技术性规避，企业级必问。**参数变更触发冻结/暂停治理**（写死，可加强）：参数修改触发冻结期（paramFreezeDays 已有）；**连续修改触发暂停治理权**（如超 paramChangeMaxPer30d 后 N 天内禁止非紧急参数变更）由风控/产品定稿写死 Runbook §10 或本节，防参数滥用。**向导 unstake 冷却期**（若存在）：若设计**向导退出质押冷却期**（如 7～14 天）以减缓**向导质押挤兑**瞬间倾泻，须写死天数并落 Runbook；由产品/风控定稿。

---

## 关键 key 与 08-4 章节映射（W-PDP-SSOT-CONSISTENCY）

08-3 中与对外承诺强相关的 key 变更须**同步 08-4 对应章节或版本号**；CI 未同步则阻断合并。**本映射表为定稿写死**；CI 以「本表 param_key 列表 + 08-4 文末文档版本（CI 校验用）」作为可机读校验依据：PR 改动上表任一 key 且 08-4 版本号未在本 PR 中变更 → **阻断合并**。**CI 校验 key 列表**：机读实现由 **scripts/check-08-consistency.sh** **从本映射表自动提取 param_key**；因此本表增删 key 时无需再同步维护脚本内的硬编码列表（避免双维护漂移），但**不得破坏本段标题与表格格式**，否则 CI 将无法提取并会阻断合并。

| param_key | 08-4 对应章节 |
|-----------|----------------|
| ofacHitPolicy | 第 8 章 合规红线与政府请求 |
| pauseCooldown、pauseAllowlist | 第 6 章 Pause |
| evidenceRetentionDays、evidenceMaxSize、evidenceTypeAllowlist | 第 3 章 仲裁、隐私政策 |
| arbFeeBase、arbFeeCap | 第 3、4 章 仲裁与裁决统计 |
| fxDisplayPolicy | 第 5 章 稳定币/脱锚（若涉及展示） |
| paramChangeMaxPer30d、paramFreezeDays | 第 5 章 / 08-4 参数变更公告；W-PARAM-ABUSE |
| freezeDisputePolicy | 第 5 章 稳定币冻结期间争议三问；W-FREEZE-DISPUTE |
| serviceStartTimeSource | 第 3 章 仲裁（服务开始时间判定） |
| minArbitratorCount | 第 7 章 仲裁员最低容量；Runbook §8 |
| chargebackPolicy | 08-4 第 5 章或治理文档（若对外有法币拒付承诺）；08-1 A6 |
| （其余 key 若涉及对外承诺） | 在 08-4 中注明对应段落或「仅内部/Runbook」 |

**扩展 key 建议**（定稿时可视需要入表）：默认裁决比例阈值、某类服务争议率异常阈值、裁决偏差指数阈值（W-DEFAULT-STAKING）；10× 争议容量相关（W-D3-CAPACITY 反推）。

---

## 08-4 承诺与 08-3 key 可测对应（P1：对外承诺须有 SSOT 可测）

审计视角：08-4 每一句对外承诺，须能在 08-3 找到对应 key，并给出「当前 value + evidence_pointer」。下表为最小对应，定稿时须逐条可测。

| 08-4 承诺（摘要） | 08-3 param_key | 可测要求 |
|------------------|----------------|----------|
| 第 6 章 Pause：阈值表条件、多签执行、冷却期限制 | pauseCooldown、pauseAllowlist | value 与 evidence_pointer 已填；变更同步 08-4 第 6 章 |
| 第 8 章 OFAC 三选一、申诉时限（如 30 天） | ofacHitPolicy | value=三选一之一；申诉时限见 08-4 第 8 章（与 08-3 一致） |
| 第 3/4 章 仲裁费、裁决统计 | arbFeeBase、arbFeeCap | value 已填；披露口径与 08-4 一致 |
| 第 5 章 稳定币冻结三选一、脱锚、冻结期间争议 | freezeDisputePolicy、08-4 策略句 | value 已填；08-4 选一写死，与 08-3 freezeDisputePolicy 一致 |
| 第 3 章 证据保留 | evidenceRetentionDays、evidenceMaxSize、evidenceTypeAllowlist | value 已填；与 08-4 隐私/仲裁一致 |
| 第 3 章 服务开始时间判定、删除后司法协助 | serviceStartTimeSource；证据删除策略见 08-4 第 3 章 | 链时间 vs 现实时间冲突防穿透；删除后保留 hash+receipt |

**证据删除与司法协助**（写死）：evidenceRetentionDays 届满后可删原文件；**hash + receipt 永久保留**（可证明「曾存在、何时、何人」）。删除后司法协助：平台可向司法机构提供 hash、receipt、上传者与时间等元数据；不保留可逆原文件。外部备份策略（若有）见 Runbook §11。

**证据存储物理级冗余**（写死，企业级）：① **多地域/跨云**：证据存储须多地域或跨云备份（具体见 Runbook §11）；单点故障不得导致证据不可用。② **S3/存储故障达 7 天**：若主存储不可用超过 7 天，**暂停新仲裁受理**；存量争议**默认按「证据缺失」适用默认裁决**（退款优先等）；恢复后按既有规则继续。③ cold archive 策略（若有）见 Runbook §11。无物理冗余要求 = 仲裁存在物理单点。

**脱锚时争议金额计价**（写死）：稳定币大幅脱锚时，争议金额按**裁决时链上价格**计算；与 08-4 第 5 章一致。

**reorg 超过 finalityN（超 finality 场景）**（写死）：若链重组深度 **> finalityN**（如 > 12 block）：**自动暂停结算**；须**人工确认 finality** 后恢复；**已基于旧链状态执行的裁决/DB 须按 Runbook §11 执行回滚或重放**（不得保留与最终链不一致的终态）。流程见 Runbook §11。未写死则 reorg 攻击可穿透。

**服务开始时间防篡改**（写死）：serviceStartTimeSource=backend_evidence 时，**backend 时间源须可验证**（如服务器时间签名、NTP 可信源或时间戳公证）；否则仲裁时间可被攻击。具体实现由运维/架构定稿并留 08-3 变更记录或 Runbook §11。

**质押与规模动态**（写死）：当前 **stakeTierThresholds / stakeToOrderCapMap 为静态档位**；**GMV 或规模上升 10×/50× 时须触发重估**（是否自动提高质押比例由治理/路线图决定，当前无自动调整函数）；若上线后规模扩大须评估 W-DEFAULT-STAKING 压力测试与动态调整，否则存在规模暴露风险。**质押覆盖率公式**（写死，企业级必答）：须定义 **覆盖率 = 质押总额 / 在途订单风险暴露**（或等效公式），并写死 **≥ X%** 的阈值；无公式则无法证明不会穿透质押池，审计必问。**默认裁决比例失衡是否会击穿质押池**（写死）：若默认裁决长期偏向游客或偏向向导，**是否击穿质押结构**须有**覆盖率公式、风险暴露曲线、最大坏账模型**写死；否则为潜在资损模型缺口。具体公式与 X 由风控/产品定稿写死 Runbook 定稿表或本节。**默认裁决击穿模拟（4️⃣）**（写死）：假设**80% 争议证据不足、默认裁决规则为退款优先**—**向导经济模型是否崩溃**须做**默认裁决击穿模拟**；**质押覆盖率压力测试**须写死（含灰色证据比例升高场景）。若未做，系统在灰色证据比例高时会经济性坍塌；由风控定稿写死 Runbook 定稿表或本节。**默认裁决连续偏向**（写死）：若**连续 N 单全部退款或全部放款**（如 100 单），是否击穿质押池、向导退出、游客薅羊毛、资金池枯竭须有**连续偏差阈值**触发**自动风控冻结**（如暂停该类别新单、强制复核、或提高质押/限单）；由风控定稿写死 Runbook §11 或本节，否则极端连续偏向无制衡。**质押覆盖率 worst-case 模型（4️⃣）**（写死）：须有**最坏情形模型**—**5 个向导同时违约、单向导最大并发、单向导最大金额、同地区集中违约**下，**质押覆盖率是否可能 < 最大赔付**；若存在击穿概率须写死：**上限**（如单笔/单向导赔付上限）、**动态风控**（如超阈值自动提高质押要求）、**自动暂停**（如覆盖率低于 X% 暂停新单）。无 worst-case 模型=经济安全未闭合；由风控定稿写死 Runbook 定稿表或本节。**争议量 10× 触发阈值（7️⃣）**（写死）：当争议量达 10×（如订单 10,000→争议 800 级）时，**SLA 自动延长/自动暂停新争议/动态提高 arbFee** 的**触发阈值**（如待裁件数 > N 或争议率 > X%）须写死；由风控/运营定稿 Runbook §11 或本节，否则属规模错觉。

**仲裁费博弈防护**（写死）：**低成本骚扰**：arbFeeBase/arbFeeCap 与 arbFeeMultiplier 已限制单次成本；**高额压制小额**：小额争议可设最低可裁门槛或费率曲线（见 Runbook §11 或 03 业务流程）。仲裁费曲线**当前不随争议率自动变化**；若发现博弈攻击可经治理调整参数。**仲裁费用途**（防逆向激励）：仲裁费**不得进入平台营收或与争议数量挂钩**；须仅用于运营/合规成本或进入公共池/治理金库（具体写死 08-4 第 3 章与治理文档）。博弈分析见风控文档。

**监管 KYC 全量升级路径**（写死）：若监管要求**全量实名/KYC**：① **仅改参数即可**时（如将 kycAmountThresholds 设为 0 或全量触发），走 08-3 变更流程 + 08-4 同步；② **需改架构**时（如新增 KYC 管线、数据留存范围），须走产品/法务评估并更新 08-3、08-4、Runbook。企业级须有「监管升级路径」文档或 Runbook §11。

**时间双轨冲突**（写死）：disputeWindow 以**链时间（block）**为界，裁决所采服务开始时间以 **serviceStartTimeSource（backend_evidence）** 为准；冲突时**窗口开闭以链为准、裁决采信以 08-3 为准**，见 08-4 第 3 章与 Runbook §11。

**代币标准/合约升级风险**（写死）：若**结算代币（如 USDC）升级合约或黑名单逻辑变更**，须**重新评估过门**（Gate-2/Gate-4 等）；是否触发重新过门由风控/法务在 Runbook 或 08-2 中写死。未写死则存在链上升级穿透风险。

**异常争议率与关联账户防护**（写死）：**异常争议率**（如某类服务/某地区争议率显著偏离均值）须触发告警或**自动冻结**该类别新单/复核（阈值与动作见 Runbook §11 或 03 风控）；**关联账户检测**（如多账号协作制造争议套利）须有检测与处置流程，否则存在「沉默攻击」风险。企业级须在 Runbook 或 03 中写死。

**证据存储与数据主权**（写死）：evidenceRetentionDays 等见上表；若证据**跨境传输**或涉及 **EU 用户**，须满足首发司法域与业务地数据合规（如 EU GDPR 传输限制、本地化）；跨境路径与 EU 合规口径须在隐私政策或 Runbook 中写死，见 08-4 第 3 章。

---

## 附录 A：运维与实现映射（非 26 key 数值；对齐代码与 Runbook）

**说明**：下列为**部署与观测用配置**，不替代上表 26 个 param_key 的 SSOT 数值；变更仍须 Runbook §10 / 变更记录留痕。与 [14-合约-API-ABI-前后端对齐](14-合约-API-ABI-前后端对齐.md) §环境变量与合约地址、`GET /meta` 一致。

| 配置项 | 环境变量 / 暴露面 | 代码或文档落点 | evidence_pointer / 备注 |
|--------|-------------------|----------------|---------------------------|
| 链 ID（字符串） | `CHAIN_ID`；`GET /meta` → `chain.chain_id` | `crates/api` intents、`routes/health_meta.rs` | 与前端 `NEXT_PUBLIC_CHAIN_ID` 对齐 |
| 链上合约地址（后端已加载 ChainConfig 时） | `ESCROW_FACTORY_ADDRESS`、`STAKING_ADDRESS`、`REGISTRY_ADDRESS` | `GET /meta` → `chain.contracts.*`；`chain::ChainConfig::from_env` | 非链下模式为 `null`；与前端 `NEXT_PUBLIC_*` 部署须一致 |
| 全局限流 / 关键写 / 证据与评价限流 | `API_RATE_LIMIT_PER_MINUTE`、`CRITICAL_WRITE_RATE_LIMIT_PER_MINUTE`、`EVIDENCE_MAX_REQUESTS_PER_MINUTE`、`REVIEW_MAX_REQUESTS_PER_MINUTE`、`REVIEW_LOW_SCORE_COMMENT_MIN_CHARS` | `GET /meta` → `rate_limits`；`middleware::meta_rate_limits_snapshot`；`chain_off/evidence.rs`、`reviews.rs` | 与 `middleware` 层同源解析 |
| 前端 Registry 只读 | `NEXT_PUBLIC_REGISTRY_ADDRESS` | `frontend/lib/registryEnv.ts`、`StakingRegistryPanel` | ABI：`contracts/abi/Registry.json` ≡ `frontend/dapp/abis/Registry.json` |
| 前端 Staking 读/写 | `NEXT_PUBLIC_STAKING_ADDRESS` | `frontend/lib/stakingEnv.ts`、`StakingContractPanel`、`StakingStakePanel`；TS ABI `frontend/lib/stakingAbi.ts` 须与 JSON 语义一致 | ABI：`contracts/abi/Staking.json` ≡ `frontend/dapp/abis/Staking.json`；门禁 `scripts/check-55-s13.sh` |
| Admin 可观测快照 | （会话）`GET /api/v1/admin/observability/overview` | `routes/admin.rs`；`frontend/app/admin/observability` | 含 `chain_id` 与 `rate_limits` |
| 双写失败策略档位（50-O-R1） | **`DUAL_WRITE_FAILURE_POLICY`**；**`GET /meta` → `dual_write`**（`failure_policy`、`strict_db_write_any`）；**`startup_snapshot`** 含 **`DUAL_WRITE_FAILURE_POLICY=`** / **`STRICT_DB_WRITE_ANY=`** | `crates/api/src/state.rs`、`routes/health_meta.rs`、`startup/mod.rs`；**[Runbook §9](../../ops/RUNBOOK.md)** | **②** 的实际 503 仍由各 **`TRAVELTRUST_STRICT_*_DB_WRITE=1`** 分路径控制；与本节末「双写失败策略」脚注一致 |
| DID 排行榜向导信誉轨入榜下限 | **`DID_RANK_GUIDE_MIN_COMPLETED_FOR_REPUTATION_SORTS`**（整数 **1～50**，默认 **3**；未设或非法则回退默认） | **`routes/did_rank.rs`** **`guide_did_rank_runtime_from_env`**；**`db/users_sessions`** **`list_guides_did_rank_*`**；**[04-附录-did-rank](04-附录-did-rank对接说明.md)** §2 **`rank_basis`** **`…_min_completed_ge_{N}`** | **`check-55` / `smoke`** 默认无 env，仍断言 **`…_ge_3`**；改阈须同步机读断言或专环境 |
| DID 排行榜向导加权 **`w_activity` / `w_reputation`** | **`DID_RANK_GUIDE_WEIGHTED_W_ACTIVITY`**、**`DID_RANK_GUIDE_WEIGHTED_W_REPUTATION`**（**须成对**设置；各自 **0～1**，运行时按和归一；仅设一侧则忽略并回退 **0.6/0.4**） | 同上；**`rank_basis`** 默认 **`w60`/`w40`**；非默认权重为 **`wNNpct`** 机器键 | 与 §3.1 **`w_*` 08-3 化** 部分闭合；**160** 联动仍 **Target** |

---

## 变更记录

| 日期 | param_key | 变更说明 | 批准/证据 |
|------|-----------|----------|-----------|
| 2025-02-20 | — | 初版定稿（20 key 写死值入表） | 本文 20 key 表 |
| 2025-02-20 | paramChangeMaxPer30d、paramFreezeDays、freezeDisputePolicy | 扩展 key 入表（W-PARAM-ABUSE、W-FREEZE-DISPUTE 可测）；23 key | 本文映射表 |
| 2025-02-20 | timeSourcePolicy、serviceStartTimeSource | 链时间 vs 现实时间判定源写死；24 key | 08-4 第 3 章、权限层级节 |
| 2025-02-20 | minArbitratorCount | 仲裁员最低容量入主表；Runbook §8；26 key（主表 finalityN～minArbitratorCount 共 26 行） | 08-4 第 7 章、Runbook §8 |
| 2025-02-25 | （定稿说明） | **执行器策略**：单笔/单日上限以 08-3 maxOrderAmount、maxDailySettlementAmount 为准；双人审批、重试次数（如 3）定稿时写死 Runbook §10 或 ChainConfig。**证据限流**：content_hash 大小/类型已落 API（≤128、hex）；QPS/每分钟上限定稿时 08-3 写死或 Runbook §10 即闭合（27-P30、01 §6 DoS P0） | 27-P0至P47 §六 |
| 2025-02-27 | （写死闭合） | **执行器策略定稿**：CHAIN_EXECUTOR_RETRY_COUNT=3；单笔/单日上限以本表 maxOrderAmount、maxDailySettlementAmount 为准；双人审批步骤见 Runbook §7。**证据限流定稿**：evidenceMaxRequestsPerMinute=60（每 order_id 每分钟最多 60 次证据提交；可选由 API 或网关实现）；content_hash 已 ≤128、hex。**对账三段式**：触发条件与单日自动修次数上限见 Runbook §10。**paid_merge_rule**：以最新 (blockNumber, logIndex) 为准，见 04 §四。 | Runbook §7/§10、04 §四、27-P0至P47-多维度深度检查报告 §二 |
| 2026-03-24 | （附录） | **附录 A**：运维与实现映射（`CHAIN_ID`、`chain.contracts`、`rate_limits`、`NEXT_PUBLIC_REGISTRY/STAKING`、admin observability）；与 14、Runbook、代码对齐；**不增删** 26 key 主表行。 | 本文附录 A；`scripts/check-55-s13.sh` |
| 2026-03-26 | （81 对齐节） | **向导订单质押与 81 对齐**：向导侧默认比例 **10%～15%**（≤20%）、Cap 示例与**游客不要求另押** MVP 定稿；见本文 §「向导订单质押与 81…」；与 [03 §一 1.2.1](03-业务流程与风控.md) 互链。**不增删** 26 key 主表行。 | 03、81、本文 |
| 2026-03-26 | （合约核对） | 上节 **「当前仓库 Staking.sol」**：`MIN_STAKE` 仅、无订单 bps/Cap/tier；**stakeTierThresholds** 与链上未一一实现。**`slasher`**：构造注入 immutable，`slash` 仅其可调；**Deploy.s.sol** 默认 deployer（测试网/主网宜多签/执行器）；ABI 与 **14** 已同步。 | `Staking.sol`、`Staking.json`、`Deploy.s.sol`、**14**、本文 |
| 2026-03-28 | （附录） | **附录 A**：增 **`DUAL_WRITE_FAILURE_POLICY`** / **`GET /meta.dual_write`** / **`startup_snapshot`** 与 Runbook §9、**`state.rs`** 映射行；**不增删** 26 key 主表行。 | 本文附录 A；`.env.example`；**W-PDP-SSOT-CONSISTENCY**（08-4 CI 版本行已 bump） |
| 2026-04-03 | （附录） | **附录 A**：**did-rank** **`DID_RANK_GUIDE_MIN_COMPLETED_FOR_REPUTATION_SORTS`**、**`DID_RANK_GUIDE_WEIGHTED_W_*`**（**`routes/did_rank.rs`** env 解析；**不增删** 26 key 主表行）。 | 本文附录 A；**04-附录-did-rank** **`1.16`**；**07** 批 **683**；**08-4** CI 版本行 bump |

**双写失败策略（50-O-R1）**：见 [Runbook §9](../../ops/RUNBOOK.md)；产品/运维定稿后在此变更记录登记所选方案（①仅日志 / ②返回 503 / ③告警不改 HTTP）。

变更须同步 01/03/04；**关键 key 变更须同步 08-4 对应章节或版本号**（见 [08-2](08-2-附录-闭合工单表.md) W-PDP-SSOT-CONSISTENCY）。

*本文与 [08-1-战略与合规风险检查清单](08-1-战略与合规风险检查清单.md)、[08-2-附录-闭合工单表](08-2-附录-闭合工单表.md)、[08-4-对外口径包](08-4-对外口径包.md)、[00-文档索引](00-文档索引.md) 配套。文档版本与最后更新见 [00-文档索引](00-文档索引.md)。*
