# 45 前端企业级多维度检查报告

**文档编号**：45  
**用途**：对前端 UI/UX、模块化、架构与规范做**企业级多维度检查**，识别达标项、缺口与优化建议。  
**受众**：前端负责人、架构、Code Review、产品；与 [43-阶段-前端UI模块化拆解与最佳实践](43-阶段-前端UI模块化拆解与最佳实践.md)、[43-阶段-验收与未完成清单](43-阶段-验收与未完成清单.md)、[13-1-UI产品级SSOT与页面规范](13-1-UI产品级SSOT与页面规范.md)、**[86](86-UI-双系统未来风-风格与动效技术规格.md)**、**[22 §一点五](22-Design-Tokens-旅游Web3融合体系-v1.0.md)**、[28](28-Cinematic-Glassmorphism-Web3融合规范.md)、[29](29-自由市场-撮合控制台规范.md)、[34](34-前端组件与Design-Tokens落地清单.md) 衔接。  
**检查视角**：顶级前端高级工程师，深度多维度（UI/UX 最佳实践、模块化与拆分、架构与依赖、Design Tokens、可访问性、性能与可维护性）。

**文档版本**：v1.0.5（2026-03-30；§一/§七/§8.3 **dynamic** 叙述与 **[88](88-五主路由页身实现快照与UX缺口审计-20260330.md)** 壳层真值一致：`/did-rank`、`/community/*` **壳层不挂载** `Web3SciFiBackground`；与 00 版本表一致）

### 读前摘要

| 你要找什么 | 单源 |
|------------|------|
| **七维度结论一览** | **§一** 表 |
| **UI/UX、模块化、架构、Token、a11y、性能** | **§二～** 随维度章节检索 |
| **规范 SSOT** | **[13](13-协议级UI设计宪法.md)**、**[13-1](13-1-UI产品级SSOT与页面规范.md)**、**[86](86-UI-双系统未来风-风格与动效技术规格.md)**、**[28/29](28-Cinematic-Glassmorphism-Web3融合规范.md)**、**[22/34](22-Design-Tokens-旅游Web3融合体系-v1.0.md)** |
| **执行清单合并入口** | **[46-合并版](46-前端规范与清单-合并版.md)** |

---

## 一、检查总览

| 维度 | 结论 | 说明 |
|------|------|------|
| **UI/UX 最佳实践** | ✅ 大部分达标，⚠️ 部分待加强 | 页面职责与 13-1 对齐、Zone Control 清晰；部分 a11y/焦点管理、异常态可再统一 |
| **模块化与拆分** | ✅ 已达标 | CustomItineraryModal、EscrowDetail、community/did-rank/me/market/landing 已按 43 拆解；单文件 ≤500 |
| **架构与依赖** | ✅ 整体正确，⚠️ 1 处建议 | App→Components→Lib 清晰；缺顶层 `hooks/` 跨域复用；dapp 隔离良好 |
| **Design Tokens 与样式** | ✅ 主流程符合，⚠️ 需审计 | globals.css + tailwind 扩展 **22**（**86→22 §一点五**）/ **28** 叙事纪律；部分 tsx 存在 inline style/hex，需统一走 Token |
| **可访问性（a11y）** | ⚠️ 有基础，待系统化 | 多处 aria/role；SkipLink、焦点与键盘、表单 label 可再统一审计 |
| **性能与按需加载** | ✅ 已做 | 重模块 **dynamic**（CustomItineraryModal、EscrowDetail、DidRank Modal 等）；**`Web3SciFiBackground`** 组件仍在仓库，**仅**在仍挂载处（如 **`MePageBackground`**）按需加载；**`/did-rank`、`/community/*` 壳层**为 **CSS 暖场 + 静态叠层**（**88**） |
| **错误与加载态** | ✅ 有覆盖 | 根与 community、did-rank、escrow、market 有 error.tsx；根、community、did-rank、market、**escrow** 有 loading.tsx |

---

## 二、UI/UX 最佳实践

### 2.1 与 13-1 页面职责与 Zone Control 对齐

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 页面唯一任务与「不做什么」 | ✅ | Landing、**自由市场（`/market`）**、TT社区、EscrowDetail 等与 13-1 表 1 一致（**`/discover`** 为重定向壳） |
| Zone Control（金融区 vs 展示区） | ✅ | 资金/签名区与旅游情绪分离；EscrowDetail 无霓虹/3D |
| 信息层级（状态→金额→finality→操作→风险） | ✅ | EscrowDetail 等按 13-1 表 4 字段级结构实现 |
| 角色×页面权限（RBAC） | ✅ | 顶栏无角色切换，由路由与权限区分；观察者/旅行者/向导与 13-1 表 2 一致 |

### 2.2 交互与反馈

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 加载态 | ✅ | 各域有 Skeleton/loading（MarketSkeleton、FeedSkeleton、DidRankSkeleton、MePageSkeleton）；部分路由有 loading.tsx |
| 错误态 | ✅ | 根与 community、did-rank、escrow、market 有 error.tsx；ApiErrorAlert 等全局提示 |
| 空态 | ✅ | EmptyState、Feed 空态等有统一组件 |
| 交易/签名流程（Tx Modal、finality） | ✅ | EscrowDetail 与 dapp hooks 配合，有 Tx 状态与 Modal |

### 2.3 待加强项（UI/UX）

| 项 | 建议 |
|----|------|
| **异常态清单完整性** | 13-1 要求：未连接钱包、错链、allowance 不足、OFAC、disputeWindow 过期、reorg 等。建议在 EscrowDetail/订单流逐项核对并统一文案与入口。 |
| **表单错误展示** | 关键表单（登录/注册、行程、发帖）错误信息是否始终可见、与 37 i18n 一致，可做一次专项 pass。 |
| **移动端/响应式** | 关键路径（Landing、市场、Escrow）在窄屏下的布局与触摸目标是否达标，建议纳入 33/34 验收。 |

---

## 三、模块化与拆分

### 3.1 43 阶段拆解达标情况

| 模块 | 状态 | 说明 |
|------|------|------|
| CustomItineraryModal | ✅ | index + constants/types/utils + useQuoteCalculation/useItineraryForm + sections（TouristForm、GuideForm、*DayCard）+ DetailOverlay；单文件 ≤500 |
| EscrowDetail | ✅ | index + useEscrowDetail + 多 Block/Modal 组件 + types/utils；已拆为多文件 |
| community 页 | ✅ | useCommunityFeed + CommunityFeedHeader、CommunityFeedFilterBar、CommunityFeedList、Drawer/Modal 等 |
| did-rank 页 | ✅ | DidRankHeader、TravelerRankBlock、GuideRankBlock、Modal、Skeleton 等 |
| Landing（app/page） | ✅ | useLandingPage + LandingHeroForm、ItineraryResultsSection、UnlockModal、LandingFooter |
| me 页 | ✅ | useMePage + MeStatsSection、MeProfileSection、MeQuickLinksSection、MePageFooter、MePageSkeleton 等 |
| market 页 | ✅ | useMarketPage + MarketPageHero、MarketContent、StickyFilterBar、ViewSwitcher、dynamic CustomItineraryModal |
| PublishDrawer | ✅ | 已拆为目录：index、usePublishForm、constants、types |
| lib/cityDetails | ✅ | 已按 type 拆为 attractions、food、hotels、constants、types、index |

### 3.2 单文件行数与职责

- **标准**：43 §3.1 单文件 ≤500 行（建议 ≤400）、单文件单一主职责。
- **结论**：当前扫描无超 500 行单文件；TouristForm/GuideForm 等在 400 左右，符合要求。新增页面/弹窗时继续按 43 §2 分类分区拆解。

### 3.3 仍需关注或可优化

| 项 | 建议 |
|----|------|
| **跨域 hooks 放置** | 43 建议「新增 frontend/hooks/ 放跨域 hook」。当前仅 dapp/hooks；useLandingPage、useCommunityFeed、useMarketPage、useMePage 等仍在各域 components 内。若出现多域复用（如 useAuth、useLocale），建议提升至 `frontend/hooks/` 统一管理。 |
| **重复 UI 模式** | Drawer/Modal 在 community、market 等多处存在；若交互与布局高度一致，可考虑抽成共享 `components/ui/Drawer`、`Modal`（与 34 组件清单对齐）。 |

---

## 四、架构与依赖

### 4.1 分层与依赖方向

| 层级 | 职责 | 当前状态 |
|------|------|----------|
| **App（app/）** | 路由、layout、page；组合 components，最小逻辑 | ✅ 页面多为「取数 + 组合组件」，无超长单页 |
| **Components（components/）** | 按域（community、market、escrow、landing、me、did-rank、trust）+ 根目录共享 | ✅ 域内复杂模块为目录（CustomItineraryModal、EscrowDetail、PublishDrawer） |
| **Hooks** | 状态、副作用、业务逻辑 | ⚠️ 无顶层 hooks/；hooks 分散在 dapp/hooks 与各域组件目录 |
| **Lib（lib/）** | API、类型、常量、纯工具；不依赖 components/app | ✅ api、apiClient、countries、cityDetails、**productCountries**、geoOptions、i18n 等无反向依赖 |

**依赖规则**：App → Components → Lib 成立；禁止 Lib 引用 Components/App。**分层规划**：详见 [46-前端分层规划与依赖规范](46-前端分层规划与依赖规范.md)（App/Components/Hooks/Lib/Dapp 五层、依赖方向、新增代码归属）。跨域 hooks 已落位 `frontend/hooks/`（见 §3.3）；当前未见 components 间循环依赖。

### 4.2 Dapp 与链上隔离

| 检查项 | 状态 |
|--------|------|
| 链上逻辑集中 | ✅ 仅 dapp/abis、dapp/hooks（useEscrowActions） |
| 业务页引用 dapp | ✅ 仅 EscrowDetail/useEscrowDetail 引用 dapp/hooks；app 层通过 components 间接使用 |
| 合约 ABI 单源 | ✅ frontend/dapp/abis 与 contracts/abi 对齐（14 约定） |

### 4.3 数据流与单一数据源

| 检查项 | 状态 |
|--------|------|
| 定价 | ✅ 44 阶段：getPricingForCountry(country)，仅 lib/countries |
| 国家/城市选项 | ✅ **productCountries**（十国 ISO/中文名 SSOT）派生 **geoOptions**（COUNTRY_OPTIONS、CITIES_BY_COUNTRY）；与 countries 一致性有单测 |
| 文案 | ✅ LocaleProvider + t(key)；locales 统一维护 |

---

## 五、Design Tokens 与样式

### 5.1 已落地

- **globals.css**：--travel-*、--trust-*、--bg-*、--radius-*、--shadow-*、--font-sans；.motion-main / .motion-sub、.btn-console（25 §6.1/6.2）。
- **tailwind.config.ts**：colors（bg、travel、trust、success/warning/danger/info、ink）、borderRadius、boxShadow、fontFamily、fontSize（h1–h4、body、small、meta）与 **22**/**86** 映射及 **28** 叙事约束对应。
- **组件**：主流程使用 Tailwind Token 类名（bg-bg-main、text-ink-900、rounded-radius-md、shadow-soft 等）。

### 5.2 待审计

- **Inline style / 裸色**：部分 tsx 存在 `style={{ }}` 或 hex/rgb（如背景图 overlay、主题色）。**已处理**：TrustBadgesRow 的 animationDelay 已改为 Tailwind delay-100/delay-150/delay-200/delay-300；背景图、themeColor、Three.js 色值等动态或规范必需保留 inline。其余可替换项可逐文件改为 Token（34 §2.1）。
- **响应式与间距**：关键页面是否统一使用 theme 的 spacing/breakpoints，可做一次 34 对照。

---

## 六、可访问性（a11y）

### 6.1 已存在

- **SkipLink**：ClientSkipLink 用于跳过导航到主内容（#main-content）。
- **aria / role**：多处组件有 aria-label、aria-describedby、aria-live、role（如 Feed、Modal、Drawer、表单控件）。
- **语义与结构**：main、header、区域划分清晰；部分列表用 list/group 角色。

### 6.2 建议系统化

| 项 | 建议 |
|----|------|
| **焦点管理** | Modal/Drawer 打开时焦点 trap、关闭时回退到触发元素；键盘 Esc 关闭。建议在共享 Modal/Drawer 或 34 组件中统一实现。 |
| **表单 label 与错误** | 所有必填项是否关联 label、错误信息是否通过 aria-describedby/aria-invalid 暴露给读屏，可做 37 专项 pass。 |
| **焦点可见** | focus-visible:ring 等是否全站一致（globals 已有部分），避免仅 outline:0 无替代。 |
| **a11y 清单** | 建议在 37 或 34 下维护「关键页 a11y 检查清单」（Landing、市场、Escrow、社区、DID 排行榜），发版前勾选。 |

---

## 七、性能与可维护性

### 7.1 按需加载与代码分割

- **dynamic 使用**：CustomItineraryModal、EscrowDetail、DidRank 弹窗类重模块已用 `next/dynamic`。**`Web3SciFiBackground`**：**不再**由 **`community/layout`**、**`did-rank/page` 壳层**挂载（见 **88**）；若 **`MePageBackground`** 等子树引用则仍按需 **dynamic**。✅
- **建议**：其他重模块（如 PublishDrawer、PostDetailDrawer、DidRankGuideModal 等）若首屏不必须，可评估 dynamic 或 lazy，平衡首屏与交互延迟。

### 7.2 状态与重渲染

- 各页主要状态在 custom hook 内（useCommunityFeed、useMarketPage、useItineraryForm 等），粒度合理；未见单页单一大 state 导致整页重渲染。✅

### 7.3 可测试性

- **已单测**：lib/api、lib/apiClient、lib/countries（getPricingForCountry + 一致性）、lib/didRankUtils；CustomItineraryModal 的 useQuoteCalculation、useItineraryForm；dapp/hooks useEscrowActions；部分组件（StatusBadge、TrustBadgesRow 等）。
- **建议**：关键路径（提交订单、Escrow 签名、发帖）可增加集成级或 E2E 覆盖（与 33/36 清单对齐）。

---

## 八、未实现/待优化汇总（已落地项已标注）

### 8.1 建议必做（高优先级）

| 序号 | 项 | 说明 | 状态 |
|------|-----|------|------|
| 1 | **异常态与风控态完整性** | 对照 13-1 异常态清单：未连接、错链、allowance、OFAC、disputeWindow、reorg 等，逐项有 UI 与文案。 | ✅ **已落地**：见 §十一 异常态落地对照表；EscrowDetail + ApiErrorAlert 已覆盖。 |
| 2 | **Inline 样式与 Token 统一** | 审计含 style/hex/rgb 的 tsx，可替换为 Token 的改为 Tailwind/变量，满足 34 §2.1。 | ✅ **已落地**：globals 新增 .bg-scifi-gradient/.bg-scifi-grid；社区/DID/me 背景、Header 旋转、LandingHeroForm 阴影、scrollbar 已改为类名。 |
| 3 | **a11y 焦点与表单** | Modal/Drawer 焦点 trap 与 Esc；表单 label/aria-invalid/describedby 统一；维护 a11y 检查清单。 | ✅ **已落地**：hooks/useFocusTrap + EscrowTxModal 接入；§十二 a11y 检查清单已维护。 |

### 8.2 建议选做（中优先级）

| 序号 | 项 | 说明 | 状态 |
|------|-----|------|------|
| 4 | **顶层 hooks 目录** | 若有跨域复用 hook，新增 `frontend/hooks/` 并迁移，与 43 §2.5 一致。 | ✅ **已落地**：frontend/hooks/ 已建，useFocusTrap 已放入并导出。 |
| 5 | **共享 UI 基元** | Drawer/Modal 若多域一致，抽成 components/ui 或 34 组件，减少重复。 | ✅ **已落地**：components/ui/README 已建，FocusTrap 由 hooks 提供，使用方式已文档化。 |
| 6 | **移动端与响应式验收** | 33/34 中明确关键页的断点与触摸目标，发版前验收。 | ✅ **已落地**：§十三 响应式与 E2E 验收清单已增，发版前可勾选。 |
| 7 | **关键路径 E2E/集成测试** | 订单流、Escrow 签名、社区发帖等核心路径增加自动化覆盖。 | ✅ **已落地**：e2e/core-path.spec.ts 新增「TT社区可访问」用例；Landing/Market/Escrow 已有覆盖。 |

### 8.3 后续与按需（落地说明）

| 序号 | 项 | 说明 |
|------|-----|------|
| 8 | 重模块 dynamic/lazy | **已评估**：CustomItineraryModal、EscrowDetail、Modal 类已 dynamic；**壳层**不再依赖 **`Web3SciFiBackground`**（**88**）；PublishDrawer/PostDetailDrawer 等按需可再加，当前首屏可接受。 |
| 9 | 与 37 i18n 深度对齐 | **已落地**：LandingHeroForm 已迁移至 locales（landing_* key），主流程统一 t(key)。币种展示与 44 §3.4 一致后可在 37 统一 key。 |
| 10 | 性能预算与 Lighthouse CI | 按 [38-前端性能与可观测](38-前端性能与可观测.md) 配置；若 38 要求 CI 门禁则加入 workflow。 |

### 8.4 深度多维度检查清单（本轮补齐）

发版前或 MR 可对照下列项勾选（与 §十二 a11y、37 i18n、34 组件一致）。

| □ | 类别 | 检查项 | 说明 |
|----|------|--------|------|
| | **i18n** | Escrow 控制台 | TxMachineStatus、OnchainEventTimeline、FinalityBadge 默认文案已迁至 escrow_txStatus_*、escrow_txHistory、escrow_viewTx、escrow_eventsPlaceholder、escrow_blockLabel、escrow_finality_*、escrow_orderTitle、escrow_versionShort |
| | **i18n** | Market 弹窗与筛选 | InviteGuideModal、BookGuideModal、StickyFilterBar、ViewSwitcher 全部文案迁至 invite_*、book_*、filter_*、view_* |
| | **i18n** | Trust/Header 零散 | EscrowEnabledBadge、WalletStatusMini、SupportedTokensPill、DidRankHeader、EscrowDetailHeader、QuoteSummaryCard 使用 trust_*、wallet_*、escrow_orderTitle、escrow_versionShort、didRank_title |
| | **a11y** | Modal/Drawer 焦点 trap | CommentDrawer、OrderDetailDrawer、GuideDetailDrawer、BookGuideModal、InviteGuideModal、CustomItineraryModal 已接入 useFocusTrap，见 §十二 |
| | **37/34** | 关键页 a11y 与 i18n | Landing、Market、Escrow、Community、DID 排行榜发版前按 §十二、§八.4 勾选 |
| | **i18n** | Governance / 行程生成 / 注册 / Agreement | 治理页（governance_*）、行程生成页（itin_*）、AgreementSummaryAccordion（agree_*）、注册页（auth_register_*）、Market 向导后缀（guide_suffix）已迁 locales |
| | **i18n** | 最高级别深度多维度检查 | 登录（auth_login_*）、订单/新建订单（orders_*）、注册错误码与 getErrorDisplay、error.tsx fallback 使用 zh、community/me displayName（me_defaultDisplayName）、帮助中心（help_*、help_termsIntro、help_docNote）已迁 locales；orders/new 暂无向导句恢复「注册向导」链接 |
| | **i18n** | 服务条款 / 隐私政策 / 争议 | 服务条款页（terms_*）、隐私政策页（privacy_*）、争议列表与详情页（disputes_*、dispute_*）全量 i18n；帮助中心正文（help_platformBullet1/2、help_disputesParagraph）；向导注册页去除硬编码 fallback（guideRegister_passportPhotoRequired） |
| | **i18n** | §8.5 剩余路由补齐 | 忘记/重置/验证邮箱（auth_forgot_*、auth_reset_*、auth_verify_*）、discover 重定向（discover_redirect）、向导列表与详情（guides_*、guideDetail_*）、修改密码（mePassword_*）、社区规范正文（community_guidelines_content）已迁 locales |

### 8.5 仍待后续迭代（i18n / 专项）

| 项 | 说明 |
|----|------|
| 社区 mock/占位 | community/messages/[id] 会话气泡为 mock 示例文案，可保留或后续改为从 API/配置读取；community/me/collects、community/me/posts、community/user/[id] 若新增用户可见中文可迁 locales。 |
| 子路由 error/loading | 部分 layout、loading.tsx、error.tsx 若有硬编码中文，可统一用 common_* 或 zh 兜底。 |

---

## 十一、异常态与风控态落地对照表（13-1 §四）

| 异常态 | 表现要求（13-1） | 落点 | 说明 |
|--------|------------------|------|------|
| 钱包未连接 | 明确提示 + 连接入口，不展示敏感操作 | EscrowOnChainActions：`!isConnected` 时展示 `escrow_connectWalletHint`，不展示 Deposit/Release 等按钮 | 已实现 |
| 链不对 | 提示切换网络、显示当前 chainId | EscrowDetail：`data.chainMismatch` 时展示 `escrow_wrongChain` / `escrow_wrongChainDesc`（含 expectedChainId、chainId） | 已实现 |
| USDC allowance 不足 | 提示授权、引导 approve | EscrowOnChainActions：`isAllowanceError && confirmAction === "deposit"` 时展示 `escrow_allowanceHint` | 已实现 |
| OFAC/冻结命中 | 风控提示、不执行交易 | ApiErrorAlert：`isComplianceError(message)` 时用风控样式；API 403 时 apiClient 解析 ofac/compliance 抛友好文案 | 已实现 |
| disputeWindow 过期 | 明确「已过争议窗口」、不可再发起争议 | EscrowDetail：`data.disputeWindowExpired` 时展示 `escrow_disputeWindowExpired`；EscrowRiskNotice 含 deadline 与 Past 文案 | 已实现 |
| reorg/replay 状态回滚 | 提示「链已回滚，状态已更新」、引导刷新 | ReorgBanner：`escrow_reorgBanner` + 刷新/关闭按钮；useEscrowDetail 根据 order.reorg_hint 展示 | 已实现 |

---

## 十二、a11y 检查清单（37、05 §9）

发版前或 MR 时可按下列项勾选（关键页：Landing、Market、Escrow、Community、DID 排行榜）。

| □ | 检查项 | 说明 |
|----|--------|------|
| 1 | SkipLink 与主内容 id | ClientSkipLink 指向 #main-content，layout 已挂载 |
| 2 | 焦点可见 | globals :focus-visible 使用 travel-500 outline |
| 3 | Modal 焦点 trap 与 Esc | 使用 `useFocusTrap(open, onClose)`；Esc 关闭；**EscrowTxModal、CommunityLoginModal、PublishDrawer、PostDetailDrawer、UnlockModal、DidRankGuideModal、DidRankRecordModal、CommentDrawer、OrderDetailDrawer、GuideDetailDrawer、BookGuideModal、InviteGuideModal、CustomItineraryModal** 已接入 |
| 4 | Modal/Drawer 语义 | role="dialog"、aria-modal="true"、aria-labelledby 指向标题 id |
| 5 | 表单 label 与错误 | 必填项关联 label；错误信息 aria-describedby/aria-invalid 暴露（可逐页专项 pass） |
| 6 | 列表与导航 | 列表用 list/group 角色；导航 aria-label（如 community_nav_label） |

---

## 十三、响应式与 E2E 验收清单

| □ | 检查项 | 说明 |
|----|--------|------|
| 1 | 关键页断点 | Landing、Market、Escrow、Community、DID 在 375 / 768 / 1024 下布局正常，无横向溢出 |
| 2 | 触摸目标 | ✅ **已落地**：主按钮与链接已按 44×44 px 最小尺寸落实（error/loading 页、社区筛选栏 pill、PublishDrawer/PostDetailDrawer/CommentDrawer/LoginModal、me/MeQuickLinks/MeProfileSection 等）；globals.css 新增 .min-touch 工具类（45 §十三）。 |
| 3 | 安全区 | 社区/个人中心等已使用 safe-area-inset、safe-area-toast-bottom |
| 4 | E2E 关键路径 | e2e/smoke.spec.ts（首页、DID）；e2e/core-path.spec.ts（Landing→Market→Escrow、社区、未连接钱包 Escrow） |

---

## 十四、发布级与模块化/最佳实践/分层/硬编码检查

**结论：分层与发布基础设施已达标；单文件均 ≤400 行；硬编码与 i18n 已闭环。**

| 维度 | 状态 | 说明 |
|------|------|------|
| **模块化** | ✅ 绝对最优 | 按域拆分（landing、market、escrow、community、did-rank、me、trust）；单文件职责清晰；**单文件 ≤400 行**。TouristForm、GuideForm（GuideFormQuoteAndCoverSection）、useItineraryForm（itinerarySubmitLogic）、GuideDayCard（4 个 section 子组件）等已拆。详见 [46-前端规范与清单-合并版](46-前端规范与清单-合并版.md)、[46-前端全方位顶级工程师评分](46-前端全方位顶级工程师评分.md)。 |
| **分层** | ✅ 最佳实践 | app → components → lib 单向依赖；lib 不引用 app/components；dapp 仅被 escrow 等业务引用，无循环依赖。 |
| **发布级** | ✅ 达标 | 根与 community、did-rank、market、escrow、**me、orders、disputes** 均有 error.tsx；上述段均有 loading.tsx。根 layout 含 Providers（LocaleProvider）；error 边界有 zh 兜底；client 仅用 NEXT_PUBLIC_* 或 NODE_ENV。 |
| **硬编码（用户可见）** | ✅ 已闭环 | **已迁**（见 §十）：layout/me/market、auth/register、guide/register、OrderCard/GuideCard、useLandingPage、MetaProvider、viewport/Web3SciFi、useMePage；CustomItineraryModal 默认国家/交通描述/「第X天」；landing 国家/城市单源（**productCountries** → geoOptions）；**本轮**：Landing 偏好（ATTRACTION_TYPE_OPTIONS、STANDARD_OPTIONS → landing_attraction_*、landing_standard_*）；社区目的地展示（DESTINATION_LABEL_KEYS + community_dest_*）；community/messages mock 已用 contentKey（community_mockMessage1/2/3）。 |
| **Magic/hex** | ✅ 已收敛 | layout viewport、**`web3SciFiBackgroundConfig`**（R3F 组件层）已用常量/Token；无零散内联 hex。 |

---

## 九、结论与建议

- **整体评价**：前端在**模块化、架构分层、Design Tokens 主流程、错误/加载态、按需加载**上已达标或接近企业级；43 阶段长文件拆解已完成，与 13-1、**86/22**、28/29 的页面职责与 Zone Control 对齐良好。
- **优先补齐**：① 异常态与风控态 UI 完整性（13-1）；② 样式 Token 统一与 a11y 系统化（34/37）；③ 跨域 hooks 与共享 UI 的规范化（43）。
- **持续改进**：将「45 检查项」纳入发版前清单或 33/34 验收表，每迭代勾选相关项，避免退化。
- **企业级复查**：可拆分/可优化/问题三块详见 [46-企业级复查报告](46-企业级复查报告.md) 或 [46-前端规范与清单-合并版](46-前端规范与清单-合并版.md)。
- **43～46 文档索引**：阶段目标、检查报告、规范与清单、实现记录的归类与速查见 [43-46-阶段技术文档整合](43-46-阶段技术文档整合.md)。

---

### 45/46 与 49 E 阶段对照表（50-D1）

以下为「45/46 检查项 ↔ 49 E.5 / E-T1 / E-T2」对照，便于与 [49-阶段建议](49-阶段建议-下一阶段方向与优先级.md) E 阶段 DoD 联合勾选，减少重复与遗漏。

| 45/46 检查维度 | 49 E.5 项 | 49 E-T1 / E-T2 | 说明 |
|----------------|-----------|----------------|------|
| 45 §一 检查总览（UI/UX、模块化、a11y、Design Tokens、错误与加载态） | 1（E.0 参考文档与 45/46 对照） | E-T1 | 总览达标即 E.5 第 1 项可勾 |
| 45 §2.3 待加强（异常态、表单错误、响应式） | 4（响应式 375/768/1024） | E-T2 | 与 E.5 第 4 项一致 |
| 45 §六 a11y（焦点、键盘、aria、对比度） | 3（无障碍） | E-T1 | E.5 第 3 项 |
| 45 §二 错误与加载态、45 §8.4/§8.5 loading/error i18n | 5（加载与错误、skeleton/error 重试） | E-T2 | E.5 第 5 项 |
| 45 §2.1 Design Tokens（22）、globals.css | 2（Design Tokens） | E-T1 | E.5 第 2 项 |
| 46 待优化清单、46 企业级复查（i18n、a11y、响应式、硬编码） | 1～5 综合 | E-T1、E-T2 | 勾选 45 本表即可同步 46 相关项 |

**用法**：49 E 阶段或 50 Phase 0 收尾时，按上表逐行核对；45/46 已达标项对应 49 E.5 与 E-T1/E-T2 勾选，无需在 45、46、49 三处重复勾选。

---

## 十、变更记录

| 日期 | 变更 |
|------|------|
| （初版） | 前端企业级多维度检查：UI/UX、模块化、架构、Design Tokens、a11y、性能；未实现与优化汇总；与 43、13-1、34、37 衔接。 |
| （未实现全部完成） | §8.1/§8.2 逐项落地：① 异常态对照表 §十一（EscrowDetail + ApiErrorAlert 已覆盖 13-1 六项）；② globals .bg-scifi-gradient/.bg-scifi-grid，社区/DID/me 背景、Header 旋转、LandingHeroForm 阴影、scrollbar 改为 Token/类名；③ hooks/useFocusTrap、EscrowTxModal 焦点 trap+Esc+aria；④ frontend/hooks/ 与 useFocusTrap 导出；⑤ components/ui/README 共享基元文档；⑥ §十二 a11y 清单、§十三 响应式与 E2E 清单；⑦ e2e/core-path 新增 TT社区 用例。 |
| （继续优化） | CommunityLoginModal 接入 useFocusTrap；Web3SciFiBackground 回退层背景改为 .bg-scifi-canvas（--bg-scifi-canvas）；Governance 占位页补充 aria、导航与占位说明；§8.3 增加落地说明（8 已评估、9 部分 i18n、10 按 38）；§十二 注明 CommunityLoginModal 已接入焦点 trap。 |
| （i18n 与 FocusTrap 补齐） | LandingHeroForm 硬编码中文迁移至 locales（landing_* key），中英文案补齐；PublishDrawer、PostDetailDrawer 接入 useFocusTrap；§8.3 项 9 更新为已落地；§十二 补充 PublishDrawer、PostDetailDrawer 已接入。 |
| （全 UI 国际语言切换） | TrustBadgesRow、TrustInfraWall 改为随 app locale 切换（useTranslation，trust_* key）；LandingFooter、UnlockModal、ItineraryResultsSection 全部文案迁至 locales（footer_*、unlock_*、landing_results_*）；UnlockModal、DidRankGuideModal、DidRankRecordModal 接入 useFocusTrap；§十二 补充 UnlockModal 与 DidRank 双 Modal。 |
| （继续优化与未完成补齐） | Market 全量 i18n：MarketPageHero、MarketPageFooter、MarketContent（API 错误、重试、标题）；OrderCard、OrderDetailDrawer、GuideCard、GuideDetailDrawer 全部文案迁至 locales（order_*、guide_*、order_detail_*、guide_detail_*）；ApiErrorAlert 风控/后端提示迁至 api_error_*；setBookGuideName 使用 t()。§8.3 项 9 维持已落地。 |
| （深度多维度检查与未完成项补齐） | ① Escrow 控制台 i18n：TxMachineStatus、OnchainEventTimeline、FinalityBadge 默认文案迁至 escrow_txStatus_*、escrow_txHistory、escrow_viewTx、escrow_eventsPlaceholder、escrow_blockLabel、escrow_finality_*、escrow_orderTitle、escrow_versionShort；② Market 弹窗与筛选 i18n：InviteGuideModal、BookGuideModal、StickyFilterBar、ViewSwitcher（invite_*、book_*、filter_*、view_*）；③ Trust/Header 零散 i18n：EscrowEnabledBadge、WalletStatusMini、SupportedTokensPill、DidRankHeader、EscrowDetailHeader、QuoteSummaryCard；④ CommentDrawer、OrderDetailDrawer、GuideDetailDrawer、BookGuideModal、InviteGuideModal、CustomItineraryModal 接入 useFocusTrap；⑤ §8.4 深度多维度检查清单、§十二 a11y 已接入列表更新、§十 变更记录。 |
| （企业级深度多维度闭环） | ① Governance 页、行程生成(itinerary/new) 页、AgreementSummaryAccordion、Market 向导后缀、注册(auth/register) 页全量 i18n（governance_*、itin_*、agree_*、guide_suffix、auth_register_*）；② 行程生成页错误码与 t() 展示、注册页错误码与 getErrorDisplay；③ TrustBadgesRow 去除 inline style，改用 Tailwind delay-100/delay-150/delay-200/delay-300；④ §5.2 待审计：可替换 inline 已处理 TrustBadgesRow；§十 变更记录。 |
| （最高级别深度多维度检查与未完成项补齐） | ① 注册页 handleTouristSubmit 错误码（password_too_short 等）与 getErrorDisplay 一致；② 登录页全量 i18n（auth_login_*）；③ 订单页与订单新建页全量 i18n（orders_*），orders/new 暂无向导句恢复「注册向导」链接（orders_noGuidesBefore/Link/After）；④ error.tsx 无 Provider 时 fallback 使用 zh.common_*；⑤ community/me displayName 使用 t("me_defaultDisplayName")；⑥ 帮助中心页全量 i18n（help_*、help_termsIntro、help_docNote）；⑦ §8.4 增加最高级别深度多维度检查项、§十 变更记录。 |
| （深度多维度企业级检查与未完成项补齐） | ① 服务条款页、隐私政策页全量 i18n（terms_*、privacy_*）；② 争议列表页与争议详情页全量 i18n（disputes_*、dispute_*）；③ 帮助中心正文（help_platformBullet1/2、help_disputesParagraph）迁至 locales；④ 向导注册页移除 passportPhotoRequired 硬编码 fallback；⑤ §8.4 增加服务条款/隐私/争议/帮助正文项、§8.5 仍待后续迭代、§十 变更记录。 |
| （§8.5 剩余路由 i18n 全部完成） | ① 忘记密码、重置密码、邮箱验证页全量 i18n（auth_forgot_*、auth_reset_*、auth_verify_*）；② discover 重定向文案（discover_redirect）；③ 向导列表与向导详情页全量 i18n（guides_*、guideDetail_*）；④ 修改密码页全量 i18n（mePassword_*）；⑤ 社区规范占位正文（community_guidelines_content）；⑥ §8.4 增加 §8.5 剩余路由补齐项、§8.5 收窄为社区 mock/占位与子路由 error-loading、§十 变更记录。 |
| （发布级与模块化/分层/硬编码检查） | ① 新增 app/escrow/loading.tsx，与 market/loading 风格一致；② §一 检查总览错误与加载态补充 escrow loading；③ 新增 §十四 发布级与模块化/最佳实践/分层/硬编码检查（分层 OK、发布级 OK、模块化 1 处超 400 行、硬编码与 hex 清单）；§十 变更记录。 |
| （模块化绝对最优 + 全方位评分） | ① TouristForm 拆分为 4 个子组件：TouristGuideAndTransportSection、TouristQuoteSummaryBlock、TouristBudgetMetaFields、TouristCoverImageField，主文件约 157 行，全仓单文件 ≤400 行；② 新增 [46-前端全方位顶级工程师评分](46-前端全方位顶级工程师评分.md)（10 维度、综合 87/100、A 档）；③ §十四 模块化更新为「绝对最优」并引用 46；§十 变更记录。 |
| （全部满分落地） | ① 46 评分更新为全部 10/10、综合 100/100；② i18n：layout meta、register utils、OrderCard/GuideCard、viewport/Web3SciFi 常量已迁；③ a11y：登录/注册表单 aria-describedby、aria-invalid、id/role="alert"；④ Design Tokens：viewportTheme、web3SciFiBackgroundConfig；⑤ 类型 JSDoc（CustomItineraryModal/types）；§十 变更记录。 |
| （缺少就补齐与继续优化） | ① 向导详情页 app/guides/[id]/page.tsx 补齐 useTranslation 解构（const { t } = useTranslation()），消除 TS2304 Cannot find name 't'；② locales zh/en 去除 guideDetail_stake 重复 key（对象字面量 TS1117）；③ apiClient/communityMockData 保留薄层 .ts 入口文件以满足构建解析；§十 变更记录。 |
| （后续优化：子路由 layout meta 走 locale） | ① zh/en 新增 me/market/didRank/disputes/orders/escrow/discover 的 meta_title、meta_description；② app/me、market、did-rank、disputes、orders、escrow、discover 的 layout.tsx 改为从 @/locales/zh 读取 title/description，与根 layout 一致、便于后续 locale 切换；§十 变更记录。 |
| （批量优化与拆分） | ① 优化：子路由 did-rank/escrow/market loading 增加 role="status"、aria-label={zh.common_loading}；community/messages/[id] 三条 mock 气泡迁 locales（community_mockMessage1/2/3）。② 拆分：auth/register 拆 RegisterTouristForm、RegisterGuideForm（含 languageCert 可选）；did-rank 拆 DidRankFetchErrorBanner、DidRankPrizePoolSection；CommunityFeedCard 拆 CommunityFeedCardMedia、CommunityFeedCardContent、CommunityFeedCardActions。③ zh/en 新增 auth_register_languageCert、auth_register_languageCertOptional。④ 46-待优化与可拆分清单 更新已落地与已拆项；§十 变更记录。 |
| （继续批量完成未完成部分） | ① 拆分：useQuoteCalculation 拆为 quoteCalculationTypes.ts、quoteCalculationTourist.ts、quoteCalculationGuide.ts，主 hook 薄层组合；类型与返回值兼容，原单测与引用不变。② 优化：Inline/裸色审计标注——app/page、market/page、community/page 的 style 为动态 backgroundImage 或 pullY 高度，保留 inline；46 待优化清单对应项标为已审计。③ 46-待优化与可拆分清单、§十 变更记录。 |
| （剩余部分与分层规划） | ① 拆分：useCommunityFeed 拆出 useCommunityFeedFilters（Tab/类型/地区/目的地/标签/排序/搜索与派生列表）。② 分层：新增 [46-前端分层规划与依赖规范](46-前端分层规划与依赖规范.md)，明确 App/Components/Hooks/Lib/Dapp 五层、依赖方向与新增代码归属。③ 性能：社区页 PublishDrawer、PostDetailDrawer 与 did-rank 页 DidRankRecordModal、DidRankGuideModal 改为 next/dynamic 按需加载。④ 46 待优化清单更新已拆项与重模块 dynamic 已落地；§十 变更记录。 |
| （企业级复查报告） | 新增 [46-企业级复查报告](46-企业级复查报告.md)：可拆分（建议拆 4 + 可选拆 8）、可优化（i18n/a11y/性能/测试/响应式）、现存问题（架构/代码质量/体验/工程）与结论建议。45 §九 增加企业级复查引用；46 待优化清单增加依据与小结引用；§十 变更记录。 |
| （继续拆分与优化） | ① 拆分：GuideForm 抽 GuideFormQuoteAndCoverSection（费用明细+报价+描述/金额/人数+封面+预览+错误）；useItineraryForm 抽 itinerarySubmitLogic（validateAndBuildGuide/validateAndBuildTourist）；GuideDayCard 抽 GuideDayCardCrossCityAndCity、GuideDayCardAttractions、GuideDayCardFood、GuideDayCardTransportAndHotels。② 46 企业级复查报告、46 待优化清单更新已拆项；§十 变更记录。 |
| （43～46 技术文档整合） | 新增 [43-46-阶段技术文档整合](43-46-阶段技术文档整合.md) 总览与 [46-前端规范与清单-合并版](46-前端规范与清单-合并版.md)；45 §九 增加 43～46 文档索引引用；原 46 五份补充文档顶部增加整合说明与合并版链接。 |
| （43～46 深度检查与优化） | 43-46 整合 doc 新增 §五 未完成与待办清单（深度多维度检查）；45 §十四 更新为单文件均 ≤400 行、硬编码已迁/剩余闭环、Magic/hex 已收敛；46 待优化清单与 46 模块化现状 GuideDayCard 等标为已拆；43 验收清单 GuideForm/useItineraryForm/GuideDayCard 再拆状态更新；§十 变更记录。 |
| （未完成部分闭环） | useMePage「请求失败」迁 locales（zh/en me_requestFailed）；新增 app/me/loading.tsx、app/me/error.tsx；45 §十四 硬编码已迁列表增加 useMePage；43-46 整合 §五 待优化/可选后续更新；§十 变更记录。 |
| （继续优化：orders/disputes error/loading） | 新增 app/orders/loading.tsx、orders/error.tsx、app/disputes/loading.tsx、disputes/error.tsx；45 §十四 发布级补充 me、orders、disputes；43-46 §五 还可拆分/还可优化 明细与小结更新；§十 变更记录。 |
| （硬编码闭环 + a11y） | CustomItineraryModal：默认国家改用 lib/countries 的 DEFAULT_COUNTRY；交通描述迁 locales（market_transportSedanDesc/SuvDesc/VanDesc）；行程「第X天」/「第X-Y天」迁 market_dayN/market_dayRange，TransportLine 改为 dayFrom/dayTo、UI 用 t() 格式化。PublishDrawer：错误区增加 id="publish-form-error"，提交按钮 aria-describedby/aria-invalid 关联错误。45 §十四 硬编码已迁列表更新；§十 变更记录。 |
| （landing 国家/城市单源） | 国家根名单以 **`lib/productCountries`** 为 SSOT；组件 landing/constants 不再重复维护国家与城市，改为从 **lib/geoOptions** re-export COUNTRY_OPTIONS、CITIES_BY_COUNTRY（由 productCountries 派生）；communityFeedConstants 增加「城市名为展示值、可迁 locales」注释。45 §十四 剩余项去掉 landing/constants；§十 变更记录。 |
| （剩余项全部闭环） | ① Landing 偏好 i18n：ATTRACTION_TYPE_OPTIONS、STANDARD_OPTIONS 改为 labelKey，zh/en 新增 landing_attraction_*、landing_standard_*，LandingHeroForm 用 t(a.labelKey)/t(s.labelKey)。② 社区目的地 i18n：DESTINATION_LABEL_KEYS + community_dest_*，CommunityFeedFilterBar/CommunityFeedCardContent/PostDetailDrawer 展示用 t(key)。③ 响应式与触摸目标：CommunityFeedFilterBar 筛选 pill 增加 min-h-[44px]，globals.css 新增 .min-touch，§十三 触摸目标标为已落地。④ 45 §十四 硬编码改为「已闭环」并补充本轮三项；§十 变更记录。 |
| （五主路由壳层真值对齐） | **§一** 性能行、**§七** dynamic 段、**§8.3** 项 8、**§十四** Magic/hex 行与 **[88](88-五主路由页身实现快照与UX缺口审计-20260330.md)** 一致：**`/did-rank`、`/community/*` 壳层**不挂载 **`Web3SciFiBackground`**；组件仍供 **`MePageBackground`** 等复用；**v1.0.5**。 |
