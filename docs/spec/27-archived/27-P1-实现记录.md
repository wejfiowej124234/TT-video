# P1 Phase 1 实现记录（领域与后端骨架）

### 读前摘要

| 你要找什么 | 单源 |
|------------|------|
| **本 Phase 记录与验收** | **正文各节** |
| **总流程** | **[27-P0至P50开发流程勾选清单](../27-P0至P50开发流程勾选清单.md)** 对应 **P1** |
| **归档** | **[27-archived/README](README.md)**、**[27-系列索引](../27-系列索引.md)** |

**用途**：落实 [27-P0～P50 开发流程勾选清单](../27-P0至P50开发流程勾选清单.md) P1 阶段四项工作项，验收通过后可勾选「P1 实现完成 → 进入 P2」。

**目标**：core 类型与状态机、api 项目骨架、DB 选型与迁移脚本；无链、无前端。

> **归档说明（迁移路径，2026-03）**：下文若出现 `migrations/001_initial.sql` 等，为当时表述。**当前唯一执行源**为 `crates/api/migrations/`（SQLx），见 [27-archived/README](README.md)、[41-后端数据库接库与落地清单](../41-后端数据库接库与落地清单.md)。

---

## P1-1：core 类型与状态机

**验收**：与 02/03 一致，单元可测。

| 项 | 落点 | 状态 |
|----|------|------|
| 领域类型 | `crates/core/src/types.rs`：User、Guide、Order、Dispute、UserRole、KycStatus、GuideStatus、ServiceType、DisputeStatus、DisputeResolution | ✓ |
| 订单状态枚举 | `crates/core/src/escrow.rs`：OrderState（Created/Accepted/Escrowed/Completed/Disputed/Refunded/PartiallyRefunded/Slashed/Cancelled）与 01 §1 一致 | ✓ |
| 合法迁移 | `OrderState::allowed_next_states()`、`OrderState::can_transition_to(next)` 与 01 §1 状态机表一致 | ✓ |
| 终态与评价 | `is_final_financial_state()`、`is_terminal()`；EscrowState::can_review/can_dispute | ✓ |
| 单元测试 | `crates/core/src/escrow.rs` §tests：Created→Accepted/Cancelled、Accepted→Escrowed/Cancelled、Escrowed→Completed/Disputed、Disputed→四资金终态、终态无下一状态、仅 Escrowed 可 dispute、仅资金终态可 review | ✓ |

**结论**：P1-1 通过；与 02/03 一致，单元可测。

---

## P1-2：api 项目骨架、基础路由

**验收**：健康检查、版本绑定等占位可用。

| 项 | 落点 | 状态 |
|----|------|------|
| 健康检查 | `GET /health` 返回 `ok` | ✓ |
| 版本绑定 | `GET /meta` 返回 api_version、ssot_version、defaults（REQUEST_TIMEOUT_SECS、REQUEST_BODY_LIMIT_BYTES 等）、indexer checkpoint | ✓ |
| 基础路由 | 04 §三 路由占位：/api/v1/guides、/api/v1/me、/api/v1/orders、/api/v1/disputes、/auth/* 等；部分 501 占位，部分返回占位 JSON | ✓ |
| 中间件 | request_id、idempotency_key、cors、timeout、body_limit、security_headers、pause_gate、authority_source | ✓ |

**结论**：P1-2 通过；健康检查与版本绑定占位可用。

---

## P1-3：DB 选型与迁移脚本

**验收**：DDL 与 04/11 一致，可执行迁移。

| 项 | 落点 | 状态 |
|----|------|------|
| DDL 与 04/11 一致 | [04-附录-DDL草案](../04-附录-DDL草案.md) 七表 + users（orders_projection 外键依赖） | ✓ |
| 可执行迁移 | `migrations/001_initial.sql`：users、event_log、checkpoints_sharded、orders_projection、reconciliation_reports、correction_log、executor_executions、idempotency_keys | ✓ |
| 执行说明 | `migrations/README.md`：psql / cockroach sql 执行方式与回滚说明 | ✓ |

**结论**：P1-3 通过；DDL 与 04/11 一致，可执行迁移。

---

## P1-4：无链、无前端

**验收**：不依赖链 RPC 与前端工程。

| 项 | 说明 | 状态 |
|----|------|------|
| 无链 RPC | crates/api、crates/core 无 alloy / web3 / ethers 等链 RPC 依赖；indexer 状态为文件/内存，无实时 RPC 调用 | ✓ |
| 无前端 | 无 frontend 或 DApp 构建依赖；API 可独立启动（PORT=8080） | ✓ |

**结论**：P1-4 通过；无链、无前端。

---

## P1 实现完成 → 进入 P2

| # | 工作项 | 验收 | 结果 |
|---|--------|------|------|
| 1 | core 类型与状态机 | 与 02/03 一致，单元可测 | 通过 |
| 2 | api 项目骨架、基础路由 | 健康检查、版本绑定等占位可用 | 通过 |
| 3 | DB 选型与迁移脚本 | DDL 与 04/11 一致，可执行迁移 | 通过 |
| 4 | 无链、无前端 | 不依赖链 RPC 与前端工程 | 通过 |

**结论**：P1 阶段四项工作项已完成，可勾选「P1 实现完成 → 进入 P2」，进入 P2（Phase 1 门禁：测试 · 审计 · 缺口检查）。

---

## 企业级 P1 检查（Phase 1 完成度与缺口）

以下按 04 §五 Phase 1、04 §六 6.4 开工前检查清单、P2 门禁预检、01 §10/02 分层进行企业级核对。

### 1. 与 04 §五 Phase 1 对照

| 04 Phase 1 项 | 27 P1 对应 | 实现状态 | 说明 |
|---------------|------------|----------|------|
| Rust workspace | core + api 两 crate | ✓ | Cargo workspace 已存在 |
| core 类型 | types + OrderState（escrow） | ✓ | User/Guide/Order/Dispute、OrderState 与 01 §1 一致 |
| api 骨架 | 路由、中间件、/health、/meta | ✓ | 占位路由与 04 §三 对齐 |
| 基础 CRUD（用户、向导、订单状态机） | 路由占位 | ⚠ 占位 | 04 字面含「基础 CRUD」；07/27 Phase 1 为「基础路由、占位」。当前为**占位**（501 或空 JSON），完整 CRUD 在 **P3（Phase 2 实现）** 验收 |

**结论**：以 27 P1 验收口径（占位可用）已满足；若严格按 04「基础 CRUD」字面，属**口径差异**——完整 CRUD 在 P3 实现并验收。

### 2. 04 §六 6.4 开工前检查清单（企业级）

| # | 检查项 | 要求 | P1 阶段结论 |
|----|--------|------|-------------|
| 1 | 01 引用 04 覆盖 | 开篇 12 项及 §二～§四 均已覆盖；01 §10 后端落点见 §四、6.2 | ✓ 文档已就绪；实现为 Phase 1 骨架 |
| 2 | 04 与 01/02 一致性 | 开篇 12 项无冲突；总览主题符合性表 7 行与 01 一致 | ✓ 文档层面已通过（04 6.3 审计结论） |
| 3 | 与 03 接口风控对齐 | §二 业务表、§四 风控与 03 一致 | ✓ 表结构与风控落点已定稿 |
| 4 | 规则变更同步 | 04 修改后同步 01/02/03 及开篇两表、6.3 | ✓ 维护责任已载明 |

**结论**：04 开工前 6.4 四项在**文档与设计**层面已满足；实现为 Phase 1 骨架，与 6.4 不冲突。

### 3. P2 门禁预检（企业级提前看）

| P2 检查项 | 当前状态 | 建议 |
|-----------|----------|------|
| 单元测试：core 与关键状态迁移有单测 | ✓ core/escrow 状态机 7 个单测；types/staking/reputation/outbox 无单测 | P2 前可补 core 其余模块单测（或 P2 门禁时补齐） |
| 集成测试：API 健康检查、占位路由可调通 | 未自动化 | P2 须提供 curl/Postman 或集成测试脚本，验证 /health、/meta、至少 1 个占位路由 |
| 审计/一致性：与 02 分层、04 表结构一致；04 6.4 可勾选 | 分层：core 无 I/O、api 应用层；表结构见 migrations/001 | P2 逐项勾选 04 6.4；确认 02 §二 与仓库一致 |
| 缺口与遗漏：对照 01 §10、04 §五 Phase 1 | 见下表「P1 缺口与问题」 | P2 门禁时逐项闭合 |

### 4. P1 缺口与问题（企业级）

| # | 问题/缺口 | 严重程度 | 建议处置 |
|----|-----------|----------|----------|
| 1 | **04 Phase 1 字面「基础 CRUD」与 27 P1「占位」口径差异** | 低 | 以 27 为准：P1 仅要求占位；完整 CRUD 在 P3 验收。可选：在 04 §五 Phase 1 加一句「Phase 1 产出含占位路由，完整 CRUD 在 Phase 2（链下）实现」。 |
| 2 | **core 仅 escrow 有单测**；types、staking、reputation、outbox 无单测 | 低 | P2 要求「core 与关键状态迁移有单测」——状态机已覆盖。其余模块可在 P2 门禁前补单测或标注「非关键路径，P3 补」。 |
| 3 | **集成测试未自动化** | 中 | P2 必须满足「API 健康检查、占位路由可调通」。建议：在 P2 前增加脚本或文档（如 `scripts/smoke_api.sh` 调 /health、/meta）或集成测试用例。 |
| 4 | **迁移脚本未在 CI 或文档中记录「已成功执行」** | 低 | 企业级建议：在 CI 或 Runbook 中增加「执行 migrations/001_initial.sql 并校验表存在」步骤，或保留「首次部署时人工执行并记录」证据。 |
| 5 | **02 §二 约定 core 无 I/O**（不依赖 tokio/sqlx 等） | 需核对 | 当前 core 仅用 std、serde、chrono、uuid；无 tokio/sqlx/alloy，满足「core 无 I/O」。✓ |

### 5. 企业级 P1 结论

| 维度 | 结论 |
|------|------|
| **27 P1 四项** | 已完成；可进入 P2。 |
| **04 §五 Phase 1** | 骨架与占位已就绪；「基础 CRUD」按 27 以占位为准，完整 CRUD 属 P3。 |
| **04 §六 6.4** | 文档层面 4 项已满足；实现为 Phase 1 骨架，无冲突。 |
| **遗留问题** | 上述 5 项为低/中风险，均可在 P2 门禁前或门禁时闭合；无阻塞 P2 的硬缺口。 |

**建议**：进入 P2 后优先补齐「集成测试（/health、/meta 可调通）」与「04 6.4 勾选记录」，再执行 P2 单元测试/审计/缺口检查。

*本文与 27-P0至P50开发流程勾选清单、04、11 配套。文档版本与最后更新见 [00-文档索引](../00-文档索引.md)。*
