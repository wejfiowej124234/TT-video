# P20 实现记录：04 后端落地子阶段（一）— API 路由与 04 §三 全量对齐

### 读前摘要

| 你要找什么 | 单源 |
|------------|------|
| **本 Phase 记录与验收** | **正文各节** |
| **总流程** | **[27-P0至P50开发流程勾选清单](../27-P0至P50开发流程勾选清单.md)** 对应 **P20** |
| **归档** | **[27-archived/README](README.md)**、**[27-系列索引](../27-系列索引.md)** |

**用途**：落实 [27-P0至P50开发流程勾选清单](../27-P0至P50开发流程勾选清单.md) P20（扩展位），对应 [04-后端与API](../04-后端与API.md) **§五 落地顺序** 与 **§三 API 路由规划（v1）**。实现完成并门禁通过后可进入 P21（可选）。

**依据**：04 §三、§五、01 总库总览、02 架构设计、07 开发流程与顺序。

---

## P20 目标

以 04 §三 路由表为基准，**全量验收**当前 crates/api 路由实现：已实现项可调、未实现项标注占位；请求/响应与 04 一致；关键路径有单测/集成覆盖；与 04/01 无冲突。

---

## P20 实现项（工作项）

| # | 工作项 | 验收 | □ 完成 |
|---|--------|------|--------|
| 1 | 04 §三 路由对照 | 逐项对照 04 §三 3.1～3.3，已实现可调、未实现标注占位 | ✓ |
| 2 | 请求/响应与 04 一致 | 已实现路由的 body/错误码与 04 无冲突 | ✓ |
| 3 | 单测/集成 | 关键路径有 chain_off 或集成测试 | ✓ |
| 4 | 与 04/01 一致 | 与 04 §五、01 状态机/风控无冲突 | ✓ |

---

## P20 门禁（检查项）

| # | 检查项 | 要求 | □ 通过 |
|---|--------|------|--------|
| 1 | 路由清单勾选 | 04 §三 路由逐项可调或占位标注 | ✓ |
| 2 | 单测/集成 | 关键路径单测通过（与 P2/P4 衔接） | ✓ |
| 3 | 与 04/01 一致 | 无冲突 | ✓ |

---

## 04 §三 路由与当前实现对照表

### 3.1 认证与账号

| 方法 | 路径 | 04 说明 | 当前实现 | 状态 |
|------|------|---------|----------|------|
| GET | /health | 健康检查 | health | ✓ |
| GET | /meta | 版本与 SSOT、defaults 快照 | meta | ✓ |
| POST | /auth/register | 邮箱注册 | auth_register（chain_off） | ✓ |
| POST | /auth/login | 邮箱+密码登录 | auth_login（chain_off） | ✓ |
| POST | /auth/logout | 登出 | auth_logout → chain_off auth_logout_stub | ✓ 占位已补 |
| POST | /auth/refresh | 刷新 token | auth_refresh → chain_off auth_refresh_stub | ✓ 占位已补 |
| POST | /auth/verify-email | 邮箱验证 | auth_verify_email → chain_off auth_verify_email_stub | ✓ 占位已补 |
| POST | /auth/forgot-password | 忘记密码 | auth_forgot_password → chain_off auth_forgot_password_stub | ✓ 占位已补 |
| POST | /auth/reset-password | 重置密码 | auth_reset_password → chain_off auth_reset_password_stub | ✓ 占位已补 |

### 3.2 个人中心与设置

| 方法 | 路径 | 04 说明 | 当前实现 | 状态 |
|------|------|---------|----------|------|
| GET | /api/v1/me | 当前用户信息 + 统计摘要 | get_me（chain_off，按角色统计） | ✓ |
| PUT | /api/v1/me | 更新资料 | put_me（chain_off） | ✓ |
| PUT | /api/v1/me/password | 修改密码 | put_me_password → chain_off put_me_password_stub | ✓ 占位已补 |

### 3.3 向导、订单、争议

| 方法 | 路径 | 04 说明 | 当前实现 | 状态 |
|------|------|---------|----------|------|
| GET | /api/v1/guides | 向导列表 | guides_list（chain_off） | ✓ |
| GET | /api/v1/guides/:id | 向导详情 | guide_get（chain_off） | ✓ |
| POST | /api/v1/guides | 向导注册 | guide_create（chain_off） | ✓ |
| POST | /api/v1/guides/:id/stake | 质押 | guide_stake（chain_off） | ✓ |
| POST | /api/v1/itineraries | P15 行程生成 | itinerary_create（chain_off） | ✓ |
| GET | /api/v1/discover/orders | P16 Discover | get_discover_orders（chain_off） | ✓ |
| GET | /api/v1/orders/:id/messages | P16 聊天列表 | get_order_messages（chain_off） | ✓ |
| POST | /api/v1/orders/:id/messages | P16 发送消息 | post_order_message（chain_off） | ✓ |
| POST | /api/v1/orders/:id/confirm-final-plan | P16 确认最终版本 | confirm_final_plan（chain_off） | ✓ |
| POST | /api/v1/orders | 下单 | order_create（chain_off） | ✓ |
| GET | /api/v1/orders | 我的订单列表 | get_orders（chain_off） | ✓ |
| GET | /api/v1/orders/:id | 订单详情 | get_order_by_id（chain_off，含 itinerary） | ✓ |
| POST | /api/v1/orders/:id/accept | 向导接单 | order_accept（chain_off） | ✓ |
| POST | /api/v1/orders/:id/cancel | 取消订单 | order_cancel（chain_off） | ✓ |
| POST | /api/v1/orders/:id/confirm-completion | 确认完成 | order_confirm_completion（chain_off） | ✓ |
| GET | /api/v1/orders/:id/reviews | 该订单评价 | reviews_list（chain_off） | ✓ |
| POST | /api/v1/orders/:id/reviews | 提交评价 | review_submit（chain_off） | ✓ |
| POST | /api/v1/orders/:id/dispute | 发起争议 | order_open_dispute（chain_off） | ✓ |
| GET | /api/v1/orders/:id/evidence | 证据列表 | get_order_evidence（chain_off） | ✓ |
| POST | /api/v1/orders/:id/evidence | 证据上传 | post_order_evidence（chain_off） | ✓ |
| GET | /api/v1/disputes | 争议列表 | get_disputes（chain_off） | ✓ |
| GET | /api/v1/disputes/:id | 争议详情 | get_dispute_by_id（chain_off） | ✓ |
| POST | /api/v1/disputes/:id/resolve | 裁决 | dispute_resolve（chain_off） | ✓ |

**额外路由（04 未列、实现需要）**：  
- POST /api/v1/orders/:id/mock-pay（P3 链下 mock 支付）、POST /api/v1/orders/:id/set-escrow-address（P18 链下 mock）、GET /api/v1/me/stats（统计摘要可选）、internal 执行器/索引等，与 04 §五、01 §9 一致。

---

## 企业级检查（P20）

**检查目的**：确认 P20 无遗漏、无未完成必须项，与 04 §三§四、01 一致，可审计留痕。

### 1. 路由对照完整性

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 04 §三 3.1 全部 9 条 | ✅ 无遗漏 | /health、/meta、/auth/register、/auth/login 已实现；logout/refresh/verify-email/forgot-password/reset-password 已补 chain_off stub |
| 04 §三 3.2 全部 3 条 | ✅ 无遗漏 | GET·PUT /api/v1/me 已实现；PUT /api/v1/me/password 已补 chain_off put_me_password_stub |
| 04 §三 3.3 全部 24 条 | ✅ 无遗漏 | 向导 4、itineraries 1、discover 1、orders 相关 18、disputes 3 均已挂载且可调或占位 |
| main.rs 与对照表一致 | ✅ 一致 | confirm-completion 已挂 order_confirm_completion；无 04 §三 列出但未挂载的路由 |

### 2. 04 §四 证据产物（请求体/超时/启动快照）

| 04 §四 要求 | 当前实现 | 状态 |
|-------------|----------|------|
| 请求体上限 1MB | `REQUEST_BODY_LIMIT_BYTES = 1024*1024`，`RequestBodyLimitLayer` | ✅ |
| 全局请求超时 30s | `REQUEST_TIMEOUT_SECS = 30`，`TimeoutLayer` | ✅ |
| 启动日志含 startup_snapshot | main.rs 启动时打印含 REQUEST_TIMEOUT_SECS、REQUEST_BODY_LIMIT_BYTES 等 | ✅ |
| GET /meta 返回 defaults 快照 | meta handler 返回 `defaults.request_timeout_secs`、`request_body_limit_bytes` 等 | ✅ |

### 3. 单测/集成覆盖

| 路径类型 | chain_off 单测 | 说明 |
|----------|----------------|------|
| 行程生成 | ✅ itinerary_create_impl_stores_draft_order_and_bundle | P15 |
| 确认最终版本 | ✅ confirm_final_plan_impl_stores_snapshot_hash | P16 |
| Discover / 消息 / 确认 | ✅ 多步测试（discover→messages→confirm） | P16 |
| set-escrow-address | ✅ set_order_escrow_address_impl_writes_address | P18 |
| 争议全流程 | ✅ dispute_list_get_resolve_flow（open→list→get→resolve→get） | P19 |
| 向导 / me / 订单 create·accept·cancel·confirm-completion / reviews / evidence | ⚠️ 无独立 chain_off 单测 | 依赖集成或 E2E；建议 P21 或后续补 |

**结论**：关键路径（行程、确认最终版本、Discover+消息+确认、set_escrow、争议）已有 chain_off 单测；向导/me/订单单步/reviews/证据 等为建议补充，不阻塞 P20 门禁 2。

### 4. 与 04/01 一致性

- 订单状态机、资金仅链驱动、争议裁决与 01 §1、§7 一致；04 §五 Phase 1～4 与当前实现（chain_off + 占位）对齐；占位项与 04 §五 Phase 4 衔接已说明。

### 5. 缺口与未完成部分（结论）

| 类型 | 内容 | 是否阻塞 P20 |
|------|------|--------------|
| 路由遗漏 | 无 | 不阻塞 |
| 占位未标注 | 无；7 项占位已明确 | 不阻塞 |
| 04 §四 证据产物 | 已落实 | 不阻塞 |
| 单测覆盖 | 部分路由无独立 chain_off 单测（见上表） | 不阻塞（建议后续补） |
| 与 04/01 冲突 | 无 | 不阻塞 |

**P20 无必须未完成项；建议后续在 P21 或 backlog 中为向导、me、订单单步、reviews、evidence 补 chain_off 单测。**

---

## 占位项说明（与 04 §五 Phase 4 衔接）

- **auth**：logout、refresh、verify-email、forgot-password、reset-password 已补 chain_off 占位（返回 `{"status":"ok","message":"chain_off_stub"}`），Phase 4 或后续由实现定稿。
- **PUT /api/v1/me/password**：已补 chain_off put_me_password_stub 占位，修改密码需旧密码或已登录态，实现时定稿。

---

## 当前进度

- **P20 实现**：04 §三 路由对照表已列（见上）；绝大部分 04 §三 路由已实现（chain_off），原 7 项占位（auth 扩展 + me/password）已补 chain_off stub，路由已挂接；请求/响应与 04 §二§三 一致；chain_off 单测已覆盖行程/订单/消息/确认/set_escrow_address/争议流程等（P15～P19）。
- **P20 门禁**：门禁 1 路由清单已逐项可调或占位；门禁 2 关键路径单测已通过（P2/P4 + chain_off tests）；门禁 3 与 04/01 一致，已勾选通过。
- **企业级检查**：见上文「企业级检查（P20）」：路由无遗漏、04 §四 证据产物已落实、与 04/01 一致；**无必须未完成项**；建议后续为向导/me/订单单步/reviews/evidence 补 chain_off 单测（P21 或 backlog）。

---

*与 [04-后端与API](../04-后端与API.md)、[27-P0至P50开发流程勾选清单](../27-P0至P50开发流程勾选清单.md)、[01-总库总览](../01-总库总览.md) 配套。*
