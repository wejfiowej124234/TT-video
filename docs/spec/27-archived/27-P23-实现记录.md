# P23 实现记录：05 前端子模块（二）— P22 建议补齐项落地

### 读前摘要

| 你要找什么 | 单源 |
|------------|------|
| **本 Phase 记录与验收** | **正文各节** |
| **总流程** | **[27-P0至P50开发流程勾选清单](../27-P0至P50开发流程勾选清单.md)** 对应 **P23** |
| **归档** | **[27-archived/README](README.md)**、**[27-系列索引](../27-系列索引.md)** |

**用途**：落实 [27-P0至P50开发流程勾选清单](../27-P0至P50开发流程勾选清单.md) P23（扩展位），将 [27-P22-实现记录](27-P22-实现记录.md) 中「建议后续补齐」项在前端落地：apiClient 封装与对应页面/入口。

**依据**：04 §三、05 §二、frontend/lib/api.ts、frontend/lib/apiClient.ts、27-P22-实现记录。

---

## P23 目标

补齐 P22 标注的前端缺口：**apiClient** 增加 putMe、订单接单/取消/确认完成、评价、争议等封装；**页面/入口**提供 me 资料编辑、订单操作（接单/取消/确认完成）、评价、链下 POST dispute（按需）；与 05/04 一致。

---

## P23 实现项（工作项）

| # | 工作项 | 验收 | □ 完成 |
|---|--------|------|--------|
| 1 | apiClient 补齐 | putMe、orderAccept、orderCancel、orderConfirmCompletion、getOrderReviews/postReview、postOrderDispute 等封装；与 04 §三 一致 | ✓ |
| 2 | 页面/入口 | me 页资料编辑（PUT me）、订单接单/取消/确认完成入口、评价入口、链下 POST dispute 入口（按需） | ✓ |
| 3 | 与 05/04 一致 | 调用与 05 §二、04 §三 无冲突 | ✓ |

---

## P23 门禁（检查项）

| # | 检查项 | 要求 | □ 通过 |
|---|--------|------|--------|
| 1 | 封装与入口勾选 | 下表「P23 补齐清单」逐项可验收 | ✓ |
| 2 | 与 05/04 一致 | 无冲突 | ✓ |

---

## P23 补齐清单（逐项勾选）

### 1. apiClient 封装（04 §三 对应路由）

| 04 路由 | apiClient 方法 | 状态 |
|--------|----------------|------|
| PUT /api/v1/me | putMe(body) | ✓ 已实现 |
| POST /api/v1/orders/:id/accept | orderAccept(orderId) | ✓ 已实现 |
| POST /api/v1/orders/:id/cancel | orderCancel(orderId) | ✓ 已实现 |
| POST /api/v1/orders/:id/confirm-completion | orderConfirmCompletion(orderId) | ✓ 已实现 |
| GET /api/v1/orders/:id/reviews | getOrderReviews(orderId) | ✓ 已实现 |
| POST /api/v1/orders/:id/reviews | postReview(orderId, body) | ✓ 已实现 |
| POST /api/v1/orders/:id/dispute | postOrderDispute(orderId, body?) | ✓ 已实现 |
| GET /api/v1/guides/:id | getGuide(id)（按需） | ✓ 已实现 |
| POST /api/v1/guides | postGuide(body)（向导端） | ✓ 已实现 |
| POST /api/v1/guides/:id/stake | postGuideStake(guideId, body)（向导端） | ✓ 已实现 |
| POST /api/v1/orders | postOrder(body)（创建订单流程） | ✓ 已实现 |

### 2. 页面/入口

| 入口 | 说明 | 状态 |
|------|------|------|
| me 页资料编辑 | 表单 + putMe() 提交；与 05 §六 一致 | ✓ 已实现（app/me/page.tsx） |
| 订单接单 | 向导端：订单详情或列表「接单」→ orderAccept(orderId) | ✓ 已实现（EscrowDetail OrderActionsBlock） |
| 订单取消 | 订单详情「取消」→ orderCancel(orderId) | ✓ 已实现（EscrowDetail OrderActionsBlock） |
| 订单确认完成 | 链下或双轨：订单详情「确认完成」→ orderConfirmCompletion(orderId)；链上仍为钱包签 | ✓ 已实现（EscrowDetail「确认完成（链下）」） |
| 评价入口 | 订单完成/争议关闭后「评价」→ getOrderReviews + postReview | ✓ 已实现（EscrowDetail ReviewBlock） |
| 链下发起争议 | 按需：Escrow 链下模式或双轨时 POST orderDispute 落库 | ✓ 已实现（EscrowDetail「发起争议（链下）」） |
| 向导详情 + 质押 | getGuide(id)、postGuideStake；向导详情页「质押」+「向该向导下单」 | ✓ 已实现（app/guides/[id]/page.tsx） |
| 向导注册 | postGuide；成为向导表单 | ✓ 已实现（app/guide/register/page.tsx） |
| 创建订单 | postOrder；选向导+金额+币种创建订单 | ✓ 已实现（app/orders/new/page.tsx，支持 ?guide_id=） |

---

## 当前进度

- **P23 实现**：已完成。apiClient 已补齐全部项（含按需：getGuide、postGuide、postGuideStake、postOrder）；me 页资料编辑、订单操作与评价、链下争议（EscrowDetail）；向导详情与质押（guides/[id]）、向导注册（guide/register）、创建订单（orders/new）已落地；getGuides/getOrders 统一解析 items 数组。
- **P23 门禁**：封装与入口勾选完成；与 05/04 一致，已勾选通过。

---

*与 [05-前端总览](../05-前端总览.md)、[04-后端与API](../04-后端与API.md)、[27-P22-实现记录](27-P22-实现记录.md)、[27-P0至P50开发流程勾选清单](../27-P0至P50开发流程勾选清单.md) 配套。*
