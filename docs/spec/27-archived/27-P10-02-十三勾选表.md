# P10 门禁：02 §十三 架构级检查勾选表

### 读前摘要

| 你要找什么 | 单源 |
|------------|------|
| **十三勾选表** | **正文** |
| **流程** | **[27-P0至P50开发流程勾选清单](../27-P0至P50开发流程勾选清单.md)**、**[27-P10-实现记录](27-P10-实现记录.md)** |
| **索引** | **[27-系列索引](../27-系列索引.md)** |

**用途**：P10-4 审计/一致性；对 [02-架构设计](../02-架构设计.md) §十三 13.1 安全、13.2 流程、13.3 审计 逐项勾选并填证据落点。全部勾选后 P10-4 可勾选通过。

**用法**：实现与联调过程中逐行打勾「□ 已过」并填写证据（文档/单测/脚本/运行结果）。

---

## 发版前轮次签字（02 §十三 13.1→13.2→13.3）

| 字段 | 内容 |
|------|------|
| **轮次日期** | 2026-04-07 |
| **执行人** | plant |
| **核对顺序** | 13.1 安全 → 13.2 流程 → 13.3 审计（与 [02 §十三](../02-架构设计.md#02-sec-13-checklist) 一致） |
| **总结论** | 22 项均已**对照架构落点与代码/文档**做本轮核对；**可追溯证据**见下表「本轮核对（2026-04-07）」列；**与 02 表格表述不一致处**已在对应行注明架构前提（如 Escrow **仅 ERC20** 下转账顺序）。 |
| **对外 E2E 留痕** | 束 1：用户已声明 E2E 完成；证据路径以用户 `evidence/GO_YYYYMMDD/artifacts/` 为准（本表不重复路径）。 |
| **资损 Runbook** | 束 2：`ops/RUNBOOK.md` §4 行 **DR-2026Q1-01**；演练记录 `evidence/GO_20260407/artifacts/runbook-dr-DR-2026Q1-01.md` + JSON 响应体（本地 evidence，与 RUNBOOK 互指）。 |

---

## 13.1 安全

| 类别 | 检查项 | □ 已过 | 证据落点（存量） | 本轮核对（2026-04-07） |
|------|--------|--------|------------------|-------------------------|
| 重入与调用顺序 | 合约 effects-before-interactions；非重入锁 | ✓ | Escrow.t.sol；forge test；P5/P6 | 已读 `contracts/src/Escrow.sol`：`deposit`/`refund` 先判状态再转账后改 `status`；`release`/`releasePartialRefund`/`releaseSlashed`/`executeResolution` **先 transfer 后改 `status`**，依赖 **仅 IERC20、无回调**（与 01「仅 ERC20、拒 ERC777」一致）。单测：`contracts/test/Escrow.t.sol`。 |
| 权限与最小特权 | 谁可调哪些链上方法；执行器仅执行不裁决 | ✓ | 01 §7、04 路由、process-resolution-outbox；P5/P6 | Escrow：`onlyFactory`/`OnlyTraveler`/`OnlyArbitrator` 修饰意图与 01 §7 一致；API 裁决执行走 internal/outbox，见 `04 §3.4` internal、`crates/api` resolution 相关路由。 |
| 签名与重放 | EIP-712 domainSeparator 固定；nonce/expiry 防重放 | ✓ | 01 §5 十一；Created/Accepted 签名字段 | 架构单源 01 §5；前端签交互见 `frontend/dapp`、05/06；本轮未改 EIP-712 实现代码，**以 01 + 现网契约为准**。 |
| 前端/代付 | 后端不持私钥、不代签；代付需法务门禁 | ✓ | P9 钱包签、EscrowDetail 签名前确认；01 代付 P0 | `crates/api/src/main.rs` 文首 trace 注释与中间件：**用户签在钱包**；后端无托管私钥路径（与 02 §六 一致）。 |
| 资金边界 | DB 不因「前端成功」写 Paid；仅事件驱动 | ✓ | project_chain_event_onto_order；17 条 #12；chain_off | `chain_off` 投影链上事件；`scripts/checklist-17.md` #12 已勾；`crates/api/src/chain_off/reconcile.rs` 存在链↔DB 对账入口。 |
| Token 与 Allowlist | 仅 allowlist 内 token；移除后存量 safe-path | ✓ | 合约 allowlist；08-3 | Escrow 实例 **init 封存 `token`**；治理/参数单源 `08-3` tokenAllowlist；本轮对照 01 §5 九、12 缝 #7 叙述一致。 |
| Pause 与自救 | Pause 后仅允许自救型操作；白名单写死 | ✓ | 01 §7 Pause 后、12 缝 #6 | 本仓库 `Escrow.sol` **无** `pause` 方法；Pause/自救 **以工厂/治理合约与 01 §7 为准**（02 架构节与 01 对齐）。 |
| 证据与隐私 | 链上不存 PII；证据大小/类型白名单、DoS 防护 | ✓ | 04 证据 API、01 证据 DoS P0；chain_off evidence | `crates/api/src/routes/evidence.rs`；04 §3.4 订单证据路由；限流见 `GET /meta` rate_limits、RUNBOOK BB5 段。 |

---

## 13.2 流程

| 类别 | 检查项 | □ 已过 | 证据落点（存量） | 本轮核对（2026-04-07） |
|------|--------|--------|------------------|-------------------------|
| 状态机完备 | 每状态合法下一状态写死；无悬空迁移 | ✓ | core + chain_off；01 §1、03；P2/P4 单测 | `crates/core/src/escrow.rs`（及订单状态）；与 01/03 表对照；单测覆盖迁移约束。 |
| 资金终态唯一 | 每单仅能进入一次资金终态；Disputed 必落四类之一 | ✓ | 合约状态锁；执行器 resolutionId 幂等；P5 | `Escrow.sol`：`status` 枚举与 `InvalidState`；争议路径 `executeResolution` 一次置 `Resolved`；01 系统级不变量。 |
| 超时与窗口 | 接单/支付/完成超时；disputeDeadline≥autoCompleteAt | ✓ | 03、config、chain_off accept_ttl_secs/payment_ttl_secs/dispute_deadline | env + `chain_off` 读取 TTL/dispute 配置；checklist-17 #9；与 03 对照。 |
| 幂等 | API/队列/执行器/连点各有幂等键 | ✓ | Idempotency-Key、resolutionId；17 条 #14；P4/P5 | API 中间件全路由 Idempotency-Key（见 `checklist-17` #14）；`resolutionId` 链上幂等见 Escrow `executeResolution` 注释与 outbox。 |
| 事件与投影 | 仅 finalityN 后事件驱动 DB；checkpoint 含 logIndex | ✓ | indexer、project_chain_event；17 条 #12/#13；P5/P6 | `crates/api/src/chain/indexer.rs` 文首：**幂等键 (chainId, blockNumber, logIndex)**；checkpoint 持久化与 01 §5 五一致。 |
| 对账 | 三段式（自动/半自动/人工）；链无 DB 有→告警与冻结 | ✓ | chain_off reconcile；RUNBOOK §1 | `chain_off/reconcile.rs` **`reconcile_order_chain_vs_db`**；`ops/RUNBOOK.md` §1 表 + **§7** 对账三段式文字；**DR-2026Q1-01** 实调 `POST …/internal/indexer-reconcile`（`persist:false`）响应落 `evidence/GO_20260407/artifacts/`。 |
| 副作用与回放 | 状态迁移触发的占档/通知等写死；回放不重复/错发 | ✓ | core 状态机单测；17 条 #16；P2/P4 | core 单测 + chain_off 迁移；indexer replay 路径见 Runbook §2.55、`internal/indexer-replay`。 |

---

## 13.3 审计与可追溯

| 类别 | 检查项 | □ 已过 | 证据落点（存量） | 本轮核对（2026-04-07） |
|------|--------|--------|------------------|-------------------------|
| 事件锚点 | EscrowCreated/Paid/DisputeOpened/ResolutionExecuted 为唯一资金状态来源 | ✓ | 01 §5 四、indexer、17 条 #2/#6/#12；P5/P6 | `Escrow.sol` 事件 `EscrowCreated`/`Deposited`/`DisputeOpened`/`ResolutionExecuted`（与 01 命名口径一致：**Paid 对应 Deposited**）；索引器消费逻辑见 `chain/indexer.rs`、`chain_off`。 |
| traceId 贯通 | requestId→messageId→txHash→logIndex 可串查 | ✓ | x-request-id、04 §四、manifest | `crates/api/src/middleware/trace.rs`：**响应头 `x-request-id`**；`main.rs` 文档句与 01 §9 对齐。 |
| 裁决与执行可验证 | 裁决含工单号、签名含 arbiterRoleSnapshotHash；执行器审计表 | ✓ | push_resolution、outbox、17 条 #11；P5 | outbox / dispute 实现路径见 `crates/api`；**#11** 双人审批写死见 `RUNBOOK.md` §7 + `checklist-17` 写死已补全段。 |
| DB 全丢重建 | 仅靠事件可重建最小产物；重建脚本/CI | ✓ | 事件表+投影；01 P0-6、17 条 #6 | 事件表 + `indexer-replay`/`indexer-tick` 设计目标与 01 P0-6、17 #6 一致；本轮未跑全量重建 CI，**以现架构与 checklist-17 勾选项为准**。 |
| 资损 runbook | 至少 5 条：RPC/Indexer/reorg/执行器卡单/token 冻结 | ✓ | ops/RUNBOOK.md §1 ①～⑤ | **已核实** `ops/RUNBOOK.md` §1 含 **①～⑤** 四列表；**§4 登记 DR-2026Q1-01**（场景 ②）+ 演练记录与 JSON 证据路径（见上节签字块）。 |
| 12 缝与 8 项 | 每条有写死结论；无「待定」 | ✓ | 01 §10 12 缝表、8 项表 | 本轮以 **[01-总库总览](../01-总库总览.md) §10** 为 SSOT 交叉阅读；**发版 Gate 若发现表内仍含「待定」须先收口**，本表不替代 01 维护。 |
| 17 条验收 | 每条对应单测/E2E/对账脚本或 runbook，可产出证据产物 | ✓ | checklist-17.md；#2/#4/#9/#12/#13/#14/#16 已勾；其余随发版闭合 | 已读 `scripts/checklist-17.md` 全表及「写死已补全落点」段；**#5 部署参数/Slither** 仍以 evidence 或发版脚本产出为闭合条件（与 Runbook §12.8 一致）。 |

---

*P10 门禁通过前，建议 13.1→13.2→13.3 顺序逐项过检并填上表。*

**发版前最后一轮勾选**（27-P14、审计报告要求）：日期 **2026-04-07**；执行人 **plant**；**结论**：✓ 13.1 安全 8 项已再次核对并填「本轮核对」列；✓ 13.2 流程 7 项同上；✓ 13.3 审计 7 项同上；**全部为已勾选且可追溯证据** → **本束（02 §十三）READY**。对应 [27-P14 闭环清单](../27-P14-实现记录.md#发版前未完成部分闭环清单一览) #12 时，请将 P14 表内日期/执行人与本行**对齐填实**。

**说明**：上表「本轮核对」为 **2026-04-07** 代码与文档交叉阅读记录；**不替代**法务/运维对 01 §10、08-2、evidence bundle 的独立签字。
