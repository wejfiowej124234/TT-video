# P2 Phase 1 门禁实现记录（测试 · 审计 · 缺口检查）

### 读前摘要

| 你要找什么 | 单源 |
|------------|------|
| **本 Phase 记录与验收** | **正文各节** |
| **总流程** | **[27-P0至P50开发流程勾选清单](../27-P0至P50开发流程勾选清单.md)** 对应 **P2** |
| **归档** | **[27-archived/README](README.md)**、**[27-系列索引](../27-系列索引.md)** |

**用途**：落实 [27-P0～P50 开发流程勾选清单](../27-P0至P50开发流程勾选清单.md) P2 阶段四项门禁，全部通过后可勾选「P2 全部通过 → 进入 P3」。

**目标**：单元测试、集成测试、审计/一致性、缺口与遗漏检查均满足 Phase 1 要求。

> **归档说明（迁移路径，2026-03）**：下文若出现 `migrations/001_initial.sql` 等，为历史路径。**当前**以 `crates/api/migrations/`（SQLx）为准，见 [27-archived/README](README.md)、[41](../41-后端数据库接库与落地清单.md)。

---

## P2-1：单元测试

**要求**：core 与关键状态迁移有单测，覆盖率满足 01 §10 对 17 条要求。

| 项 | 落点 | 状态 |
|----|------|------|
| core 状态机单测 | `crates/core/src/escrow.rs` §tests：7 个用例 | ✓ |
| Created→Accepted/Cancelled | `created_may_transition_to_accepted_or_cancelled` | ✓ |
| Accepted→Escrowed/Cancelled | `accepted_may_transition_to_escrowed_or_cancelled` | ✓ |
| Escrowed→Completed/Disputed | `escrowed_may_transition_to_completed_or_disputed` | ✓ |
| Disputed→四资金终态 | `disputed_may_transition_to_four_financial_finals` | ✓ |
| 终态无下一状态 | `terminal_states_have_no_allowed_next` | ✓ |
| 仅 Escrowed 可 dispute | `only_escrowed_can_dispute` | ✓ |
| 仅资金终态可 review | `only_final_financial_states_can_review` | ✓ |
| 01 §10 17 条 Phase 1 相关 | #16 状态机与副作用联动 → 状态迁移与 can_dispute/can_review 已覆盖；#9 Dispute 与自动放款互斥（领域侧 can_dispute 已测） | ✓ |

**说明**：01 §10 审计验收表 17 条中，与 Phase 1 实现直接相关者为状态机（#16）、争议窗口领域侧（#9）；合约/链/对账相关条（#1/#2/#3 等）在 P5/P6 实现并验收。当前 core 状态机单测已满足「关键状态迁移有单测」及 Phase 1 对 17 条的可追溯要求。

**结论**：P2-1 通过。

---

## P2-2：集成测试

**要求**：API 健康检查、占位路由可调通（如 curl/Postman）。

| 项 | 落点 | 状态 |
|----|------|------|
| 健康检查 | `GET /health` 返回 ok | ✓ |
| 版本与默认值 | `GET /meta` 返回 api_version、ssot_version、defaults（REQUEST_TIMEOUT_SECS、REQUEST_BODY_LIMIT_BYTES 等） | ✓ |
| 自动化脚本 | `scripts/smoke_api.sh [BASE_URL]`，默认 `http://127.0.0.1:8080` | ✓ |
| 执行方式 | 先启动 API（如 `cargo run -p traveltrust-api` 或 `PORT=8080 cargo run -p traveltrust-api`），再执行 `./scripts/smoke_api.sh` | 已文档化 |

**验收**：本地执行 `./scripts/smoke_api.sh` 可调通 /health 与 /meta；占位路由（如 GET /api/v1/guides、/api/v1/orders 等）返回 501 或占位 JSON，视为「可调通」。

**结论**：P2-2 通过。

---

## P2-3：审计/一致性

**要求**：与 02 分层、04 表结构一致；04 开工前 6.4 可勾选。

| 项 | 要求 | 状态 |
|----|------|------|
| 02 分层 | core 无 I/O（无 tokio/sqlx/alloy）；api 为应用层，依赖 core | ✓ |
| 04 表结构 | migrations/001_initial.sql 与 04 附录 DDL、§一表与 core 类型对应一致 | ✓ |
| 04 §六 6.4 第 1 项 | 01 引用 04 覆盖；开篇 12 项及 §二～§四 覆盖；01 §10 后端落点见 §四、6.2 | ✓ |
| 04 §六 6.4 第 2 项 | 04 与 01/02 一致性；开篇 12 项无冲突；总览主题符合性表 7 行与 01 一致 | ✓ |
| 04 §六 6.4 第 3 项 | 与 03 接口风控对齐；§二 业务表、§四 风控与 03 一致 | ✓ |
| 04 §六 6.4 第 4 项 | 规则变更同步；04 修改后同步 01/02/03 及开篇两表、6.3 | ✓ |

**结论**：P2-3 通过；04 6.4 开工前检查清单四项已勾选。

---

## P2-4：缺口与遗漏

**要求**：对照 01 §10、04 §五 Phase 1，无未实现或文档未覆盖项。

| 来源 | 检查内容 | 结论 |
|------|----------|------|
| 01 §10 审计前检查 | 12 缝写死、17 条可追溯、资损 runbook 至少 5 条、系统级不变量无冲突 | Phase 1 不要求 runbook 填满；17 条 Phase 1 相关已落单测/占位 |
| 04 §五 Phase 1 | Rust workspace、core 类型、api 骨架、基础路由、健康检查、占位、DB 迁移 | 均已实现；「基础 CRUD」按 27 为占位，完整 CRUD 在 P3 |
| P1 遗留缺口 | 见 27-P1-实现记录 §P1 缺口与问题 5 项 | 已闭合：集成测试已提供 smoke_api.sh；04 6.4 已勾选；core 状态机单测已满足 P2；迁移执行证据保留「首次部署时执行并记录」 |

**结论**：P2-4 通过；无未实现或文档未覆盖项阻塞 Phase 1。

---

## P2 全部通过 → 进入 P3

| # | 检查项 | 要求 | 结果 |
|---|--------|------|------|
| 1 | 单元测试 | core 与关键状态迁移有单测，覆盖率满足 01 §10 对 17 条要求 | 通过 |
| 2 | 集成测试 | API 健康检查、占位路由可调通（curl/Postman） | 通过 |
| 3 | 审计/一致性 | 与 02 分层、04 表结构一致；04 开工前 6.4 可勾选 | 通过 |
| 4 | 缺口与遗漏 | 对照 01 §10、04 §五 Phase 1，无未实现或文档未覆盖项 | 通过 |

**结论**：P2 阶段四项门禁已全部通过，可勾选「P2 全部通过 → 进入 P3」，进入 P3（Phase 2 实现：链下核心业务流程）。

---

## 企业级 P1/P2 检查：遗留问题与建议

以下为企业级口径下，P1 与 P2 阶段仍存在或建议在后续阶段闭合的项，**不阻塞进入 P3**，但建议在发版前或对应 Phase 内处理。

### 1. P1 遗留（仍存在）

| # | 问题 | 严重程度 | 建议处置 |
|---|------|----------|----------|
| 1 | **迁移脚本未在 CI 或 Runbook 中记录「已成功执行」** | 低 | 在 CI 增加「执行 migrations/001_initial.sql 并校验表存在」步骤，或在 Runbook/部署文档中保留「首次部署时人工执行并记录」证据；P4/P6 前建议闭合。 |
| 2 | **04 Phase 1 字面「基础 CRUD」与 27 P1「占位」口径差异** | 低 | 已以 27 为准；可选在 04 §五 Phase 1 加一句「Phase 1 产出含占位路由，完整 CRUD 在 Phase 2（链下）实现」。 |

### 2. P2 阶段未强制、企业级建议补齐

| # | 问题 | 严重程度 | 建议处置 |
|---|------|----------|----------|
| 1 | **Smoke 未入 CI**：`scripts/smoke_api.sh` 需先启动 API，当前 `.github/workflows/build.yml` 仅 `cargo test --workspace`，无「启动 API + curl /health」 | 低 | 可选：CI 增加 job「启动 API → 执行 smoke_api.sh」；或至少在 Runbook/27 中写明「发版前/PR 合并前须本地执行 smoke 验收」。 |
| 2 | **core 仅 escrow 有单测**：types、staking、reputation、outbox 无单测 | 低 | P2 门禁已接受「关键状态迁移有单测」。企业级建议：P3 或 P4 前为 reputation/权重、outbox 等补单测，便于 17 条与后续 Phase 可追溯。 |
| 3 | **17 条→Phase 落点总表**：01 §10 要求 17 条每条可追溯；当前 Phase 1 仅状态机相关有单测，无「17 条 → 各 Phase 与证据落点」总表 | 低 | 建议在 P6 或发版前：在 `scripts/checklist-17.md` 或 27 中维护「17 条 → Phase（P2/P4/P6…）与证据 artifact 路径」表，便于发版前逐条勾选。 |

### 3. P0 定稿项（企业级须在定稿时完成）

| # | 问题 | 说明 |
|---|------|------|
| 1 | **P0 评审记录表未填写** | 27-P0 中「评审记录表」（评审日期、参与人、纪要/签字落点）为定稿时必填；若仍为空，企业级须在正式定稿时由责任人填写并留存。 |

### 4. 与 07 §四 4.3、01 §10、02 §十三 的对应关系

| 检查项 | P1/P2 状态 |
|--------|------------|
| 07 §四 4.3 开工前：1.1 七项 + 01 §10 + 02 §十三 13.1 | 已在 27-P0 执行；02 §十三 13.1～13.3 已勾选（文档落点就绪） |
| 01 §10 审计前：12 缝/17 条/资损 runbook/系统级不变量 | P0 已确认；17 条「逐条可追溯」在实现期 P2/P4/P6 逐条勾选，Phase 1 相关已落单测 |
| 资损 runbook 至少 5 条 | Runbook §1 触发表已列 5 类（RPC/Indexer/reorg/执行器/token 冻结）；发版前须确认每条内容已填满且可执行（01 §9、P12 门禁） |

**结论**：P1/P2 企业级检查下，**无阻塞进入 P3 的硬缺口**；上述为低风险遗留与建议，可在 P3～P6 或发版前按表逐项闭合。

---

*本文与 27-P0至P50开发流程勾选清单、01、04、07 配套。文档版本与最后更新见 [00-文档索引](../00-文档索引.md)。*
