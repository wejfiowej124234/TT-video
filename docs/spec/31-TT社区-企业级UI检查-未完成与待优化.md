# TT社区 · 企业级 UI 检查 — 未完成与待优化

### 读前摘要

| 你要找什么 | 单源 |
|------------|------|
| **已达标摘要** | **§一** |
| **待优化清单** | **§二** 起 |
| **前置清单** | **[31-弹窗与页面全方位优化清单](31-TT社区-弹窗与页面全方位优化清单.md)**、**[31-社区企业级多维度检查与设计完善清单](31-社区企业级多维度检查与设计完善清单.md)** |
| **后端缺口指针** | **[04-附录-前端与后端数据职责…](04-附录-前端与后端数据职责及企业级符合性.md)** |

**用途**：在 31 弹窗与页面全方位优化清单、31 社区企业级多维度检查清单基础上，从**企业级 UI/UX**（05 §9.0.5、WCAG、状态与反馈、一致性）角度，列出 TT 社区**仍可优化**与**未完成**的部分，便于逐项补齐或排期。  
**范围**：仅 TT 社区前端 UI/交互，不含后端 API 与数据库（后端缺口见 04 附录、31 企业级多维度检查 §2.1）。

---

## 一、企业级维度下已达标项（摘要）

| 维度 | 状态 | 说明 |
|------|------|------|
| 登录与鉴权 | ✅ | 未登录发帖/评论/私信拦截或禁用并提示 |
| 加载与骨架 | ✅ | Feed 骨架、loading.tsx、路由切换滚动置顶 |
| 无障碍 | ✅ | role/dialog、aria-label、Esc 关闭、轮播键盘左右键、prefer-reduced-motion |
| i18n | ✅ | community_*、common_* 全量，弹窗与页面文案走 t(key) |
| 异常态 | ✅ 部分 | 错误边界 error.tsx、空态+CTA、媒体加载失败、Feed 错误条+重试、上传超大小提示 |
| 安全区与响应式 | ✅ | safe-area-inset-t/b、safe-area-pb、Toast 与底栏避开安全区 |
| 返回/关闭 | ✅ | 所有抽屉顶栏「返回」+ Esc；发帖底栏固定 |
| 风格一致 | ✅ | 赛博风 30 §4，与 DID 一致 |

---

## 二、企业级 UI 未完成 / 待优化（前端可做）

以下为**前端即可落点**的优化，按 05 §9.0.5 与 31 企业级清单对照整理。

### 2.1 加载与防连点（05 要求：按钮/提交 loading 态 + 禁用防连点）

| 项 | 说明 | 状态 |
|----|------|------|
| 发帖「发布」按钮 | 提交时 disabled + 文案「发布中…」、aria-busy | ✅ 已实现（submitting 态 + community_publish_submitting） |
| 评论「发送」按钮 | 发送时短时 disabled +「发送中…」 | ✅ 已实现（CommentDrawer、PostDetailDrawer：sending + community_comment_sending） |
| 私信「发送」按钮 | 同上 | ✅ 已实现（messages/[id]：sending + 输入受控 + Enter 发送） |

### 2.2 性能与资源（31 §2.2 已列、可再加强）

| 项 | 说明 | 状态 |
|----|------|------|
| Feed 卡片主图 | CommunityFeedCard 内 `<Image>` 增加 `loading="lazy"` | ✅ 已实现 |
| 作者页/我的帖子网格图 | 已用 media_urls?.[0] ?? media_url + loading="lazy" | ✅ 已实现 |

### 2.3 焦点管理（05：弹窗关闭时焦点还原）

| 项 | 说明 | 状态 |
|----|------|------|
| 抽屉关闭后焦点 | 打开时记录触发元素，关闭时 focus() 回 | ✅ 已实现（Feed/作者/我的帖子/我的收藏：focusReturnTargetRef + 关闭/ Esc 时 requestAnimationFrame 还原） |

### 2.4 异常态与反馈（补全 31 §3.2）

| 项 | 说明 | 状态 |
|----|------|------|
| 会话不存在 | 会话 [id] 无对应数据时统一文案 + 返回列表 | ✅ 已实现（community_conversation_not_found + 底部 safe-area-pb） |
| 网络离线 | layout 已有顶条离线提示 | 已满足 |

### 2.5 合规与信任（31 §3.3）

| 项 | 说明 | 状态 |
|----|------|------|
| 社区规范链接 | 我的页链出「社区规范」、占位页 | ✅ 已实现（我的页快捷入口 + /terms/community-guidelines 占位页，待法务正式内容） |

### 2.6 表单与输入（05：错误与必填）

| 项 | 说明 | 建议 |
|----|------|------|
| 发帖正文 | 已有字数与必填提示、aria-describedby | 已满足 |
| 评论/私信输入 | 未登录 placeholder 已区分 | 已满足；若后端返回字段校验错误，需在输入旁展示错误文案并 aria-invalid/aria-errormessage |

### 2.7 数字与日期（05：本地化）

| 项 | 说明 | 状态 |
|----|------|------|
| 时间展示 | 会话列表 last_at 与 locale 一致 | ✅ 已实现（formatCommunityDateShort + communityFormatters.ts，消息列表已用） |

### 2.8 个人中心「我的」页（参考小红书式个人中心）

| 项 | 说明 | 状态 |
|----|------|------|
| 主区 landmark | 我的 / 我的帖子 / 我的收藏 使用 `<main aria-label>` | ✅ 已实现 |
| 头像与上传入口 | 头像 + 右下角「更换头像」按钮（占位，接后端后真上传） | ✅ 已实现 |
| 简介与空态 | 有 bio 展示；无 bio 时「暂无简介」+「添加简介」CTA（占位） | ✅ 已实现 |
| 编辑资料 / 设置 | 入口按钮，编辑资料链回我的、设置链 /me，min-h 44px | ✅ 已实现 |
| 统计与 a11y | 关注/粉丝/好友/获赞可点击、aria-label | ✅ 已实现 |
| 你可能感兴趣的人 | 横向滚动推荐用户（帖子作者排除已关注），关注按钮 | ✅ 已实现 |
| 快捷入口扩展 | 创作灵感→动态、浏览记录→占位、我的帖子/收藏/社区规范 | ✅ 已实现 |
| 内容 Tab | 笔记 \| 收藏 \| 赞过（笔记/收藏链子页，赞过占位禁用） | ✅ 已实现 |
| i18n | 编辑资料、设置、上传/更换头像、简介、推荐人、创作灵感、浏览记录、Tab 等 | ✅ 已实现 |

**待后端/产品**：上传头像、编辑资料、添加简介、浏览记录真实数据、「赞过」列表与隐私开关。

---

## 三、依赖后端/产品的未完成项（仅列 UI 相关）

以下需**后端或产品**落点后，前端配合完成；UI 侧可先预留错误态与重试。

| 项 | 说明 | 前端 UI 可提前做 |
|----|------|------------------|
| 发帖/评论/点赞/收藏/关注 API | 数据持久化、错误码 | 保留 publishError/onRetryPublish、commentSendError/onRetryComment、Feed feedError 重试；接 API 后替换数据源并展示接口错误文案 |
| 举报与审核 | 举报入库、违规「已隐藏」 | 举报入口与 toast 已有；接后端后展示「已受理」或违规内容占位「已隐藏」 |
| 上传安全 | 文件类型/大小/时长、白名单 | 发帖上传为占位；接上传 API 后做前端校验与错误提示，与 04 约定一致 |
| 真实视频流 | 视频 URL、进度条、静音 | 当前占位层；接媒体 API 后接入播放器与控件 |

---

## 四、建议实施顺序

1. **P1（建议尽快）**  
   - ~~发帖/评论/私信发送按钮 **防连点**（loading 或短时 disabled）~~ ✅ 已完成  
   - ~~Feed 卡片主图 **loading="lazy"**（首屏外）~~ ✅ 已完成  

2. **P2（体验增强）**  
   - ~~社区规范 **链接占位**（我的页或 footer）~~ ✅ 已完成  
   - ~~会话不存在时 **统一文案**（i18n community_conversation_not_found）~~ ✅ 已完成  

3. **P3（可选）**  
   - ~~抽屉关闭后 **焦点还原**到触发元素~~ ✅ 已完成（Feed/作者/我的帖子/我的收藏，含 Esc 关闭）  
   - ~~时间/日期 **本地化**（会话列表 last_at 已用 formatCommunityDateShort）~~ ✅ 已完成  
   - ~~作者页/我的帖子网格 **loading="lazy"**~~ ✅ 已完成  

4. **待后端**  
   - §三 各项在 04/产品落点后，前端接 API 并保留错误态与重试。

---

## 五、与现有文档对应

- **31-弹窗与页面全方位优化清单**：弹窗与页面逐条勾选已做完，本稿在其基础上补**企业级**视角的缺口。  
- **31-社区企业级多维度检查与设计完善清单**：§2 剩余缺口、§3 设计不完善项与本稿 §二、§三 对齐；本稿侧重「UI 未完成与待优化」的**可执行列表**。  
- **05 §9.0.5 UI/UX 顶级要求**：本稿 §二 与 05 可访问性、状态与反馈、表单、焦点管理等逐条对应。

---

## 六、本次审计完成项（收尾）

| 项 | 说明 |
|----|------|
| 焦点还原 | Feed / 作者页 / 我的帖子 / 我的收藏：抽屉与视频关闭时焦点回到触发元素（focusReturnTargetRef + requestAnimationFrame），含 Esc 关闭 |
| 错误页 | community/error.tsx 增加 safe-area-inset-t / safe-area-inset-b |
| 主区 landmark | Feed 页主内容容器改为 `<main aria-label={t("community_tab_feed")}>`，便于读屏与 landmark 导航 |
| 个人中心「我的」 | 参考小红书式：头像+更换入口、简介空态+添加简介、编辑资料/设置、你可能感兴趣的人横滑、创作灵感/浏览记录、内容 Tab（笔记/收藏/赞过）、main/aria/min-h 44px、i18n 全量 | 见 §2.8 |

文档维护：实现或规范变更后，同步更新本稿与 31 企业级多维度检查清单；后端/产品落点后，将 §三 对应项移入「已闭环」或删除。
