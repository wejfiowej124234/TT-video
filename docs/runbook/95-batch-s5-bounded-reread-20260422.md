# 95 · §12.1 · **S-5** 有界机读复验（130 / 120 / 140 · 2026-04-22）

> **定位**：**§12.1** 表 **S-5**（**130/120/140** 与 **02 §十四**、**110** 互指）之**有界复验**；**不**将 **§12.1 · S-5** 主行 **`[ ]`→`[x]`**；**不**替代 **120/140** 全文 Owner 审计、**07 §五** 硬链、**ISS-007**/**93**/**§8.2 行完成**。

## 1. 门禁（仓库根）

```bash
bash scripts/check-07-version-triple.sh
bash scripts/run-check-04-routes.sh
bash scripts/check-08-consistency.sh
```

**结果（本机）**：三者均为 **exit 0**（**`run-check-04`**：**178** 路径；**`check-07`**：**07** **1.0.858** 三线）。

## 2. 锚点文件（存在性 + 行数）

| 路径 | `test -f` | `wc -l` |
|------|-----------|---------|
| **`../spec/130-阶段开发测试体系.md`** | OK | **399** |
| **`../spec/120-阶段开发观测告警日志与审计链路.md`** | OK | **407** |
| **`../spec/140-阶段开发云部署与交付架构.md`** | OK | **403** |
| **`../spec/02-架构设计.md`** | OK | （未在本表重数；篇内含 **`## 十四、完整架构层级图与主链路`**） |
| **`../spec/110-阶段开发链上索引器与事件同步器.md`** | OK | （**S-5** 表与 **v1.4.154** 读通登记已列 **110** 读前互证） |

**互链抽样（机读 `rg`）**：

- **`02-架构设计.md`**：含 **`## 十四、完整架构层级图与主链路`** 与读前表 **`§十四`** 指针。
- **`130-…`**：**§二** 强制锚 **`02 §十四`**（**`0~8` 九层**）；**§一** 列 **110/120** 依赖；**§8.5** 与 **`140`** 成熟度口径对齐。

## 3. 诚实边界

- **不**宣称 **§12.1 · S-5** 主表已闭（仍为 **`[ ]`**）。
- **不**新增 **§3 F-034**（主表仍止于 **F-033**）。
- **不**闭 **ISS-007** / **§8.2「行完成」** / **§3.1 `[x]`**。
