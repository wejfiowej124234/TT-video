# TT-B330 · 55 数据同步 vs 110 链上索引器 — 职责分界 SSOT

**卡号**：`TT-B330-55-VS-110-INDEXER-SCOPE-SSOT-001` · **母表** `B-330`  
**日期**：2026-04-15  
**范围**：仅 **文档 / 台账**；**不改** **`crates/**`**、**不改** **04** 契约表（本卡 **不** 触发 **04** HTTP 路由行变更）。

---

## 1. 机读一句（职责分界 · SSOT）

| 文档 | 独占地盘（真值） | 明确不扛 |
|------|------------------|----------|
| **[55 · 阶段数据同步与数据库功能同步](../spec/55-阶段-数据同步与数据库功能同步.md)** | **应用层**：PostgreSQL ↔ `chain_off` 双写与启动 hydrate、迁移与「表 ↔ 功能」对齐、55-S 清单与发版前勾选（含与 **15 附录〇** 互指）。 | **不定义** RPC 拉块、**`event_log` / `orders_projection` 等链上投影写入语义**、**`indexer-tick` / `indexer-reconcile` / `internal` 观测 JSON** 的规格与运维闭环（见 **110**）。 |
| **[110 · 链上索引器与事件同步器](../spec/110-阶段开发链上索引器与事件同步器.md)** | **基础设施层**：自 RPC 抓取链上日志 → 幂等写入 **`event_log` 与业务投影表** → finality / reorg / 对账 / admin·internal 可观测闭环。 | **不替代** 55 对订单/行程/社区等 **业务表** 的双写、迁移行与 **55-S** 验收义务。 |

**合并句（背板）**：**55** 管 **「人/API 写入路径上的 DB 真值与产品数据同步」**；**110** 管 **「链上事件进入 DB 投影与索引运维」** — 二者 **正交**；接链后的 **`event_log`/投影** 以 **110** 为 SSOT，**55 §〇「55 不覆盖」** 已声明之边界与本 Runbook **同义**。

---

## 2. 本轮仅读文件清单（≤8）

| # | 路径 |
|---|------|
| 1 | `docs/spec/55-阶段-数据同步与数据库功能同步.md`（**§〇** 目标/范围/**不覆盖**） |
| 2 | `docs/spec/110-阶段开发链上索引器与事件同步器.md`（**读前摘要**、**§一** 定位、**§二～§三** 目标/模块） |
| 3 | `docs/spec/04-后端与API.md`（**只读**：索引器相关 HTTP 仍以 **04** 为契约 SSOT，本卡 **不** 改 **04** 正文） |
| 4 | `docs/任务母表.md`（**B-330** 行互证） |
| 5 | `docs/AI任务卡索引.from-stash.md`（一览 **340** / **§TT-B330**） |

---

## 3. 互证落点（本卡已写入）

- **55**：读前摘要表 **「55 vs 110」** 一行；**§〇** 十字互证句 → 本 Runbook。  
- **110**：读前摘要表 **「55 vs 110」** 一行；**§一** 文档定位 → 本 Runbook。

---

## 4. 验收（本卡 · docs-only）

- 本 Runbook + **母表 B-330** + **from-stash 一览 340** 互证完成。  
- **`bash scripts/run-check-04-routes.sh`** **exit** **0**（路由脚本校验文档锚与 **04** 引用不断链）。

---

## 5. 互证索引

- **母表**：[`docs/任务母表.md`](../任务母表.md) **B-330**  
- **执行索引**：[`docs/AI任务卡索引.from-stash.md`](../AI任务卡索引.from-stash.md) 一览 **340** · **`### TT-B330-55-VS-110-INDEXER-SCOPE-SSOT-001`**
