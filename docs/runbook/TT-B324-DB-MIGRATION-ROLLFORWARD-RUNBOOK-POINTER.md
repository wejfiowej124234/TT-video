# TT-B324 · DB 迁移正向 / 回滚策略与 RUNBOOK 指针（审计登记）

**卡号**：`TT-B324-DB-MIGRATION-ROLLFORWARD-RUNBOOK-POINTER-001` · **母表** `B-324`  
**日期**：2026-04-15  
**范围**：仅 **文档 / 台账 / 索引**；**先审计后登记**；**不改** `crates/api/migrations/*.sql`、**不改** **`ops/RUNBOOK.md`** 结构、**不改** **04** 契约表正文（本卡 **不**触发 **04** 路由行变更）。

**术语**

- **正向（rollforward）**：按 **`crates/api/migrations/`** 时间戳 **`.sql`** **顺序应用**（**SQLx migrate** / 启动 **`migrator.run`**）。  
- **回滚（rollback）**：**schema 回退或前滚修复**；含 **运维备份恢复**、**受控补脚本**、以及 **110/ internal** 语境下的 **链域删尾 / reconcile**（**非** 本卡展开 110 细节）。

---

## 1. 本轮仅读文件清单（≤8）

| # | 路径 |
|---|------|
| 1 | `docs/spec/04-后端与API.md`（**§四** **数据库迁移策略（写死，P0）** 段） |
| 2 | `ops/RUNBOOK.md`（**§100** **CockroachDB 名 vs PostgreSQL 真源**；**§2.6** **53 发版回滚清单** 中与 **API/DB** 相关行） |
| 3 | `docs/spec/04-附录-DDL草案.md`（读前摘要：**迁移工具** 指 **04 §四**） |
| 4 | `docs/spec/41-后端数据库接库与落地清单.md`（**迁移单源**、**`sqlx migrate run`** / **启动** **自动迁移** 叙述） |
| 5 | `docs/spec/55-阶段-数据同步与数据库功能同步.md`（**§1.2** **现有迁移与表一览** — **Wave 2** **迁移叙事对齐** 互证） |
| 6 | `docs/spec/04-后端与API.md`（**§3.5** **`GET /api/v1/admin/schema/migrations`** 表行 — **只读** **聚合** **`schema_versions`/`migration_histories`/`migration_rollbacks`/…**） |
| 7 | `docs/AI任务卡索引.from-stash.md`（一览 **334** / **本 TT**） |
| 8 | `docs/任务母表.md`（**B-324** 行占位互证；审计后本 runbook 封口） |

**说明**：未将单支 **`20260308000020_schema_evolution_center.sql`** 全文列入 **≤8**；**`migration_rollbacks`** **账本表** 的 **存在性** 由 **§2** **与** **04 §3.5** **互证**。

---

## 2. 审计结论：策略与指针（真值分散、本卡聚合）

### 2.1 迁移 **单源** 与 **工具**

- **`crates/api/migrations/`** 为 **SQL schema 单源**（**RUNBOOK §100**；**41** 篇首 **迁移单源**；**04 §四** **迁移** 行）。  
- **04 §四 P0**：**SQLx migrations**（或等价）；须 **入库**、**可 CI 校验**；**向后兼容**；**每次 schema 变更须写明可回滚方式**（**down migration** **或** **前滚修复**）及 **数据一致性影响**。

### 2.2 **RUNBOOK** 级 **运维指针**

- **PG vs CockroachDB 规划名**：**RUNBOOK §100**，避免 **拓扑名** 与 **当前 PG** 落地混读。  
- **53 发版回滚**：**RUNBOOK §2.6** — **API 回滚** 时若 **DB 迁移有破坏性变更**，须 **回滚 SQL** 或 **备份恢复**（与 **档期文件** 并列）。

### 2.3 **Wave 2 / 55** 叙事

- **55 §1.2**：**迁移文件 ↔ 表/域** 一览与 **O10**「新增迁移须同步 **55/04/41**」— **与** 母表 **B-324** **「Wave 2 叙事对齐」** **互证**（本卡 **不**改 **55** 表体）。

### 2.4 **只读 HTTP：迁移元数据**

- **04 §3.5** **`GET …/admin/schema/migrations`**：**200** 含 **`migration_histories`**、**`migration_rollbacks`** 等 — **运维/审计只读面**，**非** 执行迁移本身。

---

## 3. 缺口登记（仅记录 · 本卡不改实现）

| 缺口 | 说明 |
|------|------|
| **无单一「DB 迁移 Runbook 专章」** | **正向/回滚** 的 **工程义务** 主要写在 **04 §四 P0** + **RUNBOOK §2.6/§100** + **41**；**未** 在 **`ops/RUNBOOK.md`** 内形成 **独立成章** 的 **SQLx 操作手册**（本卡 **仅聚合指针**，**不** 重组 Runbook 目录）。 |
| **P0「回滚方案」vs 仓库 `*.sql` 形态** | **04** 要求每次变更 **写明** **down** **或** **前滚修复**；**典型** 迁移文件为 **up 脚本**，**不** 强制同目录 **配对 `down.sql`**。**`20260308000020_*`** 引入 **`migration_rollbacks`** 等 **台账表** — **属** **运维/审计账本** 能力，**不** 自动等价于 **全量 down migration 覆盖**。**登记**：**契约句** 与 **文件级 down 覆盖** 之间 **须人工/单卡** 对齐，**非** 本卡补脚本。 |
| **55 §1.2 行数与目录计数** | 历史 **P5** 曾出现 **§1.2 滞后**（**55** 文内 **O10** 已闭环规则）；**后续** 每增迁移 **仍须** 同批更新 **§1.2** — **登记为持续纪律**，**本卡** **不** 改 **55** 计数。 |

---

## 4. 验收（本卡 · docs-only）

- 本 Runbook + **母表 B-324** + **from-stash 一览 334** 互证完成。  
- **未**修改 **迁移 SQL**、**未**改 **RUNBOOK** 正文、**未**改 **04** 契约表（本卡约定 **不** 触 **04** 正文）。

---

## 5. 互证

- **母表**：[`docs/任务母表.md`](../任务母表.md) **B-324**  
- **执行索引**：[`docs/AI任务卡索引.from-stash.md`](../AI任务卡索引.from-stash.md) 一览 **334** · **`### TT-B324-DB-MIGRATION-ROLLFORWARD-RUNBOOK-POINTER-001`**
