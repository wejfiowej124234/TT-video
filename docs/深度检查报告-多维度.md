# 深度多维度检查报告

**检查日期**：基于当前仓库状态。**范围**：构建与配置、脚本与文档引用、API/前端、CI、数据库。

---

## 一、已通过项

| 维度 | 检查项 | 结果 |
|------|--------|------|
| **构建** | rust-toolchain.toml 存在 | ✅ |
| **前端** | frontend/package.json、package-lock.json 存在 | ✅ |
| **API 安全头** | main.rs 含 security_headers_layer、x-content-type-options | ✅ |
| **端口** | 后端默认 8080、前端默认 3000、脚本强制 PORT=8080 | ✅ |
| **前端 API** | lib/api.ts 默认 http://localhost:8080 | ✅ |
| **Docker** | docker-compose.yml 存在，Postgres 16、5432、traveltrust | ✅ |
| **CI** | build.yml 仅引用 check-invariants.sh、audit-deps.sh，两者均存在 | ✅ |
| **脚本** | scripts/ 现保留 10 个文件，无冗余；README 与 00-文档索引已对齐 | ✅ |
| **日常文档** | 测试账号与本地联调.md、README 快速开始 与脚本一致 | ✅ |

---

## 二、已修复的文档引用（本轮）

以下文档曾引用已删除脚本，已改为「手工执行」或「脚本已精简」说明：

- **ops/RUNBOOK.md**：发版前检查、前端可验证发布、frontend_pre_deploy、check-ssot-deploy、gen-runtime-param、check-08-consistency、check-08-evidence-pointer 等改为手工步骤或注明脚本已精简。
- **evidence/README.md**：validate-evidence-manifest、build-frontend-manifest 改为手工校验/生成说明。
- **README.md**：scripts 目录描述改为「日常开发、CI 门禁、验收清单」。
- **contracts/README.md**：移除对已删部署脚本的引用。
- **docs/spec/04-后端与API.md**：check-ssot-deploy 改为 Runbook §10 手工校验。
- **docs/spec/05-前端总览.md**：check-node-lock-policy 改为 check-invariants 校验锁文件。
- **scripts/checklist-17.md**：#5 产出改为 slither 或手工产出，不再引用 export_deployment_params。

---

## 三、仍保留的文档表述（可不改）

以下文档中仍出现已删除脚本名称，多为**实现记录/历史报告**（27-Px、15、缺口与待补等），描述的是「当时」的落点；保留不改动可作历史对照。若需完全统一，可在各文档中增加一句：「脚本已精简，见 scripts/README.md；发版/evidence 按 Runbook、evidence/README 手工执行。」

- docs/spec/缺口与待补一览.md
- docs/spec/15-多维度文档与技术检查报告.md
- docs/spec/未完成项与缺口补充清单.md
- docs/spec/08-2-附录-闭合工单表.md
- docs/spec/27-P14-实现记录.md、27-P34、27-P29、27-P22、27-P13 等
- docs/spec/项目优化与问题清单.md
- data/README.md（p34 日志路径可改为「可选，手工执行检查后输出至 data/」）

---

## 四、建议后续关注

| 项 | 说明 |
|------|------|
| **本地验证** | 在 Windows 本机执行 `scripts\start-api-with-seed.bat` 与 `scripts\e2e-verify.bat`，确认数据库、8080、3000、登录均通过。 |
| **.env** | 若手动启动 API，确保 .env 中无 `PORT=3012`，或设为 `PORT=8080`，否则前端请求会失败。 |
| **Node** | 前端需 Node 18+；`npm run dev` 前在 frontend 目录执行 `npm install`。 |

---

*本报告与 scripts/README.md、docs/测试账号与本地联调.md、00-文档索引 配套。*
